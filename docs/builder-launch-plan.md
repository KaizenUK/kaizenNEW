# Kaizen builder — from private beta to public product

Ordered task map, written 12 September 2026 after the existing-site editing work landed. Codex implements it; Claude reviews each stage for usability, wording and safety; Sean accepts. Read [the Codex brief](handover/codex-launch-brief.md) first, then this map.

This map deliberately contains no time estimates. It gives scope, order and acceptance only.

**Verification cadence — Sean's update, 13 September:** document and verify tasks locally as they are implemented. Run CI in bulk at the end of each milestone (L0–L6), and once again at the end of the goal. Do not wait for CI between individual tasks. Intermediate task commits can use `[skip ci]`; record CI as pending until the milestone run passes.

## Where we are

**Current branch consolidation — 15 September:** Sean paused implementation and asked to commit all pending branch work into main for Claude. The [main handover](handover/claude-main-handover.md) is authoritative. The editor polish checkpoint (`e845385`) and unfinished T4 checkpoint (`e73079d`) are preserved together. Older uncommitted/launch-worktree-only statements below are chronological history. Implementation resumed with Claude; A–D are complete and E is next. L6 remains unfinished and undeployed; this merge does not accept a launch gate.

**Previous retirement checkpoint — 15 September:** D2d.1 native worker/idle maintenance is verified. [The caller guide](builder-quotas.md#native-release-maintenance-caller) records configured identities, stopped-work/checkout recovery, maintenance without a new build and exact recovery with new cleanup disabled. **257 cases across fifteen files pass with actual Nginx**, six launcher cases and full types pass, and the actual maintenance caller recovers through two forced service kills. Continue **D2d.2 — client/domain/retry and direct-call coverage**, then history, D2e–f and D3–H. The prepared runtime/timer remain uninstalled. All T4 remains uncommitted/undeployed; L5 stays live.

**Latest D checkpoint — 15 September:** D2 release-store capacity admission is implemented and verified: **149 relevant cases with real Nginx enabled**, full types and native bundling pass. Staging shares the activation lock, actual retained storage is admitted before copying, changed inputs are bounded and rollback can reuse existing assets under capacity pressure. Continue **D2 — retention and safe retirement**, then D3–D5 and E–H. See [the release-storage guide](builder-quotas.md#release-store-capacity-admission) and [takeover checklist](handover/l6-t4-claude-tasks.md). Automatic release/immutable cleanup remains unfinished. T4 stays uncommitted/undeployed; L5 remains live.

**Claude continuation plan — 15 September:** [the takeover checklist](handover/l6-t4-claude-tasks.md) breaks the work into A–H with explicit files, verification and deployment state. It records the exact dirty worktree so Claude can continue if Codex credits run out. A–C, D0–D1 and D2 capacity/SQL/eligibility/removal are verified in fixtures; continue at **E — publication abuse controls** in [the Claude checklist](handover/l6-t4-release-retirement-tasks.md). D–H cover remaining storage/abuse implementation, integration proof, L6 rollout and the final audit. The linked checklist holds the current resume point; older continuation paragraphs are historical.

**Current T4 work — 15 September:** [pending-copy cancellation and guarded purge](builder-quotas.md#cancelling-an-unfinished-website-copy) now preserve the original website, stop delayed copy work and retain physical storage charges until cleanup is acknowledged. The latest checks pass **236 relevant cases across nine files**, full types, **10 Edge tests / 68 steps**, **sixteen real PostgreSQL contention groups** and the cancellation journey in Chromium, Firefox and WebKit. The final toolbar polish also passes all eight React cases. Nine desktop/phone screenshots are inspected and saved. Earlier native cleanup/controller/deployment proof remains separately recorded below. Complete real inventory registration, retained disk limits, publication abuse controls, installation and live provider proof remain; T4 is neither complete nor deployed. Continue without an input hold. The implementation count remains 31/32.

**Latest native continuation — 15 September:** [interrupted-checkout recovery](builder-quotas.md#interrupted-checkout-recovery) now resumes a stopped deployment using its durable receipt and private Git index, preserving unknown edits and foreign locks. **117 relevant application cases**, full types and an actual killed-service/restart fixture pass. Release reconciliation repairs pending checkout work without fetching/installing another candidate. The root scanner-to-cleanup connection and remaining copy/disk/abuse/live rollout follow. The launcher/workflow remains uninstalled, cleanup disabled by default, **31/32** built and L5 live.

**Implementation checkpoint — 15 September:** L0 through L5 are verified and deployed. L6 signup, billing and [custom-domain implementation](#l6-t3--implementation-checkpoint-and-deferred-public-acceptance-15-september-2026) are locally verified: **31 of 32 implementation tasks (97% rounded)** by equal count, not effort or launch readiness. Continue **L6-T4 physical quotas/abuse**. L5 remains live; L6 provider setup, full CI, scheduler activation and rollout are still outstanding. Real public-domain acceptance requires Cloudflare access and is collected with the final human-only setup, following Sean's autonomy instruction. Paid PITR, always-on offsite storage, hosted-provider recovery, legal/provider review, notification delivery and hands-on acceptance remain explicit final setup/acceptance items. The following baseline describes the starting scope.

**Sean's autonomy update — 14 September:** use recommended choices for wording, hosting, TLS and L6 signup, Stripe wiring, domains and quotas while he is away. Continue all work Codex can do; collect remaining external setup and human-only actions at the end. Record consequential choices and their limits. This does not convert automated evidence into Sean's manual acceptance.

Sean will use the builder on the Kaizen site alone first, as a private beta with one user. When he is happy, the same product opens to Kaizen's clients and then to the public. Everything below is ordered so that the private beta is usable first and nothing done for it has to be thrown away later.

The editor itself is in good shape: builder pages with about twenty-five block types, drafts, undo and redo, autosave and recovery, releases with staging and production destinations, redirects, private previews, backups, assets, contact forms delivered through the `builder-contact` function, project members with owner and editor roles, and on-page editing of existing Astro and React pages. The automated suite is broad (214 unit tests, 42 browser scenarios, Nginx release verification in CI).

What is missing is the wrapper around the editor: a way to edit existing pages without running a helper on your own computer, invitations, a client-friendly mode that hides Git, first-run guidance and templates, public-launch hardening, and finally self-serve sign-up, billing and domains.

## Ground rules (carry these into every task)

- Plain English in the interface. Use the vocabulary already in the product: helper, website folder, edit text and links, review my changes, apply changes to the folder, preview the website, publish. Do not rename common web design terms.
- Original source stays authoritative. Only safely located literal fields and complete section ranges are patched. Nothing is pushed or published without an explicit action by the user.
- Never commit private credentials, `KAIZEN-PRIVATE-MIGRATION*` files, deploy keys or `.env` values. Server-only secrets never get a `VITE_` prefix.
- Never test against Sean's personal `.kaizen-builder` or overwrite his checkout. Fixtures create their own temporary repositories, as `tests/builder/site-fixture.ts` does.
- Every task ships with tests at the level that proves it (unit, database, browser) and a short note in the matching guide under `docs/`. Append an implementation log to the end of this file per stage, as was done in `existing-site-visual-editing-plan.md`.
- New server endpoints only where the logic must live on the server: secrets, Git, builds, database operations. Everything else stays in the client.
- Keep the local helper path working. Developers keep using it; it is also the fallback if the hosted path is down.

## What already exists (build on it, do not rebuild it)

- **Repository actions** run inside the local helper: `scripts/builder-repository.ts` (inspect, plan, apply), `scripts/builder-source-editing.ts` (fields and groups), `scripts/builder-runner.ts` (reviewed builds and the loopback snapshot server), `scripts/builder-source-preview.ts` and `builder-source-frame.ts` (framed preview with the overlay), `scripts/builder-repository-git.ts` (status and commit of applied files only). The client calls them through `storage.repository({ action })` in `client/visual-builder/storage.ts` and pairs with the helper through `companionConnection.ts`, `HostedRepository.tsx` and the standalone consent window from `scripts/builder-companion-ui.ts`.
- **Hosted publishing** already runs on the server. `supabase/functions/builder-publish` freezes a release request and dispatches GitHub; the VPS worker `scripts/builder-release-worker.mjs` claims it, builds with `corepack pnpm run build`, stages and activates through `scripts/kaizen-releases.mjs` and verifies the live site. Client projects use `builder_client_jobs` and client destinations. See `docs/website-releases.md`.
- **Accounts** use Supabase password sign-in with reset, and `BuilderAuth.tsx` already handles invite and recovery links. `ProjectMembers.tsx` adds members by raw user ID and says so.
- **Access rules** live in `supabase/migrations/202609110001_builder_projects.sql` onwards and are covered by `client/visual-builder/project-access-database.spec.ts`.
- **Kaizen-only shortcuts** exist in about ten places (`activeProjectId === "kaizen"` in `BuilderApp.tsx`, `shell.tsx`, `SitePanel.tsx`, `storage.ts`; project `"kaizen"` fixed in `builder-publish`; allowed origins in `builder-companion.ts`, `_shared/editorAuth.ts` and `builder-contact`). They are fine for the beta and must become settings before clients arrive.
- **CI** (`.github/workflows/builder-checks.yml`) runs the Deno checks, `pnpm typecheck`, `pnpm test`, the client worker compiler check, independent client repository builds, Nginx release verification and `pnpm test:builder:browser`.

## Architecture decisions

### 1. The helper moves to the server for hosted projects

Today, editing an existing page needs the repo, Node and pnpm on the editor's own computer plus a running helper. Clients will never do that. The hosted path runs the same repository actions on the VPS, against a working copy the server owns.

- One **hosted helper service** on the VPS (`scripts/builder-hosted-helper.ts`, run as a systemd service next to the release worker) imports the existing runner, repository, source and Git modules unchanged. It keeps one working copy per project under a dedicated directory, cloned with a per-project deploy key that never leaves the server. Every request is authenticated with the caller's Supabase session and checked against `builder_project_members`. All file work goes through the existing path-safety helpers.
- **Interactive actions** (inspect, source inspect, plan, apply, Git status) go over HTTPS through the existing `/editor-api/` proxy so the editor feels immediate. **Builds** become jobs with status and log polling, exactly as the local helper's `repository-build` and `repository-build-status` already behave, and one build runs at a time per project.
- **Preview snapshots** are served over HTTPS from a loopback preview server behind a new `/editor-preview/<project>/<build>/` route, protected by the editor cookie on `.kaizenweb.co.uk`, with `frame-ancestors` set to the builder origin and the existing `connect-src 'none'` and `form-action 'none'` kept. The hosted editor frames that URL the same way it frames the helper's snapshot today.
- The client keeps one interface. `storage.repository` picks the transport from the project: hosted by default, local helper when a developer connects one. `SitePageEditor.tsx` and `SitePages.tsx` do not need to know which one they are talking to.
- **Save to website** is the new end of the editing journey: apply the reviewed plan, commit the applied files, push to the project's configured branch. For Kaizen that branch is `stage`, which the deploy workflow must publish to staging. Production requires a separate explicit review and publish action in Releases, advancing `main` to the reviewed staged commit before the existing production workflow builds and verifies it. L1-T6 supplies this native-source promotion; the earlier builder-content publication did not. Developers still see and can edit the commit message; editors see "Save to website".
- Builds on the server execute the project's own build script. For the Kaizen-only beta that is Sean's own code. Before clients or the public arrive, builds must run isolated (a container or a separate build host, decided in L5-T4).

### 2. Two modes of the same product

Owners see developer details (file paths, branches, commits, build logs). Editors see the client view (pages, words, images, save, publish). It is the same screens with the technical lines hidden, not two products. A per-user "Show developer details" switch lets an owner see what an editor sees.

### 3. Self-serve is a separate stage, not a redesign

Sign-up, billing and custom domains sit on top of projects and members as they exist. Nothing in the earlier stages should assume a single tenant except the explicit Kaizen shortcuts, which L0 turns into settings.

## Stages and tasks

Each task lists what it delivers, the files most likely involved and how it is proven. "Proven by" is the minimum; add more when it is cheap.

### L0 — Beta switches and a clean baseline

- **L0-T1 Turn Kaizen shortcuts into project settings.** Replace each `activeProjectId === "kaizen"` with a project capability (`hasInventory`, `legacyWorkspace`, `publishPath: "github" | "worker"`), stored on `builder_projects` and exposed by `builder-projects`. `builder-publish` reads the project from the request rather than assuming `"kaizen"`. Proven by unit tests on the project model, the existing publish and projects function checks, and the browser suite unchanged.
- **L0-T2 Origins from configuration.** Allowed origins in `builder-companion.ts`, `_shared/editorAuth.ts` and `builder-contact` come from one documented environment variable each, with the current values as defaults. Proven by unit tests for the parsers and a note in `docs/website-releases.md`.
- **L0-T3 Beta notice and problem reports.** A small "Private beta" pill in the sidebar footer and a "Report a problem" action in Settings that copies diagnostics (project, page, browser, last error, helper state, no secrets) to the clipboard and offers a download. Proven by a unit test that the diagnostics never include tokens or keys, and a browser check of the action.
- **L0-T4 Error visibility for the operator.** Client errors and hosted helper failures are recorded with project and user IDs in a `builder_client_errors` table through the existing projects function, retained for a bounded period. If Sean prefers a hosted error service, swap the sink; keep the shape. Proven by a database test for retention and access rules.

### L1 — Hosted helper: edit existing pages without a local helper

- **L1-T1 Repository transport in the client.** `storage.repository` gains a hosted transport selected per project, with the same action names and error shapes as the local helper. Connection state, expiry and "not connected" notices map onto session validity. Proven by unit tests with a fake server and the existing `site-*.spec.ts` scenarios run against the hosted transport with a fixture API, the way `companion.spec.ts` fakes the helper today.
- **L1-T2 Hosted helper service.** `scripts/builder-hosted-helper.ts` serving the interactive actions over HTTP on a loopback port, using the existing modules, one working copy per project, per-project locks, deploy-key cloning and fetch, and membership checks on every call. Proven by integration tests on a temporary bare repository and working copy, and by security tests for auth, membership, path safety and concurrent locks.
- **L1-T3 Build jobs and logs.** Builds queue per project, run one at a time, stream a bounded log and expose status. Reuse the job conventions from `builder_client_jobs`. Proven by integration tests including a failing build and a cancelled build.
- **L1-T4 Preview over HTTPS.** Loopback preview server for snapshots, the `/editor-preview/` proxy route documented for Apache/DirectAdmin alongside `/editor-api/`, editor cookie check, correct CSP. Proven by a Playwright scenario that frames a served snapshot from an HTTPS parent (extend the M0 test in `site-canvas.spec.ts`).
- **L1-T5 Save to website.** After apply: commit the applied files with the editor's name and email from their account, push to the configured branch, and show where it went (staging link, release status). Refuse and explain when the branch moved, when there are unrelated staged files or when the push is rejected. Proven by integration tests against a temporary remote and the existing commit isolation scenario extended with push.
- **L1-T6 Kaizen first, then per-project settings.** Wire the Kaizen project's working copy and `stage` branch so Sean's beta works end to end. Add owner settings for repository URL, branch and the deploy key's public half (shown once, copyable, never the private half), in `RepositorySettings.tsx`, `builder-hosted-settings.ts`, the hosted helper/transport and shared contract. Add explicit native-source staging-to-production review/publication in `RepositoryPublish.tsx`, Releases and `builder-hosted-publishing.ts`, reusing restricted Git and the existing production deployment verification. Proven by real-Git setup/publication access and conflict tests, the hosted browser fixture journey, verified live staging/production wiring and Sean's manual Kaizen check. Keep implementation and fixture evidence distinct from that acceptance.
- **L1-T7 Operations.** systemd unit, health endpoint, log rotation, restart behaviour, disk limits for working copies and snapshots, and a runbook section in `docs/website-releases.md`. Proven by the health check in CI where possible and a documented manual check on the VPS.

### L2 — People and access

- **L2-T1 Invite by email.** New `builder-invite` function using Supabase's admin invite, creating the membership in the same step; members screen lists people by email and name, with resend and remove. `BuilderAuth.tsx` already accepts the invite link, so finish the landing copy and the set-password step. Proven by a function test with a fake auth admin, a database test for membership rules, and a browser scenario for the invited person's first sign-in.
- **L2-T2 Roles that mean something.** Owner and editor stay; `can_publish` stays separate. Editors get the client view by default; owners get developer details with a switch to see the client view. Proven by unit tests for the view mode and browser checks of both modes on the site editor and Pages.
- **L2-T3 Account page.** Name, email, password change, sign out of other sessions, and a "delete my account" request that an owner confirms. Proven by unit tests and a browser check.
- **L2-T4 Sign-in states.** Expired link, used link, wrong password, reset sent, invited but not yet set up. Each says what happened and what to do next. Proven by the existing `auth.spec.tsx` extended per state.

### L3 — Client mode and wording

- **L3-T1 Hide Git in client mode.** Site editor footer shows page, saved state and "Save to website"; no branch, file or commit text. Review dialog keeps the plain before-and-after list and hides "Show the whole file". Developer mode keeps everything. Proven by browser checks in both modes.
- **L3-T2 One status language everywhere.** Draft, Saved, On staging, Live, with the same words in Pages, the editor header, Releases and the footer. Proven by a unit test over the status mapping and a screenshot pass.
- **L3-T3 Explain once, where it matters.** Every view keeps its one-line description; replace remaining paragraphs with a "Learn more" link to a short in-app help drawer that renders the relevant section of `docs/visual-builder.md`. Proven by a browser check that every view has a description and no view has more than one paragraph of explanatory text above the fold.

### L4 — First run and templates

- **L4-T1 First-run checklist.** On a new project, Pages shows a "Start here" card: name your site, choose a site design, add a page, preview it, publish. Each step links to the right place and ticks itself off. Dismissible; stored per user and project. Proven by unit tests for the step logic and a browser scenario.
- **L4-T2 Page template gallery.** At least six page templates built only from existing blocks (Home, About, Services, Contact, Pricing, Landing page) with thumbnails, preview and "Use template". Extend the current single starter in `starters.ts` and `PagesView.tsx`. Proven by unit tests that each template validates against the block registry and a browser check of the flow.
- **L4-T3 Starter site.** One "Small business" starter that creates the six pages, navigation and footer together. Proven by a browser scenario that creates it and publishes to a staging destination in the fixture.
- **L4-T4 Sample images and text.** Licence-safe placeholders through the existing asset and licence rules; no third-party images without a recorded licence. Proven by the existing licence checks.

### L5 — Public-launch hardening

- **L5-T1 Browsers.** Add Firefox and WebKit projects to `playwright.builder.config.ts` for the core journeys and fix what fails. Sean waived these for the beta; they are required for the public gate.
- **L5-T2 Accessibility.** Keyboard paths through Pages, the editor and the dialogs; names on every control; contrast on pills and buttons; an axe check in the browser suite with zero serious violations.
- **L5-T3 Performance.** Lazy-load the page editor and the site editor; measure and record the builder's first useful paint on a mid-range laptop; fix the largest chunks flagged by the build.
- **L5-T4 Security.** Rate limits on the public functions, an RLS review of every `builder_*` table with tests, a secrets audit, dependency audit in CI, and the isolation decision for server builds of client code (container or separate build host). Document the decision and its limits in `docs/website-releases.md`.
- **L5-T5 Backups and monitoring.** Supabase point-in-time recovery enabled, release store and working copies backed up, uptime and error alerts to Sean, a written restore drill.
- **L5-T6 Legal and privacy.** Builder Terms of Service and Privacy Policy pages (Sean or a lawyer writes the words; Codex wires the pages, the version and the acceptance record at first sign-in), plus data export and deletion requests routed to the owner. Cookie notice only if analytics are added.

### L6 — Self-serve

- **L6-T1 Sign-up.** Email sign-up with confirmation, a first project created on first sign-in, and the acceptance record from L5-T6. Files: `client/visual-builder/BuilderAuth.tsx`, `BuilderSignup.tsx`, `BuilderFirstProjectGate.tsx`, `authState.ts`, `shared/builderSignup.ts`, the existing projects function and `supabase/functions/_shared/builderSignup.ts`, `202609150001_builder_signup.sql`, and [the signup guide](builder-signup.md). Proven by the signup/client/access database tests, the actual Edge handler test, native PostgreSQL concurrency and browser signup/account/legal journeys.
- **L6-T2 Billing.** Stripe Checkout and customer portal, a webhook function writing `builder_subscriptions`, and plan limits (projects, pages, storage, publishing) enforced in the projects function with clear in-app messages. Proven by function tests with recorded webhook payloads and database tests for limits.
- **L6-T3 Custom domains.** Per-project domain with verification by DNS record and automatic TLS. Apache/DirectAdmin owns ports 80 and 443 today, so decide between Cloudflare for SaaS in front of the release store, a dedicated edge host, or DirectAdmin's own domain automation. Record the decision, then implement domain add, verify, remove and the release worker's mapping to it. Proven by integration tests against a fake DNS and a documented manual check on one real domain.
- **L6-T4 Quotas and abuse.** Upload and storage limits per plan, publish rate limits, a report-and-takedown path, and suspension that keeps data but stops publishing. Proven by database and function tests.

## Gates

**Private beta gate (Sean, Kaizen only).** L0 complete; L1 through T6 for the Kaizen project; L2-T1 and L2-T2; L3-T1 and L3-T2; L4-T1. Sean edits a real Kaizen page from the hosted builder with no helper running, saves to website, sees it on staging, and publishes through Releases.

**Sean's provisional acceptance — 13 September:** Sean tried Save and received the account/email setup notice. He accepted the current behavior for now, asked to handle a later failure as a bug if needed, and explicitly resumed the goal. Continue the outstanding implementation in order; do not stop again for the same acceptance check. This records the attempted Save and his acceptance, without claiming that staging save or production publication completed or that remaining gate tasks are implemented. Account setup and its wording remain part of L2.

**Client gate (Kaizen's clients).** Everything in L1 including T7; all of L2, L3 and L4; L5-T2, L5-T4 isolation decision implemented, L5-T5 and L5-T6. A client edits text and images on their site, saves and sees staging, and an owner publishes.

**Public gate.** All of L5 and L6. A stranger signs up, pays, builds a site from a starter, connects a domain and publishes, without anyone at Kaizen touching a server.

## Definition of done for every task

- The task's relevant local tests pass, with evidence recorded. The full existing suite and CI pass at the milestone boundary and again at the end of the goal, under Sean's updated cadence above.
- The screens involved use the plain-English vocabulary and have been looked at in the browser at desktop and phone widths.
- The relevant guide under `docs/` describes the behaviour in one place, and this file's log records what shipped, what was proven and what was left out.
- No secrets, private files or personal workspaces were touched or committed.
- Claude has reviewed the stage for wording, layout and safety, and Sean has accepted it.

## Implementation log — L0

### L0-T1 — Project settings and publication boundaries (12 September 2026)

Implemented on `codex/builder-launch` in a separate worktree. Sean's original checkout and its pending visual edits remain untouched. The launch brief and task map are carried onto this branch, and AGENTS/HANDOVER link to both.

- Added validated `hasInventory`, `legacyWorkspace` and `publishPath` settings to the shared project model, hosted database and projects response. Local catalogue migration preserves all existing workspace bytes. New, copied, imported and helper-linked projects keep worker routing and no destination.
- Replaced project-ID switches in the interface, storage transport, preview form configuration and helper actions. The stable original ID remains only where needed for registration and physical storage addresses. Project configuration clears on account changes; missing settings stop hosted routing.
- Publication names its project explicitly. The function checks membership, publish permission, archive state and routing before reading the release workspace or dispatching. Database access follows the configured legacy project; only one preserved workspace can use GitHub publication. Owners cannot escalate these operator settings through the project API.
- Updated [the client-project guide](builder-client-projects.md#project-capabilities) with behaviour, limits and migration/deployment order. No additional GitHub publication destination or hosted repository service is implied by these settings.

**Local proof:** TypeScript/Astro checks: zero errors and zero warnings (172 existing hints). Deno checks pass for projects and publish functions. The final full unit/integration run passed all 237 tests across 45 files with Nginx enabled. New cases cover malformed settings, safe defaults and migration, original-ID-independent database access, denied capability updates, missing/unauthorised/archived publication requests, worker-route rejection and disconnected preview forms. The final full browser suite passed 42 scenarios with only the optional licensed-archive scenario skipped, including real Nginx publication. Tests used temporary repositories and fixture workspaces.

The first full browser run exposed an obsolete hosted-media mock and two transient editor failures. The mock now supplies project configuration rather than rewriting a production constant. Both editor cases passed a focused rerun without a code fix, and the final full run held the source steady and passed. Existing ResizeObserver notifications remain visible; no console-clean claim is made. Private-preview tests deliberately request blocked private files and verify denial.

Desktop and phone project screens were inspected: [desktop](handover/builder-launch/l0-projects-desktop.png), [phone](handover/builder-launch/l0-projects-phone.png). The existing current-project pill can make long project names wrap awkwardly on desktop; retain this for Claude's stage polish.

**Pending:** CI on the launch branch, production migration/function/frontend rollout, Claude's L0 review and Sean's gate acceptance. Nothing has been pushed or deployed by this task. Continue in order with L0-T2; this is implementation evidence, not private beta acceptance.

### L0-T2 — Configured browser origins (12 September 2026)

Added one explicit server-side allowlist per service: `BUILDER_COMPANION_ORIGINS`, `ALLOWED_STUDIO_ORIGINS` and `BUILDER_CONTACT_ORIGINS`. A shared parser preserves each documented default only when its setting is absent, replaces defaults when configured, normalises/deduplicates origins and rejects unsafe entries without reflecting their contents in errors. Explicitly empty lists permit no browser origins. The helper test override remains restricted to exact HTTP loopback origins; normal pairing still requires folder approval.

The editor API no longer derives extra allowed origins from site/Studio URL variables. [The release guide](website-releases.md#builder-browser-origins) gives defaults, syntax, environment locations and the rollout requirement to list all authorised origins explicitly. The example environment lists the new settings. CI now watches the parser and affected server modules and checks the contact function alongside projects and publish.

**Local proof:** all 255 unit/integration tests pass across 46 files with Nginx enabled; TypeScript/Astro reports zero errors/warnings and the same 172 hints; Deno checks pass for projects, publish and contact. Parser tests cover absent/empty/replaced configuration, canonicalisation, unsafe inputs, credential-safe errors, lookalike hosts and test-origin restrictions. A real temporary-repository helper test exercises a configured non-default HTTPS origin and retained folder checks. All three relevant browser journeys pass: companion reload, cross-origin pairing/editing/revocation and contact submission/idempotency. No screens changed; L0's desktop/phone evidence remains above, and the full browser suite will run again at the stage boundary.

**Pending:** hosted configuration/function rollout, branch CI and the stage/gate reviews. No credentials, live allowlists or personal workspaces were changed. Continue with L0-T3.

### L0-T3 — Private beta and problem reports (12 September 2026)

Added a **Private beta** pill in the sidebar footer and **Settings → Report a problem** for every project, including the original workspace and failed workspace loads. The action copies a report, offers the identical JSON as a download, and lets the user inspect it. Clipboard denial keeps the download available.

The report uses an explicit list of fields: project/page identifiers, a one-way reference for source routes, known browser family/version/platform, helper mode/state and a fixed last-error category/summary/time. Raw exception text, stacks, URL credentials, source paths, helper secrets, titles and arbitrary properties never enter the report. A local helper's health is explicitly unprobed. Error classification observes browser errors without suppressing them and wraps storage failures while preserving their original errors and results. The failing page survives navigation to Settings, and diagnostics clear when the account changes. No report is automatically sent in this task; operator recording is L0-T4.

**Local proof:** all 266 unit/integration tests pass across 47 files with Nginx enabled. TypeScript/Astro checks report zero errors/warnings and 172 existing hints. Privacy tests exercise credential-bearing inputs, object-coercion attempts, fixed summaries, route hashing, browser listener cleanup, error-page retention and unchanged storage outcomes. Both report browser scenarios pass, covering copy/download equality and a failed workspace with denied clipboard access. Existing client-settings and hosted-helper scenarios also passed during the task. Desktop/phone layouts were inspected, including horizontal-overflow checks: [desktop](handover/builder-launch/l0-settings-desktop.png), [phone](handover/builder-launch/l0-settings-phone.png). The current guide is [Private beta and problem reports](visual-builder.md#private-beta-and-problem-reports).

**Pending:** L0-T4, the full browser run at the L0 boundary, branch CI, deployment, Claude's stage review and Sean's private beta acceptance. No private migration files, credentials, personal workspaces or live systems were changed. Continue in order with L0-T4.

### L0-T4 — Operator error visibility (12 September 2026)

Added automatic, best-effort recording of signed-in hosted builder errors through the existing `builder-projects` function into `builder_client_errors`. The function uses the verified account ID and the requested project; the database rechecks current membership and archived state under the same project lock used by membership changes. Publish permission is not required to report a failure. Builder users, including project owners, cannot read or write the operator table or impersonate another actor through the recording RPC. Database operators have the inspection query in the [error visibility runbook](website-releases.md#builder-error-visibility).

The sink rebuilds a fixed set of safe fields: identifiers, server receipt time, error category/source, known screen, optional page ID/route hash, browser family/major version/platform and helper mode/state. Raw messages, stacks, paths, credentials, build logs and arbitrary JSON never enter the table. The browser reporter observes caught workspace/catalogue/helper errors and browser events, pins each outgoing request to its captured account, drops pending reports on account changes, bounds/deduplicates attempts, and never reports its own failures. Failed build results received successfully over HTTP are recorded too. Local and signed-out builders do not send reports; Settings explains automatic reporting in the hosted builder and keeps manual copy/download available during a reporting outage.

Recording is capped at 20 events per account/project per hour and 2,000 per project per day. Migration `202609120003_builder_error_retention.sql` requires an hourly `pg_cron` job which physically deletes records older than 14 days, including idle projects. With the scheduler running, records are deleted at the next hourly run. The runbook includes checks for the active job, recent executions and overdue rows, plus recovery steps. Live table retention is separate from database backup retention.

**Sink decision:** the database table is the task map's default and is implemented. A hosted error service would add provider search/alerting and another service to operate; use it only if Sean prefers that option later, retaining the same field shape, access controls and retention contract. No external error provider or messaging integration was added. The actual hosted helper service is still L1; its browser-visible failures can use this recording path, but this task does not claim a deployed hosted helper was exercised.

**Files:** `shared/builderDiagnostics.ts`; client `errorReporting.ts`, `BuilderApp.tsx`, `projectStorage.ts`, `useSiteBuild.ts`, `ProblemReport.tsx`; `supabase/functions/_shared/clientDiagnostics.ts`, `builder-projects/index.ts`; the two new error-table/retention migrations; `errorReporting.spec.ts`, `error-reporting-database.spec.ts`, `tests/builder/diagnostics.spec.ts` and `site-pages.spec.ts`; the builder guide, release runbook and CI path filter.

**Local proof:** all 282 unit/integration tests pass across 49 files with Nginx enabled. Database tests execute the actual migrations and cover service-only recording/reading, RLS defence, cross-project and revoked/archived access, bounded fields, account/project quotas, server-owned identity/time, physical expiry and account deletion. PGlite substitutes only the unavailable scheduler; it executes the migration's job definition and real cleanup function, not a live cron worker. Client/function tests cover credential-bearing inputs, untrusted extra fields, account-change races, stale initial sessions, deduplication, failed/slow sends, cleanup and safe server failure responses. TypeScript/Astro checks report zero errors/warnings and 172 existing hints; Deno checks pass for projects, publish and contact functions. Six relevant browser scenarios pass, including real app/storage reporting against a fixture sink, a reporting outage, sign-out, manual reports and failed builds. Desktop/phone screenshots were inspected with horizontal-overflow checks: [desktop](handover/builder-launch/l0-error-reporting-desktop.png), [phone](handover/builder-launch/l0-error-reporting-phone.png).

### L0 stage verification and handoff

All four L0 tasks are implemented locally on `codex/builder-launch` in the isolated `kaizen-launch` worktree. The final full browser run passes **45 scenarios**, including real Nginx publication/rollback and the local-helper source-editing journeys. The optional licensed UI8 archive scenario remains skipped because its private archives are not supplied. No application source was edited during that run. Existing ResizeObserver notifications still appear in the development log; deliberate error/denied-file fixtures also log expected failures. This is a passing automated suite, not a claim of a silent console or manual acceptance.

**Pending:** branch CI, migration/function/frontend deployment, verification of the actual retention job, Claude's running-product review and Sean's private beta gate. The launch branch has not been pushed or deployed. Sean's original checkout and its pre-existing edits remain untouched; no private migration files, credentials or personal workspaces were used. Continue in order with **L1-T1**, the per-project hosted repository transport. Stop at the private beta gate for Sean's hands-on acceptance as instructed in the brief.

## Implementation log — L1

### L1-T1 — Connect existing-page editing to the project's hosted helper (12 September 2026)

Added the hosted client transport behind the existing `storage.repository` interface. A hosted project tab connects to its fixed first-party `/editor-api/builder-repository` endpoint with its current Supabase session and selected project ID. The server must approve the project and return its managed website folder before source operations begin. Local folder preferences cannot redirect it. Existing Pages, source drafts, reviewed changes, Git status and preview controls share one connection interface and retain the existing action/result/error shapes.

The connection follows sign-in validity and the bounded server lease. Expiry, revoked access, failed sign-in checks, connection loss and account changes end requests while retaining appropriate recovery data. Account changes invalidate late responses; token refresh and a restored document can establish a fresh connection. A changed server folder requires reopening the project. Writes are never automatically retried, and timeout/disconnect messages explain that an accepted operation may have finished. Recovery identities separate account, project, transport and folder.

Hosted projects use this transport by default on the launch branch. **Export & handoff** shows the hosted connection, the managed folder and **Refresh connection**. **Use a helper on this computer** opens a separate tab for the same project and keeps the existing approval flow. A tab's helper choice stays fixed, so reconnecting cannot silently move open edits or reviewed operations to another folder. Direct local sessions remain local. Hosted source drafts now say **Edits saved**, without claiming they live on the editor's computer. Hiding developer folder/Git details remains L3.

Hosted frame URLs must belong to the current project's first-party preview path. Source frames require a valid nonce and check the sending window. Both canvas and window previews use an opaque sandbox so project scripts cannot access editor storage or the parent document. The real cookie-protected HTTPS proxy and authenticated asset/module delivery remain L1-T4; the fixture's CORS adapter is not a production security policy.

**Files:** client `hostedRepositoryConnection.ts`, `repositoryConnection.ts`, `repositoryMode.ts`, `storage.ts`, `BuilderApp.tsx`, `HostedRepository.tsx`, `RepositoryPanel.tsx`, `RepositoryBuild.tsx`, `SitePages.tsx`, `SitePageEditor.tsx`, source editing/build/selection hooks and `ProblemReport.tsx`; `scripts/builder-source-preview.ts`; the new transport unit tests and hosted browser fixture/journey; existing source, runner, companion and native-backup browser scenarios; CI; [the editing guide](visual-builder.md#existing-site-canvas) and [the transport contract/runbook](website-releases.md#hosted-repository-transport).

**Local proof:** all **294 unit/integration tests across 50 files** pass with Nginx enabled. The 12 transport tests cover authenticated request shape, origin/project/folder restrictions, invalid leases, absent/expired/failed sign-in, token refresh, reconnection, denied/conflicting writes, account-change races, cancellation and bounded unknown outcomes. TypeScript/Astro reports zero errors/warnings and the same 172 hints. The full browser run passes **46 scenarios**; only the optional licensed UI8 archive scenario is skipped. It includes the original local-helper consent/reload/revocation flow, real Nginx publication, native backup restoration and original source interactions.

The additional hosted command passes **10 scenarios**: `BUILDER_TEST_REPOSITORY_TRANSPORT=hosted pnpm test:builder:browser tests/builder/hosted-repository.spec.ts tests/builder/site-*.spec.ts`. The existing source journeys retain their temporary-repository, real-build, hydration, image, apply, commit-isolation and recovery assertions behind the fixture API. The hosted-default journey checks a stale local folder preference is ignored, the managed folder is read-only, access loss preserves edits and disables review, preview URLs/nonces are rejected when invalid, and storage/parent access each fail from the sandbox. The two standalone HTTPS helper checks remain part of this command; they do not establish hosted cookie authorization. CI now includes this hosted repeat after the full browser suite.

Fixture development exposed URL rewriting, fake workspace loading, response-drain and asynchronous image-apply issues; these were corrected without weakening the source/Git checks. A baseline run was interrupted after finding two obsolete preview-link selectors; the updated button selectors and the final complete baseline/hosted runs pass. Existing ResizeObserver notifications remain visible; no silent-console claim is made. The final browser runs held source steady.

Desktop and phone screens were inspected, including a 390px horizontal-overflow check: [desktop](handover/builder-launch/l1-hosted-helper-desktop.png), [phone](handover/builder-launch/l1-hosted-helper-phone.png).

**Pending:** the actual hosted service, managed clones and membership enforcement are **L1-T2**; queues, authenticated previews, Save to website, Kaizen settings and operations follow in order. Do not deploy the new hosted default by itself. Branch CI, deployment, Claude's stage review and Sean's private beta acceptance remain pending. No push, live deployment, credentials or personal workspace changes were made. Continue with **L1-T2**.

### L1-T2 — Protected hosted working copies and interactive actions (13 September 2026)

Added `scripts/builder-hosted-helper.ts`, started with `pnpm builder:hosted-helper`, on Linux loopback only. It serves the existing repository action shapes and imports the existing repository/source, drafts, Git-status, build-review and native-backup modules. Supabase Auth verifies each bearer token before the request body is read. The existing `builder_project_access` function checks current membership and archived state as that user, before disk access and again after waiting for the project's lock. The service needs an API key alongside the caller's token; it does not use a service-role key or trust the old editor cookie by itself.

Operator configuration fixes each project ID, SSH repository address and branch. The service clones into its own private working-copy directory with that project's deploy key and pinned host-key file, held in a separate credentials directory. Git/SSH receive a restricted environment, strict host verification and no agent, prompt, global configuration or service secrets. Completed clones move into place; existing folders are checked against their stored configuration, origin and branch. An explicit fetch updates remote-tracking refs while leaving the working tree and HEAD intact. Nothing is pulled, committed, pushed or published implicitly.

Every project has a bounded serial queue and a filesystem lock that also excludes another service process. Different projects can progress independently. A stale lock is retained for operator inspection rather than guessed away. Repository/source roots come from the configured project; escaped, linked and mismatched roots are rejected. Existing source path and byte-conflict checks remain authoritative. Saved source drafts are separated by account as well as project/folder, reviewed plans and backup downloads belong to their reviewer, and an apply attempt consumes its issued plan so it cannot be repeated. Unknown internal/SSH errors return fixed messages rather than exposing credentials or raw process output. HTTP concurrency, body size, encodings and waits are bounded.

The real service's latency exposed a navigation race after apply: the editor could offer **Back to pages** while the next saved draft was still loading, then reject that click. Navigation now waits through the active apply/load transition and the saved-state line says **Reading saved edits**. The local developer paths retain their original source editing and backup flows.

**Files:** new `scripts/builder-hosted-auth.ts`, `builder-hosted-folders.ts`, `builder-hosted-helper.ts`; `client/visual-builder/hosted-helper.spec.ts`; reusable `tests/builder/hosted-helper-fixture.ts` and the real-service browser scenario; the existing hosted transport fixture bridge; `SitePageEditor.tsx` and `useSourceEditingDraft.ts`; the package command, environment example, repository-config example, CI path filters, [service runbook](website-releases.md#hosted-working-copy-service) and [editing guide](visual-builder.md#existing-site-canvas).

**Local proof:** all **307 unit/integration tests across 51 files** pass with Nginx enabled; TypeScript/Astro reports zero errors/warnings and the same 172 hints. The 13 new service tests use real loopback HTTP, Git seed/bare/working repositories and `git-upload-pack`; the SSH connection and Supabase HTTP responses are fixtures. They cover project-specific key selection and safe process environment, missing/expired/forged/unavailable auth, membership/archive/revocation, account/project draft and plan isolation, apply/replay/conflict handling, fetch without replacing edits, competing applies, concurrent/stale locks, private backup access, upload/type/origin restrictions, path traversal, symlinked source/credentials/roots and unsafe key modes/configuration. The existing database tests execute the actual membership/access function and remain green; these fixtures do not claim a live Supabase or Git-host check.

Eleven relevant browser scenarios pass, including the real hosted-service inspect/draft/review/apply/reopen/revocation journey, the approved local helper and existing source journeys. A further twelve-scenario run passes the hosted transport canvas fixtures plus legacy native-backup/source-editing journeys. Native backup and the standalone HTTPS helper checks keep their existing local/fixture transport in that command. Final screenshots were refreshed by another passing real-service browser run, with desktop/390px layout checks: [desktop](handover/builder-launch/l1-hosted-service-desktop.png), [phone](handover/builder-launch/l1-hosted-service-phone.png). These show editing through the service before hosted build execution; they are not a built hosted canvas or a WYSIWYG gate acceptance.

The initial integration failure was an incorrect command-path expectation in the SSH fixture. The first browser run found the navigation race above; a later test revoked access before reopening had finished reading its draft, and was corrected to establish the intended already-open editing session first. The final runs hold source steady and pass. No test used Sean's personal workspace or a live credential. Existing development console limitations remain recorded; no silent-console claim is made.

**Pending:** **L1-T3** enables queued build execution, followed by protected HTTPS previews in T4, commit/push in T5, Kaizen/project provisioning in T6 and full service operations in T7. Arbitrary hosted folder restore and opening a repository archive are explicitly unavailable through these interactive actions; they cannot create/replace unconfigured server folders, and the approved local helper remains available for those developer operations. Resolve their hosted UI/routing when completing project setup. Do not deploy the intermediate hosted default as a finished editor. Branch CI, live Git/Supabase/proxy checks, deployment, Claude's stage review and Sean's gate acceptance remain pending. Continue in order with **L1-T3**.

### L1-T3 — Queued hosted builds, live logs and cancellation (13 September 2026)

The hosted helper now accepts reviewed builds into an account/project-bound queue and exposes queued, building, succeeded, failed and cancelled states through the existing repository actions. One build runs per project, with two projects building concurrently and bounded waiting/history/log storage. The runner still validates the reviewed command and original source fingerprint, executes the package script, preserves previous output and creates its static snapshot. Job IDs, timestamps, exclusive claims, bounded logs and explicit terminal outcomes follow the existing publication-job conventions; preview builds do not create publication jobs or activate destinations.

Access is checked when the job is accepted and again when it starts. A queued job whose session expired, membership ended, review expired or source changed fails without running unreviewed code. Start reviews are consumed once; another account/project cannot start, read or cancel the job. The captured bearer token remains in memory only until execution authorization or cancellation. Build processes receive a restricted environment and private project home/temp directories, without inherited service credentials or an SSH agent.

A separate build lock excludes apply, fetch and native-backup capture through execution and recovery, including across service processes. Source inspection, saved drafts, status/log polling and cancellation remain available. Queued cancellation prevents execution; running cancellation stays pending until the child and recovery finish. Graceful shutdown cancels outstanding work and closes snapshots. Changed or stale lock files require operator inspection. This reuses the existing runner; it does not replace source editing or the approved local helper.

The page editor and Export & handoff show queue/progress states and offer **Cancel build**. Reopening follows an active job and preserves a failed or cancelled result until **Try building again** or **Build again** is chosen. Unapplied edits and saved drafts remain available. A successful hosted build currently reports completion without returning the VPS's loopback preview URL; authenticated browser preview delivery is the next task, **L1-T4**.

**Files:** new `scripts/builder-hosted-builds.ts`, additions to hosted-helper/folders and the existing runner; `RepositoryBuild.tsx`, `useSiteBuild.ts`, `SitePageEditor.tsx`; new `hosted-builds.spec.ts` and controlled-build fixture, the real-service browser journey and updated existing build/backup/source selectors; [the build-job runbook](website-releases.md#hosted-build-jobs) and [editing guide](visual-builder.md#existing-site-canvas). Existing CI paths already include these files.

**Local proof:** all **317 unit/integration tests across 52 files** pass with Nginx enabled. Ten new build tests cover per-project serialization, global concurrency and queue capacity, bounded live logs, real successful/failed/cancelled processes, output restoration, responsive drafts, competing/stale build locks, account/project access and replay, revoked/expired queued authorization, changed source, child-environment restrictions and graceful shutdown. One controlled startup-race test holds the runner before returning its job ID and proves cancellation remains pending until process/recovery completion. The integration fixtures use real temporary Git repositories, loopback HTTP and build subprocesses; SSH and Supabase responses remain fixtures. TypeScript/Astro reports zero errors/warnings and the same 172 existing hints.

All **13 relevant browser scenarios** pass, including the real-service queue/reopen/cancel/failure/log/draft journey, existing local-helper consent and actual canvas builds. A further **12 scenarios** pass with the hosted transport canvas fixtures plus existing native-backup and source-editing journeys; the latter retain their developer transport. Initial new browser attempts exposed fixture setup/selector mistakes: configuring build controls before connection completed, and searching for the original heading after a restored draft had changed it. The corrected scenarios establish those states explicitly and pass. Final runs held source steady. Desktop and 390px screenshots were inspected with horizontal-overflow checks: [desktop](handover/builder-launch/l1-hosted-build-queue-desktop.png), [phone](handover/builder-launch/l1-hosted-build-queue-phone.png). These show build queue controls, not acceptance of a completed hosted canvas.

**Pending:** **L1-T4** protected HTTPS snapshots, then Save to website, Kaizen/project setup and operations in order. Job history is currently in memory and never automatically replayed; crash recovery evidence, service-manager cleanup and disk retention are L1-T7. The restricted child environment is not filesystem isolation: only trusted Kaizen code belongs in this beta, and the client-build isolation decision remains L5-T4. Dependencies still require operator provisioning. Branch CI, live deployment, Claude's stage review and Sean's private beta acceptance remain pending. No push, publication, live credentials, private migration files or personal-workspace changes were made. Continue with **L1-T4**.

### L1-T4 implementation — private HTTPS website previews, 13 September 2026

**Shipped locally:** the hosted service now presents the runner's frozen snapshots at `/editor-preview/<project>/<build>/<view>/…`, with a canonical HTTPS origin configured on the server. Successful build status returns that address while the snapshot is available. The real canvas and source-selection fallback reuse source fingerprint/route checks and the existing editing bridge. Original source and built files are not rewritten by preview delivery; an explicit reviewed apply remains the source mutation.

A separate random, host-only, HttpOnly editor session authorizes private documents. The legacy unsigned Studio flag cannot grant access. Each document, bootstrap and asset read verifies sign-in, current edit membership and the account/project/build scope. Cookie leases, one-use bootstrap grants, frame grants and retained views are bounded; sign-out invalidates the session, and account changes cannot let an old logout disconnect the new account. The canvas keeps its opaque sandbox, denies parent/storage/cookie access and retains CSP restrictions on network calls, forms and subframes.

Authenticated HTML supplies a private asset namespace and a short-lived bootstrap credential. A parser-blocking bootstrap installs the opaque frame's partitioned cookie before original assets load. Local scripts and imports carry that cookie; frozen local fonts are embedded within documented size limits. URL/import/asset transformations preserve ordinary visible JavaScript text. Public preview URLs do not reveal the private namespace or grant asset access. General preview links ask the trusted wrapper to navigate within the same project/build/view after sender/nonce/path checks. Apache/DirectAdmin route ordering, canonical-origin configuration, cookies, CSP, cache/log rules and deployment checks are documented in [the release runbook](website-releases.md#protected-https-previews).

**Proven:** all **331 unit tests across 53 files** pass, including dedicated negative tests for anonymous/forged/legacy/duplicate cookies, another account/project/frame, null-origin requests with only the public URL, membership removal, archived projects, Auth outages, sign-out during authorization, expired/replayed grants, traversal, unsafe methods, stale source and closed builds. Tests also prove frozen output survives later `dist/` changes and that session/navigation races cannot cross account/view boundaries. `pnpm typecheck` passes with **zero errors and zero warnings**; the **172 existing hints** remain.

All **14 relevant browser scenarios** pass. The new scenario uses a real temporary TLS reverse proxy, real hosted service and actual Astro/React build, with third-party cookies restricted. It proves font/image/style loading, React hydration, source-matched link/image selection, desktop/phone layout, direct text editing, reviewed apply, a new rebuilt frame URL, the original React counter interaction and internal link navigation. A forged navigation message, an anonymous copied URL and later membership removal are denied. A further **12 scenarios** pass with the hosted transport fixtures and existing source/native-backup journeys; the latter retain their developer transport. [Desktop](handover/builder-launch/l1-hosted-https-preview-desktop.png) and [390px phone](handover/builder-launch/l1-hosted-https-preview-phone.png) screenshots were visually inspected and checked for horizontal overflow.

Early attempts exposed the opaque-frame cookie/navigation requirements and test mistakes: cold Vite dependencies, clicking an interactive component while canvas selection owns clicks, selectors retaining edited text, leaving the editor before the new build appeared, and omitting the Back to pages step. The final test waits for the new frame URL and verifies original interaction in the general preview. Final checks held application source steady. The expected stale-source conflict during rebuild is not a console-clean claim.

**Pending:** **L1-T5 Save to website**, then Kaizen/project setup and operations. The current proof is automated Chromium over fixture TLS/Auth/Git transport, not a live Apache rollout or Sean's Kaizen acceptance. Restricted previews intentionally disable remote application calls/forms/frames; direct script navigation or routers requiring same-origin storage/history may not work. Wider browser verification and isolation for untrusted client builds remain L5. Service restart/disk procedures remain L1-T7. Branch CI, deployment, Claude's stage review and Sean's private beta gate are still pending. No push, publication, live credentials, private migration files or personal-workspace changes were made. Continue in order with **L1-T5**.

### L1-T5 — Save applied changes to the configured staging branch

**Shipped on the launch branch, 13 September 2026:** configured hosted projects now offer **Save to website** after apply, with a change summary, staging link, deployment status and optional save details. The page canvas and repository handoff share `RepositorySave.tsx`; the canvas refreshes its changed-file count after committing. Existing local **Commit these changes** behavior is preserved. A separate hosted `repository-save` action makes the push explicit; the older commit action cannot unexpectedly start pushing.

`builder-hosted-saves.ts` reuses `RepositoryCompanion.commit` and `builder-repository-git.ts` to commit only applied files as the verified account's name/email. The service checks the local and remote base, applied bytes, staging area, merge state, build lock and current membership. It proves the commit's actual single-parent relationship before an exact expected-value push lease; remote advances and rewinds at push time fail without replacing remote work. Git status and commit/push use the restricted hosted Git runner, with hooks, replacement objects, URL rewrites and configuration includes prevented from redirecting or disguising these operations.

Rejected or uncertain pushes retain the commit for an explicit retry. Read-only **Check saved state** can resolve a lost acknowledgement without pushing. Reopening restores this account's receipt while the helper remains running, and an unsent commit blocks another apply. `builder-hosted-save-release.ts` observes the configured GitHub staging workflow for the exact commit/branch, keeping deployment failure or unavailable tracking separate from Git save success. Operator staging configuration, deploy-key write access, optional server-only Actions read token, the lease invariant and recovery limits are documented in [the release runbook](website-releases.md#save-to-website), `.env.example` and `hosted-repositories.example.json`. CI path filters include the new shared save type and the reused Git module.

**Proven:** `pnpm test` passes **345 tests in 54 files**, including the Nginx-backed check with the test binary configured. `pnpm typecheck` reports **0 errors, 0 warnings and 172 existing hints**; the final TypeScript-only check also passes. Thirteen new save integration/unit tests use temporary repositories and a real bare remote behind fixture SSH. They cover verified authorship, file isolation, changed/staged work, invalid account details, foreign plans, moved branches, build locks, replacement-ref deception, restricted Git status, rejected pushes, lost acknowledgements, access revoked after committing, explicit retries and workflow status filtering/failures. The transport test also verifies capability advertisement and clearing it on sign-out.

**18 relevant browser scenarios pass**, including real HTTPS Astro/React canvas editing, apply/rebuild, staged-file refusal, a real remote hook rejecting the push, reopening, a read-only status check, retrying the same commit, the staging link and queued-to-success workflow status. Unrelated work remains out of the remote and no extra commit is created. Local helper, media, source editing and repository handoff regressions pass. A further **10 scenarios pass with the hosted transport selected**; the standalone loopback TLS check retains its intentional local boundary. Save screens were checked at desktop and 390px phone widths with no horizontal overflow: [ready desktop](handover/builder-launch/l1-save-ready-desktop.png), [ready phone](handover/builder-launch/l1-save-ready-phone.png), [saved desktop](handover/builder-launch/l1-save-complete-desktop.png), [saved phone](handover/builder-launch/l1-save-complete-phone.png).

**Pending:** **L1-T6 Kaizen and per-project setup**, then operations. Receipts are currently in memory; durable restart recovery remains L1-T7 and unknown saves require operator inspection, never automatic replay. Workflow responses, Auth and SSH identity are fixtures; the Git commits/pushes, HTTPS proxy and preview builds are real operations on temporary test repositories. These checks do not prove live GitHub/Apache deployment, served staging content or Sean's Kaizen acceptance. No launch-branch push, live deployment/publication, credentials change, private migration access or change to Sean's original checkout was made. Branch CI, Claude's stage review and Sean's gate acceptance remain pending. Continue in order with **L1-T6**.

### L1-T6 — Owner repository setup and Kaizen wiring (13 September 2026, in progress)

**Implementation:** `RepositorySettings.tsx` adds owner-only repository SSH address/branch settings, private server key creation, a one-time copyable public half, key replacement before connection, and a remote connection check. `builder-hosted-settings.ts` persists versioned settings under the existing project lock, uses the existing folder/Git safety checks, preserves old folders/keys when retargeting, and stops after interrupted transitions. `builder-hosted-helper.ts`, the auth/build/save modules and `hostedRepositoryConnection.ts` enforce ownership, active-account changes, build/save guards and fresh connection after setup changes. Only operator-approved repository/branch pairs retain staging permission. The shared settings contract, configuration example, CI path filter and [operations guide](website-releases.md#owner-repository-setup) accompany the implementation. The implementation is packaged on the launch branch while Kaizen wiring remains incomplete.

**Local proof:** all **370 unit/integration tests across 56 files pass with Nginx enabled**, including the native publication described below. Astro/TypeScript reports **zero errors, zero warnings and 172 existing hints**. Nine real-Git/key setup tests cover empty admission, ownership/revocation/archive denial before disk access and after lock waiting, stale versions, invalid hosts/paths/credential fields, key rejection/replacement, restart readback, pending source/commits/builds, retained setup changes, old-plan invalidation and interrupted/linked files. Transport tests include setup before connection, fixed project/session boundaries, stale key responses after an account change, independent server approval for production and removal of both deployment capabilities until reconnection. **15 affected browser scenarios pass**, covering setup, preview/build, save/publication, local commit isolation, repository transport, projects, Settings, diagnostics, Releases and real-Nginx client publication/rollback/unpublish. The Nginx browser case was initially skipped without its executable setting, then passed when rerun with the test binary. Auth, Git-host authorization, public deployment markers and workflow responses remain fixtures. This is not Sean's Kaizen acceptance.

Reproduce with `pnpm test`, `pnpm typecheck`, and `pnpm test:builder:browser tests/builder/hosted-publishing.spec.ts tests/builder/hosted-settings.spec.ts tests/builder/hosted-save.spec.ts tests/builder/hosted-helper.spec.ts tests/builder/hosted-preview.spec.ts tests/builder/site-commit.spec.ts tests/builder/hosted-repository.spec.ts tests/builder/settings.spec.ts tests/builder/diagnostics.spec.ts tests/builder/releases.spec.ts tests/builder/client-publication.spec.ts tests/builder/projects.spec.ts`. Set `KAIZEN_NGINX_BINARY` to the installed test Nginx executable for both unit and browser commands to include retained-release checks. Use the package test script so the runner inherits the package-manager entry point. The setup scenario was repeated after correcting its visibility assertions: creation, copy, dismissal and absence after reopening all pass. The publishing fixture now ignores Astro's generated metadata and its temporary dependency symlink; genuine unsaved work still blocks publication. [Desktop](handover/builder-launch/l1-repository-settings-desktop.png) and [phone](handover/builder-launch/l1-repository-settings-phone.png) screenshots were inspected; fields and controls fit without horizontal overflow. The pictured keys belong only to deleted test fixtures.

**VPS preparation and activation:** Sean supplied `kaizen-vps` and registered the dedicated helper deploy key. The private key stays on the VPS; pinned-host SSH reads of the intended repository succeed at `main` revision `aee20d6`. The separate staging deployment checkout and store are provisioned, Apache now routes `stage.kaizenweb.co.uk` to Nginx port 8093, and activation of `kaizen-staging-verified2-9aca128` passes all 259 HTTPS origin checks. This baseline copies production source `9aca128` and excludes Apache/PHP control files from the Nginx artifact. The source output and production/client-demo releases remain unchanged. Both original and rejected staging artifacts and configuration backups remain available. The staging branch, helper checkout/service/proxy and new frontend rollout are still pending; baseline activation does not establish the hosted editing journey.

**Supabase access and rollout:** Sean identified the management-token file in Downloads. Its project access was verified without printing the token, and a mode-0600 copy was restored to the documented private Linux configuration path. The exact L0 migrations `202609120001`, `202609120002` and `202609120003` were applied transactionally and recorded in the migration ledger. Live checks confirm Kaizen/client capability defaults, private error-table RLS and RPC grants, the active hourly retention job, and unchanged publication count/release head. A temporary scheduler check ran the real prune function successfully and was then removed; only the required hourly job remains. The four affected builder functions and matching frontend still require coordinated rollout. Access is restored; do not leave this work blocked on missing credentials.

**Native-source publication:** wiring inspection found that the existing publish function dispatched builder-owned content and a configured branch build without promoting staging source into production. `RepositoryPublish.tsx` now supplies a separate **Publish website changes** card in both Releases implementations. The new shared contract and `builder-hosted-publishing.ts` freeze the saved staging commit, current production base and staging release identity. The folder module proves ancestry from actual commit objects and pushes only that exact reviewed commit with an expected-base lease; it leaves the checkout on staging. The helper rechecks publish permission immediately before pushing. Operator-owned production approval is removed alongside staging approval on repository retargeting. Workflow tracking now distinguishes production and staging even when their commit is identical. The operations guide documents configuration, review, permission boundaries, retry and evidence limits.

The publishing browser scenario passes through real TLS, an actual Astro build and temporary Git remotes: edit → apply/rebuild → save stage → refuse an undeployed revision → review → rejected production push → reopen → read-only state check → explicit retry of the same commit → separately report production delivery. Twelve integration tests cover current publish permission, foreign/missing/expired reviews, changed staging deployments, clean-work/build guards, remote advances/rewinds, divergent history hidden by replacement refs, lost acknowledgements, strict target approval and bounded credential-free marker reads. Public deployment markers, Auth and GitHub workflow responses are fixtures; they do not establish live deployment or full served-byte verification. [Review desktop](handover/builder-launch/l1-publish-review-desktop.png), [review phone](handover/builder-launch/l1-publish-review-phone.png), [sent desktop](handover/builder-launch/l1-publish-sent-desktop.png) and [sent phone](handover/builder-launch/l1-publish-sent-phone.png) were inspected with no horizontal overflow. The L1-T6 implementation is ready for a fixed deployment commit, with the hosted rollout and real-use check still pending; L1-T7, branch CI, Claude's review and Sean's acceptance remain pending.

**Continue L1-T6:** finish the helper runtime and canonical editor proxy, create the reviewed staging branch with its guarded workflow, deploy the four affected builder functions with the matching frontend, and exercise the real hosted Kaizen build/edit/save/publication path. Keep deployment observations separate from Sean's manual acceptance. Preserve the production coordinator independently from staging. L1-T7 operations, branch CI, Claude's review and Sean's private beta gate remain pending.

**Release verification completion:** new native manifests require every public file and the exact source-bearing marker. Negative tests reject a wrong native About page, CSS, SVG or hashed JavaScript despite a matching homepage/marker, a missing file check and a changed marker commit. Apache/PHP control files stay in source and are omitted from Nginx artifacts; legacy manifests remain usable for rollback. All **377 unit/integration tests across 56 files** pass; type checking reports **0 errors, 0 warnings and 172 existing hints**. The real Nginx smoke passes activation, routing/redirect failures, automatic restoration and exact rollback. Its direct post-reload page assertions now allow the bounded graceful-worker handoff while still requiring the expected bytes. Staging's origin check verifies all **259 responses**. Cloudflare's managed robots.txt intentionally differs from the artifact, so origin verification and CDN observations are recorded separately, without relaxing checks or disabling protection. Deployment preflight tests prove staging cannot overlap production paths, symlinks or hostname; the optional separate staging Studio copy requires explicit enablement.

**Final deployment-preparation checkpoint:** all six affected browser scenarios pass with real Nginx enabled: owner setup, hosted publication, hosted repository access, guarded save/retry, client publication/rollback and project isolation. A separate workstation check through public Cloudflare matches **258 of 259** baseline files exactly; only `/robots.txt` differs and contains Cloudflare's managed policy. The origin check remains **259 of 259**. The commit packages the tested implementation for deployment; it does not close L1-T6's hosted rollout or Sean's acceptance.

**First branch checks:** draft PR #8 ran Deno, type checking, preflight, unit tests, worker compilation and both independent repository builds/browser checks successfully. Its runner step exposed missing `npm_execpath` under `pnpm exec`; CI now uses the explicit `test:builder:runner` package script. The initial staging workflow stopped at `cd` with permission denied because a legacy staging secret selected an obsolete checkout. Staging now uses dedicated repository variables with the verified paths/domain as defaults and keeps the production-overlap guard. The failed attempt did not build or activate anything. The initial helper runtime is installed at `/opt/kaizen-helper-releases/8eef7de`, and the transient `kaizen-helper-bootstrap` unit responds on loopback with anonymous operations refused. The helper's own stage checkout was cloned and verified at that initial revision; its clean bootstrap state must be reconciled to the corrected deployment candidate before use. These pre-deployment corrections are folded into the single T6 commit; the initial bundle and server checkout are retained, and branch updates require exact old-ref leases.

The corrected package-script runner was exercised against freshly generated, independently installed Astro and exported React repositories: both actual builds and private HTTP previews pass, including desktop/phone browser checks. Preflight refusal cases and workflow/package formatting checks pass. No application behavior changed in this CI invocation correction.

**Studio build boundary:** deployment preparation identified an overly broad Studio environment prefix. The prefix now exposes only public Studio settings, and a real build with three synthetic server credentials passes a scan of all 57 output files while preserving the public project configuration. CI runs this check. A sanitized rebuild of the existing production source `9aca128`, using the identical frozen publication snapshot, is live as `kaizen-studio-safe-20260913`; the database head confirms that release and all 259 origin checks pass. The first activation encountered Cloudflare's managed robots response and restored its verified baseline. Production now has the same explicit HTTPS origin mapping as staging. A preparation retry was refused before activation because its input was inside the release store; the input was moved to an isolated temporary folder before a fresh coordinated request. Staging now serves `kaizen-staging-studio-safe-20260913` and also passes 259 checks. The separate Studio host contains no affected server-token settings and was not changed. Provider-side revocation remains an operator action; corrected files alone do not invalidate downloaded credentials. No launch frontend or new builder functions were deployed by this mitigation.

**Earlier rollout checks:** the staging checkout's fetch configuration now tracks `origin/stage`; the second workflow had stopped before building because that ref was absent. The first canonical helper proxy attempt failed its anonymous HTTPS probe and restored the original Apache configuration; the corrected route is verified below. The next branch CI reached the real client Nginx smoke and exposed an observation during graceful worker handoff after rollback. Its follow-up assertions now allow bounded handoff while requiring the same complete byte checks. The current broken-route test also covers unchanged immutable assets, using the response identity described below. The real Nginx client smoke passes activation, failure restoration, unpublish, exact rollback and desktop/phone checks. Full branch CI, final helper wiring, coordinated function/frontend deployment and Sean's hands-on gate remain pending.

The security follow-up passes all **377 unit/integration tests across 56 files**, with real Nginx enabled, and type checking reports **zero errors, zero warnings and 172 existing hints**. Supabase confirms the sanitized production release as its head, no pending release and unchanged builder-publication count. Sean's original checkout retains its existing 14 pending files.

The client Nginx CI command also passes with a freshly generated and independently built media export: 13 files and 15 HTTP responses, followed by the same isolated-client, rollback and desktop/phone checks. The launch branch receives the corrected candidate for CI first; do not rerun deployment from the earlier staging source that lacks the Studio environment fix.

**Verified helper routing:** the dedicated Apache probe reproduced the old rule's duplicated path (404) and the explicit capture/backreference's correct helper path (401). The probe initially needed its own MIME configuration before it could start. The corrected canonical proxy passed Apache configuration validation and is active. Origin HTTPS checks prove anonymous POST rejection (401), wrong-origin rejection (403), and an unavailable preview (404), all with no-store/nosniff and no issued cookie. A unique fake preview path was absent from the canonical access log. Public HTTPS also returns the helper's expected 401. Production and staging still serve their sanitized baseline releases; this does not claim an authenticated build/edit/save journey or an upgraded helper runtime.

**Consistent release responses:** the third branch CI passed Studio secret isolation, all unit tests and independent builds/browsers, then stopped when its immediate next activation observed a worker from before the preceding reload. The release engine now observes the entire baseline with its existing bounded retry before any mutation. Newly staged artifacts also record `responseIdentity: release-id-v1` in both the hashed marker and manifest. Nginx supplies the release ID on each response, and verification requires it for every file and redirect, so a marker from one worker cannot combine with matching asset bytes from another. Exact legacy configurations and older artifacts remain readable and can be upgraded without rewriting retained files. Negative tests cover missing/wrong response IDs, redirect responses, removal of the manifest requirement, and a baseline that never verifies. Both real Nginx smoke commands pass, including the unchanged immutable asset behind a broken new route, rollback and the independent media export. The source correction is not yet deployed, and green branch CI remains required.

**Hosted build prerequisites:** the service-owned Kaizen checkout has frozen root and Studio dependencies and ignored, mode-0600 build settings containing only public project configuration. Provisioning used the project's own lock and build guard, checked the approved repository and `stage` branch, refused changed source or conflicting settings, and preserved its clean initial `8eef7de` revision. A fresh service check confirms that runtime is running with no project or build lock. Reconcile it to the final verified staging revision before use; this setup did not build, save, push or publish a page.

**Current local verification:** the response-identity correction passes all **385 unit/integration tests across 56 files**, both real Nginx smoke commands, and type checking with **zero errors, zero warnings and 172 existing hints**. The full browser rerun passes **52 scenarios with one optional licensed-archive skip**, with real Nginx enabled. The preceding run exposed an ambiguous access-revocation assertion because both the preview and draft-save notices reported the same denial. The assertion now identifies the draft-save notice by its retry control; the scenario still requires application to be disabled and recovery download to remain available. Both focused hosted-helper scenarios and the complete rerun pass. No product access check was weakened. Supabase readback still reports the sanitized production artifact, no pending release and the earlier four builder function versions. Green branch CI, matching function/frontend rollout, final helper revision, Claude's review and Sean's Kaizen acceptance remain outstanding.

**Verified rollout candidate:** `c3b7d0a` passes the complete [builder CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34763958716), including both real Nginx smoke commands, the full browser suite and the additional hosted-transport scenarios. Its inactive VPS helper runtime is prepared with the matching frozen dependencies, root ownership and the restricted Studio environment prefix; importing the service as `kaizen-helper` succeeds. The serving process and managed website source remain at the retained bootstrap revision. Credential replacement and the coordinated live rollout remain open. Operations work below can proceed without those credentials; this does not close T6's live Kaizen check or Sean's acceptance.

**Live rollout — 13 September 2026:** `e747452` passed the complete [builder CI](https://github.com/KaizenUK/kaizenNEW/actions/runs/34769638375). The normal workflows then deployed it to [staging](https://github.com/KaizenUK/kaizenNEW/actions/runs/34770525644) as `gh-34770525644-1` and [production](https://github.com/KaizenUK/kaizenNEW/actions/runs/34770924919) as `gh-34770924919-1`; each passed **259 complete HTTPS origin response checks**, including release identity and file bytes. Production's database head matches the live artifact and has no pending release. The separate production Studio copy succeeded. The client-demo artifact remains `300d7710-69c9-46fe-82ed-5e940997d80d`.

The four matching Supabase functions are deployed: `builder-projects` version 8 and `builder-publish`, `builder-content` and `builder-contact` version 7, all active with their existing handler-based authentication (`verify_jwt=false`). Prior function bodies are privately retained; unrelated functions and database configuration were not changed. The helper runtime and its clean, service-owned staging checkout now use `e747452`, with private build settings preserved. The active transient unit is `kaizen-helper-rollout-e747452.service`; the earlier bootstrap is stopped. Permanent service installation remains T7. Canonical origin probes return 401 for unsigned requests and 403 for foreign origins, with no-store, across the helper and authenticated builder functions. Cloudflare rejects direct workstation script probes separately; the actual signed-in browser succeeds.

**Actual hosted journey:** the signed-in production builder listed Kaizen's existing source pages without a local helper. Opening `/about/`, reviewing its real build command and clicking **Build** produced a VPS build and the rendered HTTPS canvas. Desktop and phone previews were visually inspected, with zero unapplied changes and clean tracked website source. Releases reports the new production deployment as **Live now**; native publication review correctly reports that production already contains the saved staging revision. No test content was applied or published. Computer-use input could not complete a click inside the scaled iframe because its coordinate inspection rejected the frame; this is not evidence of successful manual typing. The automated edit/save/publication scenarios remain the implementation evidence, and Sean's own edit/save/publish check and gate acceptance remain open. Continue T7 and the ordered beta tasks; the credential request is fulfilled.

### L1-T7 — Hosted service operations (in progress)

Added a loopback-only `/health` liveness response, without credentials, account or project details, cookies, or a Supabase request. It rejects foreign Host values, Origin, forwarded headers and browser fetch-site headers. A healthy process is not a claim that every project, credential or deployment works. Shutdown is idempotent and waits for build cancellation, project operations and connection closure; the health response changes to stopping during shutdown.

The systemd template uses the dedicated helper account, a process-held service lock, control-group termination, bounded restart attempts, a restricted writable filesystem and a separate journal namespace with retention and size bounds. Its package-manager path is explicit, so reviewed builds can run when the helper starts directly through Node. These templates are not installed as the permanent service yet. VPS systemd validation using the prepared runtime path, TypeScript compilation and **26 focused helper/auth/health/shutdown/build tests** pass. Initial probe tests were corrected to send an exact Host header through Node HTTP, since Fetch can replace it; the server continues to reject that hostile Host. Full Astro checks and the complete regression suite remain required after the rest of T7 is implemented.

Save and publication receipts now survive helper replacement in bounded, private files outside the checkout. Applying files and committing record their intent before mutation; publication records uncertainty before pushing. Fresh account/project access and the configured destination are checked again when reopening. Completed applied hashes restore an explicit save; a lost push acknowledgement is resolved by reading the remote. A partial apply or an unknown commit retains an operator-check state and offers no automatic replay. Invalid, oversized, linked, public-readable, retargeted or concurrently changed records are refused while preserving existing files and commits.

**Focused recovery evidence:** **62 save/publication/settings integration checks** and **32 transport/build checks** pass, plus TypeScript compilation. Three actual browser scenarios pass: publication, save/rejected-push recovery across a fresh helper, and an interrupted commit with no second save action. The restart scenario found that an unavailable old preview returned HTTP 404, which the transport correctly treated as a missing helper. Unavailable build IDs now consistently return 410 after the same account/project checks, preserving the connection for recovery; permission denials still disconnect. These are temporary repositories and fixture accounts/remotes, not the live VPS restart check or Sean's acceptance.

**Credential follow-up:** Sean's updated Sanity token authenticates locally and from the VPS. The requested local/VPS/Supabase replacements are verified; the launch workflow now reads the separate VPS environments instead of forwarding the old GitHub token. The small follow-up is its own commit, `e747452`, on top of the previous green candidate. See [rotation configuration](handover/sanity-token-rotation.md). Its branch CI and coordinated rollout are separate from the uncommitted T7 operations work. No further Sanity precaution decision is pending.

**Complete regression checkpoint for receipt recovery:** **417 unit/integration tests across 57 files** pass with real Nginx enabled. Full type checking reports **zero errors, zero warnings and 172 existing hints**. The first complete run identified one older unavailable-build expectation that still required 404; only that API expectation changed to 410, preserving the separate public-path 404 denial. Astro checking also required a test's array indexing and callback type to match the existing target. The corrected full runs pass. [Recovery desktop](handover/builder-launch/l1-save-recovery-desktop.png) and [recovery phone](handover/builder-launch/l1-save-recovery-phone.png) show the preserved source, operator-check message and read-only status control; both were inspected after loading finished, with no horizontal overflow. This checkpoint does not finish the rest of T7 or the full end-of-stage browser suite.

**Storage implementation checkpoint:** `builder-hosted-disk.ts` measures complete project storage without following links and checks filesystem free space. Default bounds are 8 GiB per project and a 2 GiB free-space reserve, with serial checks during clones, fetches and hosted builds. Apply and Save reserve capacity from verified file sizes before their first mutation; draft writes reserve serialized bytes. A refused Save remains applied and retryable. An oversized build is stopped, its previous output restored and its failed output retained. GNU `du` is a Linux runtime dependency; these are monitored bounds for trusted beta code, with possible growth between samples, not hard quotas or client-build isolation. Existing snapshot size/entry/count caps remain in place.

**Storage proof:** all **425 unit/integration tests across 58 files** pass with real Nginx enabled; full type checking reports **zero errors, zero warnings and 172 existing hints** across 349 files. The targeted storage/save/publication/settings run passes **70 checks**, and the build/health/storage run passes **19 checks**. Eight new cases cover malformed limits, sparse and ignored files, external links, linked roots, free-space/write reserves, an oversized clone, pre-build refusal, growth cancellation/restoration, Apply preservation and retryable Save capacity refusal. A test initially assumed no receipt file existed before Apply; setup already creates an empty record, so it now proves the exact prior bytes remain unchanged. The initial direct Vitest command lacked the package-manager context required by reviewed builds; the documented `pnpm test` runs pass. The live project/free-space measurement fits the defaults, but enforcement is not deployed yet.

**Build recovery implementation checkpoint:** hosted builds now persist a bounded private record before moving output, then finish it after the process and output restoration. An interrupted or unresolved build blocks new builds, source/Git writes, publication and repository changes without replay. Source inspection and saved drafts remain available when the project operation lock can be acquired. If completion cannot be persisted, the preview closes and the job reports an operator check. Generated-output retention runs before storage admission, keeps two recent completed outputs and only removes recorded terminal UUID directories. It preserves unknown files, source recovery, previous setups, links and other filesystem devices. A durable pruning phase can resume an interrupted cleanup. The [operations guide](website-releases.md#hosted-helper-operations) records the reconciliation procedure.

**Build recovery proof:** all **435 unit/integration tests across 59 files** pass with real Nginx enabled; full type checking reports **zero errors, zero warnings and 172 existing hints** across 350 files. The focused build/storage/settings run passes **35 checks**. Ten new cases prove intent before mutation, refused persistence without changes, restart guards with readable source/drafts, terminal-only retention, interrupted cleanup, unexpected files/links and changed binding, malformed/foreign/private-file records, failed completion, failed restoration and bounded history. One initial test supplied a public URL instead of the source-inspection API's repository-relative route; its corrected read-recovery assertion passes. A final filesystem-parent/device check was followed by all ten retention/recovery tests and TypeScript validation. No browser or manual VPS acceptance is implied by these local checks.

**Remaining:** complete the operational runbook and permanent service installation, live restart/storage checks, end-of-stage browser verification and the T7 commit. The verified `e747452` transient helper remains serving while this task is implemented. The storage and receipt changes remain uncommitted together as T7.

**L1 browser boundary and rollout preparation — 13 September:** the complete Chromium builder suite passes **53 scenarios with one optional licensed-archive skip**, with real Nginx enabled. This includes hosted source editing, saving, explicit publication, interrupted-commit recovery and owner setup, alongside the local developer helper and existing builder journeys. Existing ResizeObserver notifications remain visible in the test log; they were not suppressed to make the suite pass. The operations guide now includes installation, preservation and rollback steps. The VPS cloud-init hosts template contains both existing HTTPS origin mappings and passes a rendered-template check without a reboot or changing current resolution. An independent root-owned runtime dependency directory is prepared. Sean's newly applied About-page source and its recovery/draft state are present and must be preserved during activation. The commit packages the verified T7 implementation; permanent activation, live restart/storage readback and branch CI are the remaining rollout checks.

**Runtime-link correction:** the real systemd switch exposed a CLI startup check that compared a symlink path in `argv` with the resolved module path. The direct-path preflight passed, but the permanent unit exited successfully without listening. The previous `e747452` transient service was recreated and restored, with Sean's About-page bytes unchanged and the private project backup retained. The helper now resolves its entry path before starting; a real subprocess test proves startup, loopback health and graceful shutdown through a runtime symlink. The corrected full suite passes **436 tests across 59 files**, and full type checking remains at **zero errors, zero warnings and 172 hints**. The corrected revision still needs its exact VPS link-path activation and branch CI; the earlier browser proof is unchanged, and CI repeats the full browser suite on the corrected revision.

**Permanent VPS activation:** the corrected `3688a08` runtime passed a preflight through the actual `/opt/kaizen-hosted-helper` link, then the installed `kaizen-hosted-helper.service` became active and enabled. A real restart changed its process ID from `350077` to `350135` and returned healthy. Sean's About-page edit, Git state, saved draft and source recovery matched the private pre-upgrade snapshot, with no remaining operation locks. The second-process service lock was refused. HTTPS API probes preserve 401 for unsigned requests, 403 for foreign origins and no-store. The installed journal namespace is active with the documented bounded policy. Actual project usage fits the default limit; the same runtime refused a deliberately lower read-only capacity threshold. No source was reset, committed or published by these operations checks.

**CI recovery-browser correction:** the first candidate's CI passed logic, types, independent builds and Nginx checks, but its save/restart scenario lost the Pages edit button while the helper stopped. The transport correctly disconnects on a proxy outage; the test had relied on its Pages read missing that outage. It now deliberately returns one restart-unavailable response, verifies the reconnect state, then reloads after the helper restarts and recovers the same saved commit for an explicit retry. Both save/restart browser scenarios pass locally, retaining the no-duplicate-commit and source-isolation assertions. The final corrected CI run and matching frontend rollout remain required before moving to L2.


**L1-T7 verified rollout — 13 September:** final commit `6358086` passes [CI 34776260772](https://github.com/KaizenUK/kaizenNEW/actions/runs/34776260772): all 436 tests across 59 files, full type checking, independent builds, real Nginx activation/rollback, 53 Chromium scenarios with one optional licensed-archive skip, and 10 additional hosted-transport scenarios. Production [34776913010](https://github.com/KaizenUK/kaizenNEW/actions/runs/34776913010) and staging [34776913050](https://github.com/KaizenUK/kaizenNEW/actions/runs/34776913050) both succeeded. Their artifacts `gh-34776913010-1` and `gh-34776913050-1` each pass a separate 259-response HTTPS origin verification. Supabase reports the production artifact live and zero pending releases. Client-demo is unchanged. T7 did not change the deployed Edge functions.

The permanent helper remains enabled and healthy with the matching helper modules. Its managed staging checkout advanced to the final revision by fast-forward after preserving and checking Sean's pending About edit, account draft, source recovery and private build settings. Those bytes are unchanged and no user edit was committed or published. The private backups and before/after proof remain on the VPS. The deployment branches advanced with verified ancestry and explicit expected-ref leases. A first read-only Git preflight required a per-command ownership exception for the transfer repository; it changed no branch. A release-state readback was corrected to use the existing private environment path and then passed.

**Next:** L2-T1, invitations by email, followed by the remaining account/access tasks. Sean's provisional acceptance above clears the earlier hold. Claude's stage review and the remaining gate scope stay distinct from this implementation and deployment proof; this is not completion of the launch goal.


## Implementation log — L2

### L2-T1 — Email invitations and the first account setup (13 September 2026)

Implemented the `builder-invite` function using Supabase Auth administration behind verified sign-in and current owner checks. Membership completion rechecks ownership, archive state, Auth ID/email and a database membership version. A delayed Auth response cannot restore removed access. A bounded per-project invitation counter limits attempts. The owner-only member directory exposes selected names, emails and setup state instead of raw IDs. Existing confirmed accounts receive access without another creation email; repeat invitations never silently change roles or publishing permission.

The real Projects screen now offers email invitations, explicit resend, editing of permissions, confirmed removal, refresh and honest partial-failure messages. It does not retry an email automatically. New invited users supply their own name and matching password before entering the project; their Auth update must succeed first. The original account/recovery path remains. Sean's existing missing-account-details notice belongs to the upcoming account page in T3; his name or email has not been invented.

**Files:** new invitation migration and `builder-invite/index.ts`; shared `builderInvitations.ts`; `ProjectMembers.tsx`, `invitations.ts`, `BuilderAuth.tsx`, styles and the projects function; database/handler/client/Auth tests, `tests/builder/invitations.spec.ts`; function configuration, milestone CI paths and the [client guide](builder-client-projects.md#invitations-and-first-sign-in) / [operator guide](website-releases.md#builder-invitations). The documentation also records the completed T7 rollout and Sean's updated milestone CI cadence.

**Local proof:** the full unit/integration checkpoint passes **470 tests across 61 files**, with real Nginx enabled. Full Astro/TypeScript checks report **zero errors, zero warnings and 172 existing hints**; Deno checks pass for the projects and invitation entrypoints. After the final client failure handling and added name/account-switch cases, all **43 targeted tests across four files** and TypeScript validation pass. Three browser journeys pass through the actual Auth gate, Projects/members UI, client transport and invitation handler, using fake Supabase administration/database responses: invite/resend/access/remove, the recipient's first setup and project entry, and a partially failed invitation followed by an explicit retry. The database tests execute the actual migration and access rules. No real invitation email was sent.

The first browser fixture accidentally combined duplicate differently-cased Authorization headers; the real handler correctly refused it. The fixture now sets a single header without weakening authentication. Visual inspection then caught the existing project-form layout squeezing the new fields on phones. A scoped layout fix preserves full-width email and role controls; the browser checks their rendered width as well as overflow. An overly exact label-text lookup in that geometry check was corrected to the accessible combobox role. The final three journeys pass. [Members desktop](handover/builder-launch/l2-invitations-desktop.png), [members phone](handover/builder-launch/l2-invitations-phone.png), [account desktop](handover/builder-launch/l2-invited-account-desktop.png) and [account phone](handover/builder-launch/l2-invited-account-phone.png) were visually inspected.

**Remaining for the L2 milestone:** T2 view defaults/switch, T3 account page and T4 sign-in states, followed by full milestone CI and the matching migration/function/frontend rollout. CI is deliberately batched under Sean's latest instruction; do not stop after this task for a CI run. Production remains the verified T7 release. Claude's stage review and gate evidence remain separate. Continue with **L2-T2**.

### L2-T2 — Roles and a remembered view choice (13 September 2026)

Editors now use client view, independent of `can_publish`. Owners start with developer details and can switch to client view under **Settings → Your view**. The preference is stored by account and project in this browser, with an honest current-visit fallback when storage is unavailable. The local developer helper keeps its developer default and switch. No role, membership or publishing permission is written by changing views.

The provider reads current membership through the existing projects endpoint with the captured account's bearer token, without trusting the shared unscoped project cache. Late initial sessions and membership responses cannot restore a previous owner's details after an account change or sign-out. Project changes and browser focus refresh membership; failed, absent or archived access defaults to client view. An editor cannot enable owner controls through a saved preference.

Both Pages and the real website editor consume the mode. Client view uses **Website pages**, omits supported pages' code-ownership pills and keeps **Needs a developer** when applicable. Build panels explain the preview action, preserve retry and show a useful failure message. Developer view keeps commands, scripts, detailed errors and logs. Both modes retain real canvas editing, draft recovery and the existing source/build consent checks. File/branch/commit hiding remains **L3-T1**; this task does not claim the whole client experience is complete.

**Files:** new `client/visual-builder/viewMode.tsx` and `viewMode.spec.tsx`; `BuilderApp.tsx`, `ClientSettings.tsx`, `SitePages.tsx`, `SitePageEditor.tsx`; new `tests/builder/view-mode.spec.ts`; [client guide](builder-client-projects.md#client-and-developer-views) and HANDOVER. Existing checkbox styling is reused. No migration or new endpoint is needed for this display preference.

**Local proof:** **497 unit/integration tests across 63 files** pass with real Nginx enabled, including 21 view-mode cases for role defaults, separate publish permission, unavailable access, account/token/profile changes, delayed responses, refresh, per-account/project preferences and unavailable storage. Full Astro/TypeScript checks pass with **zero errors, zero warnings and 173 existing hints**; the extra hint since T1's earlier full checkpoint is in its later invitation-client change, not a new T2 type issue. The first TypeScript pass caught an unsupported `.at()` call in the new test; it uses compatible indexing now. Six relevant browser journeys pass: three new owner/editor scenarios and the three existing local-helper build/draft/recovery scenarios. The owner edits the actual rendered fixture page in client view and recovers that draft after reload; editors with and without publish permission retain clear failed-build retry. Original fixture source is unchanged.

Screenshot review caught the new label inheriting column layout. It now uses the existing checkbox component class, and the final three view journeys pass again. The phone captures scroll to the changed controls and page list; their document width remains 390 pixels. Inspected evidence: owner Settings [desktop](handover/builder-launch/l2-view-owner-desktop.png) / [phone](handover/builder-launch/l2-view-owner-phone.png), client Pages [desktop](handover/builder-launch/l2-view-client-pages-desktop.png) / [phone](handover/builder-launch/l2-view-client-pages-phone.png), preview action [desktop](handover/builder-launch/l2-view-client-build-desktop.png) / [phone](handover/builder-launch/l2-view-client-build-phone.png), and failed preview [desktop](handover/builder-launch/l2-view-client-build-error-desktop.png) / [phone](handover/builder-launch/l2-view-client-build-error-phone.png). These tests use fixture Auth/membership responses and the real isolated repository/helper/build paths; they are not manual Kaizen acceptance.

**Next:** **L2-T3**, the account page, including Sean's existing missing-name/email Save notice, then T4 sign-in states. Full milestone CI and the matching L2 migration/function/frontend deployment follow T4, under Sean's batched cadence. Production remains the verified T7 release. No live account, original checkout or pending hosted About edit was changed by this task.

### L2-T3 — Manage your own account (13 September 2026)

Added **Account** to the hosted sidebar and a direct account link in Save that opens separately so the pending editor stays open. It remains accessible when a website workspace fails to load. People enter their own name, request a confirmed email change, change their password with an email confirmation code when required, and sign out other sessions while keeping the current window. The same validated name/email rules now produce Save authorship and a useful Account instruction. No profile is invented for Sean, and saving account details does not publish website changes.

Each profile operation uses the captured account in an isolated Supabase client, checks the current account before submitting and discards results after an account change. The shared client's refresh never receives an older captured refresh token. The screen distinguishes a pending email change from its current address and a confirmed update from a failed session refresh. Initial session and verified-profile responses cannot restore an older account. Password fields clear after submission, and provider failures use bounded, actionable messages.

Account deletion starts with the person's explicit request. Another current owner of every website confirms its own scope; the requester cannot self-approve, a foreign owner cannot review, and the final owner of a website cannot be removed. A changed website list needs a fresh request. The database closes project access before Auth removal and keeps an interrupted deletion available for an explicit retry. Website content, uploads and release history remain. An operator-only command handles reviewed requests with no websites; the database refuses that override whenever website approvals are needed. No live invitation, code, session removal or account deletion was used as a test.

**Files:** `AccountPage.tsx`, `AccountDeletion.tsx`, `accountAuth.ts` and five account test files under `client/visual-builder`; `shared/builderAccount.ts`; `client/lib/supabaseConfig.ts` and its existing client wrapper; sidebar/BuilderApp/RepositorySave/builder CSS; `scripts/builder-hosted-auth.ts` and `builder-account-delete.ts`; `202609130002_builder_accounts.sql`, the new `builder-account` function/shared handler and function configuration; account browser fixture/journeys and the hosted-save link assertion. The [client guide](builder-client-projects.md#your-account), [operator guide](website-releases.md#builder-accounts), CI path/Deno entries and HANDOVER accompany the implementation.

**Local proof:** the full suite passes **571 unit/integration tests across 68 files** with real Nginx enabled, including **74 new account cases**. Actual-migration database tests cover per-website approvals, cross-project visibility, self/foreign/editor refusal, cancellation, changed access, removed approvals, archived last-owner protection, interrupted removal, future-membership refusal, the operator fallback, retained content/Storage/release identities and service-only grants. Handler and real-SDK adapter tests cover confirmed/unknown outcomes, explicit retries, malformed/oversized bodies, caller identity, error handling and account switches. React tests cover late initial sessions/profile responses, pending mutations during account changes, sign-out and development effect replay. The latter exposed and fixed a stuck-loading case. Full Astro/TypeScript checks report **zero errors, zero warnings and 173 existing hints**; Deno checks the new account function successfully. One fixture byte-array type and two new inference hints were corrected before the final type check.

**Browser proof:** ten journeys pass across Account, invitations, role-based views and hosted Save. The Account fixture uses the real Supabase SDK and actual PostgreSQL deletion migration behind a fake Auth HTTP service. It proves missing-name repair and shared Save validation, pending email, password/code flow, other-session scope, request/cancel, separate approvals by two website owners, and explicit retry after interrupted Auth removal while retaining uploaded files. It does not establish real email delivery. An initial fake-Auth response omitted the browser-exposed API-version header, which hid its error code; the fixture now exposes it. The code-request check waits for the visible acknowledgement before inspecting the request. No production error rule was weakened.

Screenshot review removed a repeated email and moved confirmation of another person's deletion into **Account requests to review**. Final TypeScript, **55 affected tests** and **both account browser journeys** pass after that layout correction. Desktop/phone captures were inspected with no horizontal overflow: profile [desktop](handover/builder-launch/l2-account-desktop.png) / [phone](handover/builder-launch/l2-account-phone.png); password/code [desktop](handover/builder-launch/l2-account-password-desktop.png) / [phone](handover/builder-launch/l2-account-password-phone.png); owner requests [desktop](handover/builder-launch/l2-account-review-desktop.png) / [phone](handover/builder-launch/l2-account-review-phone.png); confirmation [desktop](handover/builder-launch/l2-account-confirm-desktop.png) / [phone](handover/builder-launch/l2-account-confirm-phone.png).

**Next:** **L2-T4** sign-in states. At L2 completion, run full browser/CI verification and deploy the migrations, functions, frontend and matching hosted helper runtime together. T3 adds a shared helper import; follow the existing upgrade/backup procedure and retain Sean's pending About edit and recovery files. The L2 work is not deployed yet, and Claude's stage review remains separate from automated proof. Broader retention/erasure policy remains in L5. CI stays batched at the milestone under Sean's instruction.


### L2-T4 — Clear sign-in and password-link states (13 September 2026)

Sign-in now explains incorrect passwords, unconfirmed email, expired or previously used links, reset requests and unfinished invited accounts in plain English. Provider responses can represent both expiry and prior use, so the screen explains that overlap and the next action. A failed link does not discard an existing valid sign-in or offer to change its password: the person explicitly continues with that account or signs out of this browser. A lost reset response explains that the email may already have been requested and leaves any retry to the person.

The shared client records callback intent and a bounded error code before SDK initialization consumes the URL. Only an authenticated recovery event for the current account opens recovery setup; a bookmarked setup query alone cannot. Invited accounts still supply their own valid name and matching password. Setup uses the captured account in the existing isolated SDK adapter. Late initial sessions, account changes, focus events and delayed setup/reset/sign-out responses cannot restore a previous account's form or dismiss the current account's setup. Callback fields are removed from the URL after initialization. The workspace listener also discards stale initial reads and runs its updates outside the SDK callback lock.

**Files:** new `client/lib/authRedirect.ts`, `client/visual-builder/authState.ts` and `authSession.ts`; shared client, `BuilderAuth.tsx`, `BuilderApp.tsx`, `accountAuth.ts` and scoped Auth card styling; extended `auth.spec.tsx` and account adapter tests, new `auth-session.spec.ts`; real-SDK account fixture and new `tests/builder/auth.spec.ts`, with existing browser SDK fixtures updated to the same initialization boundary. The [client guide](builder-client-projects.md#sign-in-and-password-links), [operator guide](website-releases.md#sign-in-callback-handling), CI path and HANDOVER accompany the change. No new endpoint or migration is needed for T4.

**Local milestone proof:** **610 unit/integration tests across 69 files** pass with real Nginx enabled. The final 65 focused Auth/session/adapter tests pass, including stale callbacks and account-switch negatives. Full Astro/TypeScript checks report **zero errors, zero warnings and 173 existing hints** across 370 files. Deno checks pass for all six builder functions. The full browser suite passes **65 journeys with one optional licensed-archive skip**. The additional ten browser journeys through the hosted repository transport also pass. The four new journeys use the real Supabase SDK, real application screens and actual account migration behind a fake Auth HTTP service: expired link and an interrupted reset request; successful recovery followed by reuse of that link; wrong password and unconfirmed email; invited name/password setup. No real email, invitation, session removal or account deletion was performed. These fixtures do not establish production email delivery or manual acceptance.

Desktop/phone screenshots were inspected and retained: expired link [desktop](handover/builder-launch/l2-auth-expired-desktop.png) / [phone](handover/builder-launch/l2-auth-expired-phone.png); used link [desktop](handover/builder-launch/l2-auth-used-desktop.png) / [phone](handover/builder-launch/l2-auth-used-phone.png); incorrect password [desktop](handover/builder-launch/l2-auth-wrong-password-desktop.png) / [phone](handover/builder-launch/l2-auth-wrong-password-phone.png); recovery [desktop](handover/builder-launch/l2-auth-recovery-desktop.png) / [phone](handover/builder-launch/l2-auth-recovery-phone.png); reset requested [desktop](handover/builder-launch/l2-auth-reset-requested-desktop.png) / [phone](handover/builder-launch/l2-auth-reset-requested-phone.png). The successful request now clears the earlier expired-link notice; both final captures were inspected. The earlier invitation screenshots cover the invited setup screen.

**Milestone rollout pending:** run full L2 CI on this revision, then deploy migrations `202609130001` and `202609130002`, `builder-invite`, `builder-account`, the updated projects function, frontend and matching hosted helper runtime. Read-only preflight confirms the existing production Auth redirect allowlist covers the builder callbacks, the helper is healthy and Sean's pending About source hash is unchanged. The new migrations/functions are not deployed at this checkpoint. Preserve that edit, drafts, source recovery and private settings throughout rollout. Continue with **L3-T1** after L2 verification and deployment. Claude's stage review remains separate.


### L2 milestone CI correction — wait for the page before enabling Save

The first exact milestone revision, `28b1e64`, passed CI's types, unit suite, independent exports and release/rollback checks, then failed the interrupted-save browser journey: **64 passed, one failed and one optional archive skipped**. [The CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34785493768) is retained. Five local repetitions reproduced one failure. The failing trace showed no Save request: its status response arrived before source inspection, briefly enabling Save before the preview/draft initialization changed the button's state during the click. No live rollout occurred.

`SitePageEditor.tsx` now uses the existing editing lock for Save, waiting for the saved draft to load and keeping unresolved draft recovery locked. The original interrupted-save browser journey deterministically holds source inspection and draft loading separately, checks Save stays disabled, then releases both and retains the existing commit-failure/restart/no-replay assertions. The added loading assertion failed before the fix; **five corrected repetitions and all eight affected save/draft/view-mode journeys pass**. Full Astro/TypeScript checking again reports **zero errors, zero warnings and 173 hints**. The next exact milestone revision must pass full CI before deployment. The prepared helper's source is unaffected by this frontend correction.


### L2 milestone rollout — 13 September 2026

The corrected `16a1cc4` revision passed [milestone CI](https://github.com/KaizenUK/kaizenNEW/actions/runs/34786543005) and [PR #10](https://github.com/KaizenUK/kaizenNEW/pull/10) is merged. CI passed types with no errors/warnings, independent exports, release verification, 65 browser journeys with one optional licensed-archive skip and 10 additional hosted-transport journeys. Its initial unit pass had 609 passes and one Nginx-dependent skip; after Nginx installation all 18 publication-database cases passed. The equivalent local full run passed all 610 tests across 69 files. The first candidate's interrupted-save browser failure revealed Save becoming enabled before initial inspection/draft loading completed; the fix has a deterministic regression case, five repeated passing recovery runs and eight passing affected browser journeys.

Production [run 34787575626](https://github.com/KaizenUK/kaizenNEW/actions/runs/34787575626) deployed `gh-34787575626-1`, including the separate Studio copy. Staging [run 34787575689](https://github.com/KaizenUK/kaizenNEW/actions/runs/34787575689) deployed `gh-34787575689-1`. Both independently pass all 259 served-file checks over HTTPS at the origin. The database live head matches production with no pending release; the separate client-demo artifact remains `300d7710-69c9-46fe-82ed-5e940997d80d`.

Migrations `202609130001` and `202609130002` are applied with their recorded SQL matching the reviewed source. Functions `builder-projects` v9, `builder-invite` v1 and `builder-account` v1 are active. Read-only checks confirm private-table access and RPC grants; unsigned allowed-origin requests return 401 and foreign origins return 403 through the public proxy. Existing Auth redirects already cover the Account and password-setup routes. No real invitation, confirmation email, account deletion or session removal was performed.

The permanent helper passed isolated startup and a real service restart using root-owned `/opt/kaizen-helper-releases/28b1e64`, verified to contain the same helper source as `16a1cc4`. An initial private startup probe used an inaccessible operator working directory; setting its working directory to the runtime fixed that probe before the live service was stopped. The managed staging checkout then advanced to `16a1cc4` by fast-forward. Sean's modified About page, saved draft, recovery, private settings and other working state survived unchanged; private backups are retained on the VPS.

Read-only browser use of the actual production Account page confirms successful profile/request-state loading, the current email and the missing name notice. This proves loading, not real email delivery or a completed Save/publication. Sean supplies his own name through Account. Claude's stage polish and further Sean usage remain distinct from this evidence. Continue with **L3-T1**, using local checks and `[skip ci]` task commits until the L3 milestone boundary.


## Implementation log — L3

### L3-T1 — Readable client reviews and Save outcomes (14 September 2026)

Client view now shows the page and saved editing state in the existing-site footer, without source paths, branch names, commits, change-summary input or deployment diagnostics. Account details, Save, explicit retry, state checks and staging remain available. Known account, conflicting-change, permission and interrupted-save failures have plain next actions; unknown technical failures do not print raw operator output. Developer view retains Git state, the editable change summary, deployment links and Save details. Source-location labels and raw recovery values are also restricted to developer view. This is presentation only; existing backend checks and the local developer commit path are unchanged.

The source review previously showed only whole-file output. Both modes now have an actual **Before / After** list of changed text, link addresses, image choices, supported design values with device/units, and ordered sections identified by their content. Shared-content changes explain their effect on other pages. Responsive image replacements name the chosen asset instead of its generated storage name. Developer mode also retains each changed source file and **Show the whole file**. Empty reviews cannot apply. The summary is captured from the same edits as the prepared plan; changed edits during or after preparation require a fresh review. Draft status reports saved only after the helper acknowledges the current values.

**Files and guide:** `SitePageEditor.tsx`, `RepositorySave.tsx`, `useSourceEditingDraft.ts`, new `sourceReview.ts` and `repositorySaveCopy.ts`, scoped review styles, two unit files, new `source-review.spec.ts` browser journeys and the extended hosted-save/media scenarios. [The existing-site guide](visual-builder.md#existing-site-canvas) documents the client and developer paths. The handover and release runbook also record the completed L2 rollout.

**Local proof:** all **614 tests across 71 files** pass with real Nginx enabled. Full Astro/TypeScript checking reports **zero errors, zero warnings and 173 existing hints across 374 files**. All **13 relevant browser journeys** pass: client/developer review and apply, real hosted Save/push rejection/reconnect/retry, interrupted-save recovery, existing canvas, original local Git isolation, image replacement, section ordering and role-based views. New controlled cases hold a draft acknowledgement and a review request: the UI waits for persistence, and later typing cannot be applied through the earlier review. These use temporary repositories, fixture accounts and fake remotes; no personal checkout or production content was edited.

The first focused run exposed a test still expecting the developer's staging wording in client mode and a fixture closing before its rebuild finished; both were corrected. Screenshot inspection then found that the phone dialog extended beyond the viewport despite the document having no horizontal scroll. Scoped border-box sizing and automatic width fixed it; browser assertions now check the dialog's actual left/right bounds. All **four affected review/media journeys** passed again after that CSS-only fix. An earlier invocation accidentally selected the full suite because of an extra argument separator; it was interrupted and is not counted as a completed verification run.

Desktop and phone screenshots were inspected: review [client desktop](handover/builder-launch/l3-review-client-desktop.png), [client phone](handover/builder-launch/l3-review-client-phone.png), [developer desktop](handover/builder-launch/l3-review-developer-desktop.png), [developer phone](handover/builder-launch/l3-review-developer-phone.png); Save [ready desktop](handover/builder-launch/l3-save-ready-desktop.png), [ready phone](handover/builder-launch/l3-save-ready-phone.png), [saved desktop](handover/builder-launch/l3-save-saved-desktop.png), [saved phone](handover/builder-launch/l3-save-saved-phone.png). Phone reviews scroll vertically to the remaining changes and Apply action. Screenshots are fixtures, not Sean's acceptance or Claude's polish.

**Next:** L3-T2 unifies Draft, Saved, On staging and Live across views, then L3-T3 moves repeated explanations into contextual help. This task is not deployed; the live frontend remains L2 `16a1cc4`. Full stage browser checks and CI run at the L3 milestone boundary under Sean's cadence. Continue without repeating the cleared acceptance hold.


### L3-T2 — Consistent saved and published states (14 September 2026)

Pages, both main editor headers/footers, native Save/publication and Releases share **Draft**, **Saved**, **On staging** and **Live** wording. Draft means current edits are not yet acknowledged; Saved means they are kept and does not imply deployment. The developer field-editor fallback uses the same badge while retaining its recovery explanation. A builder-page edit newer than the verified publication stays Saved, even when it occurred immediately afterwards. Pages' Saved filter includes both new pages and newer saved changes. Local preview publication alone does not establish a public release.

Active staging releases now say On staging; active production releases say Live. Replaced releases say Earlier release and active unpublish jobs say Offline. Progress/failure labels are shared as well. Native Save and production receipts require both a successful deployment workflow and the expected destination marker before displaying a destination badge, and keep observing after success so replacement is visible. A reviewed native version can use the matching checkout observation; a different checkout cannot lend its status to that review.

The existing protected repository endpoint gains one read-only `repository-website-status` action, reusing current membership, project locks, Git status and publication marker validation. It checks the entire current checkout before and after destination observation and returns only this account's draft-route names. Each open view makes one project observation; it discards results from an earlier connection/edit generation. Saved private drafts override a deployed folder for their route. Dirty/unconfirmed folders stay Saved conservatively, including unrelated pending changes. Builder pages without a staging baseline also stay Saved; their staging release is shown in Releases. These limits are documented in [the status guide](visual-builder.md#saved-and-published-states) and [operator contract](website-releases.md#observing-website-status). A marker identifies the observed version and does not replace inspecting rendered output.

**Local proof:** all **626 unit/integration tests across 73 files** pass, with zero TypeScript/Astro errors or warnings (173 existing hints across 379 files). New tests cover status mapping, edits immediately after publication, active/earlier/offline destinations, required workflow/marker agreement, malformed observations, private draft-route isolation, denied membership, dirty/concurrently changed Git checkouts, bounded marker reads and cache replacement. Temporary real Git repositories and the real helper exercise read-only status without changing source or remote history; SSH, account responses and destination observations remain fixtures.

**29 distinct affected browser journeys pass across the recorded local runs.** The main 20-journey pass covers native Save/publish, real Nginx update/rollback/offline, builder editing, field-editor fallback, review isolation and status displays. Additional runs cover both transports, helper queues/recovery, view switching and Saved/Live filters. The final rerun passes all five hosted-helper/view-mode journeys. An initial assertion expected old failure/restoration copy; another rejected every tooltip rather than testing for technical detail. Those assertions were corrected while retaining their outcome/privacy checks. The reload test now waits for the edited draft's actual acknowledgement before reloading, rather than accepting the identical initial Saved label. Existing development ResizeObserver notifications remain recorded; this is not a silent-console or live-deployment claim.

Visual inspection found and corrected a phone row that hid its title after the badge was added, and desktop source rows that pushed Edit onto a second line. The phone row now places Saved below the readable title; desktop source badges share one group beside Edit. Follow-up browser checks enforce visible, untruncated phone titles, viewport bounds and aligned desktop controls. The main browser suite passed before these small layout adjustments; the relevant status and transport/view journeys were rerun afterwards.

Screenshots inspected at desktop and phone widths: saved page ([desktop](handover/builder-launch/l3-status-page-saved-1440.png), [phone](handover/builder-launch/l3-status-page-saved-390.png)); native Pages ([desktop](handover/builder-launch/l3-status-pages-live-1440.png), [phone](handover/builder-launch/l3-status-pages-live-390.png)); native editor ([desktop](handover/builder-launch/l3-status-editor-live-1440.png), [phone](handover/builder-launch/l3-status-editor-live-390.png)); builder editor [Draft](handover/builder-launch/l3-status-editor-draft-1440.png) and [Saved](handover/builder-launch/l3-status-editor-saved-1440.png). Release cards: staging ([desktop](handover/builder-launch/l3-status-release-0-1440.png), [phone](handover/builder-launch/l3-status-release-0-390.png)), production ([desktop](handover/builder-launch/l3-status-release-1-1440.png), [phone](handover/builder-launch/l3-status-release-1-390.png)), earlier ([desktop](handover/builder-launch/l3-status-release-2-1440.png), [phone](handover/builder-launch/l3-status-release-2-390.png)), offline ([desktop](handover/builder-launch/l3-status-release-3-1440.png), [phone](handover/builder-launch/l3-status-release-3-390.png)). These are isolated fixtures, not Sean's pending page edit.

**Pending:** L3-T3's short descriptions/help drawer, then full milestone browser/CI verification and matching helper/frontend rollout. T1 and T2 are not deployed; L2 remains live. Sean's checkout, account details, pending hosted About edit and client-demo destination are unchanged. Claude's review and Sean's future hands-on use remain distinct from this implementation evidence; the cleared provisional acceptance is not reopened.


### L3-T3 — Contextual help and concise view descriptions (14 September 2026)

**Implemented and locally verified:** every workspace view has a short description and a **Learn more** link. The drawer reads the relevant marked section of this guide's companion, [the visual builder guide](visual-builder.md#in-app-help), as a static asset when opened. Seventeen short sections cover the twelve workspace views, both editors, project access, helper setup and original-folder backups. General setup prose has moved into that guide. Current errors, empty states, counts, destinations and action-specific account/publication/restore consequences stay beside their controls.

The existing dialog library provides modal keyboard focus and Escape/close behavior; closing returns focus to the help link. Related-topic links stay in the drawer with a Back action. The drawer follows the current theme, fits a phone, cancels an unfinished read when closed and offers a working retry after an unavailable guide. It does not submit an open form or apply website edits. The maintained Markdown subset renders paragraphs, headings, lists, emphasis, code and validated links as React content; it does not execute HTML. No new package or server endpoint was added.

The shared heading and individual views were updated along with the builder/source editors, account/access settings, library, exports, previews and backup controls. `HelpLink.tsx`, `helpContent.ts`, the maintained guide sections, their unit tests and `tests/builder/help.spec.ts` provide the new foundation. The regression workflow also follows guide-only changes now that this document is an application input.

**Local proof:** the final complete unit/integration suite passes **647 tests in 74 files**, including every guide section and the stale-review wording regression described below. The focused guide/review run also passes all 23 cases. Full Astro/TypeScript checks report **zero errors and zero warnings**, with 173 existing hints. All seven focused help browser journeys pass (including the chosen dark theme): all twelve views at desktop and phone widths, load failure/retry, keyboard focus and related topics, unsubmitted account/settings fields, the page editor, and changed words in a real built source preview with unchanged original source. The explanatory-paragraph assertion measures the actual visible view; data, empty/error/status messages and review consequences are separate from introductory guidance. Phone measurement begins at the view heading because the sidebar precedes it.

The complete browser run reached all 79 scenarios: 73 passed, one optional licensed-archive scenario skipped, and five required attention. Three assertions still expected prose that intentionally moved into help; those now open the appropriate drawer and retain their original behavior checks. The Projects scenario was interrupted by a development reload triggered by Codex updating this task map during the run; its trace's second navigation matches that edit's timestamp. The unchanged Projects scenario passes in the final run with files held steady. No source-reset, retry or weakened assertion was used to mask it.

The fifth result exposed a wording gap: when newer saved edits reached the helper during a delayed review, the helper correctly refused the older draft, but client mode replaced its specific message with a generic failure. That response now says to wait for **Saved** and review again. The browser scenario deterministically waits for the newer draft acknowledgement before releasing the older request, confirms the original file is unchanged, and then reviews the newer text. The focused unit case covers both stale-review messages and keeps unknown errors generic.

All **18 correction/affected browser journeys pass** with files held steady, including that unchanged Projects journey, the full client/developer review cases and all help scenarios. Across the complete and final affected runs, the latest outcomes cover **79 distinct passing journeys**, plus the one optional archive skip. Existing development ResizeObserver notifications and deliberate denied-file fixtures remain visible; this is not a silent-console or manual acceptance claim.

Eighteen inspected desktop/phone screenshots are stored beside the stage evidence. View examples: [Pages desktop](handover/builder-launch/l3-view-pages-1440.png), [Pages phone](handover/builder-launch/l3-view-pages-390.png), [Settings desktop](handover/builder-launch/l3-view-settings-1440.png), [Settings phone](handover/builder-launch/l3-view-settings-390.png), [Export desktop](handover/builder-launch/l3-view-repository-1440.png), [Export phone](handover/builder-launch/l3-view-repository-390.png). Drawer examples: [Pages](handover/builder-launch/l3-help-pages-390.png), [Settings](handover/builder-launch/l3-help-settings-390.png), [export and folder help](handover/builder-launch/l3-help-repository-390.png), [page editor](handover/builder-launch/l3-help-editor-390.png), [website editor](handover/builder-launch/l3-help-source-390.png), [dark theme](handover/builder-launch/l3-help-dark-390.png); the corresponding desktop files are retained too.

**Next:** full L3 milestone CI, then the coordinated helper/frontend rollout. L2 remains deployed. A read-only VPS check confirms its helper is healthy, the managed checkout is still `16a1cc4`, and Sean's pending About source hash is unchanged. Preserve the edit and its private draft/recovery data when advancing the checkout. Claude's running-product review and Sean's future use remain separate from automated proof; the previous provisional acceptance stays recorded above. Do not proceed to L4 until this L3 rollout is finished.

### L3 milestone CI correction — discover the helper fixture's dependencies before opening it

The first [L3 milestone run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34793342970), at `594a523`, passed types, all 647 unit/integration tests, independent exports and Nginx activation/rollback checks. Its browser suite passed **78 journeys**, skipped the optional licensed archive, and failed the local-helper companion scenario before the first **Helper address** field appeared. All new L3 review, status and help journeys passed. The subsequent hosted-transport step did not run.

The CI log records two dependency-optimization batches and a reload request during that fixture's initial load. The fixture injected its application entry through a virtual module that Vite's initial file scan could not discover. Two local cold-cache probes, including delayed app-module loading, did not reproduce the timeout; the CI artifact could not be downloaded, so there is no inspected failure-trace claim. This identifies a fixture startup dependency rather than establishing a new production defect.

The same host app is now a real `tests/builder/companion-app.tsx` entry, explicitly scanned by the fixture's `optimizeDeps.entries`. Each fixture server starts with a fresh temporary dependency cache. The scenario retains all original consent, cross-origin transport, real file editing, draft recovery, revocation and backup assertions, without an added retry, reload or longer timeout. [Vite's dependency-optimization documentation](https://vite.dev/config/dep-optimization-options) describes supplying entries for sources outside the default HTML scan.

**Corrected local proof:** the companion journey and the independent helper-window reload/server-stop journey each pass three times: **six passing browser checks**. Full Astro/TypeScript validation reports zero errors/warnings and 173 existing hints. Product, helper and deployment source are unchanged by this correction. Run full milestone CI on the corrected revision before activating the prepared helper or advancing production/staging; L2 remains live and L4 has not started.

### L3 milestone rollout — 14 September 2026

**Implemented, verified and deployed at `8a9c94c`.** The [corrected milestone run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34794587467) passes all **79 browser journeys** with one optional licensed-archive skip, then all **10 additional hosted-transport journeys**. The main unit step passes 646 cases and skips its conditional Nginx case; the dedicated Nginx step then passes that complete 18-case file, including the conditional case. Together these cover all **647 distinct unit/integration cases**. Types report zero errors/warnings. Independent client exports, real Nginx activation/rollback and the cold companion entry all pass. [PR 11](https://github.com/KaizenUK/kaizenNEW/pull/11) is merged.

The helper runs root-owned `/opt/kaizen-helper-releases/594a523`, whose executable source is identical to the final revision: only the companion fixture and progress documents differ. Its exact runtime passed an isolated startup, activation and a real service restart. The managed checkout advanced by fast-forward to `8a9c94c`; Sean's pending About edit, saved draft, source recovery, private settings and operation records were preserved byte-for-byte. Private pre-upgrade/source-forward backups remain on the VPS. No Supabase function or migration changed in L3.

The first frontend deployment attempts stopped before activation because the rollout's root-run Git observations refreshed `.git/index` files as root with mode 0600. This was an operator-script error. The repair changed the owner of exactly three index files to their existing service accounts, preserving their bytes and restrictive permissions. Source remained clean in the deployment checkouts and retained only the pending About edit in the helper checkout. Future root observations disable optional Git index writes; the runbook and `AGENTS.md` now state this rule. Final checks confirm the owners remain correct after deployment and verification.

Both deployment retries pass: [production](https://github.com/KaizenUK/kaizenNEW/actions/runs/34795700579), artifact **`gh-34795700579-2`**, and [staging](https://github.com/KaizenUK/kaizenNEW/actions/runs/34795700619), artifact **`gh-34795700619-2`**. An independent VPS readback validates **260 HTTPS origin checks for each**, including the matching release markers. Production's database head is live at the matching artifact with no pending release; project/member counts are unchanged. The separate client-demo artifact remains `300d7710-69c9-46fe-82ed-5e940997d80d`.

Read-only use of the actual public builder confirms the deployed Account help drawer loads its maintained guide text, Escape returns focus to its trigger, and Pages finishes loading the existing website through the upgraded helper. Saved badges correctly retain the unpublished working-copy state. This verifies public UI loading separately from origin file checks. A separate public CDN marker read could not be completed by these workstation tools (HTTP 403 / blocked URL); no CDN-wide marker or cache-purge claim is made. Account details, website content and pending edits were not submitted as tests. Existing desktop/phone screenshots remain valid because the correction changed only the fixture.

Continue with **L4-T1**, the first-run checklist, then page templates, the small-business starter and sample content in order. L0–L3 account for **18 of 32 tasks**, about **56% by equal task count**. Claude's running-product review and Sean's future complete editing/publication use remain separate from automated evidence; the cleared provisional acceptance stays cleared.

## Implementation log — L4

### L4-T1 — A first-run checklist tied to real work (14 September 2026)

New, empty builder projects show **Start here** on Pages. Its five actions open Projects, Site design, a new page, the actual rendered preview and Releases. The existing project name counts immediately; design completion requires a saved site design, including an explicitly saved initial design. Created pages count from the workspace. A preview counts after successful rendering and retains a SHA-256 fingerprint of the page, site design and asset identities/hashes; changing these clears the preview step. No page contents or private asset addresses are stored in the preference. Publication completion comes from an active, verified live worker release for this project. Saving, opening Releases, a failed job or an unpublish action cannot supply that completion; a verified restoration can.

Enrollment, the remembered preview and dismissal use a versioned browser preference for each account and project. **Show start here** restores a dismissed card. Existing website folders, the legacy Kaizen workspace, archives and previously populated projects do not enroll. Account changes clear the displayed state, and asynchronous reads and preview fingerprints discard stale results. If browser storage is blocked, the checklist remains usable for the visit and explains the persistence limit. Local work uses the local profile. This is a display preference, with no new server endpoint or permission grant.

**Files and guide:** `firstRun.ts`, `useFirstRun.ts`, `FirstRunCard.tsx`, `BuilderApp.tsx`, `PagesView.tsx` and scoped styles; logic tests, `tests/builder/first-run.spec.ts` and the existing real-Nginx client publication journey. The [Pages guide](visual-builder.md#help-pages) is also the in-app explanation; the client-project guide links to it.

**Proof:** all **659 unit/integration tests in 75 files** pass, with the Nginx-dependent tests enabled. After the final help update, all 32 focused logic/help cases also pass. Full Astro/TypeScript checks report **zero errors and zero warnings**, with 174 hints. All **12 distinct affected browser journeys** have passing final outcomes: four first-run scenarios, seven contextual-help scenarios and the real-Nginx publication/rollback/unpublish journey. These cover saved design versus merely opening the screen, creating and previewing a page, remembering progress after reload, invalidating a changed preview, account/project separation, dismissal/restoration, blocked storage and existing-site exclusion. The publication scenario verifies the checklist's live tick and its removal after serving the offline result.

The first browser run exposed an undefined account-loading state in the new hook; it is fixed and the real hosted-auth fixture now passes. Initial test assumptions about a preview title element and an exact Releases navigation name were corrected to check the renderer's actual document marker and the existing pending-count label. Visiting Pages during the publication journey also correctly resets the Releases destination selection; the test now selects its destination again. The original publication, failure, rollback, draft-retention and offline assertions remain. The initial help draft exceeded the existing 220-word limit; its wording was shortened and the unchanged limit passes. No retries, timeout increases or suppressed errors were introduced.

**Visual evidence:** inspected [desktop](handover/builder-launch/l4-first-run-1440.png) and [phone](handover/builder-launch/l4-first-run-390.png) screenshots. The browser checks the card's actual bounds and document width at both sizes. The card fits the existing light theme and becomes a single column on phones.

**Next:** L4-T2's six-template gallery, then the starter site and sample content. T1 is not deployed; production and staging remain L3 `8a9c94c`. Full milestone browser/CI checks and deployment remain batched until L4 is complete. The original checkout, hosted About edit, account details and client-demo destination were not changed. Claude's running-product review and Sean's future use remain separate from this local proof; no repeated acceptance hold is required.


### L4-T2 — Six editable page templates (14 September 2026)

**Implemented and locally verified:** Pages offers **Browse templates** with Home, About, Services, Contact, Pricing and Landing page. Each card renders the actual block layout; the full preview offers Desktop and Phone widths. **Use template** creates an editable saved draft with fresh block/page IDs and a free URL, preserving existing draft, published and redirect URLs. Browsing makes no workspace changes. New client pages use the project's shared design; original-site pages start with independent styles. Missing client design is initialized only when a template is used, respecting a concurrent saved design. A failed save preserves the selection, and a lost acknowledgement recovers the same saved page without duplication.

**Corrections proven:** gallery work exposed inactive site metadata reaching preview validation; `resolveSiteDocument` now removes that metadata from its cloned output while preserving the saved source. Missing initial design and inherited thumbnail button height are corrected. Image-decoding checks diagnosed HTTP 403 responses from Astro's development cross-origin guard for opaque sandbox frames. Preview HTML now embeds only the two bundled SVG illustrations from source; saved asset URLs, exported files and sandbox permissions stay unchanged. A regression preserves non-bundled media URLs. Type validation required the project's supported split/join string API. The existing responsive-editor revision assertion now follows the descriptive template-creation label while retaining restore/publication checks.

**Files and guide:** `starters.ts`, new `PageTemplateGallery.tsx`, new `templateCreation.ts`, Pages/BuilderApp/CSS integration, `previewHtml.tsx`, `shared/builderSite.ts`, template/site unit tests and `tests/builder/page-templates.spec.ts`. [Pages help](visual-builder.md) and [client projects](builder-client-projects.md) describe the gallery, editable drafts and sample-content responsibilities.

**Local proof:** all **677 unit/integration tests in 76 files** pass. All **21 affected browser journeys** pass together: five gallery cases (decoded images, all six previews/creation, retry, lost acknowledgement, keyboard/dark theme), five editor cases, four first-run cases and seven help cases. After the string-API compatibility correction, all 36 focused preview/capability tests pass and full Astro/TypeScript validation reports zero errors, zero warnings and 174 existing hints. Existing development ResizeObserver notifications remain visible. The initial failed image test is retained in the private diagnostic log; it was fixed, not waived.

**Visual evidence:** inspected gallery, full preview and dark-theme captures at desktop and phone widths: [gallery desktop](handover/builder-launch/l4-templates-gallery-1440.png), [gallery phone](handover/builder-launch/l4-templates-gallery-390.png), [preview desktop](handover/builder-launch/l4-templates-preview-1440.png), [preview phone](handover/builder-launch/l4-templates-preview-390.png), [dark desktop](handover/builder-launch/l4-templates-dark-1440.png), [dark phone](handover/builder-launch/l4-templates-dark-390.png). Decoded illustrations and dialog bounds are checked, including remounted thumbnails before capture.

**Next:** continue directly with L4-T3, creating the six-page Small business starter with shared navigation/footer, then L4-T4 sample content. L4 is not deployed; production and staging remain `8a9c94c`. The L4 rollout needs matching helper source because the shared resolver changed. CI remains batched at the milestone. Sean reiterated that failed tests must be diagnosed and fixed without stalling the full goal; questions requiring only routine implementation choices are deferred or resolved under his authorization.


### L4-T3 — Create and publish a complete Small business starter (14 September 2026)

**Implemented and locally verified:** an empty builder project offers **Start with a website**. The Small business dialog previews Home and lists all six page addresses. **Create starter site** saves Home, About, Services, Contact, Pricing and Landing page together with one shared navigation component and one shared footer. Each page uses the current site theme; Home is first, so it is also served at `/`. Links use the created addresses, including addresses adjusted around existing redirects. The single-page gallery remains available for populated projects and original websites.

**Atomic creation:** a `create-starter` action uses the existing local/projects endpoints, the existing membership checks and the hosted workspace version transaction. `shared/builderStarter.ts` validates the complete future workspace before writing, requires an empty page list, and rejects a changed design or route version. Assets, saved components, client settings and existing design components are retained. The action saves drafts and never creates a release. A failed response can recover only when all six page IDs/documents, their order and the shared design still match; a newer edit is preserved. Original websites cannot invoke the starter action. No new endpoint or database migration was needed. A backup-restore action was considered but not reused because starter creation must have its own clear history and empty-project contract.

**Files and guide:** `starterSite.ts`, `StarterSiteDialog.tsx`, `shared/builderStarter.ts`, the existing project draft dispatcher, local service, storage and projects function, Pages/BuilderApp/CSS integration, four unit cases and `tests/builder/starter-site.spec.ts` plus its isolated Nginx fixture. [Pages help](visual-builder.md) and [client project guidance](builder-client-projects.md#start-with-a-complete-website) explain creation, shared editing and publication.

**Proof:** the full unit/integration suite passes **681 tests in 77 files**; Astro/TypeScript reports zero errors/warnings and 174 existing hints. The projects function passes Deno validation. All **19 affected browser journeys** pass together, including three new starter journeys, four first-run, seven help and five gallery cases. The starter journey creates all six pages through the UI, reviews and publishes through the actual local release worker to an isolated **staging** destination, waits for **On staging**, visits every page using shared navigation, verifies the root Home route, decoded illustration, shared footer and phone keyboard menu. Separate scenarios prove failure/lost-response recovery and rejection of another window's page without partial writes. The retention unit case also checks populated assets and client settings. No hosted client, personal checkout or real account was used as a fixture.

**Visual evidence:** inspected [dialog desktop](handover/builder-launch/l4-starter-dialog-1440.png), [dialog phone](handover/builder-launch/l4-starter-dialog-390.png), [published starter desktop](handover/builder-launch/l4-starter-staging-1440.png) and [published starter phone](handover/builder-launch/l4-starter-staging-390.png). The first retention fixture used a reserved redirect URL; corrected to the valid Pricing route while retaining the collision/remapping assertions.

**Next:** L4-T4 sample content, then full milestone browser checks and CI. L4 is not deployed; production/staging remain `8a9c94c`. Roll out the updated projects function/shared source and matching helper with the frontend at the L4 boundary. Existing sample/form configuration guidance remains explicit; staging fixture publication is not Sean's manual acceptance.


### L4-T4 — Starter illustrations and retained licence records (14 September 2026)

**Implemented and locally verified:** Assets offers **Import starter illustrations**, using the existing ZIP importer, asset classification, licence handling and duplicate detection. The small pack contains only the two original Kaizen SVG illustrations and their recorded permission. The existing mixed demonstration pack remains available separately. Templates use editable sample text; the guide explains replacing business facts, prices, contact details and promises before publication.

Exports that reference the built-in illustrations now include `sample-assets.json` with the original URL, bundled URL, full SHA-256 checksum and licence path. The licence stays under `reference-packs`, outside executable source. Exports without those illustrations do not acquire this attribution. Imported assets retain their existing pack names, files, checksums and licence records. The canonical licence lives beside the export module; the sample generator writes its public copy and the ZIP. This avoids importing a public text asset as JavaScript in the standalone Vite helper fixture.

**Files and guide:** `AssetLibrary.tsx`, `sampleContent.ts`, `sample-illustrations-licence.txt`, `exportProject.ts`, the public licence/illustrations ZIP and `scripts/create-builder-sample.mjs`; new sample-content unit and browser scenarios; [Assets help](visual-builder.md#help-assets) and [client projects](builder-client-projects.md). The CI path filter now includes the new shared starter module.

**Local proof:** the complete unit/integration suite passes **683 tests across 78 files**. After the canonical-licence adjustment, all 12 focused sample, asset and export cases pass; full types report zero errors/warnings and 174 hints. Tests compare actual ZIP/source bytes and licence copies, verify exported checksums and absence of misattribution, import through the UI, download the licence and prove duplicate reimport preserves all assets.

The full milestone browser run completed **91 passing journeys**, one failed companion reconnection journey and one optional licensed-archive skip. Every new L4 journey passed. The retained trace shows the companion fixture accepted the previous **Saved** badge before its newly typed frame text reached the parent editor. The test now waits for that specific text in the parent before checking Saved and disconnecting. It retains consent, revocation, disconnected editing, recovery, apply/rebuild and source-byte assertions, with no retry or timeout increase. The corrected journey subsequently reported **three passing repeats**; its orchestration session returned signal 143 after that report, and the final companion-plus-sample command then passed both journeys and exited successfully. This is not a claim of one uninterrupted clean full local run. Full milestone CI remains required.

**Visual evidence:** inspected [desktop](handover/builder-launch/l4-samples-1440.png) and [phone](handover/builder-launch/l4-samples-390.png) captures. Both illustrations render, the licence remains identifiable, and the browser checks document width at both sizes.

**Next:** complete L4 CI, then deploy the updated projects function, helper/shared source, client-worker export compiler and frontend together. The older client-worker runtime must be upgraded to retain these export records. L4 is not yet deployed; production/staging remain `8a9c94c`. No migration is required. Continue directly with L5 after rollout; automated checks do not replace Claude's review or Sean's later use.


### L4 milestone rollout — 14 September 2026

**Implemented, verified and deployed at `d2d9e99`.** [Milestone CI](https://github.com/KaizenUK/kaizenNEW/actions/runs/34835454354) passes all **92 browser journeys**, with one optional licensed-archive skip, followed by **10 additional hosted-transport journeys**. The main unit step passes 682 cases and skips its conditional Nginx case; the dedicated Nginx step passes all 18 cases in that file, covering **683 distinct unit/integration tests** overall. Types report zero errors/warnings. All six function checks, independent client exports, standalone worker compilation and real Nginx activation/rollback pass. [PR 12](https://github.com/KaizenUK/kaizenNEW/pull/12) is merged.

The current projects function was backed up before deploying **`builder-projects` v10**, whose active version and JWT configuration were read back. Other functions and database state remained unchanged; no migration was needed. The root-owned helper and client publication worker now run the exact `d2d9e99` source with matching frozen dependencies. The helper passed isolated startup, activation and a real restart. Its full project backup is retained privately, and its managed staging checkout advanced by fast-forward with Sean's pending About edit, drafts, recovery, settings and operation records preserved byte-for-byte. The client worker passed its standalone compiler check and normal service invocation; its timer is active. Only its dedicated Vite cache is writable by the worker.

[Production deployment](https://github.com/KaizenUK/kaizenNEW/actions/runs/34837444724), artifact **`gh-34837444724-1`**, and [staging deployment](https://github.com/KaizenUK/kaizenNEW/actions/runs/34837445122), artifact **`gh-34837445122-1`**, both pass. Separate VPS readback verifies the exact markers and **262 HTTPS origin responses for each**. Production's database head is live at the matching artifact with no pending release. Project/member counts remain unchanged, and client-demo retains artifact `300d7710-69c9-46fe-82ed-5e940997d80d`.

Read-only use of the actual public Kaizen builder confirms Pages loads the existing website through the upgraded helper, the gallery offers all six templates, and Home renders with its illustration in Desktop preview and the responsive menu in Phone preview. Escape returns focus to **Browse templates**. No page, starter, asset or account change was submitted as a live test. The isolated fixture remains the creation/publication proof; this live inspection does not claim Sean's manual gate acceptance or a separate CDN-wide file audit.

**Next:** L5-T1 and the remaining L5/L6 tasks. Firefox and WebKit have been installed and passed simple launch checks on the workstation in preparation; their actual builder journeys are not yet verified. The goal remains active, with human-only follow-up collected at the end.


## Implementation log — L5

### L5-T1 — Core journeys in three browser engines (14 September 2026)

**Implemented:** `playwright.builder.config.ts` retains the complete Chromium suite and adds 37 core scenarios each in Firefox and WebKit across 15 files: account, auth, projects, editor, first run, page templates, starter site, site canvas, site media, frame assets, hosted preview, hosted helper, hosted Save, companion and uploads. `.github/workflows/builder-checks.yml` runs the extra engines in independent jobs, with separate failure artifacts. The main integration job and additional hosted-transport pass remain explicitly Chromium. Retries and test timeouts were not increased.

The initial failures exposed real browser differences. `scripts/builder-hosted-preview-content.ts` and `scripts/builder-hosted-previews.ts` now route dynamic imports through a credentialed private module while retaining classic-script scope, original relative bases and import options. `SitePageEditor.tsx` mounts a fresh iframe for each rebuilt snapshot. `importQueue.ts` stores portable bytes rather than WebKit-rejected Blob records, while retaining reads of existing Blob recovery packs. `useSourceEditingDraft.ts` makes readiness belong to the current inspection; Back to pages stays unavailable during its pending draft read. This closes the post-Apply loading gap found by full Chromium regression.

The Supabase browser SDK moves from 2.98.0 to [2.116.0](https://github.com/supabase/supabase-js/releases/tag/v2.116.0), replacing the older refresh-lock behavior that produced Firefox errors. Real SDK account, recovery, invitation and companion journeys pass without suppressing console errors or supplying a custom lock bypass. The auth fixture now issues password-link redirects through a real temporary HTTP server because WebKit cannot fulfill a routed response as an HTTP redirect.

Browser fixtures explicitly select video captions and focus the window being resized. Drag checks wait for decoded, visible images and the actual dragged image reaching its target, then verify the committed order after the temporary clone disappears. An earlier pre-drop placeholder-position check was removed because the sortable placeholder can stay at its original slot until release; the final order assertion remains and is stronger. Hosted preview checks verify React hydration after rebuilding as well as on first opening.

**Proven:** the broad local run passed **165 browser scenarios**, skipped one existing optional licensed archive and found one WebKit rich-text insertion failure. Its drag setup now waits for the visible nested drop target; the complete editor suite then passes **15 scenarios across all three engines**, without retries. Together these runs cover all **166 applicable scenarios**: Chromium 92, Firefox 37 and WebKit 37. The 15-case command received a termination signal after Playwright wrote its passed result; an independent final invocation reran the corrected rich-text case in all three engines and exited normally with all three passing. The single broad run is not described as entirely passing; the full workflow repeats at the L5 milestone. `pnpm test` passes **686 tests across 78 files**, including Nginx; `pnpm typecheck` reports zero errors, zero warnings and 174 existing hints. Three added hosted-preview unit tests cover import behavior and reject the loader without the correct view cookie, with a hostile origin or after membership removal. A real browser recovery probe also reads a Blob record written in the earlier format. Controlled initial/post-Apply draft-read delays verify the loading boundary before completing apply, reopen and revoked-access recovery in all engines. All source, Git, account and upload operations use isolated fixtures and fake remotes.

Desktop and phone captures were inspected for each engine:

| Engine | Hosted canvas | Template gallery |
| --- | --- | --- |
| Chromium | [Desktop](handover/builder-launch/l5-browsers-hosted-preview-chromium-1440.png) · [Phone](handover/builder-launch/l5-browsers-hosted-preview-chromium-390.png) | [Desktop](handover/builder-launch/l5-browsers-templates-chromium-1440.png) · [Phone](handover/builder-launch/l5-browsers-templates-chromium-390.png) |
| Firefox | [Desktop](handover/builder-launch/l5-browsers-hosted-preview-firefox-1440.png) · [Phone](handover/builder-launch/l5-browsers-hosted-preview-firefox-390.png) | [Desktop](handover/builder-launch/l5-browsers-templates-firefox-1440.png) · [Phone](handover/builder-launch/l5-browsers-templates-firefox-390.png) |
| WebKit | [Desktop](handover/builder-launch/l5-browsers-hosted-preview-webkit-1440.png) · [Phone](handover/builder-launch/l5-browsers-hosted-preview-webkit-390.png) | [Desktop](handover/builder-launch/l5-browsers-templates-webkit-1440.png) · [Phone](handover/builder-launch/l5-browsers-templates-webkit-390.png) |

**Limits and next:** WebKit blocks the HTTPS hosted editor from embedding an HTTP loopback helper. Its developer checks therefore use the supported HTTP local builder; real HTTPS hosted editing is tested separately in every engine. The connection message and [builder guide](visual-builder.md#browser-support) explain this choice. No browser security flags, cookie permissions or preview CSP were weakened. Linux WebKit automation does not establish manual Safari/macOS/iOS acceptance, Claude's review or Sean's use. L5 milestone CI and deployment remain pending; continue directly with **L5-T2 accessibility** and the remaining L5/L6 work under Sean's autonomy instruction.


### L5-T2 — Keyboard editing and accessible controls (14 September 2026)

**Implemented:** workspace screens have a Skip to page content link and a main landmark. Canvas blocks have descriptive group names, a named iframe and keyboard instructions in the correct document. Enter selects a block; Space/arrow keys/drop/Escape provide keyboard movement and cancellation, including nested destination detection. Group semantics preserve interactive descendants. The existing Puck patch retains the earlier pointer fixes; a narrow dnd-kit patch uses group-compatible drag state instead of button-only `aria-pressed` on groups. Layers and first-run status icons also have valid semantics.

Publish and page-preview dialogs now use the existing Radix dialog primitive for focus containment, Escape and background hiding, with explicit focus return. Cancel initially receives focus in the publish check. Preview first transfers focus to the preview and later returns it to the Preview toolbar button. Source review returns focus to Review my changes. Separate light/dark foreground tokens improve links, labels, pills and buttons without changing their brand backgrounds.

**Files:** `client/visual-builder/{BuilderApp,EditorDialog,PublishDialog,SitePageEditor,FirstRunCard,shell}.tsx`, `builder.css`, the two package patches, package/lock files, `tests/builder/accessibility.spec.ts`, the affected editor/component selectors, test setup and `playwright.builder.config.ts`. The [builder guide](visual-builder.md#keyboard-and-accessibility) documents the keyboard paths. Test setup now clears only its fixed `test-results/builder-browser-workspace` before each run, so old fixture pages and palette entries do not accumulate.

**Proven:** full type checking reports zero errors/warnings. The unit run passes 685 cases with its conditional Nginx case skipped; the explicit Nginx run passes all 18 tests in that file, covering all **686 distinct unit/integration cases**. Relevant browser evidence covers **80 distinct scenarios** across Chromium, Firefox and WebKit: the original 68 accessibility/editor/first-run/template/canvas/library cases plus 12 shared-component, native-island and starter-site cases. All are covered by passing final checks; this is combined evidence, not a claim that the initial broad run passed in one invocation.

- The 68-case run passed 65 and found three test-harness issues: a pixel-exact drag-centre assumption and Firefox keyboard paths returning through the end of the document. Corrected checks retain real focus, visible movement, exact final block IDs, saved source bytes and rebuilt output. The completed keyboard and source-review journeys pass in every engine.
- Extra component checks caught selectors relying on the old button semantics. They now select the actual movable group. A local starter-publication invocation omitted the Nginx binary path; its corrected command uses the installed fixture server. The final component/starter run passes all 11 scenarios, including genuine Nginx staging publication in every engine; the independent native-island case also passes.
- Axe records **174 state/theme/engine scans** with zero serious or critical violations. [The retained summary](handover/builder-launch/l5-accessibility-audits.json) includes moderate landmark findings rather than suppressing them. Full reports are retained privately; the scanner uses no rule exclusions. Its temporary results tab is closed before returning to the original page for keyboard actions. Tests use Tab/Shift+Tab and verify the focused element's own document is actually focused, including the canvas iframe.

**Visual evidence:** the following desktop/phone captures were inspected for readable controls and unclipped dialogs/previews:

| Engine | Captures |
| --- | --- |
| Chromium | [Pages light desktop](handover/builder-launch/l5-accessibility-chromium-light-1440.png), [Pages dark phone](handover/builder-launch/l5-accessibility-chromium-dark-390.png), [Publish light phone](handover/builder-launch/l5-accessibility-publish-chromium-light-390.png), [Preview dark phone](handover/builder-launch/l5-accessibility-preview-chromium-dark-390.png) |
| Firefox | [Publish dark desktop](handover/builder-launch/l5-accessibility-publish-firefox-dark-1440.png), [Preview light desktop](handover/builder-launch/l5-accessibility-preview-firefox-light-1440.png) |
| WebKit | [Pages light desktop](handover/builder-launch/l5-accessibility-webkit-light-1440.png), [Publish dark phone](handover/builder-launch/l5-accessibility-publish-webkit-dark-390.png), [Preview light phone](handover/builder-launch/l5-accessibility-preview-webkit-light-390.png) |

**Limits and next:** automated coverage is not a screen-reader assessment, blanket WCAG certification, manual Safari acceptance or Sean's hands-on acceptance. Moderate landmark findings remain visible in the report. Existing development ResizeObserver notifications are still recorded; no console errors were suppressed. L4 remains deployed, and full L5 milestone CI/rollout remain pending. Continue immediately with **L5-T3 performance**, then the rest of L5 and L6; human-only actions stay at the goal's end.


### L5-T3 — On-demand editor loading and production measurements (14 September 2026)

The page editor, existing-website editor, standalone Assets view and local/hosted website-folder tools now download when opened. `PageEditor.tsx` retains the existing editing implementation and its asset provider. `BuilderFeature.tsx` supplies a named loading state with **Back to pages**, and a failed-download state with **Reload**. Saved pages survive leaving either state. A separate `downloadText.ts` stops Settings and Site design from pulling the asset library and Puck into the opening screen. Explicit Puck dependency preparation prevents the local development server's first editor import from reloading the workspace. The build warning threshold remains unchanged.

**Production measurement:** two isolated source/dependency snapshots were built with an empty publication fixture and fake public Supabase configuration; neither artifact is suitable for deployment. `scripts/measure-builder-performance.mjs` serves real gzip-compressed HTTPS, uses the production app and SDK with fixture accounts/API replies, and rejects unexpected requests and early editor downloads. Five fresh Chromium contexts per build run in alternating order, with cache disabled, fourfold CPU slowdown, 40 ms network latency, 10 Mbps download and 1 Mbps upload. Google/Inter font requests are stubbed. First useful paint is two animation frames after Pages has a visible enabled Blank page button and project name; the earlier Astro opening message does not count. The [raw measurements](handover/builder-launch/l5-performance.json) include all runs, initial scripts, hardware, limits and the expected disconnected-repository fixture responses.

The final comparison records **1.29 seconds → 0.93 seconds** median useful paint (about **28% faster**) and **574,052 → 340,710 gzip JavaScript bytes** before that point (**40.6% fewer**). The largest JavaScript chunk falls from **1,358,937 to 504,575 bytes**; the BuilderApp entry is **115,933 bytes**. Neither editor nor the asset-library chunk is requested before opening a feature. The production build passes without the previous oversized-chunk warning. An earlier valid repetition measured 1.26 → 0.89 seconds with the same transferred bytes. Initial harness attempts used HTTP, which production's hosted helper correctly rejected, and had an unstubbed external font; those failed attempts are excluded.

**Browser proof:** all **45 selected scenarios pass** in the final two runs: 35 editor/feature-loading/library/source-canvas/media journeys, plus ten first-run/shared-component/hosted-helper journeys. The new nine cases cover all three engines, delayed downloads, Back, real save/reopen, cached import failure and successful explicit Reload, and an existing-site build after delayed loading. Existing checks retain asset replacement/live isolation, rich text, pointer dragging, preview/publication, source review/apply, hosted build failures/cancellation and shared-component overrides. The initial interrupted run found a first-use local reload and an exact 100% viewport assertion failing at a fractional-pixel ratio; Puck preparation and a 99% visibility requirement fix these while preserving actual dragging and final block-order checks. The new test's nested-button selector was corrected to click the page-row button itself. The final run starts from a fresh development cache. Deliberately failed-import console errors remain visible.

**Types and unit/integration checks:** `pnpm typecheck` passes with zero errors/warnings and 173 hints across 400 files. The full `pnpm test` run with the explicit local Nginx binary passes **all 686 tests across 78 files**, including real temporary Nginx activation/rollback checks. `git diff --check` passes. Milestone CI is deliberately deferred under Sean's batching instruction.

**Screens:** loading and recovery states were inspected at both widths: [desktop loading](handover/builder-launch/l5-feature-loading-1440.png), [phone loading](handover/builder-launch/l5-feature-loading-390.png), [desktop recovery](handover/builder-launch/l5-feature-unavailable-1440.png), [phone recovery](handover/builder-launch/l5-feature-unavailable-390.png). The [builder guide](visual-builder.md#loading-the-editors) documents behavior and the comparison runner.

**Limits and next:** this Intel Core Ultra 7 host with CPU/network constraints is a repeatable proxy, not proof on a physical mid-range laptop. That hardware check, real-network/workspace timings and manual gate use remain goal-end acceptance items. L4 remains deployed; L5 milestone CI/rollout remain pending. Continue directly with **L5-T4 security**, then backups, legal and L6. No live account, personal workspace or production content was used as test data.

### L5-T4 — Request, database and build boundaries (14 September 2026)

**Implemented and verified locally:** all six public builder functions use durable database request counters. The global counter precedes expensive authentication; the account counter uses the verified Auth identity. Denials return 429 and a bounded Retry-After; unavailable counters return 503 without performing business work. The contact receiver retains its existing sender limits. Publication requests now have a streamed 64 KiB JSON limit and explicit action/version validation. Counter rows contain endpoint names and global/account identifiers, with hourly deletion of idle rows older than one day. These limits bound function work, not invocation charges or volumetric traffic.

The full migration fixture seeds all **23 builder tables** with synthetic canaries, including separate accounts/projects and expired previews. It exposed inherited TRUNCATE/REFERENCES/TRIGGER privileges on older tables under permissive platform defaults. Migration `202609140003_builder_explicit_access.sql` replaces browser table privileges with explicit grants, retains column restrictions on worker fields, and limits browser RPC execution to the existing reviewed API. Definer functions put pg_temp after public in their search path. The tests cover anonymous/private reads, cross-project reads, direct mutations, revocation, archived access, public redirect projection writes and temporary-table shadowing. Only the two scheduling-only migrations are excluded from PGlite; actual cron operation remains a rollout check.

The client build implementation uses Linux namespaces and a separate cgroup for each build. Read-only source/dependency inputs feed disposable memory-backed working copies, allowing normal framework caches without network or a writable host bind. The parent accepts only a bounded regular-file output stream and retains the existing runner's review, source fingerprint, preview verification and output recovery. Defaults are 2 GiB memory, no swap, 128 processes and two CPU cores per build. An immutable operator setting can retain the existing trusted Kaizen build path; website owners cannot grant that exception. The local developer helper stays available. The [runbook](website-releases.md#client-build-isolation-l5-implementation-rollout-pending) records the shared-kernel boundary, configuration and scope.

**Local proof:** all **727 unit/integration tests across 80 files** pass with Nginx enabled; full types report zero errors/warnings and 173 existing hints. Deno checks all six functions. Actual entry points pass **17 request-boundary steps plus bounded-body checks**, using the real SDK and fixture HTTP responses without network permission. They exercise global/user/outage denial before business work, forged/missing/malformed/expired identity, wrong origin, body size/format, action types and invalid redirect versions. The final Deno check found unknown-body type errors; explicit runtime validation corrects them and the added negative cases pass.

The independent VPS service passes **14 npm checks** and **17 pnpm checks**, including a real Astro 7.3.2/React island build. Tests prove denied host-file/environment/network access, rejected output and dependency links, cancellation, actual cgroup OOM for memory and tmpfs writes, process limits, previous-output recovery, successful served-preview verification and cleanup. The host's source, private canary and dependencies remain unchanged. Early runs exposed runtime ownership, namespace/procfs, npm configuration, bundled CommonJS context and read-only Vite cache problems; these were corrected. A later added negative fixture left an empty dependency directory and blocked its subsequent copy; removing that fixture-owned empty directory fixed the final pnpm run. CI now reproduces the real namespace/cgroup test. No live account or website supplied fixture data.

The browser command passed **12 of 13** selected journeys; the client Save case stopped on `socket hang up` in the local fixture's project-list GET before Apply. Its trace is retained privately. An unchanged focused rerun passes all three hosted Save journeys, including client/developer modes, source/Git isolation, rejected-push recovery and interrupted-commit restart. Combined passing evidence therefore covers all **13 selected journeys** across accounts, invitations, contact, Releases and real HTTPS hosted editing/building. No retries, assertions or timeouts were changed, and no application or documentation file was edited during browser/probe execution. The first command is not represented as one clean run. These changes add no new screen; the existing desktop/phone views were exercised by the retained journeys.

The pinned, fully redacted new-change secret scanner and its synthetic scope tests pass against only the changes after the L4 baseline. CI audits the root and separate Studio lockfiles plus the actual function graph, failing on high/critical advisories or registry errors. Studio/functions have no known findings; the root graph has one moderate adm-zip CLI advisory with no patched version listed, documented in the runbook without an ignore rule. The inspected CLI helper creates ZIPs, and builder imports use their existing archive implementation. This records audit scope rather than claiming a fix to the dependency.

**Files:** shared function limiter/body reader, all six function handlers, three migrations, the 23-table access matrix, account/invitation/contact tests and fixtures; `scripts/builder-build-{sandbox.ts,payload.py}`, runner/helper/unit integration, the VPS verification fixture and preparation script; `scripts/check-new-secrets.py`, its fixture and CI wiring; the release runbook and this log.

**Deployment and next:** no L5 migration, function, helper or frontend rollout has happened. L4 remains live. Apply migrations `202609140001`–`202609140003` before the functions, verify retention cron, and deploy the matching helper/unit and immutable package manager at the L5 milestone. Implementation is now **26/32 tasks (81% rounded, by equal count)**. Continue immediately with **L5-T5 backups and monitoring**, then legal and L6; milestone CI and manual/external-only follow-up retain their agreed cadence.

### L5-T5 — Encrypted backups and recovery (14 September 2026, in progress)

**Implemented so far:** `scripts/ops/backup.py` coordinates helper/release locks, captures working copies and retained releases/settings with non-dereferencing copies, detects changes during capture, and stores them in an encrypted restic repository. It verifies encrypted data and an actual private restore against the path/hash/mode/owner/link manifest before recording success. Failed database captures remove their partial private stage and preserve the prior verified receipt. Restore requires an exact snapshot and a new unserved directory. Retention keeps seven daily, four weekly and three monthly calendar buckets.

`supabase_backup.py` adds a native full PostgreSQL archive, role definitions without passwords, and original Storage object bytes. It uses a short-lived read-only login, explicit `BYPASSRLS`, session-pooler TLS with certificate/hostname verification, bounded readiness retries using the same password, and renewal before a new connection after expiry. No schema exclusions or row-security filtering make an incomplete dump appear successful. Download checks cover object identities, recorded sizes/content hashes, bounded bytes, encoded remote names, hashed local names and a stable Storage inventory across database capture. Database/Storage and filesystem captures are not one cross-service transaction; recovery must reconcile database release heads with retained artifacts.

`replicate.py` copies the exact verified source snapshot into an independently encrypted repository, verifies its original identity, manifest and encrypted data, retains recovery points independently and preserves the previous receipt on failure. Repeated copying is idempotent. Examples live in `deploy/ops`; [the recovery guide](builder-backups.md) records the operational commands, scope and limits. The new milestone CI job runs the complete operations suite with real restic and PostgreSQL 17.

**Proof:** all **25 operations tests pass together** as an unprivileged user: eleven file-backup, ten provider-boundary, one native database restore and three replica cases. The database drill stops its source server before restoring on a second private Unix-socket server, proving both owners' rows, role grants, RLS, a revision trigger and matching Storage metadata/bytes. The replica drill makes the source repository unavailable before recovering the original contents from its independent copy. Tests use synthetic temporary data and never send email.

The native fixture initially lacked client binaries; matching signed-repository packages were extracted beside its server tools. A different PostgreSQL bootstrap identity failed the recorded predefined-role grantor; the documented preparation now recreates the original bootstrap role and omits only its already-created `CREATE ROLE` statement. Removing the default `public` schema also failed because this native dump relies on PostgreSQL's existing empty default schema; the corrected procedure preserves it. A test-file syntax error was corrected before execution. These failures were diagnosed without weakening ownership, grant, row or trigger assertions. The passing fixture establishes native recovery of its actual full synthetic archive, not arbitrary reconstruction of every Supabase-managed extension or service.

**Actual operations:** the VPS snapshot `cd6e5b9b852a40451b207c1dc35deabc29863b3df3627a1133f20aec180a4b42` is verified: 17,074 paths across the helper working copies, production/staging/client-demo stores and two operator-settings directories; all 13 non-system PostgreSQL schemas; ten Storage objects across three buckets. Its temporary plaintext capture/verification directories are removed and the helper remains active. An independently encrypted workstation copy, `19af5a908f3f06496ba6fccd6e2d583cbd1bb35cd6a050412e4b6cdde389e6b4`, is verified against that source. Private encryption passwords are retained separately. These are authorized operational backups and read-back verification, distinct from synthetic application tests.

**Remaining at the first-backup checkpoint:** server-configuration inventory, schedules, monitoring, served-release recovery and final guidance/checks. The continuation below supersedes this initial operational state. Paid PITR activation, an always-on offsite destination and manual delivery acceptance remain goal-end items; they do not hold implementation.

### L5-T5 scheduled operations continuation — 14 September 2026

**Operational:** the backup timer, workstation replica timer and VPS monitoring timer are enabled. A real capture under the installed systemd restrictions verified snapshot `c33cd3634d9242152478e2a3037ffb1dffc507549642791c03b3c464f0c5f2f9`: **17,129 paths across 39 sources**, with the database and Storage capture. The expanded sources include selected Kaizen DirectAdmin routing/TLS files, Nginx configuration, service units, service-account inventory and the operations runtime. The temporary force-capture override was removed after success; the service again uses its normal due check. Temporary plaintext capture/restore directories are gone and the helper remains active.

The actual user service verified independent replica `83d25035de4e5dd7cd9d987492dddef746ec93fd6ae3d1c8e5bf35bc31613234` and acknowledged that source/destination pair to the VPS. The monitor uses the source capture age, so repeating a copy cannot make stale data appear fresh. The receiving workstation still must be online. The root-owned immutable VPS runtime and private workstation runtime are now `fb6c73d789f9a091`.

`monitor.py` and `receive_replica.py` cover service state, helper health, selected production/staging/client-demo markers, backup freshness, disk reserve and aggregate hosted client/publication errors. A first successful scheduled-service run passed **all 17 checks**, with `alertQueued: false`. Confirmed failures queue one Exim alert after two observations and one recovery message; repeated unchanged failures do not send repeated messages. No real email was sent as a test, and inbox delivery is not claimed. `.github/workflows/kaizen-uptime.yml` adds external HTTPS marker/homepage checks; its schedule becomes available after the L5 merge to the default branch, with GitHub notification routing still to be accepted.

**Additional proof:** the full operations suite passed **39 tests together on the VPS**, including an actual served website restore through the real release engine and Nginx. The fixture makes the original store unavailable, rejects its old absolute-path include, serves the independently restored artifact, verifies redirects/private-path denial, publishes a retained candidate and rolls back. Existing working-copy edits remain unpublished. The first operational hosted-error check then found that the Management API returns HTTP 201 for its successful read-only query; the monitor now explicitly accepts 200/201 for that request, retaining HTTP 200 for website/helper checks. All nine monitor tests pass locally after the correction, including a new real loopback HTTP 201 regression case. This gives **40 distinct passing operations cases across the runs**; the complete 40-case rerun is still pending. All **727 application tests** and full type checking pass, with zero errors/warnings and 173 hints.

Client-demo's retained artifact lacked release-identity response headers. Its selected configuration was refreshed through the deployed release engine as the store owner: transaction `6aea52a1-fe79-4750-bd4b-228f92ec283b` is live, all 11 explicit identity checks pass, and the artifact identity and manifest bytes are unchanged. Nginx reloaded successfully. Both real public HTTPS checks then passed. This was an operational configuration rollout; no L5 application, helper, function or migration rollout has happened.

**Next:** finish the recovery guide and relevant backup/native-backup/release browser checks, commit T5's implementation, then continue T6 and L6. Provider PITR is still disabled and requires paid activation at the goal's end; logical snapshots do not satisfy that requirement. Notification delivery and an always-on offsite destination remain explicit final setup items. T5 is still in progress, the implemented count remains **26/32**, and the goal remains active without a user-input hold.


**T5 final recovery correction:** a replacement host could not use the original restore command because construction required every old source directory to exist. Recovery now needs only an independent repository, its own password and private state/configuration. It reads authenticated source paths from the snapshot before writing and rejects restoration over those paths even when they no longer exist. Recovery configuration cannot initialize/capture backups. `deploy/ops/restore.example.json` and the rewritten guide document replacement-host recovery, numeric ownership, database/Storage reconciliation and provider limits. The replica test now calls the actual CLI with original repository, source folders and source password unavailable; the website drill removes original stores before restoring. All **41 operations tests pass together on the VPS**, no skips; the workstation passes 40 with the native PostgreSQL case skipped. Runtime `b0c52276330b1c39` is installed on both hosts. The earlier real capture/replica remains authoritative, without claiming a new capture for this revision. Relevant browser checks are next; task count remains 26/32 until the implementation is verified and committed.


**T5 implementation verification complete:** all three relevant Chromium journeys pass (editable backup restore/edit, native folder restore with unapplied draft, explicit release rollback/unpublish review), following all 41 operations tests, 727 application cases and clean full types. Repository files remained unchanged throughout the browser run. Inspected backup review screenshots are retained at [desktop](handover/builder-launch/l5-backup-review-1440.png) and [phone](handover/builder-launch/l5-backup-review-390.png) widths; T5 adds no new application screen. The final installed runtime's due check correctly retains the fresh actual backup, and all 17 read-only health observations pass. The agent implementation count advances to **27/32 (84% rounded)**. This does not claim paid PITR activation, an always-on offsite service, hosted-provider recovery acceptance, real email delivery, milestone CI or Sean/Claude acceptance. Those remain explicit final/milestone work. Continue directly with **L5-T6 legal/privacy**, then L6.


### L5-T6 — Versioned documents and personal-data requests (14 September 2026)

**Implemented:** standalone proposed private-beta Terms of Service and Privacy Policy pages retain dated source and registered SHA-256 hashes. Hosted sign-in requires an explicit choice and a server-confirmed acceptance of those exact documents; password setup comes first, previously accepted versions retain their original timestamp, and stale/malformed/unknown outcomes require refresh or reload. Account switching discards older responses. Acceptance does not opt anyone into optional marketing or tracking. Email enquiries remain available without accepting or signing in. Sean's autonomy instruction authorises the draft wording; final legal/provider review stays at the goal's end and public subscription activation needs a reviewed public version.

Account now separates personal-data copy/erasure requests from login deletion. A requester can view history and cancel an open request; only a current website owner can review that website's requests. Account and orphan requests go to the private operator CLI. Request creation is idempotent, daily-bounded and paginated; response updates check the current version, with explicit secure-delivery acknowledgement before completion/decline. These controls record a response and do not automatically export data, delete a website, erase records or send email. Closed requests and deleted-account acceptance have scheduled retention with operator holds. The [privacy operations guide](builder-privacy.md) covers responsibility, secure handling, immutable version changes, private operator commands, hard-deletion limits and erasure after restoration.

**Files:** `BuilderLegalGate.tsx`, `PrivacyRequests.tsx`, `accountService.ts`, `AccountPage.tsx`, `BuilderAuth.tsx`, `builder.css`, shared legal/privacy types, the existing account function and new shared privacy actions, migrations `202609140004`/`202609140005`, the operator script, standalone Astro layout and dated Markdown pages. Unit/HTTP/database/operator/browser fixtures prove the boundaries; CI now includes these paths and runs the new journeys in its Firefox/WebKit core projects.

**Proven:** all **770 tests across 84 files** pass with the local Nginx binary; full types report **zero errors, zero warnings and 174 existing hints** across 412 files. The actual account function passes Deno checks and the existing real-entrypoint boundary suite passes two tests with 17 negative steps. Sixteen database cases execute the real legal/privacy migrations, and the access matrix now covers all **26 builder tables**. Seven gate cases cover explicit confirmation, Strict Mode, prior acceptance, newer versions, stale account responses, sign-out during a pending read and recovery. HTTP tests include full-length UTF-8 owner responses and preserve the smaller legacy account-deletion envelope. Operator tests use only temporary private files and fake RPCs.

The selected account/auth/legal suite passes **all 27 browser journeys** in Chromium, Firefox and WebKit. After screenshot-led checkbox/textarea corrections and the UTF-8 envelope change, all **nine legal/privacy journeys** pass again. These use the real Supabase browser SDK through fake provider HTTP boundaries into actual fixture database functions. They prove first acceptance, timestamp persistence, account switching, refusal/sign-out, current-owner routing, cancellation, response history and public document availability at both widths. Repository source was held steady throughout each browser run. No live account, email, invitation, website content or personal workspace was used as test data.

Initial checks exposed a missing `pg_temp` search-path restriction, an old sign-in fixture that did not update its simulated session, narrow literal types in a new test, an operator-guide link unsupported by embedded help, and a changed invalid-body status after expanding UTF-8 support. Each was corrected and the complete final suite passes. The first selected browser run also logged one React cross-component state-update warning in Firefox's incorrect-password/email-confirmation journey; that journey passed. Keep this observation for milestone investigation rather than suppressing it or claiming a warning-free browser run.

**Screenshots:** acceptance [desktop](handover/builder-launch/l5-legal-acceptance-1440.png) / [phone](handover/builder-launch/l5-legal-acceptance-390.png); owner response [desktop](handover/builder-launch/l5-privacy-owner-1440.png) / [phone](handover/builder-launch/l5-privacy-owner-390.png); Terms [desktop](handover/builder-launch/l5-legal-terms-1440.png) / [phone](handover/builder-launch/l5-legal-terms-390.png); Privacy [desktop](handover/builder-launch/l5-legal-privacy-1440.png) / [phone](handover/builder-launch/l5-legal-privacy-390.png). All eight were inspected.

**Deployment and next:** implementation advances to **28/32 tasks (88% rounded, equal count)**. L4 remains the live application; backups and monitoring are already scheduled. Run full L5 milestone browser checks and CI, then deploy the prepared database/function/helper/unit/frontend changes together. Verify both retention cron jobs and the new public document routes without accepting terms for Sean. Final legal/provider review, privacy-address delivery/operator review cadence, PITR/offsite/notification acceptance and manual gate use remain goal-end actions. Continue directly into L6 signup, billing, custom domains and quotas after the L5 rollout; the goal remains active without a human-input hold.

### L5 milestone verification corrections — 14 September 2026

The first [milestone CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34893837033) failed. Its main job passed the application tests and release checks, then could not package the Linux isolation fixture because `esbuild` was only an indirect dependency. It is now an explicit development dependency at the already locked 0.28.1 version. Both additional browser engines found that the first-run account-switch test had retained the Account fixture's deep link: switching identity correctly remounts the legal gate/workspace and reopens that link. The checklist journey now starts at its actual Pages URL and still checks separate remembered state for both accounts.

The local full browser run also found two hosted fixtures without a legal-state response and a conversion test clicking the rendered article beneath the canvas overlay. Diagnostics and invitations now declare their synthetic accounts' existing acceptance of the exact current documents, reject unexpected account mutations, and retain their original journey assertions. Conversion selection uses the named canvas group, then still edits, publishes and verifies the responsive result. The failing diagnostic run was deliberately interrupted before source edits; it is not a completed full-suite result.

The recovery CI job passed 40 of 41 cases. After a successful rollback, its immediate second observation met a draining Nginx worker from the preceding release. The fixture now uses the release suite's bounded full-observation pattern after activation and rollback. Every response's bytes and release identity remain checked; no publication or rollback is replayed and no failed assertion is skipped.

**Corrected local evidence:** 31 selected browser journeys pass across the configured Chromium, Firefox and WebKit projects; full types report zero errors/warnings and 174 hints. All 40 locally available operations cases pass, including served website recovery; the native PostgreSQL case passes separately on the VPS. The explicit isolation packager succeeds, and its unchanged real namespace/cgroup fixture passes all 17 checks with the prepared root-owned Node 22.23.2 and pnpm 10.32.1, including a real Astro/React build. Both tool archives were checked against their pinned integrity values; the live helper process and active toolchain remain unchanged. An initial local operations command omitted the required tool paths, and the first VPS fixture copy omitted a redirect module and selected client-only PostgreSQL binaries; these harness setup failures are retained in the private logs. The complete corrected VPS operations run remains pending.

The earlier React cross-component update warning did not recur in the selected auth rerun or the first CI browser jobs. A WebKit module-import rejection during account-fixture navigation remains recorded; its journey passed. No console errors are suppressed and these observations are not presented as a warning-free browser result. **Next:** corrected full milestone browser/CI verification, coordinated L5 rollout, then all four L6 tasks. The count stays **28/32** and no user-input hold is required.


### L5 milestone CI toolchain and fixture cleanup — 14 September 2026

The complete local browser run on `a64dc33` passes **202 scenarios with one optional licensed-archive skip**, across Chromium, Firefox and WebKit. The second [milestone CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34896084702) passes all **41 recovery cases** together and all **49 Firefox journeys**. Its main job passes the 770 application tests and release checks, then rejects the runner-cached npm executable under the existing ownership/mode policy. The isolation step now copies Node/npm into a separate root-owned directory without group/world write permission. It uses that copy for the actual namespace/cgroup fixture; the production guard remains unchanged.

WebKit exposed two fixture cleanup failures: a disposed API response and a route already handled while the forwarding handler was replying. An initial wait-only correction still failed Chromium recovery and WebKit hosted editing (43 selected passes, two failures). A separate two-request probe reproduced the route-removal race in the installed Playwright version. The fixtures now close the page to stop new requests, then wait for every tracked handler before disposing their database, API context or helper. Handler errors still propagate. The new browser regression holds two requests across page closure, verifies cleanup waits for both and reads real API response bodies before completion. Account, hosted helper/preview/save/setup/publication and automatic repository-fixture cleanup use this lifecycle. Existing user actions, authorization, delayed draft reads, source/remote bytes and recovery assertions remain; retries and error suppression were not added.

First-run account switching now begins directly at Pages, avoiding an unnecessary navigation that cancelled the initial Account module download. Hosted draft-read delays use one callback inside the existing HTTP forwarder. Full types report zero errors/warnings and 174 hints; TypeScript-only checking also passes. The final targeted run passes **62 browser scenarios** across the configured three engines, including the new regression, authentication/accounts, legal/privacy, first-run, hosted editing, preview, Save, recovery, setup and publication. Repository source stayed fixed throughout that run. The previously recorded module-import and React state-update errors did not appear in this selected run; this does not erase the earlier observations or claim every console message was absent.

**Next:** the corrected milestone revision must pass full CI, then the five database migrations, six functions, immutable helper/worker tools, service configuration and frontend ship together. The prepared runtime/deployment drivers remain inactive. Live L4 and Sean's pending About edit are preserved. The implementation count remains **28/32**; continue into all four L6 tasks after rollout, with human-only follow-up collected at the end.


### L5 milestone isolation profile and nested typing — 14 September 2026

The third [milestone CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34900748255) passes all **770 application tests**, all **41 recovery cases** and all **50 WebKit journeys**. The immutable runner toolchain now passes its ownership guard, but Bubblewrap's loopback setup is denied by Ubuntu's restricted unprivileged-user-namespace policy. The VPS already has a loaded, scoped Bubblewrap AppArmor profile. CI now installs the distribution's `apparmor-profiles` package and loads its `bwrap-userns-restrict` profile when that host restriction is enabled. Both the setup and child profiles must be enforcing, and the global restriction stays enabled. The existing production profile is unchanged; the operator guide records this prerequisite and its recovery requirement. Workflow YAML/shell syntax and the distribution profile's parser check pass; the new runner configuration still needs actual CI proof.

Firefox's remaining CI failure showed old rich-text content at its formatting assertion. Eight repetitions of the original fill-based journey passed locally, so that exact intermittent result was not reproduced. Replacing the fixture interaction with actual character typing and explicit selection/content checks exposed a deterministic input-loss bug: after the first letter, the nested editor was detached and later keys reached the frame body. Puck creates a new slot-render function when content changes; mounting that changing function as a component replaced its descendants. The builder now invokes the slot render function so the underlying component remains mounted. Temporary diagnostic code was removed. The regression checks full text selection, uninterrupted typing and focus, matching canvas/sidebar content, bold, safe links, autosave, reopening and preview. WebKit's selected paragraph includes a trailing newline; only that selection boundary is normalized.

**Final local evidence:** all **60 relevant browser scenarios** are covered by passing checks across Chromium, Firefox and WebKit: nested editing, keyboard/accessibility, templates, starter creation, registered slots, conversions and content bindings. The broad run passed 57 and failed three starter-publication fixtures because the local command omitted its Nginx executable path. The unchanged three cases then pass with the correct path; this is combined evidence, not a claimed single green 60-case run. Earlier keyboard and fixture failures, diagnostic logs and the broad run's artifacts are retained privately. Full types report zero errors/warnings and 174 hints. The full application run passes 769 with the conditional Nginx case skipped; the real-Nginx follow-up passes all 18 cases in that file, covering all **770 distinct cases**. Source stayed fixed throughout each browser run.

Prepared exact-`9b0ef45` helper and worker runtimes remain inactive. An empty-project service using the planned helper settings passes startup, restart and delegated cpu/memory/pids checks; the live helper process and selected runtimes remain unchanged. Initial temporary-directory and fixture-parent-permission errors were corrected in that separate fixture. Current function backups are retained privately; no L5 migration, function or application activation has happened. The final correction needs new exact-revision runtimes/backups and successful milestone CI before coordinated rollout. The count stays **28/32**; proceed through L6 afterward, collecting human-only actions at the goal's end.

### L5 milestone existing dependency cache verification — 14 September 2026

The fourth [milestone CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34903912385), at `ddac865`, passes all **770 application cases**, all **41 recovery checks**, **50 Firefox journeys** and **50 WebKit journeys**. Ubuntu's packaged, scoped Bubblewrap profiles load and permit the actual isolated build with the global namespace restriction still enabled. The main job then fails because the isolation fixture assumes `node_modules/.vite` did not exist before its build. CI has already built with those dependencies. Chromium and the additional hosted transport step are skipped after this assertion; the whole milestone is not passing yet.

A separate VPS fixture using copied dependencies and a pre-existing cache reproduces the original failure while that cache remains unchanged. `tests/builder/verify-build-isolation.ts` now fingerprints the complete host dependency tree before and after the build, including file bytes, links, permissions, ownership and modification times. It ignores access times because reading files changes them. The sandbox deliberately overwrites an existing cache canary and creates another cache file before building Astro. The host tree must remain identical, while the generated HTML, React island, JavaScript, runner recovery/cancellation and resource cleanup checks still pass.

**Proven:** the corrected fixture passes all **17 checks** in actual VPS Linux namespaces and delegated cgroups against the same provisioned dependency tree containing the pre-existing cache. The external cache canary remains unchanged. Full types report zero errors/warnings and 174 hints; packaging, formatting and whitespace checks pass. The baseline failure and corrected run logs are retained privately. This changes only the verification fixture, with no production isolation change. Existing browser/application evidence for `ddac865` remains recorded above; full CI must verify the final milestone revision before rollout.

Exact `ddac865` helper/worker runtimes and refreshed function backups are prepared but inactive. The live helper and selected L4 runtimes remain unchanged. Refresh preparation for the final verified revision, complete coordinated L5 rollout, then continue all four L6 tasks. Implementation stays **28/32**; human-only actions remain at the goal's end.

### L5 milestone parallel Chromium verification — 15 September 2026

The fifth [milestone CI run](https://github.com/KaizenUK/kaizenNEW/actions/runs/34905287872), at `eb244b1`, passes all **770 distinct application cases**, all **41 recovery checks**, all **17 real namespace/cgroup isolation checks**, and **205 applicable browser journeys**: Chromium 105, Firefox 50 and WebKit 50. The existing optional licensed-archive scenario is skipped. The main job then reaches its overall job deadline during the last additional hosted-transport scenario; nine of ten pass before cancellation. No test assertion failed, but the interrupted final check and the cancelled workflow do not count as completed verification.

`.github/workflows/builder-checks.yml` now runs Chromium alongside Firefox and WebKit in the existing browser matrix. Chromium still selects the complete suite; the other engines retain their core-journey selection. The main job keeps application/function checks, independent exports, Nginx release checks, real build isolation and all ten hosted-transport scenarios. Each browser uses its own fixture workspace and installed Nginx. Browser jobs have a bounded execution limit separate from the main job; individual test deadlines, zero retries and assertions are unchanged. This removes the serial browser workload that exhausted the main job's execution allowance without dropping coverage or changing production code.

Workflow YAML parsing, formatting and whitespace checks pass. A comparison against the previous workflow confirms that every main-job command remains except the Chromium command moved into the matrix, all browser setup steps remain identical, and operations are unchanged. Exact `eb244b1` helper/worker runtimes and six function backups are ready but inactive, and release/database readback scripts now cover the two public legal routes, publication head and preserved working copy. No L5 database, function, helper or frontend activation has occurred. Verify the corrected workflow and its final revision, roll out L5, then continue all four L6 tasks. The count remains **28/32**, and the goal continues without a user-input hold.

### L5 milestone rollout — 15 September 2026

**Deployed:** `48823c6f04d49073eb1eb20be7bacc92556032cb`. [Full milestone CI](https://github.com/KaizenUK/kaizenNEW/actions/runs/34907172695) completed successfully with all five jobs green. It covers all **770 distinct application cases**, full types and function checks, independent exports, **41 recovery checks**, **17 real namespace/cgroup isolation checks**, **205 applicable browser journeys** (105 Chromium, 50 Firefox, 50 WebKit; one optional licensed-archive skip) and all **ten additional hosted-transport journeys**. The earlier local 60-journey combined evidence remains scoped to its recorded revision; the final CI verifies the exact deployed revision. The workflow correction changes scheduling without dropping checks.

All five L5 migrations were applied atomically after fresh private backups. The migration ledger matches the exact SQL sources; verification covers 52 role/table matrix rows for 26 Builder tables, the four new private RLS tables, hidden worker/token columns, function grants/search paths, both retention schedules and the legal document hashes. Existing project/member counts and the prior publication head were preserved during migration. Active functions are `builder-projects` v11; `builder-publish`, `builder-content` and `builder-contact` v8; and `builder-invite` and `builder-account` v2. Unrelated functions are unchanged.

Root-owned immutable helper and client-worker runtimes both select exact `48823c6`, with the recorded toolchain and build-isolation configuration. The helper passed startup and a real restart; its managed checkout advanced with pending About source, drafts, recovery, receipts and private settings preserved. The activation's server-side success record and fresh independent health read prove completion. Its outer local SSH transport stayed open after the remote process completed and was closed only after that proof; the resulting local exit 255 is recorded, not represented as a clean command exit or replayed activation. Client-worker activation and its ordinary scheduled oneshot passed, and its timer is active.

Main and stage advanced by ordinary atomic fast-forward. [Production deployment](https://github.com/KaizenUK/kaizenNEW/actions/runs/34909251359) and [staging deployment](https://github.com/KaizenUK/kaizenNEW/actions/runs/34909251210) both succeed on attempt 1. Independent release-engine readback as the deployment service owner verifies **283 HTTPS origin checks each**, including every retained file, response identity, redirects and the versioned Terms/Privacy routes. Production artifact is `gh-34909251359-1`; staging is `gh-34909251210-1`. The production database head is live at the same artifact, with zero pending jobs and unchanged project/member counts. Client-demo still serves `300d7710-69c9-46fe-82ed-5e940997d80d`.

Read-only browser inspection confirms that both public documents render and the signed-in Builder presents the current `2026-09-14` acceptance screen with its checkbox unchecked and Accept disabled. No account details, acceptance, project content, Save or publication action was used as a test. Desktop/phone fixture evidence remains in the T6 log; this live observation does not claim workspace use beyond the legal gate. All **17 operational observations** pass. The recovery inventory now includes 43 sources, adding the existing AppArmor profile and the toolchain/helper/worker receipts without changing the profile; the recovery guide records actual captured snapshot evidence separately. The external uptime workflow is now present on the default branch; scheduled execution and notification delivery are not inferred from that fact.

The post-rollout restricted capture service verifies encrypted snapshot `d737414bc8f55c276827805f2ba9e25ff2431b69d88bdb158740e78bce59c836`, covering **18,124 paths and all 43 sources**. The ordinary workstation replica service verifies independently encrypted snapshot `24f18c0307bc396c608dacb9c2043c2e9ea39266d3b8454b14e71249cbdf7bf0` and acknowledges the matching source snapshot on the VPS. Both services succeed; the [recovery guide](builder-backups.md#verification-and-operational-record) retains the exact identities and operational limits.

Continue **L6-T1 signup**, then L6-T2 billing, L6-T3 domains and L6-T4 quotas/abuse. Implementation remains **28/32**. Legal/provider review, paid PITR, an always-on offsite destination, actual notification/privacy-address delivery, hosted-provider recovery and manual gate use remain goal-end actions. These do not pause autonomous implementation.

## Implementation log — L6

### L6-T1 — Confirmed signup and one first website (15 September 2026)

**Implemented:** `BuilderSignup.tsx` uses the real Supabase SDK for email/name/password signup and confirmation resends, with neutral duplicate-account wording, a canonical Builder redirect and recoverable unknown outcomes. `BuilderAuth.tsx` retains invitation/recovery/sign-in flows and mounts the new `BuilderFirstProjectGate.tsx` only after current legal acceptance. The gate binds requests/results to the current account, keeps the workspace unmounted while checking, discards stale responses and navigates to the actual first project before mounting its storage/editor state. Existing valid project links retain their access checks.

The existing projects function's new `bootstrap` action supplies only the Auth-verified actor to `builder_bootstrap_account`; submitted actor, email-confirmation and acceptance claims are ignored. `202609150001_builder_signup.sql` adds a private initialization table and membership trigger, backfills existing accounts, and checks the live confirmed account and current legal acceptance before creating the first project. A per-account transaction lock covers project, owner membership, empty workspace and initialization together. Unknown responses retry the same project; failed transactions roll back all rows. Existing/invited accounts and later archive, deletion or membership revocation cannot cause automatic replacement projects. `My website` starts without a deployment destination. The [signup guide](builder-signup.md) records these decisions and rollout requirements.

**Verification:** all **794 application tests across 86 files** pass with actual Nginx, including the expanded private-table access matrix and signup UI/database cases. Full types report zero errors/warnings and 174 hints. Deno's complete function test set passes **3 tests and 22 steps**, including the real projects handler/SDK and five new actor/origin/session/confirmation/legal cases. A separate native PostgreSQL fixture observes **eight independent connections simultaneously waiting on the actual initialization lock**, then proves exactly one project, membership, workspace and initialization with the same returned ID, and no recreation after deletion. Its private cluster is stopped and removed. The existing operations CI job automatically discovers this native test.

All **39 account/auth/legal/signup/invitation/diagnostics browser journeys** pass, followed by **three client hosted-Save journeys**, one per engine: **42 relevant journeys total**. New signup, confirmation, explicit acceptance, lost bootstrap response, retry, reload and resend failures run in Chromium, Firefox and WebKit through the real SDK and PostgreSQL functions, with only provider boundaries replaced. Hosted Save still handles push rejection and reopening against an isolated helper/repository. No live accounts, real emails or personal workspace/content were used as tests.

Earlier verification corrections remain explicit. The initial browser run passed 26 cases and failed one Firefox resend case: its trace contains no resend request because the action targeted the old navigation control before the new form appeared. The navigation label now distinguishes **Confirm your email** from the form's **Resend confirmation email**, and the test waits for the form heading. An initial Deno fixture override mixed accessor/value property descriptors; it now uses the established serve-stub pattern. The strict Edge compiler also caught a nullable URL argument; an explicit empty-string fallback fixes it without disabling checking. The first full application run printed all 794 passes but its outer tool session ended with signal 143. A separate run again passes all 794 and explicitly records the actual `pnpm test` subprocess return code **0**; its outer tool session also reports 143 after that completed record. The outer termination cause is unestablished and is not represented as a clean session exit. The first browser run also reported the pre-existing Sidebar/BuilderWorkspace render warning during recovery; it did not recur in the expanded passing run. Deliberate diagnostics-fixture errors remain expected evidence, not suppressed production errors.

Eight screenshots were inspected for layout, labels and overflow:

| Screen | Desktop | Phone |
| --- | --- | --- |
| Signup | [1440](handover/builder-launch/l6-signup-form-1440.png) | [390](handover/builder-launch/l6-signup-form-390.png) |
| Confirmation | [1440](handover/builder-launch/l6-signup-confirmation-1440.png) | [390](handover/builder-launch/l6-signup-confirmation-390.png) |
| First-project retry | [1440](handover/builder-launch/l6-signup-retry-1440.png) | [390](handover/builder-launch/l6-signup-retry-390.png) |
| First website | [1440](handover/builder-launch/l6-signup-first-project-1440.png) | [390](handover/builder-launch/l6-signup-first-project-390.png) |

**Deployment and next:** the agent implementation count advances to **29/32 (91% rounded)**. L5 remains live; T1's migration, function and frontend are not deployed separately. Read-only provider inspection confirms signup is disabled, email confirmation is required, email Auth is enabled and the password minimum is 12. The production Builder site URL and existing production/www redirect entries are present; no SMTP host, user, password or sender is configured. Finish provider/email wiring and any remaining external setup at the coordinated L6 rollout; no real delivery is claimed and no question is raised now. Full browser CI remains batched at the L6 boundary and again at the goal's end. Continue directly with **L6-T2 billing**, then domains and quotas, using recommended choices and deferring only genuine human-only actions.


### L6-T2 work in progress — Billing accounts and Stripe flow (15 September 2026)

The billing foundation and Account panel are implemented locally, but **T2 is not complete or deployed**. The [billing guide](builder-billing.md) records the selected Free/Plus/Agency limits, the proposed £19/£59 monthly catalogue, preserved private-beta access, actual-price readback, subscription states and recovery behavior. No real customer, subscription, payment or provider configuration was created or changed. Website billing ownership and actual resource enforcement remain the immediate next work.

The new migration records private customer bindings, subscription state, event deliveries, durable Checkout attempts and the plan catalogue. It serializes current-provider reads with an expiring lease bound to both account and delivery, and rejects stale workers. Customer and Checkout idempotency keys survive lost responses; retries can recover provider resources before another create. Completed deliveries are deduplicated without treating event timestamps as a total order. The raw-body webhook uses the official pinned Stripe 22.6.2 SDK, and rejects wrong mode/account, invalid signatures and oversized input. Authenticated actions select account identity, prices, portal configuration and return URLs on the server. The billing panel rechecks sign-in and discards stale responses. Account deletion cannot finish with stale billing state, an unfinished Checkout or an ongoing subscription.

**Current proof:** 67 focused application cases pass across the billing database, billing UI/service, existing Account and complete 32-table access matrix. Full types pass with zero errors/warnings and 174 hints. The complete Edge suite passes four tests with 36 steps, including 14 billing steps using the real Auth/Stripe SDKs and synthetic signed events at the HTTP boundary. Native multi-connection billing proof, real browser rendering/screenshots, the full application suite and milestone CI are still pending. A passing PGlite sequence is not represented as proof of simultaneous PostgreSQL transactions.

Checks caught a fixture result without a row type, a redirect property read without narrowing, and a recreated request-limit function which lost the hardened search path; all are corrected and rechecked. Checkout fixtures now consistently represent accounts without an existing subscription. The provider deletion case explicitly clears paid entitlement. The two new function entry points are included in the existing milestone CI check command; no CI run was launched for this partial task.

Continue with project billing-owner assignment/transfer and transactional limits, integrate those messages into the existing projects and account flows, then complete native concurrency, browser proof and task verification. Preserve the uncommitted T2 work. Do not advance the implementation count or create a task-complete commit yet. The goal remains active at 29/32 completed tasks, with no external-input hold.

### L6-T2 continuation — website billing ownership and concurrent limits, 15 September 2026

**Still in progress; this supersedes the preceding billing checkpoint's next steps.** Added the private website-to-billing-account mapping, aggregate account usage, UTC monthly publication reservations and service-only transfer/summary functions. The existing projects endpoint binds transfer requests to verified Auth and current owner membership, requires explicit confirmation and returns clear quota failures. The first owner pays for a new website; invited access is free. Archived websites count. A transfer must fit the receiver's plan, and a current payer cannot leave until another owner takes billing. Pending publication/recovery prevents transfer.

Workspace page and registered-asset growth now passes transactional guards. The original site's page/asset tables supply its usage rather than the compatibility workspace. Downgrades preserve edits while preventing new over-limit growth and publication. Client publish requests reserve monthly usage under the account lock, check the reviewed snapshot again before activation and settle/refund once. Rollback, removal and operator recovery remain available. The Settings screen shows the website's plan and usage, restricts account-wide totals to its payer, and provides an explicit transfer confirmation; Account explains the billing requirements before deletion. See [the billing guide](builder-billing.md) for exact boundaries.

**Proof:** all **853 application tests across 90 files** pass with Nginx, including **88 focused billing/account/access cases**. Full types report zero errors/warnings and 174 hints. All **five Edge tests with 40 steps** pass; the additional entry test exercises membership, confirmation, actor binding and quota denial. The native PostgreSQL fixture passes five groups of simultaneous requests: only one of eight Free project creates succeeds; only one of eight webhook workers claims the customer; eight Checkout starts reuse one attempt; two of three concurrent 800 MiB saves fit the 2 GiB account limit; only one of eight publishes can take the final monthly allowance, with one refund after failure. Every contender is observed waiting at an actual database lock before release. The final migration revision was rerun successfully in a fresh Unix-only cluster on the VPS as the unprivileged helper account.

All **24 billing/account/signup browser journeys pass across Chromium, Firefox and WebKit**. They cover uncertain Checkout recovery without duplicates, current server plan on return, the portal, closing Checkout, billing transfers, denied transfers and ordinary signup/deletion regressions. The browser fixture now loads all applicable Builder migrations, with the production-style existing-owner backfill. Inspected screens: [billing desktop](handover/builder-launch/l6-billing-desktop.png), [billing phone](handover/builder-launch/l6-billing-phone.png), [website billing desktop](handover/builder-launch/l6-website-billing-desktop.png), [website billing phone](handover/builder-launch/l6-website-billing-phone.png). No personal account, payment, email or website was used for these tests.

The first expanded browser run exposed a genuine migration failure for an owned website without a saved workspace. It now backfills zero usage and has its own regression test. Other corrected verification issues were the fixture's initial Settings navigation, a database test's total after adding that new seeded website, an unsupported string helper in an ES2020 test and a Deno startup descriptor. No limit or assertion was removed to make a failure pass. The corrected complete browser run and application suite pass. No milestone CI or rollout has run for this partial task.

**Continue immediately:** the existing repository helper publishes via a separate Git path and still needs trusted source-page/disk measurement and publication accounting. Finish this integration, remaining original-site/recovery/deletion limit cases and task verification before the T2 commit. Registered asset metadata is not physical Storage enforcement; direct/resumable uploads, reservations and retained-object cleanup remain explicit L6-T4 work. Processed billing-event scheduling/monitoring and provider setup remain the coordinated L6 rollout work. L5 stays live, T2 remains uncommitted, completed tasks remain **29/32**, and the goal has no user-input hold. Domains/TLS and quotas/abuse follow T2 in order.


### L6-T2 continuation — repository publishing allowance and recovery, 15 September 2026

**Implemented locally:** the existing hosted helper reserves a publication allowance before Git starts and reconciles its durable receipt with the new private repository publication ledger. The helper signs fixed review/project/commit/attempt observations with a server-only key; the billing function independently verifies Auth, current access and the signature. The database serializes reservations, returns definitive failures once, charges confirmed live work once, rejects stale attempts and prevents pending publications from changing payer. Lost Git acknowledgements remain reserved until resolved; an elapsed deadline cannot authorize a refund. A definite abort before Git can arrive before a delayed reservation without allowing that old attempt to start later. Explicit retries use the current payer and UTC month. Existing local developer flows remain intact.

Files include `202609150004_builder_repository_billing.sql`, `shared/builderRepositoryBilling.ts`, `shared/builderHelperSignature.ts`, `scripts/builder-hosted-billing.ts`, the existing hosted helper/publishing modules and shared billing function. Deployment templates and the [website release guide](website-releases.md) document the new required private helper key and coordinated migration/function/helper ordering. The [billing guide](builder-billing.md) records behavior and the remaining usage gap.

**Passing proof:** 40 database/access cases include all 36 private/access-controlled tables, identity/attempt fencing, downgrade, payer/month rollover and recovery. The 31 real helper/Git cases include lost reservations, uncertain pushes, restart recovery and successful Git during a billing outage. Six actual Edge tests pass all 45 steps; independent fixture signatures reject changed bodies/bearers. Full types report zero errors/warnings and 176 hints. Isolated unprivileged PostgreSQL 17 passes seven contention groups, adding eight identical repository requests sharing one reservation and eight different repositories competing for one remaining allowance. This run used `/var/lib/kaizen-billing-check-madmawod`, retained its result log and removed its disposable database. The repeated client-refund assertion was restored beside its original operation before these groups ran. After adding the repository migration to the browser fixture's ordered billing backfill, all four Chromium billing journeys pass. Earlier 853-case full-suite and 24-journey three-engine evidence predates repository integration; full task checks remain due after the remaining implementation.

**Continue immediately:** implement trusted repository source/compiled-page and storage measurement, finish remaining legacy snapshot/deletion limit cases and full T2 checks, then commit the complete task. Physical object/upload quotas and abuse controls remain T4. Provider/key provisioning, event cleanup monitoring and full CI/deployment remain coordinated at the L6 boundary. L5 stays live; T2 is uncommitted and completed tasks remain **29/32**. No user-input hold.


### L6-T2 continuation — trusted source and rendered preview usage, 15 September 2026

**Implemented locally:** the helper measures actual source bytes and the projected reviewed replacements before a file change. It records existing usage, reserves permitted growth through the signed billing connection and refuses to write after quota denial or an uncertain acknowledgement. A fresh explicit proposal reconciles actual files and uses a new version; a late older request cannot lower current usage. The runner counts each generated HTML page and actual frozen output byte before presenting a preview. A refused build uses the existing output recovery path. Hosted build admission rechecks the captured actor's permission before measurement. Local developer builds retain their existing workflow.

`202609150005_builder_repository_usage.sql` adds private versioned measurement columns and combined account storage guards without adding a table. Billing transfer and project deletion move/remove repository usage, and managed workspace saves cannot reset it. Existing over-limit observations remain intact after a downgrade; growth and publication checks use the current plan. The current model records source plus the largest measured output copy and the greater of managed/rendered page counts. Final staged/production reservation, replacement and recovery semantics remain part of the next integration, not a completed storage-contract claim.

Files: `scripts/builder-repository-usage.mjs`, the existing runner, repository apply, hosted helper/build queue and billing transport, `shared/builderRepositoryBilling.ts`, the shared billing function, migration `202609150005_builder_repository_usage.sql`, `ProjectBillingPanel.tsx`, account/browser fixtures and the corresponding database/source/runner/helper/Edge/native checks. The [billing guide](builder-billing.md) explains the measurement scope. Website plan and usage now says website storage; refreshed [desktop](handover/builder-launch/l6-website-billing-desktop.png) and [phone](handover/builder-launch/l6-website-billing-phone.png) screenshots were inspected.

**Passing proof:** all **878 application tests across 91 files** pass with Nginx enabled. Full types report zero errors/warnings and 177 hints. Targeted evidence includes 46 database/access cases, the complete 36-table access matrix, bounded source measurement and runner admission, and all 34 hosted publishing cases. The actual hosted build refuses six generated pages under the five-page fixture allowance, restores output and then accepts a fresh five-page build. Quota/lost-response apply tests preserve original bytes and refuse to replay the consumed proposal. Actual Edge tests pass six tests and **46 steps**, including signed measurements bound to verified Auth and rejection of helper-selected production measurements. Four Chromium billing browser journeys pass with all four billing migrations. Nine native PostgreSQL contention groups pass in isolated unprivileged `/var/lib/kaizen-billing-check-l6wa1jhb`; its result log is retained and disposable database removed. Added groups admit only two of eight 200 MiB source reservations with 1600 MiB already registered under a 2 GiB fixture cap, and only one of eight writes using the same version.

**Corrections:** the downgrade fixture now edits an existing managed page rather than adding one after exceeding the limit. A digest helper removed during fingerprint extraction was restored, and the byte fixture was corrected to its actual 70 bytes. A pre-mutation wrapper had converted typed quota/unavailable errors to a generic conflict; it now preserves the original typed error when no source was applied. The permanent helper startup fixture now supplies its synthetic billing key. Source measurement rejects named pipes before opening and uses bounded reads that detect file growth. The first full run passed 876 cases and failed only the missing-key startup fixture; the corrected full run above passes all 878 cases, including the new named-pipe check. No limit, access guard or assertion was weakened to obtain that result.

**Continue immediately:** connect trusted staging/production output measurements to the release worker and require the exact staged revision's usage before native publication. Preserve accounting during failed/uncertain activation and allow a valid smaller replacement after downgrade. The release worker is unchanged so far; helper source/preview measurements are not final deployed-artifact proof. Finish remaining legacy snapshot/deletion cases and T2 verification before its one task commit, then proceed to T3 domains/TLS and T4 physical upload/storage/abuse controls. Provider setup, helper key provisioning, cleanup monitoring, full milestone CI and coordinated rollout remain L6 boundary work. L5 stays live and the completed count stays **29/32**; no user-input hold.


### L6-T2 continuation — measured staging/production output, 15 September 2026

**Implemented locally:** both ordinary static deployments and the original-site release coordinator now measure source before building and recheck its fingerprint before switching. The verified retained manifest supplies actual bytes and HTML page count. Migration `202609150006_builder_repository_outputs.sql` adds private output reservations tied to website/channel, request, artifact, commit and manifest digest. Native publication carries the reviewed staging artifact and requires that exact artifact and commit to be the currently verified staging measurement. The actual signed Edge transport and helper receipt retain that identity.

A verified switch replaces the channel's usage. Failed preparation or a verified restoration returns unused bytes; an uncertain acknowledgement keeps them counted. A failed-before-start tombstone rejects a delayed begin. Recovery holds older uncertain attempts, fences their late callbacks and retains conservative source/output peaks until the chosen artifact passes verification. A valid smaller candidate can proceed after downgrade while older copies still count for other websites. Pending output prevents billing transfer; archiving does not prevent finishing an already-owned recovery. The logical total is the largest source copy plus largest output copy, alongside registered assets. Physical retained copies, caches and uploads remain T4.

The new output recovery command uses the existing stopped-process filesystem recovery lock and actual retained-file/HTTP checks. It measures artifacts retained before accounting when no ledger exists, and refuses a conflicting existing record. It reconciles output usage and files; a pending original-site publication snapshot still needs a combined, proven recovery path before T2 is complete. The [release guide](website-releases.md#l6-output-usage-configuration-implementation-pending-rollout) and new private `deploy/systemd/public-release.env.example` document the required fixed project setting for both release destinations. No production environment was changed.

**Focused proof:** 53 database/access cases pass with all 37 private tables inventoried; the combined database/deployment run passed 87 cases before the additional restoration tests. The final worker/helper run passes all 72 cases, including actual retained files and HTTP with controlled Nginx reloads, database promotion rejection/lost acknowledgement, usage acknowledgement loss after promotion, pre-accounting restoration, missing source and mismatched ledger refusal. The actual Edge suite passes six tests and 46 steps. Full types report zero errors/warnings and 177 hints; a subsequent TypeScript check passes after the restoration cases. Isolated unprivileged PostgreSQL 17 at `/var/lib/kaizen-billing-check-vn239rix` passes ten contention groups. With 2010 MiB already used under a 2048 MiB fixture cap, only one of five simultaneous 30 MiB output reservations succeeds; failed settlement returns those bytes exactly once. Its result log is retained and its disposable database removed. No production account, payment, email or website was used.

**Continue immediately:** finish original-site publication/output recovery, remaining original-site snapshot and account-deletion/transfer cases, then complete T2 checks and its one task commit. The generic reviewed-snapshot storage check must include the payer's other websites, and the original-site release guard must inspect its frozen snapshot. Provider setup, helper key provisioning and processed-event cleanup monitoring remain coordinated L6 rollout work. T3 domains/TLS and T4 physical upload/storage/abuse follow. T2 is uncommitted and undeployed; L5 stays live, completed tasks remain **29/32**, and no user-input hold is needed.

**Final checks for this continuation:** the full application suite passes **896 tests across 91 files** with Nginx enabled, and all **four Chromium billing journeys** pass with the fifth billing migration. No UI changed in this continuation; the inspected billing screenshots remain applicable. The first targeted command accidentally supplied `--run` twice and stopped before running tests; the corrected command and full run above pass. No failed assertion or product limit was removed. All test processes have completed. These results prove the current output integration, not completion of the remaining T2 scope or the L6 milestone.


### L6-T2 — Billing, plan limits and verified recovery (15 September 2026)

**Implemented and verified locally.** The Account billing screen supports one recoverable Stripe Checkout and the customer portal; signed webhook observations reconcile current provider state into private subscriptions. Fixed account/website ownership, atomic billing transfer and current plan limits cover projects, managed and rendered pages, registered assets, actual repository source/output and monthly publication allowances. Invited access is free. Existing private-beta access is preserved; no migration purchases a plan. Explicit cancellation/downgrade preserves data. Clear UI messages explain what must change before retrying. The [billing guide](builder-billing.md) is the current behavior and rollout reference; it supersedes the WIP sections above.

The remaining integration is now complete. Reviewed client snapshots include other websites' storage; the original-site guard reads frozen native page sets. Actual account deletion requires completed billing transfer and current provider state, preserving the receiving site's content and totals. Recovery of the original site's publication and output usage commits in one database transaction after retained-file and served-output verification. The operator claims a fresh worker identity and output reservation under the stopped-process lock; delayed old callbacks cannot change either. A failed usage settlement rolls back publication promotion too. Exact-baseline restoration refunds its pending publication once, in-place recovery cannot silently roll back an already committed publication, and newer drafts remain intact. Original-site unpublish remains available after quota exhaustion; explicit recovery also remains possible after archival. The coordinator refuses another project's billing destination.

`202609150007_builder_billing_recovery.sql` adds the recovery fence and atomic finish RPCs; `202609150008_builder_billing_retention.sql` schedules completed-event cleanup. Monitoring checks aggregate delayed events/recovery and the actual latest cleanup run, reporting missing/failed/stale evidence as unhealthy. Unresolved events are retained. Both release destinations need their fixed project setting; the helper and billing function share their independent private signing key. These settings and provider catalogue/portal/webhook/signup configuration remain the coordinated L6 rollout work; none was changed in production for this task.

**Final proof:** `pnpm test` passes **905 tests across 91 files** with Nginx enabled; `pnpm typecheck` reports zero errors/warnings and 177 hints. The actual Edge suite passes **six tests and 46 steps**. All **24 account/signup/billing journeys** pass across Chromium, Firefox and WebKit. The complete access matrix covers all **37 tables**. Native PostgreSQL 17 at isolated, unprivileged `/var/lib/kaizen-billing-check-63m2l42r` passes **11 contention groups**, adding one winner among eight simultaneous recovery owners. It also executes the actual retention migration and monitor query against a synthetic scheduler boundary; absent, failed and stale runs fail the check and a recent success passes. Its result log remains and the disposable database was removed. Ten operations-monitor tests pass with a fake mail queue. Existing inspected screenshots remain applicable: [account desktop](handover/builder-launch/l6-billing-desktop.png), [account phone](handover/builder-launch/l6-billing-phone.png), [website desktop](handover/builder-launch/l6-website-billing-desktop.png), [website phone](handover/builder-launch/l6-website-billing-phone.png).

Final fixture corrections supplied a valid native snapshot to the access canary, valid synthetic Stripe identifiers, and a page-publication request for a page without shared site design. All relevant checks were rerun; no limit or assertion was removed to obtain success. The recovery review also found that reusing an old output reservation would leave its callbacks unfenced, so explicit recovery now always starts a new reservation and holds the older one. All final test processes completed successfully. No real user, payment, email, personal checkout or public website was used as a fixture. Full milestone CI and rollout remain batched at L6 completion as requested.

**Continue immediately with L6-T3:** custom-domain ownership, DNS verification, DirectAdmin/Apache TLS and release destination mapping; physical storage/upload quotas and abuse controls follow in T4. Implementation advances to **30/32 tasks**. This is local implementation proof, not a live Stripe service, manual acceptance or completed L6 milestone. No user-input hold is required.

### L6-T3 — Domain DNS checks in progress (15 September 2026)

The [domain guide](builder-domains.md) records the decision to use DirectAdmin/Apache for domains and certificates with the existing Nginx release engine. Read-only retrieval of the installed server's API schema confirmed current ACME configuration, provisioning/dry-run and certificate inventory endpoints. No hosting configuration or domain was changed by that inspection.

Implemented shared hostname/IDNA normalization and operator reservations in `shared/builderDomains.ts`, plus 256-bit claim challenges and DNS ownership/routing checks in `scripts/builder-domain-dns.ts`. TXT fragments are joined within one record only. Every returned A/AAAA address must match fixed server configuration; missing, conflicting and failed DNS answers remain distinct. A resolver timeout cannot pass as an absent AAAA record, and DNS answers are not fetched as URLs.

**Focused proof:** all **44 cases** in `client/visual-builder/custom-domains.spec.ts` pass, including fake DNS integration and Node resolver boundary errors. `pnpm exec tsc --noEmit` passes. The first type check caught use of `Array.at` beyond the project's ES2020 target; indexed access fixes it without changing the target. No full application, Astro, browser, provider or live-domain completion is claimed for this increment.

**Continue T3:** durable domain claims and add/verify/remove actions, Settings, provider ownership/retries, certificate name restrictions and TLS/readback, mapping to immutable retained releases, and the documented real fixture-domain check. These changes are uncommitted until the complete T3 task is verified. The count remains **30/32**; L5 remains live, and L6 CI/deployment stay at the milestone boundary. No user input is required to continue.

### L6-T3 — Authenticated domain lifecycle in progress (15 September 2026)

Added migration `202609150009_builder_domains.sql`, the shared domain action handler and dispatch through the existing projects function. Owners with publishing permission can add a hostname, queue a check and confirm removal. The service generates the challenge and chooses the configured worker. Status reads remain available when hosting configuration is missing; browser bodies cannot supply provider evidence or private routing.

Unverified claims do not reserve a hostname across projects. Verified ownership becomes exclusive before provisioning. Worker operations use explicit ownership and recovery tokens; late callbacks cannot complete a replacement attempt. Completion validates fresh DNS, exact certificate names/renewal/validity and the matching current artifact. New client primaries require their own empty baseline. Existing client addresses and the original repository site use aliases without changing retained manifest identities.

Removal retains files, drafts, destinations and release history. Reconnecting a removed primary creates a new destination. Enabled destinations retain unique project/environment and origin constraints; disabled historical destinations can coexist. The client live-workspace and history projections now omit disabled destinations from current/live status. Domain operations and publications share the project lock; already-pending publication or recovery prevents domain changes. Authorized removal cleanup can finish after requester access changes.

**Focused proof:** 141 tests across five relevant suites pass with Nginx enabled, including the expanded 38-table access matrix. A follow-up domain run passes all 21 cases with two additional tests for retired release history and the original site's alias. All seven actual Edge tests pass 53 steps, including seven domain steps; TypeScript passes. The initial migration parser error in a retry `CASE` expression was corrected before these successful runs. Certificate receipts and provider responses are synthetic; no live certificate or domain setup is claimed.

**Next:** Settings and browser journeys; the DirectAdmin/root hosting worker with owned provider objects, existing release-store mapping, retries and stopped-process recovery; periodic DNS/TLS state; native PostgreSQL concurrency and a real fixture-domain check. The [domain guide](builder-domains.md) records current behavior and the still-uninstalled server configuration. T3 remains uncommitted and incomplete, **30/32** tasks remain complete, and L5 stays live. Continue without a user-input hold.

### L6-T3 — Domain Settings and browser journeys, 15 September 2026

**Implemented and verified locally:** hosted Settings now provides the domain form, exact TXT/A/AAAA records and copy controls, connection/HTTPS status, direct Releases navigation and explicit removal confirmation. Account/project changes discard earlier responses; unknown mutation outcomes require a fresh read and preserve add-request identity. Queued/running operations use read-only status polling, paused in hidden tabs and during confirmation. Permissions and archival govern the controls. New primary domains keep publication explicit; aliases explain that the existing address continues. Destination changes refresh the project display, and empty Releases directs users to domain Settings.

**Proof:** 58 focused UI/DNS/publication cases and `tsc --noEmit` pass. All five domain journeys pass in Chromium, Firefox and WebKit (**15 cases**). The main journey was then strengthened with a real fixture Save and reopening the retained page after removal; all three engine versions pass. All **18 existing account/billing browser journeys** also pass. The domain scenarios exercise actual Edge handlers and database routines, but DNS/TLS/provider receipts remain synthetic. Six inspected [desktop/phone screenshots and the exact verification limits](builder-domains.md#current-local-evidence) accompany the guide; overflow and serious/critical accessibility checks pass.

The initial browser fixture lacked a persisted workspace; the corrected fixture tests retained data. Browser inspection also found inherited definition-list columns overflowing on phones, corrected with an explicit responsive grid. The stronger Save fixture was initially refused for missing ID/version, then corrected without weakening the Save guard. The failed runs remain recorded separately from passing evidence. Read-only VPS inspection confirmed the installed Apache virtual-host templates and custom insertion points; it made no hosting changes.

**Continue immediately:** implement the DirectAdmin/root domain worker, only its owned provider objects, exact certificate names and renewal, retained release-store routing, retry/recovery and periodic DNS/TLS state. Then complete native PostgreSQL contention and real fixture-domain verification before the single T3 commit. **30/32 tasks remain complete; L5 stays live.** This screen is not yet a deployed domain service. L6 CI and deployment remain at the milestone boundary, with no user-input hold.

### L6-T3 — HTTPS evidence and DirectAdmin transport, 15 September 2026

Implemented [`scripts/builder-domain-https.ts`](../scripts/builder-domain-https.ts), which verifies actual TLS and the existing retained-release checks through each fixed hosting address. Exact hostname/SNI, certificate names/trust/validity, served marker and bytes, response identity and redirects must all match. Native and client aliases preserve their original store/manifest identities. Requests cannot follow a DNS answer or redirect to a different network destination. Deadlines, bounded responses and cancellation close failed checks.

Implemented [`scripts/builder-directadmin-api.ts`](../scripts/builder-directadmin-api.ts) for the root worker. It obtains temporary keys through the local CLI and releases an HTTPS socket to HTTP only after its certificate matches the root-owned daemon certificate file. API paths and loopback routing are fixed, responses are bounded, redirects are refused and errors retain no key, URL or provider detail. Read-only checks on the VPS verified the certificate match and a successful domain-list request through this transport. The administrator's returned list was empty; this establishes daemon access, not any new domain or certificate.

**Proof:** all **70 cases** in the DNS, HTTPS/transport and client-destination suites pass, with `tsc --noEmit` clean. [`domain-https.spec.ts`](../client/visual-builder/domain-https.spec.ts) supplies 22 real local TLS/transport cases, including two ingress addresses, both alias types, unchanged manifests, negative certificates/content/redirects, interruption/size limits and credential transmission/redaction checks. The first native fixture lacked the required builder entry; correcting the fixture preserved that release guard. An initial daemon configuration lookup found that `config-get cacert` is unsupported; the factory now uses the actual root-owned certificate file, verified against the live listener. No UI changed in this continuation; the inspected domain screenshots remain applicable.

**Continue T3:** connect these primitives to owned DirectAdmin domain creation/ACME, the release registry and Nginx routes, durable operation ownership/recovery and maintenance scheduling. Then native PostgreSQL and a real domain connection/publication/removal must prove the complete service before its task commit. **30/32 remain complete; L5 remains live.** The [domain guide](builder-domains.md) records current behavior, sources and verification limits. No user-input hold.

### L6-T3 — Owned hosting objects and real Apache routing, 15 September 2026

Implemented `scripts/builder-domain-provider.ts` for the dedicated worker account: creation intent before provider mutation, durable domain/project ownership, known-hash custom configuration, disabled initial routing, idempotent creation/removal, fresh ownership guards and stopped-process recovery. External domains/configuration and conflicting identities are preserved. Removal withdraws the route, confirms provider absence and reload, retains website data and records a tombstone before another claim can reuse the hostname. The dedicated account and new Nginx listener are still uninstalled; the lifecycle worker must compose these operations with database ownership, DNS, ACME and release stores.

Implemented `scripts/builder-domain-apache.ts` for account-scoped rewrites, generated virtual-host checks, configuration validation, reload/active-state checks and final readback. New custom rules accept the exact hostname, preserve challenges, serve 503 during setup and proxy verified HTTPS routes to loopback. `tests/ops/check-domain-apache.ts` runs those rules against the installed VPS Apache in an isolated temporary instance. It does not touch the production service, DirectAdmin resources or public listeners.

**Proof:** all **104 focused cases** pass, including **34 provider/configuration cases**, with TypeScript clean. Tests include actual stopped-child-process recovery, unknown provider outcomes, pending writes, permissions/symlinks, external edits, wrong ownership, removed requests and stale/mismatched virtual hosts. The actual VPS Apache passes **22 routing checks**. That check initially found encoded spaces becoming literal spaces in HTTP redirects; removing the `NE` flag corrected the behavior, with the repeated live-binary check passing. Initial fixture umask permissions and a TypeScript array-inference error were corrected without weakening checks. The [domain guide](builder-domains.md#owned-provider-objects-and-apache-routing-implemented) records reproduction and limits. No UI changed.

**Next:** finish the actual lifecycle worker, exact-hostname ACME setup/renewal, retained-store registry/Nginx mapping and automatic DNS/TLS observation/recovery. Complete native PostgreSQL contention and a real fixture-domain connection/publication/removal before the single T3 commit. **30/32 tasks remain complete, T3 remains uncommitted and L5 remains live.** No user-input hold; quotas/abuse, coordinated L6 verification/rollout and the final goal audit follow.

### L6-T3 — Certificate lifecycle and actual provider proof, 15 September 2026

Implemented `scripts/builder-domain-certificates.ts`: disabled ACME planning, exclusion of every other pending/failed/obsolete name, exact HTTP-01 issuance, configuration/renewal readback and recovery of an issued certificate after a lost reply. The HTTPS verifier now supplies its observed serial number; renewal evidence must match that leaf's serial, names and expiry. The provider's new `withOwned` operation holds its ownership lock and restricts certificate/SSL requests to the current domain, with authority/configuration checks throughout.

Actual VPS inspection found both normal-user licence slots used, while the administrator's domain workspace was empty. Hosting therefore uses the reserved `admin` workspace rather than creating another account; the observed domain limit is 20, with five existing domains. No account or licence was purchased. The root transport adds opt-in read-only licence inspection; it does not add account-management routes.

`tests/ops/check-domain-provider.ts` exercises a temporary owned hosting object on the real VPS and explicitly stops before enabling ACME or calling public certificate issuance. The final run proves creation, HTTP 503, disabled SSL/PHP/CGI, persisted disabled ACME settings, five excluded names and removal. The administrator's domain list is restored and the production release hash is preserved. All temporary objects are removed.

Live verification found and corrected three integration details. Legacy unchecked checkbox fields must be omitted: sending `OFF` enabled SSL/PHP/CGI. Custom routing files need daemon-readable mode 0644; private journals remain 0600, and the initial failed readback was recovered through its exact removal journal. Finally, the modern certificate API defaults to enabled even with the legacy creation trigger off, so initial provider setup now explicitly writes and verifies disabled ACME. The VPS's `admin_ssl_cert_on_create` changed from 1 to 0, with a verified daemon restart; the two broad missing/invalid-certificate triggers were already off. The root factory checks all three before creation. The [domain guide](builder-domains.md#exact-name-certificates-and-host-preparation) records the effect on future manually added domains, reproduction and limits.

**Proof:** all **82 focused cases** pass: 20 certificate, 37 provider/configuration/ownership and 25 HTTPS/API cases. TypeScript and diff whitespace checks pass. The actual provider fixture passes after the recorded corrections; the prior isolated Apache routing evidence remains 22 passing cases. No UI changed. This is not a public DNS/ACME/publication lifecycle or full T3 completion.

**Continue:** compose the database lifecycle worker and service, retained-store/registry/Nginx routing, periodic DNS/TLS checks with withdrawal/recovery, and native PostgreSQL contention. A real verified public domain lifecycle still requires proof before final acceptance. T3 remains uncommitted and incomplete, **30/32**, and L5 remains deployed; only the DirectAdmin preparation above has been applied. No input hold; human-only DNS/provider actions remain collected for the end after all autonomous implementation.

### L6-T3 — Retained stores and verified Nginx routing, 15 September 2026

Implemented `scripts/builder-domain-releases.ts`, reusing the retained release engine for primary baselines and client/native aliases. A new primary receives only a neutral holding page; drafts are not read. Alias origins, manifests and active includes remain unchanged. Registry validation is now available as an in-memory function in `scripts/client-publication.mjs` so updates are validated before replacing the operator-owned file.

Per-domain ownership records and a serialized, durable registry/Nginx transaction support interrupted writes, lost reload acknowledgements and removal superseding an incomplete enable. Exact before/after bytes prevent overwriting external changes. Primary withdrawal removes its registry entry and domain route, preserving releases and unrelated destinations. The default post-reload observer checks the exact release marker through the fixed loopback listener, or verifies that a removed hostname receives the default 404 without a release identity. Public TLS and complete artifact evidence remain separate checks in the lifecycle worker.

**Proof:** all **158 focused cases** pass, including **28 new release-routing cases** and the existing DNS, destination, provider, certificate and HTTPS suites. A separate real local Nginx process serves a primary holding page and an existing client alias, then the existing release engine activates an explicitly staged publication through the primary. Removal withdraws each hostname while retaining both stores and the original destination. Native aliases preserve their original manifest/origin in the adapter tests. Negative and recovery cases cover changed authority, unsafe files/paths, exact identities, corrupt release bytes, external edits, every update boundary, repeated interruption and a real lock-owner process that must exit before recovery. TypeScript and diff checks pass. No UI changed.

Initial fixture stores inherited group-write permission and were corrected without weakening the production check. An interruption test exposed authority errors being classified as reload failures; ownership checks now remain outside that error conversion. Read-only VPS inspection confirms the registry/store permissions, publication account and active `/usr/local/sbin/nginx` service entry points. This continuation made no VPS change and did not install the new listener or worker.

**Continue:** implement the database lifecycle worker and service using these adapters, complete automatic DNS/TLS checks and withdrawal/restoration without overriding operator suspension, then native PostgreSQL contention and the real public fixture lifecycle. L6-T3 remains uncommitted and incomplete; **30/32 tasks** are complete and L5 remains live. Physical quotas/abuse, full L6 verification/rollout and the goal-end audit follow. No user-input hold.

### L6-T3 — Domain lifecycle and automatic maintenance, 15 September 2026

Implemented `scripts/builder-domain-worker.ts`, composing the existing DNS/provider/certificate/release/HTTPS adapters with durable process-bound claims, lost-reply reconciliation and bounded queue processing. Setup and maintenance check fresh DNS before and after provisioning and bind the actual served certificate and release to completion. Unknown finalization preserves its claim; a stopped prior process must be proven before recovery.

Migration 009 adds due retries, recurring checks, private current-state/assertion/cleanup routines and withdrawal receipts. Failed primary routes suspend a separate readiness flag, so recovery cannot reverse operator disablement. Aliases retain the original destination, and connected maintenance survives ownership/billing transfer and archive. Physical withdrawal must be confirmed before failure releases a hosting claim. The provider handles withdrawal without creating/deleting objects and preserves an exact pending certificate plan. Settings now observes automatic progress and failures while retaining hidden-tab, account/project and removal-confirmation guards.

**Proof:** 199 focused cases pass; the final database/worker suite additionally passes all 41 cases after transport error-code coverage. TypeScript, whitespace checks and all 53 Edge steps pass. The 15 domain browser journeys are covered by 12 initial passes and three corrected retries across Chromium/Firefox/WebKit. Those failures enforced the new physical-withdrawal receipt; only the simulated provider fixture needed correction. The lifecycle suite uses actual migrated PGlite and retained release code, explicit fake network/provider evidence and real stopped child processes. It does not claim native database contention or public CA issuance. No layout changed; existing inspected screenshots apply.

**Continue:** root configuration, CLI/service installation and native PostgreSQL contention, then real public DNS/certificate/publication/removal proof. No new VPS mutation was made in this continuation. T3 remains uncommitted and incomplete, **30/32**, with L5 live. Quotas/abuse, combined L6 checks/provider setup/rollout and the final audit follow; no user-input hold.

### L6-T3 — Prepared root service and native contention, 15 September 2026

Implemented the production root factory/CLI, standalone bundler, private configuration example, systemd service/timer and guarded preparation script. The bundle composes existing adapters and imports no mutable helper/project renderer. Nginx must consume the exact managed include before reload. The installer checks the observed configuration hash, preserves prior bytes and release stores, installs an immutable content-addressed bundle and leaves scheduling disabled for coordinated rollout.

The actual VPS is prepared with bundle `8ef48143a4efbe4fb25f68200de2cebbfbaaa90eede40441f5322cd886feaa97` and a dedicated `127.0.0.1:8094` listener. The bundled host check passes under systemd with its private environment; unit validation passes. The existing three release includes and destination registry hashes are unchanged. Production, staging and client-demo remain reachable with their retained release markers over loopback and public HTTPS. The domain timer is disabled, and migration 009 is not deployed. No customer domain or public certificate was created by preparation.

**Proof:** 133 focused cases, three installer cases and full Astro/TypeScript checking pass. The actual standalone-bundle test confirms only the intended CLI runs; review caught and corrected library CLI guards becoming entry points when bundled. All 12 native PostgreSQL contention groups pass in a disposable Unix-only cluster: up to eight simultaneous callers are observed at actual locks before release, including publication exclusion in both orders and exclusive hostname ownership across projects. Provider/network evidence in that database fixture is synthetic. The cluster and fixture checkout are removed; CI discovers the new Python checks through its existing operations job.

**Continue:** finish full task checks and the real public domain lifecycle, then T4 quotas/abuse and coordinated L6 provider/CI/rollout work. All 1,138 application tests now pass; final domain browser verification is running. T3 remains uncommitted and incomplete, **30/32**, L5 stays live and there is no user-input hold. The [domain guide](builder-domains.md#prepared-root-service-and-native-database-verification) records installation and verification limits.

### L6-T3 — Implementation checkpoint and deferred public acceptance, 15 September 2026

Custom-domain implementation is complete: authenticated add/verify/remove and Settings, ownership/DNS checks, exact-hostname certificates, durable worker recovery, automatic maintenance, provider/release withdrawal and restoration, retained publication mapping and the prepared root service. Final proof passes **1,138 application cases**, full Astro/TypeScript checks with zero errors/warnings, **all 15 domain browser journeys** across Chromium/Firefox/WebKit, **53 Edge steps**, three installer cases and **12 native PostgreSQL contention groups**. The actual VPS host/bundle and unit checks pass with existing release/registry hashes preserved. Six inspected screenshots accompany the guide.

**Acceptance limit:** this is implemented and locally verified, with the host prepared; it is not a completed real public DNS/CA/publication/removal lifecycle or an active L6 rollout. Kaizen's DNS is authoritative at Cloudflare, targeted project configuration has no DNS API settings, and the available dashboard reaches sign-in. No DNS records or login methods were changed. The [documented public-domain check](builder-domains.md#public-domain-acceptance-at-rollout) records the exact remaining sequence. Sean explicitly instructed that human-only access be collected at the end after all autonomous implementation, so this acceptance item moves to coordinated rollout rather than holding T4.

**Implementation count: 31/32 (97% rounded by equal task count). Continue directly with L6-T4 physical upload/storage quotas and abuse controls**, then full L6 CI/provider setup/activation/deployment and the final audit. Keep real domain acceptance, external setup and Sean's hands-on acceptance visible. L5 stays live; the domain scheduler stays disabled until the matching migration/configuration is active.

### L6-T4 — Upload accounting checkpoint, 15 September 2026

Migration 010 adds private upload reservations and observations of existing physical Storage objects to billing. Reservations bind the original asset identity, count retained/unregistered provider bytes and pending transfers, follow billing ownership, and survive cancellation or expiry until removal is verified. Current account/project access and worker ownership are checked before receiving and completion. A completed operation accepts only its identical saved receipt, so a retry cannot change its file identity or cleanup evidence.

**Proof:** all 55 focused database cases pass, comprising 14 new upload cases and the 41 existing plan-limit regressions. These execute real migrations in PGlite with fixture provider rows; no live upload or deletion is claimed. The first run found an invalid PL/pgSQL local-variable qualifier, which was corrected before the passing runs. Additional cases then verified immutable asset kind and exact stored/removal receipt replay.

**Decision and remaining work:** the [quota guide](builder-quotas.md) documents a controlled VPS resumable upload service that reuses the local TUS implementation. Provider preflight permission checks alone cannot enforce the required durable byte reservation. The service, browser cutover, clone/import integration, retained-file cleanup/deletion, disk limits and abuse controls remain. No Storage schema or production configuration is changed. T4 remains uncommitted/incomplete, **31/32**, L5 remains live, and combined L6 CI/rollout follows task completion. Continue autonomously; human-only provider and acceptance items stay at the end.

### L6-T4 — Bounded temporary upload storage, 15 September 2026

Implemented `scripts/builder-upload-spool.ts`, reusing the existing folder privacy checks, process recovery locks and TUS FileStore interface. Durable reservations bound temporary bytes and free-disk headroom; exclusive creation cannot truncate an existing transfer. Actual file size supplies the resume offset and completion verifies SHA-256. Interrupted writes remain resumable. Cleanup releases local capacity only after removal; recovery metadata remains charged until acknowledged terminal completion allows it to be forgotten. Private database worker reads support reconnects while preserving worker and uploader access checks.

**Proof:** all **72 focused cases** pass: 15 upload database cases, 16 filesystem/TUS cases and 41 plan-limit regressions. TypeScript passes. Actual HTTP creation, partial PATCH, HEAD, overflow refusal and verified resume exercise the reserved file store. Tests also use actual stopped processes, competing service instances, recovery after reopening storage, failed cleanup, changed links/permissions and free-disk bounds. Initial stream-close, metadata-cleanup and HTTP unlock-order failures were corrected; responses must be sent after the operation lock is released. The [quota guide](builder-quotas.md#temporary-upload-storage) records operational limits and the evidence boundary.

**Continue:** authenticated request/provider composition, browser transport cutover and clone/import reservations, then retained cleanup/deletion, remaining disk/release limits and abuse controls. This is uncommitted/undeployed progress; the standalone spool does not enforce live account access or prove provider cleanup. **31/32 tasks remain built, L5 remains live**, and full task/milestone CI/rollout follows implementation. No input hold.

### L6-T4 — Authenticated transfer and provider adapter, 15 September 2026

Implemented `scripts/builder-hosted-uploads.ts` and `scripts/builder-upload-provider.ts`. The request handler binds resumable transfers to the verified original account and immutable project/asset, reserves capacity first, and composes local/provider verification with durable SQL completion. A lost creation reply reuses the original file. Provider writes cannot overwrite existing objects; completion downloads and hashes the observed version and checks metadata again. Cancellation targets only that version and confirms absence before releasing capacity. The spool retains prior process attempts so recovery can identify SQL's actual owner when a newer intent stopped before its claim committed.

**Proof:** all **94 focused cases** and TypeScript pass: 24 quota/service database cases, 16 filesystem/TUS cases, 13 provider cases and 41 plan-limit regressions. The service runs against real migrations in PGlite with fixture Auth/provider services; the provider adapter streams actual files through a fixture transport. Tests prove quota refusal before file creation, cross-account refusal, resume without truncation, complete/cancel behavior, unknown finish and cleanup recovery, access revocation during transfer, version changes and stale-owner fencing. Actual hosted provider and production listener/proxy behavior remain unverified. The [quota guide](builder-quotas.md) records the exact boundary and primary provider references.

**Continue:** production HTTP host/configuration, browser transport and Storage-policy cutover, clone/import reservations, abandoned/retained cleanup and deletion, remaining disk/release limits and abuse controls. Keep all L6 provider setup, full milestone CI, deployment and final acceptance work in scope. This remains uncommitted/undeployed T4 progress, **31/32**, with L5 live and no human-input hold.


### L6-T4 — Browser transport, HTTP host and automatic cleanup, 15 September 2026

Implemented `scripts/builder-upload-host.ts`, the standalone bundle builder and systemd/environment templates. Original Kaizen and client projects now use `client/visual-builder/hostedUploads.ts`: captured account/project identity, refreshed bearer credentials, constrained resumable URLs, verified finish before registration and explicit server cancellation before discarding recovery files. Existing local-helper uploads remain functional. Already-counted provider files can be registered after downgrade without increasing storage.

The service also checks unfinished-upload cleanup automatically. New accepted bytes refresh a seven-day resume window; no-op requests cannot prolong it. Cancelled, expired or inaccessible transfers are rechecked under the same local lock before removal. Failed provider/SQL outcomes keep their reservations; failed items rotate behind other cleanup. Terminal local cleanup survives a lost acknowledgement and preserves completed provider assets. Lost-POST reservations with no local file need only bounded recovery metadata to clean up.

**Proof:** all **135 focused cases** across nine files pass, including 34 upload database/service cases, nine HTTP-host cases, nine browser-transport cases and four original-Kaizen storage cases. TypeScript passes. Actual HTTP tests connect the client transfer through TUS and the migration fixture to verified completion. Auth/provider services remain fixtures; the client integration uses its Node transfer adapter, so actual browser UI proof remains separate. The standalone bundle works without a checkout, and the actual VPS validates the systemd unit against its installed Node runtime. No upload user, private service configuration or public proxy was installed or activated.

**Continue:** clone/import reservations, strict registration and coordinated Storage-policy cutover, reference-aware stored/unregistered/retained cleanup and project/account deletion, retained release/cache/disk limits, publish abuse limits, report/takedown and suspension, desktop/phone browser proof and actual provider integration. Then complete task checks, the one T4 commit, full L6 milestone CI and coordinated rollout, followed by the goal-end verification and human-only setup/acceptance list. T4 is uncommitted/undeployed, **31/32 tasks remain built**, and L5 remains live. No input hold.


### L6-T4 — Durable copies through the quota service, 15 September 2026

Migration 011 reserves a frozen website copy's full page/asset allowance before transferring files and records one stable destination. Repeated requests and resumed batches use that snapshot. Pending work cannot be edited; final completion requires every expected stored receipt and matching current provider size/version/ETag. It commits once, discards the redundant private snapshot and preserves later destination edits on retry.

`_shared/builderProjectCopies.ts` and `_shared/builderCopyUploads.ts` replace direct Storage writes and the old best-effort project deletion in the projects Edge handler. Source reads use the authenticated user's JWT; exact source bytes go through the same resumable quota service as browser imports. Each invocation handles up to three remaining files. `ProjectsView.tsx` continues these batches, preserves the request ID and shows an interrupted destination with **Resume copy**. Leaving/reopening the screen does not require allocating another project. Explicit resume can restart an expired partial file only after verified removal and acknowledged local cleanup.

**Proof:** all **154 focused application/database/HTTP/DOM cases** pass, TypeScript passes, and the full Edge suite passes **eight tests / 58 steps**. The actual projects handler has five new scenarios for membership/quota denial, interrupted transfer, frozen retry, final resume and missing trusted configuration. The Edge copy transport additionally runs through the real loopback HTTP host, TUS handler, spool and quota database fixture. Negative cases reject foreign destinations, changed source bytes, impossible offsets, invalid completion receipts and changed provider metadata. The tests retain fixture Auth/Storage services; jsdom proves UI behavior, not desktop/phone browser rendering. Initial nullable-user typing and incomplete rate-limit fixture failures were fixed before the passing full run.

**Rollout and remaining work:** configure the Edge function's `BUILDER_UPLOAD_ORIGIN` with the canonical HTTPS builder origin, install the upload service/proxy and apply migrations 010/011 with the final strict-registration/policy cutover at the coordinated L6 rollout. Pending copies retain their full allowance until completed or safely cancelled/deleted; whole-copy and stored-file removal still need reference-aware cleanup. Strict registration/adoption, project/account deletion, retained release/cache/disk limits, publish abuse limits, report/takedown and suspension, actual browser/provider proof, full task checks, the T4 commit, milestone CI/rollout and the goal-end audit remain. T4 is uncommitted/undeployed, **31/32 tasks built**, L5 live, no input hold.

### L6-T4 — Verified file registration and coordinated Storage policies, 15 September 2026

Migration 012 records immutable library file identities. Existing entries retain an explicit legacy baseline; new registrations require fresh worker verification and current provider size/version/ETag. Original Kaizen and client projects now share the service-only registration RPC through the existing projects function. Trusted configuration supplies canonical URLs, metadata changes preserve identity, and a missing provider key cannot free an existing library ID for another upload.

The upload service adds bounded existing-file adoption. It verifies actual bytes without a new upload reservation or deletion authority, permits recovery of already-counted storage after downgrade, and reconciles a provider write whose ordinary upload completion was lost. Current account access and original worker/process ownership still apply. New files are registered only after stored completion; failed adoption leaves existing bytes intact.

Migration 013 closes both old direct Storage insert policies and adds restrictive write guards scoped to builder buckets. Overlapping permissive policies cannot restore browser writes; authorized reads, service-role API writes and unrelated buckets retain their intended access. Actual signed/resumable provider-session behavior, including in-flight transfers, remains rollout proof. See [the quota guide](builder-quotas.md#coordinated-storage-policy-cutover) for coordinated activation.

**Evidence:** all **169 focused cases across 11 files**, **19 complete access-matrix cases**, TypeScript and the full **nine-test/63-step Edge suite** pass. The access inventory includes the new upload, copy and file tables. Asset fixtures now supply real immutable identities, and malformed metadata explicitly fails the identity guard. The final focused command includes the existing hyphenated cloud-project test filename. These are local SQL/service/client/Edge checks, not a live provider cutover or completed T4.

**Continue:** reference-aware stored/unregistered/retained cleanup, pending-copy cancellation and project/account deletion, retained release/cache/disk limits, publication abuse limits, reporting/takedown and suspension that preserves data. Then complete native concurrency, actual desktop/phone browser evidence, VPS service/proxy installation and live provider checks, full task verification and the one T4 commit. L6 provider setup, combined CI/rollout and the final goal audit remain. T4 stays uncommitted/undeployed, **31/32 tasks built**, L5 live, with human-only setup/acceptance collected at the end and no input hold.

### L6-T4 — File reference and project-deletion guards, 15 September 2026

Migration 014 inventories references in libraries, saved pages/revisions/blocks, site design, redirects, active previews/reviews, retained publication history and unfinished website copies. Pending copies retain both source and destination files; original public Kaizen media is checked across client content too. Content triggers share-lock referenced catalogue rows. A removal transition waits for earlier writers and reads their committed references, while a later writer is refused after removal starts. Privileged project deletion cannot cascade away provider objects, unfinished reservations, local cleanup or file identities.

**Evidence:** **196 combined cases across 12 files**, TypeScript and the final **62-case database rerun** pass. The latter adds redirect coverage. The original page-reference fixture was corrected to include a valid slug; existing route validation remains intact. The new Python operations test passes **three native PostgreSQL 17 contention groups** on the VPS with real lock observation: save before cleanup, cleanup before save and upload reservation before project deletion. The temporary Unix-only cluster and fixture checkout are removed; no production database or provider file changed. The latest unchanged Edge handler suite remains 63 passing steps.

**Scope and next:** these guards do not implement provider removal. Continue the retained-file cleanup coordinator and native working-copy/retained-release reference proof, then pending-copy cancellation and deletion completion. The current original-Kaizen removal guard deliberately refuses to proceed until that native integration is implemented. Account closure already soft-deletes Auth after ownership transfer and preserves websites/ledgers; it does not need a replacement destructive account flow. Retained disk limits, abuse controls, actual browser/provider/concurrency proof, full T4 verification/commit, L6 provider setup/CI/rollout and final goal verification remain. T4 is uncommitted/undeployed, **31/32 tasks built**, L5 live, no input hold.

### L6-T4 — Verified removal of unused catalogued client files, 15 September 2026

Migration 015 adds private cleanup jobs and bounded discovery, a seven-day recovery window, fresh reference/ownership checks, worker claims and strict completion/acknowledgement. New saved references and file verification extend pending recovery. `scripts/builder-asset-cleanup.ts` runs through the existing upload host, shares the original upload/adoption lock and uses a separate durable process/receipt record. Cleanup refuses a live previous process, preserves unknown provider data and retries lost provider/database/local acknowledgement outcomes.

Storage accounting now includes outstanding cleanup jobs, deduplicated against provider objects and original uploads. A provider deletion whose completion RPC failed therefore retains its bytes, including for an adopted file without an upload ledger. Confirmed removal updates the catalogue, original upload and billing atomically. Local recovery state is forgotten and acknowledged afterwards. Empty legacy objects can be removed; new empty uploads still fail before provider requests.

**Evidence:** all **206 combined cases across 12 files**, TypeScript and whitespace checks pass. There are 71 database/service cases, 14 provider cases and the explicit new private-table access decision. Tests cover both uploaded/adopted removal, grace/reset, fresh accounting after provider deletion, lost replies/acknowledgements, changed bytes, the upload/adoption lock, actual live/stopped child processes, private claims, stale/altered receipts and empty files. Existing standalone host/bundle checks include the new worker. All provider data is synthetic; no production object, migration or service changed.

**Continue:** discover and verify files absent from the catalogue, connect native working-copy/retained-release references, finish pending-copy cancellation and guarded purge. Then retained disk/cache/release limits, abuse controls, native/browser/provider integration, complete T4 checks/commit, L6 provider setup and combined CI/activation/rollout, followed by the final goal audit. This is substantial T4 progress, not task completion: **31/32 tasks built**, L5 live, no input hold.

### L6-T4 — Uncatalogued client-file discovery, 15 September 2026

Migration 016 and the existing upload host now discover eligible client Storage objects absent from both the upload ledger and catalogue. The provider adapter reads bounded actual bytes and MIME, hashes them, and verifies unchanged size/version/ETag. A private **observed** identity starts the ordinary reference-aware cleanup path without creating an asset-library entry or granting immediate removal authority. Canonical assets remain recoverable through adoption; older filenames retain their actual keys with literal, JSON-escaped and encoded reference matching. Existing records are never overwritten, concurrent adoption is rechecked, and unknown completion/absence is retried without discarding bytes or fabricating success.

**Proof:** **222 combined cases across 12 files**, the full Astro/TypeScript check and standalone host/bundle checks pass. The SQL/service group has 82 cases, provider transport 19 and private access matrix 19. Tests cover recovery, referenced older filenames, grace, concurrent adoption, changed versions, lost replies, shared locks, private/fresh receipts, bounded/empty reads and denied arbitrary writes. Initial accounting assertions omitted the baseline reconciliation when inserting historical provider fixtures after migrations; that fixture setup was corrected and quota refusal is explicitly checked. Real provider/proxy/browser integration remains outstanding. Unsafe keys, absent version/ETag and oversized historical files are excluded from automatic discovery while physical accounting remains enforced; the [quota guide](builder-quotas.md#files-outside-the-catalogue) records that limit.

**Continue:** native working-copy/retained-release references, pending-copy cancellation/purge, retained disk/cache/release limits and abuse controls, then native/browser/provider proof and full T4 verification/commit. Complete L6 provider setup, combined CI/activation/rollout and the final goal audit afterward. **31/32 tasks are built**, T4 is uncommitted/undeployed and L5 stays live. There is no input hold; human-only setup and acceptance stay at the end.

### L6-T4 — Native reference inventory, 15 September 2026

Added `scripts/builder-native-asset-references.ts` and its real-repository tests. The scanner reuses the source-accounting reader's new optional streaming observer and covers configured recovery/draft/output and release stores. It reads actual local Git objects, verifies their hashes, retains packed/history references and decodes named compressed blobs after reading all retained trees. ZIP/gzip signatures also identify otherwise unreachable archives. No source contents or private paths are returned, and no archive is extracted. The hosted wrapper uses the existing project/build locks and verifies the configured repository and branch.

**Proof:** the final **343-case / 21-file combined run** and full Astro/TypeScript check pass. The native group has 14 cases for current/untracked files, packed and unreachable history, compressed deleted working files, saved drafts and old releases, escaped/encoded Unicode and chunk boundaries, corruption, limits, links/FIFOs/alternates, cancellation and actual helper locks. The existing helper, source/repository, build/recovery, save/publication and 222 upload/copy/discovery checks pass together. The corrupt-object test fixture needed permission to alter its own read-only temporary Git object; the corrected test verifies refusal without exposing Git stderr.

**Still required:** serialize every native producer with cleanup, check all operator-configured source/recovery/release roots and native clients, persist removal authority and prevent future edits/builds/publications from adding references after removal is claimed. A returned snapshot is not deletion authority, so the native removal refusal remains. Continue that integration, pending-copy cancellation/purge, retained disk/cache/release limits, abuse controls, native/browser/provider proof, full T4 verification/commit, L6 setup/CI/activation/rollout and the final audit. **31/32 tasks built**, T4 uncommitted/undeployed, L5 live, no input hold.

### L6-T4 — Native operation coordination, 15 September 2026

**Implemented:** migration 017 introduces private operator inventory, retained native project scopes, durable producer identities and generation-based clearance for native removal. Ordinary cleanup excludes configured native clients and serializes with configuration changes. Native claims require the exact attempt token and a complete scan from an unchanged generation with no active producer. An intervening operation invalidates the clearance even if it finishes before the claim. Once removal is claimed, the producer API returns retired identities for checks on later candidate bytes.

`scripts/builder-native-operations.ts` persists private, synced intent and finished records around the full work promise. It keeps uncertain completion recoverable and requires controller proof before releasing an unfinished operation. End-before-begin tombstones prevent delayed requests from reopening finished work. The existing signed `HostedRepositoryBilling` connection and `builder-billing` handler provide begin/read/end actions; only signed end can bypass expired Auth. The shared protocol validates exact process identities and bounded pages. The scanner's existing compressed-content reader now also checks prospective bytes. The [quota guide](builder-quotas.md#native-operation-and-cleanup-coordination) records the configuration and recovery contract.

**Proof:** the final combined `pnpm test` command passes **388 cases across 23 files**, including seven new SQL scenarios and eight new journal/transport cases. The full Astro/TypeScript check reports zero errors/warnings. The complete Edge suite passes **10 tests / 67 steps**, including signed recovery after Auth expiry and refusal of unsigned, altered or malformed requests. The expanded disposable PostgreSQL fixture passes **eight observed contention groups** on the VPS: the previous three reference/deletion cases plus native configuration versus ordinary discovery, active producer versus clearance, an intervening completed producer versus claim, claim versus a later producer, and end versus delayed begin. The temporary database cluster and checkout were removed; no live website/provider data changed.

The initial SQL run exposed two fixture assumptions: a billing owner cannot be removed without transfer, and library registration rejects an unavailable receipt before reaching the content-reference guard. The corrected cases simulate expired account access and test both registration and the actual settings-reference rejection. The first type/Edge checks also found typed-key and test-library compatibility issues, corrected before the final passing results. An initial combined run used `pnpm exec vitest`, which omits the package-manager context required by real build fixtures; it produced 14 failures. The corrected project-script invocation passes all 388 without product or assertion changes. Use `pnpm test` for suites that exercise actual builds.

**Remaining integration:** connect the journal to actual helper operations and the complete asynchronous build lifetime, check real replacement/output bytes, and wrap deployment checkout/build/activation/recovery with controller-based stopped-work proof. Connect the actual root inventory to the native cleanup attempt and provider worker, register every native client and retained checkout, and only then enable cleanup. No producer caller, root cleanup service, migration or L6 frontend was deployed in this continuation. Pending-copy cancellation/purge, retained disk/cache/release limits, abuse controls, native/browser/provider integration, full T4 verification/commit, L6 setup/CI/activation/rollout and final goal audit remain. **31/32 tasks built**, T4 uncommitted/undeployed, L5 live; no human-input hold.

### L6-T4 — Hosted native editing and build callers, 15 September 2026

**Implemented:** `HostedHelperService` uses the native journal around configured website-folder requests and the existing recovery work inside them. Draft/settings payloads and the actual replacements passed by `RepositoryCompanion.apply` are checked before writing. `HostedBuildQueue` keeps a separate operation through the entire asynchronous runner and recovery, checks each rendered file before preview, and waits for confirmed native completion before reporting success. Polling and cancellation retain their current verified access checks and use the already-active build operation, avoiding a new dependency on coordination availability.

The helper's startup accepts the paired operator worker/configuration fields in `.env.example`, reuses the signed billing connection and retries finished records. Periodic maintenance also retries finished records and drains on shutdown. A controller can retain an unfinished operation when cleanup or recovery is uncertain. The explicit sandbox-cleanup error now propagates a recovery-required result through the real runner and preserves native protection. The [quota guide](builder-quotas.md#hosted-helper-and-build-integration) distinguishes implemented caller behavior from the complete host recovery still required.

**Proof:** **173 relevant cases across 14 files** and the full Astro/TypeScript check pass, including eight new hosted-native integration cases and the journal's new uncertain-work case. Real temporary repositories and processes prove pre-write refusal, late retirement between review/apply, held-build autosave/cancellation, rendered-file refusal with prior-output restoration, delayed/lost completion and no repeated build. An injected sandbox-cleanup failure tests propagation through the actual runner; complete live-host descendant/recovery proof is still pending. Initial type errors in the fixture's `calls` field, async command and replacement-byte callback were corrected. Existing helper, builds, recovery, save, publication, settings, preview, repository and runner tests pass together. Earlier unchanged protocol SQL/Edge/native-PostgreSQL evidence remains separately recorded.

**Continue:** complete the actual host/controller stopped-work proof (including trusted-build descendants), native deployment checkout/build/stage/activation/recovery callers, and the root scanner-to-cleanup/provider connection. Then finish pending-copy cancellation/purge, retained disk/cache/release limits, abuse controls, native/browser/provider checks, full T4 verification/commit, coordinated L6 setup/CI/activation/rollout and the final audit. This continuation changed only the launch worktree and disposable local fixtures; no provider, service, database migration or production deployment changed. T4 remains uncommitted/undeployed, **31/32 tasks built**, L5 live; no input hold.

### L6-T4 — Hosted build processes and stopped-service recovery, 15 September 2026

**Implemented:** `builder-controlled-build.ts` reuses the existing delegated cgroup and trusted launcher for the hosted helper's trusted-project build path. All package-manager descendants are killed and drained before output is accepted, including detached children which keep standard-output pipes open. The helper CLI selects this controller; local developer behavior and the existing isolated client-build path remain available. The default cgroup limits also bound trusted hosted builds. If cgroup cleanup is uncertain, the actual runner preserves current output and the previous recovery copy in place, reports recovery required and retains the native operation.

`builder-native-controller.ts` verifies the actual service cgroup, systemd invocation, running state and complete process-stop settings. New native journal records persist the private controller stamp before work begins. A subsequent verified invocation of the same service can reconcile old unfinished intents; the same invocation and records without adequate controller proof cannot. Stamped records never fall back to a PID-only check, and their controller metadata is excluded from signed API/SQL payloads. The helper's existing startup/periodic recovery uses this controller when native coordination is configured. The service definition explicitly records `SendSIGKILL=yes`. See the [controller guide](builder-quotas.md#build-processes-and-service-restart-recovery).

**Proof:** **174 relevant cases across 14 files** and full Astro/TypeScript pass. The updated negative runner test proves that uncertain cleanup does not move either output. The new journal case proves durable controller stamping and refusal of a PID-only fallback. The actual `verify-controlled-build.ts` fixture passes five scenarios through the real hosted helper, journal, runner and cgroup: detached child, inherited pipes, build failure, cancellation and deadline cancellation. It checks the actual child group, absence of runnable descendants, output restoration and operation completion. The `verify-native-controller.ts` fixture then has its main process killed while a detached child runs; the next invocation of the same disposable unit observes the old group gone and reconciles two old intents. Same-invocation refusal, mismatched invocation identity, idempotent recovery and rejection of `KillMode=process` are verified. Both services and all temporary private files were removed. These are native controller/process tests with fixture provider boundaries, not production upload/removal acceptance.

The first VPS fixture bundle stopped before application work because bundled TypeScript expected Node filename globals. Supplying those globals corrected the fixture packaging; the five cases then passed, including a final run with controller stamping. No application assertion was weakened. Earlier protocol SQL/Edge/PostgreSQL evidence remains separately recorded and was not rerun for this host-only change.

**Continue:** wrap native deployment checkout/build/stage/activation and restoration in the operation protocol; connect the complete root inventory to the native cleanup attempt and actual provider worker. Then finish pending-copy cancellation/purge, retained disk/cache/release limits, abuse controls, browser/provider checks, full T4 verification and its one commit. Complete coordinated L6 setup/CI/activation/rollout and the final goal audit afterward. L5 remains live; no production service, provider, migration or website was changed here. T4 remains uncommitted/undeployed, **31/32 tasks built**, with no human-input hold.


### L6-T4 — Native release coordinators and artifact restoration, 15 September 2026

**Implemented:** existing Builder/public repository release and recovery coordinators now hold a native journal operation through their complete work and error reconciliation. Source/history, snapshots, output and retained artifacts are checked using the existing bounded native byte/archive reader. `activateRelease` invokes the candidate/baseline guard under its lock before copying immutable assets; explicit restoration checks its desired artifact before usage reservation or mutation. Unknown child cleanup keeps the native intent active. Lost completion is reconciled without another build or activation.

The service-owned native transport uses the existing private RPCs with exact receipt validation. The new `builder-native-release.ts` entry composes the verified systemd controller, journal, existing release worker and controlled build process. `build-native-release-worker.mjs` bundles the runtime for Node 22. The ordinary release entry refuses enabled native configuration without the native adapter. The private release environment example records the required worker, inventory, state-directory and package-manager fields. See the [release integration guide](builder-quotas.md#native-release-and-restoration-integration).

**Proof:** **158 relevant cases across nine files** and full Astro/TypeScript pass. Eight new release scenarios exercise real Git/file/retained-manifest/HTTP behavior: completion held through verification/accounting, compressed output refusal, staged-artifact recheck before immutable copies, explicit older-artifact restoration refusal, Builder rollback refusal, uncertain-child retention, lost completion without repeated activation, and unsafe snapshot refusal before writing/building. Two worker-boundary cases reject altered responses/invalid requests and native configuration without the native adapter. Standalone bundle construction, syntax and executable initialization pass; invalid-action/native-bypass checks stop before provider access. Earlier helper/VPS controller and SQL/Edge/PostgreSQL evidence remains separately recorded. No VPS, production service, provider, migration or website was changed in this continuation.

**Continue:** cover the entire outer deployment bootstrap. The current workflow fetches into its checkout before extracting `deploy-public-release.sh`, which resets/installs before invoking the worker. Connect an installed fixed-target service entry so candidate preparation, authoritative checkout changes, dependency scripts, builds and recovery all share the native lifetime; verify prospective source before replacement. Then connect the complete root inventory to the native cleanup attempt/provider worker, finish pending-copy cancellation/purge, retained disk/cache/release caps and abuse controls, and complete browser/provider checks, full T4 verification/commit, L6 setup/CI/activation/rollout and the final audit. The worker is packaged but uninstalled; native cleanup remains disabled. **31/32 tasks built**, T4 uncommitted/undeployed, L5 live, no input hold.


### L6-T4 — Complete deployment preparation, 15 September 2026

**Implemented:** the fixed-target root launcher starts the existing deployment account under a delegated service and holds the release-store lock across preflight, private candidate preparation, checkout/dependency changes and the existing release worker. Source/history are verified before replacing the checkout, private/untracked collisions are refused and dependency lifecycle commands use the same process controller as builds. The workflow changes invoke the installed entry; they remain local and uninstalled. The existing HTTPS Git remote works as the deployment account. See [deployment preparation](builder-quotas.md#complete-deployment-preparation).

**Proof:** **111 relevant cases across eight application files**, five Python launcher cases, full Astro/TypeScript, deployment preflight, formatting and standalone bundling pass. Eight new temporary-repository cases exercise real Git and native journals. All five prior controlled-build scenarios pass again on the VPS. A separate disposable service using the actual launcher properties, deployment account, outer lock, real Git and pinned pnpm passes, preserving private files and proving lifecycle execution inside its child group. Both fixture services and all temporary files/groups are removed. An initial read-only SSH lookup failed host verification because this deployment account uses the existing public HTTPS origin; the configuration was corrected to that observed origin and its lookup passes. No credential or production service changed.

**Continue:** durable interrupted-checkout recovery, then root scanner-to-provider cleanup, pending-copy cancellation/purge, retained disk/cache/release/candidate caps and abuse controls. Complete browser/provider/native proof, full T4 verification and its one commit, followed by coordinated L6 setup/CI/activation/rollout and final goal audit. Native cleanup remains disabled, T4 uncommitted/undeployed, **31/32 tasks built**, L5 live and no human-input hold.


### L6-T4 — Interrupted deployment checkout recovery, 15 September 2026

**Implemented:** a private, synced checkout receipt now protects the approved old/new Git trees, default index identity, candidate and native service ownership. Git updates source using a private index under its normal index lock, then installs the complete index and updates HEAD against the expected prior commit. Recovery recognises known partial states, preserves unexpected edits/locks and rechecks retired references. Explicit release reconciliation repairs pending work without fetching/installing a new candidate. See [checkout recovery](builder-quotas.md#interrupted-checkout-recovery).

**Proof:** **117 relevant cases across eight application files**, full Astro/TypeScript, formatting and standalone bundling pass. Five new checkout scenarios and one scanner case cover the interruption/ownership/refusal boundaries. A real disposable VPS service is killed after Git source replacement and then restarted under the same unit; the next invocation recovers the checkout, preserves private files and finishes pinned pnpm installation. Its lock, candidate, native journal, child groups and all fixture files/services are removed. Initial general-lock scanner refusals and fixture type annotations were fixed. Database/native receipts remain fixture boundaries; no production/provider/service installation changed.

**Continue:** root scanner-to-native-cleanup/provider connection, pending-copy cancellation/purge, retained disk/cache/dependency/candidate/release caps and abuse controls. Complete native/browser/provider proof, full T4 checks and its one commit, then coordinated L6 configuration/CI/activation/rollout and the final goal audit. **31/32 tasks built**, T4 uncommitted/undeployed, L5 live and no human-input hold. Unknown external Git/ref locks or edited recovery files remain explicit preservation refusals, not blanket automatic lock removal.


### L6-T4 — Native inventory-to-claim proof and Claude takeover plan, 15 September 2026

**Implemented:** the native inventory coordinator now connects complete configured filesystem scans to the existing cleanup worker's exact generation and durable attempt token. It reads legacy catalogue URLs, scans retained candidate Git history and other projects' roots, and resets recovery grace when a reference remains. Existing immutable provider removal and completion replay are reused. See [the quota guide](builder-quotas.md#native-inventory-and-cleanup-claim-integration).

**Verified:** all **100 focused inventory/upload/database cases** pass. Full types report zero errors/warnings and 192 hints across 455 files. The tests use temporary repositories, actual packed history, separate spools and a fixture provider; no live provider deletion or service installation occurred. Previous focused test handles are closed. The new reference/grace routine still needs its meaningful real PostgreSQL contention proof; older native contention results do not cover that addition.

**Handover:** Sean requested a written task breakdown in case Codex credits run out. [L6-T4 continuation tasks](handover/l6-t4-claude-tasks.md) records the correct worktree and dirty state, completed work, evidence, eight ordered remaining parts with files and proof, host references and human-only items for the end. No runtime implementation was started after this request; only the handover/guide/checkpoint were updated. Resume at A's unchecked contention proof, followed by B's root cleanup service. **31/32 tasks built**, T4 uncommitted/undeployed, L5 live; no external input hold.


### L6-T4 — Packaged root cleanup service and native contention, 15 September 2026

**Implemented:** the private root inventory loader, bounded cleanup entry, standalone bundle and systemd service/timer reuse the existing scan/claim/provider/recovery path with a separate root spool and actual process identity. The read-only Git object command trusts only its exact configured repository so the root service can inspect helper-owned checkouts. See [root cleanup service](builder-quotas.md#root-native-cleanup-service).

**Verified:** **192 relevant cases across nine files**, full types, Node bundle checks, **twelve real PostgreSQL contention groups** and an actual disposable root-service kill/restart scenario pass. The service fixture uses simulated network/provider responses; separate database tests exercise real migrations. The final service run verifies loader permissions, helper-owned source reads, stopped-process takeover and completion without another provider deletion. Temporary units/files are removed. Two fixture-driver setup/signal errors were corrected; no production provider, database, service or website changed.

**Continue:** [the takeover checklist](handover/l6-t4-claude-tasks.md) now marks A and B verified and points to **C — pending-copy cancellation and guarded purge**. Complete retained disk limits, publication abuse controls, actual provider/browser/installation proof, full T4 checks and its one commit, then L6 configuration/CI/activation/rollout and the final audit. The complete real server inventory is still to be registered. T4 remains uncommitted/undeployed, **31/32 tasks built**, L5 live; no input hold.


### L6-T4 — Pending-copy cancellation and guarded purge, 15 September 2026

**Implemented:** a current owner can explicitly cancel an unfinished copy while preserving the original website. Migration 018 fences future writes and late retries, releases the frozen copy references in order and retains physical storage charges until cleanup is acknowledged. The existing upload host performs bounded guarded purge after maintenance. A minimal private request record survives to prevent a delayed duplicate request from recreating the destination. Projects provides keyboard-accessible confirmation, cancellation during a running transfer, visible cleanup status and automatic removal of the card after confirmed purge. See [the cancellation guide and nine inspected browser screenshots](builder-quotas.md#cancelling-an-unfinished-website-copy).

**Verified:** **236 relevant cases across nine application/database/service files**, full Astro/TypeScript, the complete Edge suite (**10 tests / 68 steps**), **sixteen real PostgreSQL contention groups** and the Projects cancellation journey in **Chromium, Firefox and WebKit** pass. Final cleanup-card toolbar polish also passes all eight React cases. The new real database races cover cancellation versus finish in both orders, purge versus duplicate retry and upload reservation versus cancellation. The temporary VPS cluster and checkout were removed. The standalone upload bundle and Node syntax check pass. Initial fixture allowance and access-matrix omissions were fixed before the final runs. Browser network/provider behavior remains simulated; this is not production rollout evidence.

**Handover and continuation:** [the Claude checklist](handover/l6-t4-claude-tasks.md) marks A–C verified and resumes at **D — retained disk and cache limits**. Its remaining parts name files, dependencies, proof, deployment order and final human-only work; it includes a paste-ready takeover prompt and the private dirty-file inventory path. Next come publication abuse controls, combined installation/provider/browser/T4 proof and one T4 commit, coordinated L6 setup/CI/activation/rollout, then the final goal audit. All T4 changes remain uncommitted and undeployed in `/home/sean/Documents/GitHub/kaizen-launch`. **31/32 tasks built**, L5 live, no input hold; no production provider, migration, service installation or website changed.


### L6-T4 — Native checkout and worker-state disk admission, 15 September 2026

**Implemented:** native deployment preparation and static builds now reuse the existing disk guard to measure their checkout and private worker state together. Dependency stores, caches and retained candidate/history/recovery files participate in the same allowance; new preparation checks headroom, and growing trusted commands receive cancellation through the controlled process group. Interrupted checkout recovery remains accessible before new-work admission. An uncertain child-stop error takes precedence over a simultaneous storage failure, preserving the native operation and its files. The launcher keeps npm cache in worker state. See [storage behavior and limits](builder-quotas.md#native-deployment-storage-admission).

**Verified:** **112 relevant cases across eight application files**, full Astro/TypeScript, five launcher cases and native worker bundling/syntax checks pass. The final root-overlap validation also passes all 23 focused cases. Actual disposable VPS services prove an over-limit pinned-pnpm lifecycle command and its detached child are stopped, its retained file remains charged, the next preparation starts no commands, and killed-checkout recovery still works. All temporary services/cgroups/files were removed. Fixture directory permissions and lockfile normalization errors were corrected before final passes. Database responses remain fixture boundaries; production was untouched.

**Continue:** task D still needs retained candidate/cache cleanup, release/immutable-store capacity and retention, interrupted/shared admission where required and complete inventory integration. These new checks are monitored operational bounds for trusted commands; they do not establish hard filesystem quotas. Then finish E abuse controls, F full installation/provider/browser/T4 proof and one commit, G L6 configuration/CI/rollout, and H final goal audit. The [Claude checklist](handover/l6-t4-claude-tasks.md) records the new checkpoint. T4 remains uncommitted/undeployed, **31/32 tasks built**, L5 live; no input hold.


### L6-T4 — Durable candidate ownership and stopped-work reclamation, 15 September 2026

**Implemented:** a bounded private candidate intent now precedes cloning, recording its source binding, native/service ownership and directory inode before Git can run. Pruning verifies stopped work and current directory identity, defers a candidate referenced by unfinished checkout recovery, and reconciles interrupted allocation/removal. The checkout recovery module clears its source transaction before the candidate journal reclaims its generated files. Cleanup runs inside a current native operation before new-work disk admission; unknown/legacy files remain retained and counted. See [candidate ownership and limits](builder-quotas.md#retained-deployment-candidates).

**Verified:** **128 relevant cases across nine application files**, full Astro/TypeScript, standalone bundling and Node syntax checks pass; final fixture typing corrections also pass all 31 focused cases. Actual disposable VPS services prove stopped-child/candidate reclamation after a kill, source-checkout recovery before candidate removal, and continued correctness of over-limit pnpm lifecycle cancellation. A 600 MiB abandoned candidate is removed before the new invocation's smaller storage allowance is checked. All temporary services, cgroups and files were removed. Negative mount cases use simulated mount metadata; service database replies remain fixtures. No production/provider/deployment change occurred.

**Continue:** D dependency/cache retention and release/immutable-store capacity and retention, including shared/hard admission where needed and the matching inventory protection. Then E publication abuse controls, F combined installation/provider/browser/T4 proof and one commit, G L6 configuration/CI/rollout and H final audit. The [Claude checklist](handover/l6-t4-claude-tasks.md) now marks owned-candidate reclamation verified. T4 stays uncommitted/undeployed, **31/32 tasks built**, L5 live; no input hold.

### L6-T4 — Detailed takeover checkpoints, 15 September 2026

**Documented:** the [Claude checklist](handover/l6-t4-claude-tasks.md#smaller-steps-within-d) now divides the unfinished storage work into D1 package caches, D2 release retention, D3 other generated directories, D4 shared admission/inventory and D5 integration proof. Each step names starting files and its completion evidence. D0's native storage/candidate work remains verified; D1 has research notes only, with no cache implementation started. E–H retain the remaining abuse controls, full T4 proof/commit, L6 rollout and final goal audit. The checklist also records how to refresh the next step, partial edits, running commands and private changed-file inventory if credits run out.

**Scope:** documentation only in this continuation. No code, production service, provider configuration or deployed revision changed. All unfinished T4 files remain in the launch worktree and must be preserved; a fresh GitHub clone cannot restore them. Continue at D1.


### L6-T4 — Owned pnpm maintenance before storage admission, 15 September 2026

**Implemented:** the native worker now records ownership of its fixed package store/cache before the first install. Both dependency installs pin these locations and disable global virtual storage/package-manager switching. Ordinary pnpm pruning runs above a 2 GiB trigger or under capacity pressure, inside the existing native operation, deployment lock and controlled child lifecycle. Root identity, bounded layout and mount checks preserve unsupported data. Actual retained bytes are remeasured before growing work; referenced packages are not treated as reclaimed capacity. Unknown child shutdown before candidate creation retains the native operation. See [behavior and limits](builder-quotas.md#native-package-cache-maintenance).

**Verified:** 138 cases across ten files, all 27 focused cache/deployment cases, full types (zero errors/warnings, 192 hints, 458 files) and native bundle/syntax checks pass. A reusable offline real-pnpm fixture proves unused package/metadata/tmp removal, installed-module execution, repeat pruning and private-file preservation. Actual VPS command cgroups repeat this proof and the over-limit descendant-cancellation regression. A killed-service fixture interrupts after pruning but before command-completion acknowledgement; its next invocation proves stopped children, journal recovery, safe prune replay and a usable installed module. All disposable units/cgroups/files were removed; native database responses remain fixtures. pnpm's project-registration links and the disk fixture's new maintenance-only command allowance were corrected before final passes.

**Continue:** D2 release/immutable-store capacity and retention; D3 other generated/dependency/cache scopes; D4 shared/hard admission and complete inventory protection; D5 integration proof. Then E abuse controls, F full T4 integration/installation preparation and one commit, G coordinated L6 CI/rollout and H final goal audit. The Claude checklist and private checkpoint identify D2 as the exact resume point. All T4 work remains uncommitted/undeployed; 31/32 implementation tasks built, L5 live, no input hold. No production configuration, migration, service installation or website changed.


### L6-T4 — Release-store admission and bounded materialization, 15 September 2026

**Implemented:** the shared release engine now holds its existing activation lock through staging as well. A dedicated storage module counts actual retained, incomplete and unknown bytes; checks store, immutable and free-space capacity; and bounds file/metadata copying against its source observation. Immutable additions are admitted as a batch before copying or switching the serving include, with parent-link protection preserved. Rollback can reuse installed assets even when new bulk storage is refused. Native staging now uses the existing client 200 MiB / 32 MiB / 10,000-file envelope. Private default store/immutable/free limits are 8/4/2 GiB. Current production/staging actual file-size measurements fit those settings; the read-only inventory changed no server files.

**Verified:** 149 cases across eight files with real Nginx enabled and zero skips, full types (zero errors/warnings, 192 hints, 459 files) and standalone native bundling/syntax checks. New cases prove same-store exclusion, refusal before staging/immutable writes, unchanged serving configuration, rollback with retained assets, changed-source refusal, unlinked immutable parents, actual retained accounting and a fresh restart measurement. An older negative fixture now clears its own synthetic activation lock before testing independent source guards. See [evidence and limits](builder-quotas.md#release-store-capacity-admission).

**Continue:** D2 release/immutable retirement and cleanup, with authoritative queued-job/rollback/recovery protection and retention/grace policy. Capacity tests do not authorize deletion. Then D3 other generated storage, D4 shared admission/native inventory, D5 integrated proof and E–H. The Claude checklist records this exact point. T4 remains uncommitted/undeployed, 31/32 implementation tasks built, L5 live; no input hold or production/provider/service change.


### L6-T4 — Claude interruption checkpoint, 15 September 2026

Sean requested an actionable takeover map before further implementation. Updated `docs/handover/l6-t4-claude-tasks.md` and added `docs/handover/l6-t4-release-retirement-tasks.md`: six ordered D2 checkpoints now name the current partial files, known SQL defects, dependencies, implementation boundaries and required proof. The main checklist retains D3–D5, abuse controls, T4 integration, L6 rollout and the final goal audit. AGENTS, HANDOVER and the quota guide point to the current resume state.

The verified capacity evidence predates untested migration 019 and its access-matrix edit. No code was fixed or tested in this documentation checkpoint; filesystem retirement is still unimplemented. Local document links and the partial/planned-file statements were checked against the actual worktree. The private checkpoint and modified/untracked path inventory were refreshed. No test/fixture handles are running, HEAD remains `d7435e64926a09f72da7eca7503cf0ab7fbbe2e2`, all T4 remains uncommitted/undeployed and L5 remains live. Resume at D2a without a human-input hold; this handover does not complete or block the launch goal.


### L6-T4 — database release-retirement fence, 15 September 2026

Implemented and verified D2a in migration 019. Private durable receipts bind artifact/worker/store/manifest identity, preserve publication/rollback/recovery/domain references, enforce grace and reject later references after claim. Generic trigger defects are corrected; indirect artifact identities cannot change beneath existing references, and renewed terminal recovery ownership is checked. Exact retries survive project/destination deletion. READ COMMITTED is required to ensure fresh reads after lock waits.

The final six-file regression run passes **252 cases**, including 18 focused retirement cases; full types pass with zero errors/warnings. Actual disposable PostgreSQL passes **24 contention groups**, including eight new retirement orders, plus stale-isolation refusal. The concurrency fixture now waits for a completed-statement marker, eliminating the earlier BEGIN/idle ambiguity. Initial missing canary and fixture JWT/membership setup failures are resolved. Exact final commands are in the private logs referenced by [the guide](builder-quotas.md#database-fence-for-release-retirement).

No files were retired; filesystem eligibility, removal/restart, caller/UI integration and immutable/abandoned-state cleanup remain D2b–f. No production/provider/service/frontend change occurred. All test handles are closed and disposable VPS state is removed. T4 remains uncommitted/undeployed on unchanged HEAD `d7435e64926a09f72da7eca7503cf0ab7fbbe2e2`, L5 remains live, and the full goal stays active. Continue at D2b using [the updated Claude checklist](handover/l6-t4-claude-tasks.md).


### L6-T4 — retained-release filesystem eligibility, 15 September 2026

Implemented D2b in `scripts/release-retention.mjs`, reusing the staging/activation lock and existing release verification. It protects selected/rollback/recovery/recently used artifacts, retains five newest releases by default, checks both file age and journal use, includes all retained immutable references and observes scoped database grace only after verifying served bytes. Bounded metadata reads/hashing, owner/path/file-identity checks and Linux mount-table checks preserve uncertain or foreign files. Unknown root files and staging remain counted and untouched. The [quota guide](builder-quotas.md#filesystem-release-retention-eligibility) records exact defaults, bounds and remaining integration.

The final combined run passes **167 cases across nine files**, including 18 inspector cases, with actual Nginx enabled and no skips. Real Nginx detects an include changed without a reload and proves rollback still works after inspection. A disposable VPS root mount namespace proves actual same-device bind-mount and foreign-owner refusal while preserving outside data, selected configuration and all releases. All fixture state is removed. Full types and native-worker/fixture bundles pass; exact logs and sessions are in the private checkpoint. An initial tied-history test incorrectly retained a superseded baseline; the corrected case still protects both ambiguous latest baselines. The actual server ownership inventory was read-only.

No release removal, production migration/provider/service/frontend deployment or T4 commit occurred. D2c durable removal/restart, D2d caller/history integration and D2e immutable/abandoned-state cleanup remain, followed by D2f and D3–H. The goal stays active on unchanged HEAD `d7435e64926a09f72da7eca7503cf0ab7fbbe2e2`, T4 remains uncommitted/undeployed and L5 stays live. The [Claude takeover point](handover/l6-t4-claude-tasks.md) now resumes at D2c.


### L6-T4 — current Claude takeover map, 15 September 2026

Refreshed the requested interruption handover before further implementation. The [main checklist](handover/l6-t4-claude-tasks.md) gives the exact worktree/branch, completed evidence, the ordered D2c–H continuation, files and completion conditions, and a ready-to-paste Claude prompt. The [D2 detail](handover/l6-t4-release-retirement-tasks.md) now makes persistent store binding and partial-removal recovery explicit. Incoming agents must update the checkpoint after each verified step and preserve all modified/untracked files.

Recovered the final D2b type-check result: `l6-t4-release-eligibility-types4.log` reports 461 files, zero errors/warnings and 192 hints; its command handle is closed. The existing 167-case Nginx-enabled run and actual VPS namespace proof remain recorded. The private checkpoint and path inventory are current. This continuation changes documentation only; D2c implementation has not started. No partial commit, production change or goal-status change occurred.


### L6-T4 — durable retirement records and lock session, 15 September 2026

Implemented D2c's private store/attempt records, permanent retired-ID staging fence and scoped inspection/capture session under the existing store lock. Binding also retains the release-directory inode and origin; exact publication-pair recovery preserves unrelated private files. Completion requires physical absence plus the matching server receipt/fence, and a repeated completion cannot clear another attempt. Read-only inspection enforces an existing persistent binding. Shared bounded mount validation is reused by metadata operations. See [the guide](builder-quotas.md#durable-release-retirement-records) for bounds and precise limits.

The combined release/client/domain run passes **179 cases across ten files** with actual Nginx enabled, including ten new record cases and two session cases. Actual disposable VPS mount/foreign-owner checks also pass for the metadata paths; all four releases and outside canary data remain unchanged. Native-worker and fixture bundles pass; full types pass, with final logs/handles in the private checkpoint. The initial new fixture omitted the required builder page; adding the proper fixture entry resolved its ten setup failures without changing the release requirement.

D2c remains unfinished: its SQL claim/finish integration, actual physical deletion, pending-attempt abandonment and native stopped-service/child recovery coordinator are not wired. Current record tests simulate completed physical absence explicitly; they do not establish removal or service-restart acceptance. No production/provider/migration/service/frontend change, deletion or commit occurred. The Claude handover now points to this exact partial state; the full launch goal stays active.


### L6-T4 — release retirement and actual restart recovery, 15 September 2026

D2c now performs and recovers actual owned-file removal through `retireRelease`, using the current native operation and existing store lock. It preserves serving/rollback/recovery data, durable intent and permanent ID fences, rechecks actual file identities/controls, syncs individual removals and releases the local attempt only after exact SQL completion and physical absence. Recovery works after the target manifest or whole release root is gone. Current executor ownership is durably transferred before further work, so recovery can itself be interrupted safely.

The unshipped retirement migration now versions attempts and provides cancellation under the claim lock. A cancelled old generation can never claim or complete later, while a claim that wins cancellation stays owned. This closes delayed-request races without per-token cancellation history. The configured client assignment is also checked before permanent retirement metadata can be written. See [the guide](builder-quotas.md#release-removal-and-restart-coordination) for the coordinator/caller boundary.

The final combined run passes **453 cases across seventeen files** with actual Nginx and zero skips. Actual PostgreSQL passes **26 contention groups** plus stale-isolation refusal. A disposable VPS systemd service was killed twice with actual detached build descendants; the next invocation proved shutdown, recovered the missing-manifest state, completed retirement and retained working rollback. All generated services/cgroups/files were removed. Types and native/fixture bundles pass; exact final logs and handles are in the private checkpoint. The initial type-only fixture failure used `replaceAll` against the existing ES2020 library and was corrected without changing the target.

D2d publication/recovery scheduling and history availability remain next, followed by D2e immutable/abandoned-state cleanup, D2f and D3–H. The coordinator is tested but not installed or called by production workers. All T4 remains modified/untracked/uncommitted/undeployed on unchanged HEAD `d7435e64926a09f72da7eca7503cf0ab7fbbe2e2`; L5 stays live and the full goal remains active. No new permission or human-input hold is needed.


### L6-T4 — interruption handover refreshed, 15 September 2026

Before further implementation, split D2d into four ordered checkpoints: native worker/recovery, client/domain/retry coverage, history availability and combined caller proof. Each names its files and completion evidence in [the detailed checklist](handover/l6-t4-release-retirement-tasks.md); the [main handover](handover/l6-t4-claude-tasks.md) retains D2e–H, the exact worktree and a Claude takeover prompt. Added Claude entry-point guidance in both worktrees and minimal continuation pointers in the original checkout's AGENTS/HANDOVER, preserving pre-existing changes.

Recovered the final D2c type-check handle: `l6-t4-retirement-coordinator-types3.log` passes with 463 files, zero errors/warnings and 199 hints. All known implementation command handles are closed. The private checkpoint and modified/untracked index are refreshed. D2d.1 remains unstarted; this continuation changes documentation only. No commit, deployment or goal-status change occurred.


### L6-T4 — native release maintenance caller, 15 September 2026

Connected the native deployment worker to release retirement under its existing outer lock and current native operation. Shared task orchestration now refuses deferred native recovery, repairs interrupted checkout state, and supports maintenance without a new commit/build/publication. The launcher restricts this mode to its fixed main/stage destinations; new service/timer examples reuse the same stable native deployment unit. New cleanup is opt-in at rollout, while a previously owned removal still recovers after the flag is disabled. Publication reconciliation takes priority over starting new cleanup. Foreign activation locks and changed identities are preserved.

The combined affected run passes **257 cases across fifteen files**, with actual Nginx and zero skips; six launcher cases pass. Final types report 463 files, zero errors/warnings and 199 hints. Native/fixture bundles and syntax checks pass. The actual maintenance caller, under an outer flock in a disposable VPS service, survives two forced kills with detached children and partial-manifest removal, then completes with cleanup disabled and retains working serving/rollback. The fixture uses simulated database/HTTP receipts; no SQL changed and earlier real PostgreSQL proof remains separate. All generated service/cgroup/files are removed and command handles are closed. See [native maintenance](builder-quotas.md#native-release-maintenance-caller).

D2d.1 is verified; D2d.2 client/domain/retry coverage is next, followed by history and the remaining storage/abuse/integration/rollout work. Pre-intent metadata/abandoned cleanup remains D2e. All T4 remains uncommitted/undeployed on the same HEAD; L5 remains live and the goal stays active.

### L6-T4 D2d.2 — client, domain, retry and direct-call coverage (15 September, Claude)

Client destinations now retire through the client worker's own boot/start-time process identity, never borrowed native protection. The scheduled worker finishes owned attempts, publishes queued jobs, then considers at most one new retirement per invocation. That new retirement is default off and rate-limited. The release engine refuses activation or restoration of pending/retired releases for every direct caller, and listings omit a partially removed target so domain verification and pre-claim checks keep working. `l6-t4-client-retirement-combined1.log` passes 266 cases across fifteen affected files with actual Nginx and zero skips; `l6-t4-client-retirement-types1.log` reports 465 files, zero errors/warnings and 200 hints. Native release and domain worker bundles pass Node syntax checks, and the client worker CLI still refuses missing configuration. Evidence includes a real killed child process and the actual migration 019 caller in PGlite; see [the client caller guide](builder-quotas.md#client-release-maintenance-caller). Committed as `2ad9ccf` ([skip ci]); not installed or deployed; no SQL changed.

### L6-T4 D2d.3 — release history availability (15 September, Claude)

Release history now says whether an earlier verified release can still be restored, in hosted client history, native Kaizen releases and the local helper. Removed or removing releases keep their history but show plain wording instead of a restore button. A restore refused because a release was removed after the screen loaded shows the server's message, closes the review and reloads history. Migration 020 projects availability through an access-checked function; retirement records stay private. `l6-t4-release-history-combined2.log` passes 290 cases across eighteen affected files with actual Nginx and zero skips. An earlier identical run, `combined1`, had one real-Nginx domain assertion read the previous page immediately after a graceful reload; that file then passed 29/29 standalone and the full rerun passed, and no D2d.3 change touches that path. `l6-t4-release-history-browser1.log` passes both affected browser journeys; `l6-t4-release-history-types1.log` reports 466 files, zero errors/warnings and 200 hints; `l6-t4-release-history-edge1.log` passes the Deno check of both functions and the full Edge suite (10 tests, 68 steps). Desktop/phone screenshots: [desktop](handover/builder-launch/release-availability-desktop.png), [phone](handover/builder-launch/release-availability-mobile.png). Committed as `b13e69a` ([skip ci]); not installed or deployed.

### L6-T4 D2d.4 — release retirement caller checkpoint (15 September, Claude)

All release callers are now covered and documented: which callers retire releases, how each proves an interrupted owner stopped, the installation order for migrations 019–020, functions, workers and timers, and the recovery order after a crash. `l6-t4-caller-checkpoint-combined1.log` passes 290 cases across eighteen affected files with actual Nginx and zero skips, including the hardened reload assertion; `l6-t4-caller-checkpoint-types1.log` reports 466 files, zero errors/warnings and 200 hints; native release and domain worker bundles pass Node syntax checks; six launcher cases pass. `l6-t4-client-retirement-service3.log` kills a transient user-systemd client retirement service (verified MainPID, `KillMode=control-group`) while it holds the destination lock after removing the target manifest, with a detached descendant; a new service invocation proves the old process and descendant stopped, recovers with new cleanup disabled, completes removal and preserves the selected site and rollback. The first two service runs failed in the driver, not the product: the host rejected `systemctl kill --kill-whom=main`, and the held Node process exited early because an unresolved promise does not keep the event loop alive. D2d is complete in fixtures; continue D2e. Not installed or deployed.

### L6-T4 D2e — generated release state retention (15 September, Claude)

Old generated release files are now reclaimed only when provably abandoned: interrupted staging copies, unpublished retirement records, finished journals for releases that no longer exist, visitor asset files no retained release uses (after 30 days), finished native build snapshots and finished client job directories. Live, rollback, recovery and unknown files stay. `l6-t4-generated-retention-combined1.log` passes 296 cases across eighteen affected files with actual Nginx and zero skips; `l6-t4-generated-retention-types1.log` reports 466 files, zero errors/warnings and 200 hints; native release and domain worker bundles pass Node syntax checks. A retirement case that rewrote `active.conf` by substring could corrupt a random fixture path containing `r3`; it now replaces whole words. See [generated state retention](builder-quotas.md#generated-release-state-retention). Not installed or deployed. Continue D2f.

### L6-T4 D2f — integrated release retention proof (15 September, Claude)

D2 (release capacity and retention) is complete in fixtures. `l6-t4-d2f-tests2.log`: the complete `VITE_BUILDER_CLOUD=0 KAIZEN_NGINX_BINARY=… pnpm test` passes 1,517 cases across 122 files, zero skips. `l6-t4-generated-retention-combined1.log`: 296 affected D2 cases across eighteen files with actual Nginx. `l6-t4-d2f-pg1.log`: the actual PostgreSQL 17 contention fixture, packaged from this checkout's migrations (including 019 and 020) into a disposable non-root VPS cluster, passes all 26 contention groups; its files were removed. `l6-t4-d2f-types1.log`: 466 files, zero errors/warnings, 200 hints. Native release and domain worker bundles pass Node syntax checks; the Edge suite passed at D2d.3 and the functions are unchanged since. Actual stopped-service proof: `l6-t4-retirement-worker-vps1.log` (native) and `l6-t4-client-retirement-service3.log` (client). The first complete run (`l6-t4-d2f-tests1.log`) had six `hosted-publishing.spec.ts` timeouts. They reproduce identically at `afb9d3a`, before any D2d work: real Git cases taking 5.1–6.3s against Vitest's 5s default. Those groups now declare a 30s timeout. Not installed or deployed. Continue D3 remaining generated directories.

### L6-T4 D3 — generated directory coverage (15 September, Claude)

Every generated directory now has a documented owner, byte accounting and retention rule. Files left by a killed hosted build (isolated input copies, clone temps and build temp files) are reclaimed at the next build start, and client unpublication no longer leaves a temporary folder. `l6-t4-d3-tests1.log`: complete `pnpm test` with actual Nginx, 1,519 cases across 122 files, zero skips. `l6-t4-d3-types1.log`: 466 files, zero errors/warnings, 201 hints. See [generated directory coverage](builder-quotas.md#generated-directory-coverage). Not installed or deployed. Continue D4.

### L6-T4 D4–D5 — shared storage admission and the D checkpoint (15 September, Claude)

Services writing to the same disk can now share free space safely: each reserves the space it may use, and two producers can no longer both spend the same headroom. Reservations left by a stopped service are cleaned up only when that process is proven gone. The native cleanup inventory is reconciled with every file later D work removes. Retained disk and cache limits (D) are complete in fixtures. `l6-t4-d4-tests2.log`: complete `pnpm test` with actual Nginx, 1,524 cases across 123 files, zero skips; `l6-t4-d4-types1.log`: 467 files, zero errors/warnings, 201 hints. The first complete run (`l6-t4-d4-tests1.log`) and one standalone rerun hit the graceful-reload race in the retention spec's real-Nginx case (a single read right after reload reached the draining worker); that read now polls and passed three standalone reruns and the full rerun. See [shared storage admission](builder-quotas.md#shared-storage-admission-and-native-inventory-coverage). Not installed or deployed. Continue E.

