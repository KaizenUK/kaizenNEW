# L6-T4 D2 — release-retirement continuation

Checkpoint prepared 15 September 2026. This expands D2 in [the Claude takeover checklist](l6-t4-claude-tasks.md); D3–H remain there. Use `/home/sean/Documents/GitHub/kaizenNEW` on `main` after the pause is lifted. Read [the consolidated handover](claude-main-handover.md); the user requested committing incomplete T4 for this handover. These remain checkpoints within one unfinished launch task.

## Exact stopping point

- **Verified:** D2 capacity and D2a–c, including actual release removal and restart recovery. The coordinator is `scripts/builder-release-retention.ts`; its helpers are `scripts/release-retirement-state.mjs`, `scripts/release-retirement-files.mjs` and the held-lock inspector.
- **Latest proof:** `l6-t4-retirement-coordinator-combined3.log` passes 453 cases across seventeen files with actual Nginx and zero skips. `l6-t4-retirement-coordinator-pg1.log` passes 26 real contention groups, including both cancellation/claim orders. `l6-t4-retirement-coordinator-vps1.log` proves two actual service kills, detached-child cleanup, last-executor ownership, missing-manifest recovery, completion and retained rollback. Types/bundles pass; exact final logs and handles are in the private checkpoint.
- **D2 complete in fixtures:** D2a–f are verified and committed on main. Continue D3 generated directories in [the Claude checklist](l6-t4-claude-tasks.md). Nothing is installed or deployed. The code is connected to the native worker but remains uninstalled. D2e immutable/abandoned cleanup and D2f combined proof remain afterward, then D3–H.
- No production migration/service/provider/frontend deployment occurred. All tests used disposable state. T4 is now committed for handover on main, but remains unfinished and undeployed; L5 stays live.

## D2a — Correct and prove the database fence

**State: implemented and verified. Preserve these invariants while adding filesystem integration.**

Files: [migration 019](../../supabase/migrations/202609150019_builder_release_retention.sql), [focused retirement suite](../../client/visual-builder/release-retention-database.spec.ts), [access matrix](../../client/visual-builder/access-matrix-database.spec.ts), [existing database fixture](../../client/visual-builder/plan-limits-database.spec.ts), [actual PostgreSQL contention fixture](../../tests/ops/test_asset_reference_concurrency.py).

- [x] Replace the invalid JSONB projection with explicit comparisons and separate generic-trigger table branches. Exercise insertion, active changes, history annotations, expiry and recovery transitions.
- [x] Acquire the retirement lock before referenced head/job/baseline lookups. Freeze indirect artifact/pointer identities; only a native first worker claim can assign its initially null artifact. Recheck a renewed terminal recovery owner.
- [x] Protect live releases and immediate rollback baselines, queued/current publication and rollback, unexpired client reviews, repository-output reservations/holds, recovery targets and domain evidence. Verify scope isolation and surviving receipts after related records are deleted.
- [x] Bind worker/store/manifest/bytes identities, enforce database-clock grace and reference-driven reset, refuse wrong tokens and support exact retries/completion receipts. Claim replays recheck database protection; future callers must still recheck physical state.
- [x] Prove browser/direct-table denial and service-only RPC access, including canary rows under permissive platform defaults.
- [x] Observe real PostgreSQL lock waits in both orders for client review, native rollback, repository output reservation and domain evidence. Wait for an explicit marker after the holder statement, not a transient idle state immediately after BEGIN. Retained transaction snapshots are refused at the lock boundary; the protocol requires READ COMMITTED.

The short global `builder-release-retention` advisory lock is taken after existing publication project/destination/billing locks. Retirement reads those external tables without taking their row locks, writing them or performing foreign-key checks. Its own records have no project/job foreign keys, so a tombstone survives deletion and does not reverse publication lock order.

Scopes are `repository:staging`, `repository:production` and `client:<destination UUID>`. Records bind project, artifact, worker, store fingerprint, manifest hash and observed bytes. The phases are `pending → removing → removed`; pending grace is seven days using the database clock. This database grace is separate from local eligibility and visitor-asset retention in D2b/e. A completion receipt states the worker verified absence; it does not inspect or remove files itself.

Existing no-write retries were reviewed: active native/client claims and reserved repository outputs are protected by their persisted rows. Completed/failed history can still return its historical outcome; the existing workers require a buildable/reserved phase before doing work. New database queue/pointer references to a claimed artifact are rejected. Direct filesystem artifact-ID reuse fencing still belongs to D2c; SQL proof alone does not complete that integration.

Initial failures were corrected: the access matrix needed a nonempty retirement canary; real PostgreSQL client/legacy queue fixtures needed their actual service JWT role and legacy membership. The stronger holder-readiness marker exposed those fixture permission failures instead of mistaking BEGIN for the tested lock. Final runs above include the fixes. This checkpoint changed SQL/tests only, with no UI change or new Nginx/provider acceptance claim.

## D2b — Define retained-release eligibility from actual state

**State: implemented and verified.**

Files: [retention inspector](../../scripts/release-retention.mjs), [release store](../../scripts/kaizen-releases.mjs), [bounded file reads/hashing](../../scripts/release-storage.mjs), [filesystem suite](../../client/visual-builder/release-retention.spec.ts), [actual namespace fixture](../../tests/builder/verify-release-retention.ts), [private environment example](../../deploy/systemd/public-release.env.example), [quota guide](../builder-quotas.md#filesystem-release-retention-eligibility).

- [x] Defaults retain five newest releases, require 30 days since manifest creation/actual file changes/last recorded use, and keep seven-day visitor grace around transitions. SQL's independent seven-day observation grace remains necessary too.
- [x] Join the existing staging/activation lock. Read bounded manifests, exact active configuration and activation/recovery journals; verify actual served bytes before observing any database candidate. SQL responses must match the configured project/scope/worker/store/manifest/bytes.
- [x] Protect the selected release, recorded rollback baselines, incomplete activation/recovery targets, recent transitions and current/pending database references. Equal latest journal timestamps preserve all possible baselines. Include immutable references from every retained manifest, including proposed candidates.
- [x] Refuse linked/hard-linked/world-writable/foreign-owned/generated paths, mounted stores or descendants including same-device bind mounts, incomplete/unknown journals, missing protected artifacts and mismatched manifests. Preserve unknown root files/staging in physical accounting. Recheck file identity and mount boundaries after delayed observations.
- [x] Verify ordinary selection, preservation and actual Nginx serving/rollback; verify real bind-mount and foreign-owner refusal in a disposable VPS namespace. The initial tied-history fixture expected the superseded original baseline to remain; the corrected fixture proves both tied newer baselines are protected while the older baseline can become eligible.

This is a Linux host inspector, currently not wired into installed workers. It preserves complete store-local recovery journals; D2c/d must also retain the outer native operation and reconcile each worker's existing private recovery ownership before using results for deletion. A pending SQL receipt is a grace observation, not deletion authority. `removing` receipts are returned separately for owned recovery; a completed receipt with reappeared files refuses inspection. The native worker bundle still passes with the shared bounded-read/hash changes. No new UI or live provider acceptance is claimed.

The inspector now uses the scoped `withReleaseRetentionStore` callback. Keep selection, claim and deletion in that same lock lifetime; do not nest activation locks or delete from a stale returned plan. Interrupted owned retirement must be reconciled before full inventory, since partial deletion may leave an incomplete manifest. Reuse the new persistent binding and stable attempt journal before making a database claim; the coordinator still needs implementation.

## D2c — Implement durable removal and restart recovery

**State: implemented, verified in fixtures and committed for handover; uninstalled and undeployed. D2d is complete; D2e is next.**

Files: [coordinator](../../scripts/builder-release-retention.ts), [recorded-file remover](../../scripts/release-retirement-files.mjs), [coordinator tests](../../client/visual-builder/release-retirement.spec.ts), [actual service fixture](../../tests/builder/verify-release-retirement.ts), [durable records](../../scripts/release-retirement-state.mjs), [record tests](../../client/visual-builder/release-retirement-state.spec.ts), shared retirement module from D2b, [release store](../../scripts/kaizen-releases.mjs), [native operations](../../scripts/builder-native-operations.ts), [native release guard](../../scripts/builder-native-release-guard.mjs), [storage accounting](../../scripts/release-storage.mjs). Extend the existing [retention filesystem suite](../../client/visual-builder/release-retention.spec.ts) and [root namespace fixture](../../tests/builder/verify-release-retention.ts) with actual removal/restart proof.

- [x] Persist the store's project/scope/worker binding before accepting retirement work, including repository stores without an existing client binding. Also bind the origin and release-directory inode. Refuse reassignment of the same physical store. Bound one active attempt to 8 MiB and 20,000 entries; retain at most 10,000 small permanent fences before refusing another retirement. Interrupted unpublished records stay charged. This metadata limit is not a new bulk data reservation.
- [x] Implement an attempt with immutable token/generation/artifact/file identities and a recorded current executor/controller. Transfer only executor ownership after stopped-work proof, before any further claim/removal. Add exclusive durable publication, exact-pair recovery and physical-absence/exact-receipt checks before forgetting it. Eleven record tests, the coordinator suite and actual two-kill service proof cover these boundaries.
- [x] Connect those records to SQL claim/finish/cancel. Cancellation advances a bounded attempt generation under the same database lock; stale delayed claims and completions refuse permanently. A claim that wins cancellation remains owned and must finish. Exact cancelled receipts release only their own pending local attempt.
- [x] Refactor inspection/capture/rechecks into `withReleaseRetentionStore`. Expire the session after its lock ends and reject altered/stale candidates; use this callback for the coordinator.
- [x] Reconcile an interrupted owned retirement before full release inventory: partial removal may have removed its manifest. Keep claim/deletion within that same session; first reconcile prior worker recovery ownership and prove stopped work.
- [x] Hold the store lock and the applicable active native operation through observation, removal and acknowledgement. This must also invalidate native asset-cleanup inventory when retained roots change.
- [x] Recheck local protection and the recorded identity immediately before removal. Resume only after proving the previous worker and child processes stopped; preserve uncertain state.
- [x] Verify physical absence before sending the database completion receipt. Lost responses, partial removal and a restart after deletion must reconcile without removing another artifact or prematurely releasing accounting.
- [x] Retain a durable fence against recreating a retired artifact ID, including the direct `stageRelease`/CLI path. Pending attempts also refuse reuse; unrelated new IDs remain available. The coordinator publishes the fence before deleting bytes.
- [x] Validate actual stopped-service/restart composition: recover the old native journal, reconcile the selected release/stale activation lock, and resume retirement in a new protected native operation. Two service kills prove the latest executor is recorded and its detached child is gone before final removal.

**Current proof:** `l6-t4-retirement-coordinator-combined3.log` passes 453 cases across seventeen files, with actual Nginx enabled and zero skips. This includes eighteen coordinator cases, eleven record cases, twenty-one inspector/session cases and twenty focused SQL cases. `l6-t4-retirement-coordinator-pg1.log` proves 26 actual contention groups plus stale-isolation refusal. `l6-t4-retirement-coordinator-vps1.log` proves two service kills, detached-descendant cleanup, current-executor transfer and partial/absent-manifest completion in real temporary files. Current bundles and types pass. Database/HTTP in the service fixture are simulated; separate actual PostgreSQL and Nginx proof establish those boundaries. No production migration or service changed.

**Done when:** ordinary removal, interrupted removal and every lost-response boundary are proven with real temporary files. Wrong owner/token/store, changed identity and uncertain process cleanup preserve data. Repeated completion is idempotent and actual bytes drive capacity.

**Caller contract for D2d:** retire at most one artifact per invocation using the configured server RPC transport and an actual `NativeFileProtection` from the current native journal. Resolve the outer worker's existing recovery first. After a service crash, recover native intents through the verified controller and use existing `reconcileRelease` to verify the selected artifact and clear its stopped activation lock before entering the normal retention session. Keep outer/native protection through all of this. Never recover a lock by age or delete lock files directly. Runtime scheduling and worker/history integration remain D2d/F–G. Client destinations use the client worker process identity instead of native protection; see D2d.2.

## D2d — Connect publication, recovery and history

**State: complete in fixtures (D2d.1–4). Uninstalled and undeployed.**

Files: [release worker](../../scripts/builder-release-worker.mjs), [client worker](../../scripts/builder-client-worker.ts), [client publication](../../scripts/client-publication.mjs), [native release](../../scripts/builder-native-release.ts), [repository output](../../scripts/builder-repository-output.mjs), [domain release operations](../../scripts/builder-domain-releases.ts), [client publication UI](../../client/visual-builder/ClientPublications.tsx), the existing history projections in publication SQL and their tests.

- [ ] Wire configured repository staging/production and client stores to the coordinator using their existing identities, locks and recovery paths. Ensure scheduled/retry/manual callers use the same rules.
- [ ] Recheck all publication/rollback/recovery paths against retirement, including a claim retry that returns without an update. Preserve rollback under capacity pressure when required assets already exist.
- [ ] Expose retained versus expired availability in release history, with clear plain-English outcomes. Do not offer a rollback that the server already knows cannot be restored. Preserve audit/history records.
- [ ] Document installation/configuration order and recovery behavior. Keep runtime activation in F–G of the main checklist.

**Done when:** worker/database tests and relevant browser cases cover publication versus retirement, old rollback refusal, retained rollback success and understandable history. Capture desktop/phone evidence for changed UI.

### D2d checkpoints — complete

D2d.1–4 are verified in fixtures and committed on main. Continue at D2e. Complete them in order and update the current pointer after each verified result; they remain part of the unfinished T4 task, whose current source is checkpoint-committed for handover.

1. **D2d.1 — Native repository worker and recovery. VERIFIED, uninstalled.** Files: `scripts/builder-release-worker.mjs`, `scripts/builder-native-release.ts`, `scripts/builder-native-deploy.ts`, `scripts/builder-native-operations.ts`, `scripts/release-recovery.mjs`, `scripts/builder-release-retention.ts`, `deploy/systemd/native-deploy.json.example` and `deploy/systemd/public-release.env.example`. Map the existing outer lock, native journal and controller lifetime first. Call bounded retirement with the configured store/project/scope/worker after reconciling prior work, including an invocation with no new deployment. Preserve the tested recovery order: prove stopped native work, recover its journal, reconcile selected release/activation lock, then enter retirement under current native protection. **Proof:** extend `client/visual-builder/deployment.spec.ts`, `native-deploy.spec.ts`, `recovery-guards.spec.ts` and `release-retirement.spec.ts`; exercise the actual caller through `tests/builder/verify-native-deployment.ts` or `verify-release-retirement.ts`. Prove idle maintenance, lost replies, interrupted retirement and retained rollback through the caller, with no nested activation lock or new deployment required to recover.
2. **D2d.2 — Client, domain, retry and direct-call coverage. VERIFIED, uninstalled.** Files: `scripts/builder-client-worker.ts`, `scripts/client-publication.mjs`, `scripts/builder-client-publisher.ts`, `scripts/builder-domain-releases.ts`, `scripts/builder-repository-output.mjs`, `scripts/kaizen-releases.mjs`, `scripts/release-recovery.mjs`, `deploy/systemd/client-worker.env.example` and `supabase/migrations/202609150019_builder_release_retention.sql`. Trace each configured caller and document its identity and coordination model before wiring maintenance. Audit activation, rollback, reconciliation, direct CLI and no-write idempotent claim retries against pending/retired IDs. Do not fabricate native protection for a client caller that does not have it; integrate with its real producer/journal lifecycle. **Proof:** extend `client-publisher.spec.ts`, `client-destinations.spec.ts`, `client-publication-database.spec.ts`, `domain-releases.spec.ts`, `domain-https.spec.ts` and `release-retention-database.spec.ts` under `client/visual-builder/`. Prove reference/claim orderings, configured-store mismatch refusal, successful retained rollback under capacity pressure and refusal of retired artifacts. Extend `tests/ops/test_asset_reference_concurrency.py` only for new concurrency behavior.
3. **D2d.3 — History availability and user actions. VERIFIED.** Files: `client/visual-builder/ClientPublications.tsx`, `shared/builderReleases.ts`, `supabase/functions/builder-projects/index.ts`, `supabase/functions/builder-publish/index.ts`, existing publication/history projections in SQL, and migration 019 where needed. Trace both native and client history responses; expose authoritative retained/expired/unavailable status while preserving audit rows. Avoid offering known-expired restore actions, and handle a retirement race after the screen loads with a clear recoverable response. **Proof:** extend `client/visual-builder/client-history.spec.ts`, `client-publication-database.spec.ts`, `tests/builder/client-history.spec.ts` and `tests/builder/releases.spec.ts` as applicable. Verify retained restore, expired history and stale-screen refusal; capture and inspect desktop/phone screenshots for changed screens.
4. **D2d.4 — Caller integration checkpoint. VERIFIED.** Files: all modules changed above, `scripts/build-native-release-worker.mjs`, `docs/builder-quotas.md`, this checklist, `docs/handover/l6-t4-claude-tasks.md`, `HANDOVER.md` and the launch task map. Record configuration/recovery order and exactly which callers are covered. **Proof:** affected combined worker/recovery/domain/history tests, full types, bundle/syntax checks and the actual stopped-service caller evidence from D2d.1–2 pass. Record commands, logs and any limitations in the private checkpoint. Mark D2d complete and move to D2e only then; leave installation and deployment for F–G.

**D2d.1 evidence:** `l6-t4-retirement-worker-combined1.log` passes 257 cases across fifteen files with actual Nginx and zero skips; six launcher cases and full types pass (463 files, zero errors/warnings, 199 hints). `l6-t4-retirement-worker-vps1.log` runs the actual `runNativeReleaseTask` maintenance path through two service kills under an outer flock: stopped native/checkout recovery, current-executor adoption, partial-manifest cleanup with the new-work flag disabled, completion and retained rollback pass. Service RPC/HTTP replies are simulated; separate earlier PostgreSQL/Nginx proof remains. All temporary services/files are removed. Current worker and fixture bundles pass syntax checks. No runtime, timer or migration was installed.

**D2d.2 evidence:** new `scripts/builder-client-release-maintenance.ts` and `maintainClientDestinations` connect client destinations through the client worker's own boot/start-time process identity; no native protection is fabricated. The worker finishes owned attempts before queued jobs and considers new retirement afterwards, at most one release per invocation, default off and rate-limited. `activateRelease` and restoration targets in `reconcileRelease` refuse pending/retired IDs for every direct caller; `listReleases` omits an unselected partial target so client pre-claim checks and domain verification keep working. Caller trace: hosted client worker (retires client scopes), local `ClientPublisher` (never retires; receives the guards), domain worker (read/verify only), repository output (native worker, D2d.1). No-write client claim retries were already fenced by migration 019; no SQL or contention fixture changed. `l6-t4-client-retirement-combined1.log` passes 266 cases across fifteen affected files with actual Nginx and zero skips; `l6-t4-client-retirement-types1.log` reports 465 files, zero errors/warnings and 200 hints. Native release and domain worker bundles pass Node syntax checks, and the client worker CLI still refuses missing configuration. New cases are in `release-retirement.spec.ts` (seven, including a real `SIGKILL` child), `release-retention-database.spec.ts` (actual migration caller) and `domain-releases.spec.ts` (alias during partial retirement); the shared fixture is `client/visual-builder/releaseRetirementFixture.ts`. Remaining for D2d.4: a disposable systemd run of the client unit if installed-unit evidence is wanted. See [the client caller guide](../builder-quotas.md#client-release-maintenance-caller).

**D2d.3 evidence:** migration `202609150020_builder_release_availability.sql` adds an access-checked availability projection to hosted client history and native release summaries without exposing retirement records or deleting history. The local `ClientPublisher` derives availability from its actual store and refuses a removed restore. Both release screens replace restore actions with plain wording and reload after a stale-screen server refusal. `l6-t4-release-history-combined2.log` passes 290 cases across eighteen affected files with actual Nginx and zero skips. An earlier identical run, `combined1`, had one real-Nginx domain assertion read the previous page immediately after a graceful reload; that file then passed 29/29 standalone and the full rerun passed, and no D2d.3 change touches that path. `l6-t4-release-history-browser1.log` passes both affected browser journeys; `l6-t4-release-history-types1.log` reports 466 files, zero errors/warnings and 200 hints; `l6-t4-release-history-edge1.log` passes the Deno check of both functions and the full Edge suite (10 tests, 68 steps). Desktop/phone screenshots are inspected and linked from [the availability guide](../builder-quotas.md#release-history-availability). For D2d.4: the flaky real-Nginx domain assertion (immediate read after a graceful reload) is pre-existing and should be hardened or noted there.

**D2d.4 evidence:** `l6-t4-caller-checkpoint-combined1.log` passes 290 cases across eighteen affected files with actual Nginx and zero skips, including the hardened reload assertion; `l6-t4-caller-checkpoint-types1.log` reports 466 files, zero errors/warnings and 200 hints; native release and domain worker bundles pass Node syntax checks; six launcher cases pass. `l6-t4-client-retirement-service3.log` kills a transient user-systemd client retirement service (verified MainPID, `KillMode=control-group`) while it holds the destination lock after removing the target manifest, with a detached descendant; a new service invocation proves the old process and descendant stopped, recovers with new cleanup disabled, completes removal and preserves the selected site and rollback. The first two service runs failed in the driver, not the product: the host rejected `systemctl kill --kill-whom=main`, and the held Node process exited early because an unresolved promise does not keep the event loop alive. Caller coverage, installation order and recovery order are in [the coverage guide](../builder-quotas.md#release-retirement-caller-coverage-and-installation-order).

**Recovery boundary for later checkpoints:** metadata can now be read before lock recovery without unlinking its exact first-publication pair; the pair is repaired only within the held retirement session. Foreign locks/metadata are preserved. D2e still owns interrupted pre-intent metadata/staging/transaction cleanup and immutable retention; do not call all generated-state recovery complete from the verified post-intent service-kill cases.

## D2e — Finish immutable and abandoned generated-state retention

**State: verified in fixtures; see [generated state retention](../builder-quotas.md#generated-release-state-retention). `l6-t4-generated-retention-combined1.log` passes 296 cases across eighteen affected files with actual Nginx and zero skips; `l6-t4-generated-retention-types1.log` reports 466 files, zero errors/warnings and 200 hints; native release and domain worker bundles pass Node syntax checks. A retirement case that rewrote `active.conf` by substring could corrupt a random fixture path containing `r3`; it now replaces whole words.**

Files: shared retirement module, [release store](../../scripts/kaizen-releases.mjs), [release recovery](../../scripts/release-recovery.mjs), [storage accounting](../../scripts/release-storage.mjs) and the focused retention tests.

- [x] Reclaim immutable files only after checking every retained manifest and the documented visitor grace. Incomplete manifests keep the files protected.
- [x] Reclaim abandoned staging directories and old requests/transactions only with durable ownership and terminal/stopped-work proof. Preserve recovery receipts needed by incomplete work.
- [x] Keep enumeration/removal bounded, refuse linked/mounted/foreign targets, and retain unknown files in actual-byte accounting. Reconcile partial cleanup and lost acknowledgement on restart.

**Done when:** fixtures prove shared immutable files survive while any retained release needs them; visitor grace is honored; known abandoned data is reclaimed; unknown/active/recoverable data survives. Actual Nginx still serves the active release and can restore the protected rollback afterward.

## D2f — Integrated proof and handover update

**State: verified.** `l6-t4-d2f-tests2.log`: the complete `VITE_BUILDER_CLOUD=0 KAIZEN_NGINX_BINARY=… pnpm test` passes 1,517 cases across 122 files, zero skips. `l6-t4-generated-retention-combined1.log`: 296 affected D2 cases across eighteen files with actual Nginx. `l6-t4-d2f-pg1.log`: the actual PostgreSQL 17 contention fixture, packaged from this checkout's migrations (including 019 and 020) into a disposable non-root VPS cluster, passes all 26 contention groups; its files were removed. `l6-t4-d2f-types1.log`: 466 files, zero errors/warnings, 200 hints. Native release and domain worker bundles pass Node syntax checks; the Edge suite passed at D2d.3 and the functions are unchanged since. Actual stopped-service proof: `l6-t4-retirement-worker-vps1.log` (native) and `l6-t4-client-retirement-service3.log` (client). The first complete run (`l6-t4-d2f-tests1.log`) had six `hosted-publishing.spec.ts` timeouts. They reproduce identically at `afb9d3a`, before any D2d work: real Git cases taking 5.1–6.3s against Vitest's 5s default. Those groups now declare a 30s timeout.

Files: all D2 modules/tests, [takeover checklist](l6-t4-claude-tasks.md), [quota guide](../builder-quotas.md), [task map](../builder-launch-plan.md), [HANDOVER](../../HANDOVER.md).

- [x] Run focused new SQL/filesystem tests, affected publication/recovery/domain/storage suites with real Nginx enabled, the actual PostgreSQL contention fixture, full `pnpm typecheck`, and native-worker bundling/syntax checks. Use disposable stopped-service fixtures for process/restart claims.
- [x] Record exact commands, final results, log paths, fixture versus provider limits and any UI evidence. Fix failures; do not count skipped native checks as passing proof.
- [x] Update D2's status and move the resume pointer to D3 only when every D2 condition is met. Refresh the private checkpoint and modified/untracked file inventory. Full T4 tests, one T4 commit and L6 CI/rollout remain F–G.

Useful existing fixture entry points are [the access matrix](../../client/visual-builder/access-matrix-database.spec.ts), [release capacity](../../client/visual-builder/release-storage.spec.ts), [deployment](../../client/visual-builder/deployment.spec.ts), [client publication database](../../client/visual-builder/client-publication-database.spec.ts), [recovery guards](../../client/visual-builder/recovery-guards.spec.ts) and [actual PostgreSQL contention](../../tests/ops/test_asset_reference_concurrency.py). Local Nginx is `/home/sean/.cache/kaizen-launch-tools/nginx`; set `KAIZEN_NGINX_BINARY` when running its suites. Native PostgreSQL uses the disposable-cluster instructions in the main checklist; do not point destructive fixtures at production.

After D2, continue D3 generated-directory coverage, D4 shared admission/inventory, D5 storage integration, E abuse controls, F T4 integration/installation preparation, G L6 rollout and H the final audit. No new permission or human-input hold is needed for this continuation.
