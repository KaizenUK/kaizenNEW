# L6-T4 continuation tasks for Claude

Prepared 15 September 2026 at Sean's request so the work can continue if Codex credits run out. This is the current resume point for the unfinished launch goal. Read this before the older chronological checkpoints in [HANDOVER](../../HANDOVER.md).

## Start here

Continue from **`/home/sean/Documents/GitHub/kaizenNEW`**, branch **`main`**, when implementation is resumed. Sean paused work and then requested branch consolidation. Read [the consolidated handover](claude-main-handover.md) first. All source through D2d.1 and the separate editor polish is committed for handover; old statements about T4 existing only as dirty launch files no longer apply.

The launch checkpoint is `e73079d`, the original editor checkpoint is `e845385`, and both are merged into main. **L0–L5 are deployed; L6 is not.** The recorded live L5 application revision is `48823c6f04d49073eb1eb20be7bacc92556032cb`; verify live state when preparing the eventual rollout.

Read [the launch brief](codex-launch-brief.md), [the task map](../builder-launch-plan.md), and [the quota implementation guide](../builder-quotas.md). The local private checkpoint is `/home/sean/.local/state/kaizen/l6-t4-working.md`; it contains older detailed evidence and fixture commands. This document supersedes its older next-step paragraphs.

**Immediate next work: D2d.2 — client/domain/retry and direct-call coverage.** D2d.1 now connects retirement to the configured native deployment worker and a separate idle-maintenance action, under the same service identity, outer lock and native operation. It repairs stopped checkout/retirement work without a new commit or build. Explicit publication recovery precedes new cleanup; owned retirement still completes after new cleanup is disabled. Client/domain integration and history availability remain unfinished. Follow [D2d.2–4](l6-t4-release-retirement-tasks.md#d2d-checkpoints--resume-at-d2d2), then D2e–f, D3–D5 and E–H.

**Latest checkpoint:** 257 cases across fifteen files pass with actual Nginx and zero skips; six launcher cases and full types pass (463 files, zero errors/warnings, 199 hints). The actual native-maintenance caller passes two forced service kills, detached-child cleanup, partial-manifest recovery with new cleanup disabled, completion and retained rollback. Exact logs are in the private checkpoint. All command handles are closed. The service/timer examples and runtime are prepared but uninstalled; no production site changed.

| Part                                           | Current state                                                             | Result needed                                                     |
| ---------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| A–C: native cleanup and copy cancellation      | Implemented and verified in fixtures                                      | Preserve the implementation and evidence below                    |
| D: retained disk and cache limits              | Native storage, candidates and pnpm maintenance verified; releases remain | Bound remaining generated storage and reclaim only eligible files |
| E: publication abuse controls                  | Pending D                                                                 | Rate limits, reports, takedown and recoverable suspension         |
| F: T4 integration and installation preparation | Pending D–E                                                               | Complete native/provider/browser proof and one T4 commit          |
| G: L6 rollout                                  | Pending F                                                                 | Coordinated configuration, milestone CI and live verification     |
| H: final goal audit                            | Pending G                                                                 | Goal-wide verification and a concrete human-only checklist        |

### Paste into Claude when taking over

> Resume the Kaizen launch goal from `/home/sean/Documents/GitHub/kaizenNEW` on `main`. Read `docs/handover/claude-main-handover.md`, `HANDOVER.md`, the launch brief and the remaining T4 task maps. The consolidated source includes both editor polish and unfinished T4. Start at D2d.2: client/domain/retry and direct-call coverage. Preserve private files, keep evidence current, collect human-only questions at the end, and follow the documented coordinated rollout; this checkpoint has not been deployed.

### Resume safely after an interruption

1. Check the worktree and branch above, then read the latest private checkpoint. If code has progressed beyond this document, reconcile its changes and evidence before choosing the next unchecked step.
2. Follow **D2d → D2e → D2f → D3 → D4 → D5 → E → F → G → H**. Each section names its starting files, dependencies and completion proof. D2c is verified in `scripts/builder-release-retention.ts`, its file/state helpers and the shared inspection/staging paths. D2d.1 is verified; D2d.2 client/domain/retry coverage is next in the detailed D2 checklist.
3. Before moving to the next step, record files changed, the exact passing or failing command, proof log, unresolved issue and any running process in the private checkpoint. Update this document's immediate next step and refresh the modified/untracked path index. An interrupted check remains unverified until its result is recovered or rerun.
4. Keep the actual worktree available to Claude. The source checkpoint is committed on main. The private path index is historical; retain private configuration and proof logs separately. Record remaining completion work at F and deployment at G.

## Sean's instructions to preserve

- Continue through L0–L6 autonomously. Use recommended routine choices and put questions, provider sign-ins, final wording and human acceptance at the end. Failed checks mean diagnose and fix; they are not a user-input hold.
- No time estimates. The latest rough estimate was **65–70% of T4 implementation**, not measured launch readiness. **31/32 implementation tasks built** is an equal task count; this unusually large last task makes that count misleading as an effort percentage.
- No subagents. Claude taking over this worktree does not require creating another Codex task.
- Reuse the existing upload, repository, build, release, billing and auth modules. Keep the local helper usable.
- Preserve Sean's original checkout, source changes, private migration files and personal `.kaizen-builder`. Use disposable repositories, accounts, projects and provider objects for destructive tests.
- The earlier Sanity incident is closed. Do not add incident review or credential checks back into the task list, repeat the incident warning, or rotate those credentials as part of this work. Never print credentials or include them in tracked files.
- Relevant local verification continues per change. Batch CI at the L6 milestone and again at goal end, not per subtask. The user-requested handover checkpoint is committed before T4 completion; remaining continuation steps still belong to the same unfinished launch task.
- Deployment within this goal is authorized. Coordinate the matching migrations, functions, services and frontend. Keep native cleanup disabled until the complete installation is verified. Sean's provisional private-beta acceptance remains accepted; automated tests do not replace his final hands-on acceptance.

## Work already implemented — retain and build on it

| Area                                            | Implementation state                                                                                                                                                                        | Evidence and limits                                                                                                                                                                                              |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Upload quotas and transfer                      | Physical observations, reservations, bounded private spool, authenticated resumable upload handler, immutable provider verification and completion/cancellation receipts                    | Database/service/filesystem/provider fixtures pass. Actual production upload service/proxy is not installed.                                                                                                     |
| Browser uploads and website copies              | Original Kaizen and client uploads use the new transport; copies reserve their full snapshot, resume the same destination and support owner cancellation with guarded cleanup/purge         | Database, React, Edge and three-engine cancellation fixtures pass. Complete live upload/copy/provider proof remains.                                                                                             |
| Catalogue and ordinary cleanup                  | Verified registration/adoption, Storage policy cutover, reference guards, uncatalogued client-file discovery, durable version-specific removal with accounting retained until completion    | Implemented in the unshipped migrations and existing upload worker. Native scopes are excluded from ordinary cleanup.                                                                                            |
| Native source inventory                         | Reads source, drafts, recovery/output, retained releases and actual Git objects, including packed history and supported compressed content                                                  | Existing scanner fixtures pass. It refuses incomplete reads and foreign Git locks.                                                                                                                               |
| Native producer coordination                    | SQL generation/operation protocol, durable local journal, hosted edit/build callers, controlled child processes, stopped-service recovery, release/rollback integration                     | Separate protocol, helper, actual VPS process and recovery fixtures pass. These are not live rollout evidence.                                                                                                   |
| Deployment preparation and interrupted checkout | Fixed-target launcher, candidate validation, private Git index, durable checkout receipt and verified recovery under the service lock                                                       | 117 relevant cases, full types and actual disposable VPS service-kill/restart proof pass. Launcher/workflow remains uninstalled.                                                                                 |
| Native inventory → cleanup claim                | Complete configured roots scanned before clearance; exact epoch/configuration/attempt binding; retained references restart recovery grace; existing worker performs provider removal/replay | **A–B checkpoint: 192 cases across nine files pass; full types, twelve native PostgreSQL contention groups and actual root-service restart proof pass.** Real inventory registration/installation remain in F–G. |

Do not add test totals from different checkpoints together: they overlap. Historical evidence remains useful but is not a claim that the complete current T4 or L6 suite has passed.

## Ordered continuation tasks

The order below continues the existing T4 work. Mark a box only when its stated proof exists; record the exact result in the task map and update this document's resume point.

### A — Finish proof of native scan-to-claim integration

**State: implemented, verified and committed for handover; undeployed.**

Files: [native inventory coordinator](../../scripts/builder-native-asset-cleanup.ts), [existing cleanup worker](../../scripts/builder-asset-cleanup.ts), [cleanup migration 015](../../supabase/migrations/202609150015_builder_asset_cleanup.sql), [native migration 017](../../supabase/migrations/202609150017_builder_native_asset_operations.sql), [inventory tests](../../client/visual-builder/native-asset-cleanup.spec.ts), [upload/database integration tests](../../client/visual-builder/upload-quota-database.spec.ts), [real PostgreSQL contention fixture](../../tests/ops/test_asset_reference_concurrency.py).

- [x] Bind a canonical immutable inventory to project coverage, producer IDs and every configured root. Scan every root for the target identity, including another project's roots.
- [x] Use the catalogue's actual `file_url`, including legacy URLs with no asset UUID. Scan retained candidate repositories through actual Git objects.
- [x] Obtain a fresh SQL observation before the scan and pass the exact epoch, inventory fingerprint and durable attempt token into clearance/claim. A producer beginning or ending invalidates earlier evidence.
- [x] Reset the seven-day recovery grace and clear removal evidence when a retained reference is observed. Do not grant deletion authority on that path.
- [x] Prove reference retention in source/drafts/releases/packed candidate history, safe unused-file removal, cross-worker adoption refusal, changed generations, missing roots, wrong coverage and lost database completion.
- [x] Extend the native PostgreSQL fixture for `builder_native_cleanup_referenced` versus a competing claim/producer, using actual blocked transactions and the native → project → catalogue → cleanup lock order. The updated fixture passes twelve actual contention groups, including four reference/grace versus claim/producer orderings. Evidence: `/home/sean/.local/state/kaizen/l6-t4-native-cleanup-pg1.log`; its disposable cluster/checkout were removed.

**Done when:** the focused suite and meaningful native contention cases pass without provider removal on a refused claim. The current command is `pnpm test client/visual-builder/native-asset-cleanup.spec.ts client/visual-builder/upload-quota-database.spec.ts`. The latest successful output is `/home/sean/.local/state/kaizen/l6-t4-native-cleanup-integration1.log` (100/100); types output is `l6-t4-native-cleanup-types1.log` (455 files, zero errors/warnings, 192 hints). Both test command handles have closed; no typecheck process remains. No rerun is needed solely to recreate a missing tool handle.

### B — Package and verify the root cleanup service

**State: implemented and verified in fixtures; packaged but uninstalled.**

Reuse [the inventory coordinator](../../scripts/builder-native-asset-cleanup.ts), [cleanup worker](../../scripts/builder-asset-cleanup.ts), [UploadSpool](../../scripts/builder-upload-spool.ts), [provider adapter](../../scripts/builder-upload-provider.ts), and the existing private RPC client in [release worker](../../scripts/builder-release-worker.mjs). Follow the bundle/service conventions in [upload bundle](../../scripts/build-upload-worker.mjs) and [upload unit](../../deploy/systemd/kaizen-upload-worker.service). The dedicated files now exist: [service entry](../../scripts/builder-native-cleanup-host.ts), [bundle builder](../../scripts/build-native-cleanup-worker.mjs), [private configuration example](../../deploy/systemd/native-cleanup.json.example), [environment example](../../deploy/systemd/native-cleanup.env.example), [service](../../deploy/systemd/kaizen-native-cleanup.service), [timer](../../deploy/systemd/kaizen-native-cleanup.timer), [batch tests](../../client/visual-builder/native-cleanup-host.spec.ts) and [actual service fixture](../../tests/builder/verify-native-cleanup.ts).

- [x] Load the complete installed inventory from a bounded, root-owned private file with trusted parents. Validate it using `nativeAssetInventory`; do not accept request-supplied paths or fingerprints.
- [x] Compose a bounded cleanup batch using `NativeAssetCleanup` and `AssetCleanupWorker`. Keep errors/credentials out of public output; report counts and actionable operator status.
- [x] Use a **separate root-owned private spool and actual process identity**, with a single service lock and clean shutdown/recovery. Never fabricate the upload worker's PID or claim its spool ownership.
- [x] Use the native queue only. Retain version-specific provider deletion and the existing completion/acknowledgement recovery. A previously claimed removal can reconcile without rescanning; a pending removal needs fresh evidence.
- [x] Package and verify the actual Node entry and systemd restrictions. Exercise startup, stopping, restart/reconciliation, malformed/private configuration and inaccessible roots in a disposable service.

**Done when:** an actual service invocation performs inventory → clearance → claim → provider fixture removal → accounting completion using its own durable identity, and restart resumes the same operation. This is now proven by the actual disposable VPS service: root-owned configuration checks, reading a helper-owned Git repository, SIGKILL after simulated provider removal and reconciliation by the next real process. The exact repository is trusted through a per-command `safe.directory` argument; no global wildcard is configured. Network/database responses in that service fixture are simulated; separate database tests and real PostgreSQL contention establish the SQL behavior. Keep the production timer disabled until task G.

**Evidence:** `l6-t4-native-cleanup-service-combined1.log` (192/192, nine files), `l6-t4-native-cleanup-service-types2.log` (zero errors/warnings), and `l6-t4-native-cleanup-service-vps3.log` (actual service kill/restart passed), all under `/home/sean/.local/state/kaizen/`. The bundle builds and passes Node syntax/argument-guard checks. The fixture driver is `run-l6-native-cleanup-fixture.py` in that private directory. Two earlier fixture-driver errors were corrected: replacement ordering corrupted temporary paths, and the host rejected the auxiliary-process form of `systemctl kill` after killing the main process. The final driver targets the actual main process and verifies terminal state before restarting. All disposable services/files are removed.

**Implementation constraints:** root and upload spools do not share a filesystem lock. The SQL adoption/registration state guards and immutable/version-targeted provider operation are the cross-worker boundary; preserve both. Declare retained Git checkouts as `repository` or `repository-collection`, not generic `tree`. Required missing roots must refuse cleanup. Do not claim the configuration is complete until the real server inventory has been reconciled.

### C — Complete pending-copy cancellation and guarded purge

**State: implemented, verified in fixtures and committed for handover; undeployed.**

Files: new [cancellation migration 018](../../supabase/migrations/202609150018_builder_copy_cancellation.sql), [copy migration 011](../../supabase/migrations/202609150011_builder_project_copies.sql), migrations 010 and 012–017 under `supabase/migrations/`, [projects endpoint](../../supabase/functions/builder-projects/index.ts), [upload maintenance](../../scripts/builder-hosted-uploads.ts), [Projects screen](../../client/visual-builder/ProjectsView.tsx), [shared project types](../../shared/builderProjects.ts), [database/service cases](../../client/visual-builder/upload-quota-database.spec.ts), [access matrix](../../client/visual-builder/access-matrix-database.spec.ts), [React cases](../../client/visual-builder/project-copies.spec.tsx), [actual Edge handler cases](../../tests/functions/project-copies.test.ts), [browser journey](../../tests/builder/projects.spec.ts) and [real PostgreSQL contention](../../tests/ops/test_asset_reference_concurrency.py). Existing [account deletion](../../scripts/builder-account-delete.ts) retains its ownership-transfer and soft-deletion behavior.

- [x] Add an explicit owner-authorized cancellation state/action for a pending copy, with clear confirmation and recoverable UI state.
- [x] Stop new copy batches and delayed completion from reviving cancelled work. Recheck current access and immutable request identity.
- [x] Release source/destination reference protection in the correct order so verified cleanup can remove only abandoned destination files. Preserve the source website.
- [x] Retain physical storage charges and records until local/provider cleanup is acknowledged; only then allow the guarded project purge. Preserve account-closure ownership-transfer behavior.

**Done when:** database, actual Edge-handler and React/browser cases cover cancellation during transfer, late callbacks, lost responses, resumed cancellation, revoked access and safe final purge. A remaining provider file or unacknowledged cleanup must prevent record deletion.

**Implemented:** the current destination owner can explicitly cancel a pending copy, including during a running transfer and without retaining source access. Writes are fenced before the private snapshot releases its source/destination references. Untransferred allowance is released; actual or unconfirmed file bytes stay charged. Existing upload maintenance removes files and acknowledges its local records before a bounded purge can delete the destination. A minimal private cancellation record survives to refuse a late duplicate retry with the same destination ID; it retains no workspace or file content. The original website remains intact. Purge refuses native-configured projects, domains, deployment destinations and any remaining provider/cleanup state. Existing account closure was not rewritten.

The Projects confirmation has focus/Escape behavior, remains usable during copying, and stops further automatic batches. The cancelled card stays visible while its files are being removed, has no misleading Restore/Duplicate actions and disappears after maintenance completes. See [behavior, limits and nine inspected screenshots](../builder-quotas.md#cancelling-an-unfinished-website-copy).

**Evidence:** the following logs are under `/home/sean/.local/state/kaizen/`; all command handles have closed successfully.

- `l6-t4-copy-cancel-combined2.log`: **236/236 across nine application/database/service files**. The final toolbar polish followed this run and passes **8/8 React cases** in `l6-t4-copy-cancel-ui-final.log`.
- `l6-t4-copy-cancel-types3.log`: full type check, zero errors/warnings, 192 hints across 456 files.
- `l6-t4-copy-cancel-edge2.log`: full Edge suite, **10 tests / 68 steps**.
- `l6-t4-copy-cancel-pg2.log`: **sixteen actual PostgreSQL contention groups**, including cancellation versus finish in both orders, purge versus delayed duplicate retry, and upload reservation versus cancellation. Each competing transaction is observed blocked before release. The disposable VPS cluster and checkout were removed.
- `l6-t4-copy-cancel-browser3.log`: **three passing browser runs**, one per Chromium/Firefox/WebKit; desktop/phone confirmation and cleanup-state screenshots are inspected and saved with the guide.

The standalone upload bundle and Node syntax check pass. Browser network and provider operations are fixtures; production upload/proxy setup and live provider copying/removal remain in F–G. Initial fixture plan-capacity and access-matrix omissions were fixed before the final successful runs. Do not rerun passed checks solely because an old tool handle is unavailable.

### D — Finish retained disk and cache limits

**State: native storage admission, candidate reclamation, D1 package maintenance, D2 capacity, D2a SQL fencing and D2b filesystem eligibility are verified. D2c–f removal/integration and D3–D5 remain unfinished.**

Files: [build sandbox](../../scripts/builder-build-sandbox.ts), [controlled build](../../scripts/builder-controlled-build.ts), [hosted builds](../../scripts/builder-hosted-builds.ts), [repository usage](../../scripts/builder-repository-usage.mjs), [repository output](../../scripts/builder-repository-output.mjs), [native deployment](../../scripts/builder-native-deploy.ts), [checkout recovery](../../scripts/builder-native-checkout-recovery.ts), [release store](../../scripts/kaizen-releases.mjs), [release recovery](../../scripts/release-recovery.mjs), service definitions under `deploy/systemd/`, and matching build/release/native tests.

The candidate lifecycle now lives in [NativeCandidates](../../scripts/builder-native-candidates.ts), with [focused filesystem cases](../../client/visual-builder/native-candidates.spec.ts), [deployment integration](../../client/visual-builder/native-deploy.spec.ts) and the extended [actual service fixture](../../tests/builder/verify-native-deployment.ts).

- [x] Reuse [the hosted disk guard](../../scripts/builder-hosted-disk.ts) to count the native checkout and worker state together, including retained dependencies, caches and candidate history. Connect preparation and the native build entry; preserve uncertain child-cleanup evidence. Verify admission refusal, actual command cancellation and stopped-service recovery.

- [ ] Record explicit limits and ownership for retained dependencies/caches, prepared candidates, failed builds, previews and retained releases. Reuse existing limits rather than imposing duplicate conflicting accounting.
- [ ] Check/reserve capacity before materialization; reconcile interrupted reservations against actual disk state. Stop known child processes before reclaiming their files.
- [ ] Add bounded retention cleanup of eligible generated state. Keep serving, rollback, pending copy, active native operation and recovery-receipt data protected.
- [x] Clean abandoned deployment candidates only with proof they are no longer owned/recoverable. A durable intent precedes cloning; the recorded directory inode, stopped-service proof and checkout receipt gate reclamation. Unknown/legacy candidates remain retained and charged.
- [ ] Reconcile retained-root inventory changes with native cleanup coordination.

**Done when:** disposable filesystem/build/release tests prove capacity refusal before growth, crash/retry accounting and retention without deleting active, recoverable or private files. Disk charges/limits must reflect actual retained bytes, not only saved metadata.

**Current D evidence:** `l6-t4-native-disk-combined1.log` has **112 passing cases across eight application files**. Final root-overlap validation also passes all **23 focused disk/deployment cases** in `l6-t4-native-disk-focused3.log`. Full types pass in `l6-t4-native-disk-types4.log`; the five launcher cases pass in `l6-t4-native-disk-launcher1.log`. Actual VPS proof is `l6-t4-native-disk-vps3.log`: real pinned pnpm exceeds the configured allowance, its actual build cgroup and detached descendant are stopped, generated files remain charged and the next preparation is refused before commands. `l6-t4-native-disk-restart1.log` proves killed-checkout recovery still works. The disposable services, cgroups and fixture files are removed. Logs are under `/home/sean/.local/state/kaizen/`; see [the storage guide](../builder-quotas.md#native-deployment-storage-admission).

**Latest candidate evidence:** `l6-t4-native-candidates-combined2.log` has **128 passing cases across nine files**. Final fixture typing fixes also pass all **31 focused candidate/deployment cases** in `l6-t4-native-candidates-focused4.log`; `l6-t4-native-candidates-types3.log` reports zero errors/warnings, 192 hints across 457 files. `l6-t4-native-candidates-vps1.log` proves a real service killed with a 600 MiB candidate and detached child can reclaim that candidate on its next verified invocation before storage admission. `l6-t4-native-candidates-checkout1.log` proves interrupted checkout recovery still preserves and then releases its candidate. The private fixture driver now accepts `--candidate-restart` as well as `--restart`. All generated services/cgroups/files were removed. The bundle builds and passes Node syntax checks. See [candidate ownership and limits](../builder-quotas.md#retained-deployment-candidates).

**Remaining D boundaries:** the default remains the existing 8 GiB allowance across checkout + private worker state with a 2 GiB free-space floor. This is monitored storage for trusted native commands, not a hard filesystem quota or a shared durable reservation across services. Unknown command growth can exceed the allowance between samples. Separate release/immutable stores, remaining generated storage beyond D1 pnpm maintenance, and additional shared/hard admission limits still need implementation and proof. Existing helper output retention already removes only recorded terminal UUID directories; reuse it. Candidate reclamation occurs inside the current native operation without changing inventory roots; preserve that generation protection in further retention work. Do not mark D complete from the current checks.

#### Smaller steps within D

These are checkpoints within the same L6-T4 task, not separate commits. Finish each step's focused proof and record the result before moving on. The limits and retention policy still need to be chosen and documented where marked unfinished below.

| Step                                 | State and concrete work                                                                                                                                                                                                                                                                     | Files to start with                                                                                                                                                                                                                                                             | Proof required                                                                                                                                                                                                                                                           |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D0 — Native storage and candidates   | **Verified.** Preserve aggregate measurement, controlled cancellation, candidate ownership and interrupted-checkout recovery.                                                                                                                                                               | `scripts/builder-hosted-disk.ts`, `scripts/builder-native-candidates.ts`, `scripts/builder-native-checkout-recovery.ts`                                                                                                                                                         | Existing disk/candidate evidence above; rerun affected regressions when later changes touch these paths.                                                                                                                                                                 |
| D1 — Dependency and package caches   | **Verified.** Owned pnpm store/cache maintenance now precedes new-work storage admission, under the native operation, deployment lock and controlled process lifecycle. Installed dependencies and unknown/private files remain usable. Full generated-scope coverage still belongs to D3.  | `scripts/builder-native-package-cache.ts`, `scripts/builder-native-deploy.ts`, `deploy/systemd/public-release.env.example`, `client/visual-builder/native-package-cache.spec.ts`, `tests/builder/native-package-cache-fixture.ts`, `tests/builder/verify-native-deployment.ts`. | A disposable real-pnpm fixture reclaims unused cache data, still uses installed packages successfully, preserves outside/private files, and proves interrupted maintenance retains operation protection until child shutdown is known.                                   |
| D2 — Release capacity and retention  | **In progress. Capacity and D2a–c verified.** Continue D2d–f caller/history integration, remaining cleanup and combined proof. Protect queued rollback/publication, serving state and recovery before authorizing deletion. Preserve the new admission and bounded-copy checks.                                                | `scripts/release-storage.mjs`, `scripts/kaizen-releases.mjs`, `scripts/release-recovery.mjs`, `scripts/builder-release-worker.mjs`, `scripts/builder-client-worker.ts`, `scripts/builder-native-release.ts`, and matching publication/output SQL and tests                      | Disposable release fixtures prove refusal before copying, activation/rollback after maintenance, preservation of serving/recoverable artifacts and assets needed by retained releases, and safe restart of interrupted cleanup.                                          |
| D3 — Remaining generated directories | **Unfinished.** Check dependency installs, npm/XDG caches, build-isolation directories, failed outputs and previews against an explicit ownership/limit/retention table. Reuse the existing two-terminal-output retention; close actual gaps without adding a second output cleanup system. | `scripts/builder-hosted-build-recovery.ts`, `scripts/builder-hosted-builds.ts`, `scripts/builder-build-sandbox.ts`, `scripts/builder-repository-output.mjs`, `docs/builder-quotas.md`                                                                                           | Each generated scope is covered by actual-byte accounting and a documented admission/retention rule. Fixture restart and preservation cases cover any newly added reclamation. Unknown retained files stay charged.                                                      |
| D4 — Shared admission and inventory  | **Unfinished.** Establish how concurrent helper/deployment/release producers share disk capacity; implement any required reservation or hard-admission protection. Reconcile the complete retained roots with native inventory and its generation protocol.                                 | `scripts/builder-hosted-disk.ts`, `scripts/builder-native-operations.ts`, `scripts/builder-native-asset-cleanup.ts`, relevant service configuration and existing contention fixtures                                                                                            | Competing producers cannot each spend the same reserved headroom. Interrupted reservations reconcile against real retained bytes. No cleanup clearance survives a relevant producer or inventory change. State precisely which bounds remain monitored rather than hard. |
| D5 — D integration checkpoint        | **Pending D1–D4.** Run the affected combined storage/build/release/native checks and full types; update the storage guide, task map and this resume point.                                                                                                                                  | The modules and fixtures changed in D1–D4, `docs/builder-quotas.md`, `docs/builder-launch-plan.md`, this checklist                                                                                                                                                              | Record exact commands/results and distinguish simulated provider responses from actual VPS process/filesystem proof. D's original done condition above is met; then start E. Full T4 checks, its commit and rollout still belong to F–G.                                 |

**D1 evidence and limits:** `l6-t4-native-cache-combined1.log` passes 138 cases across ten files; `l6-t4-native-cache-focused2.log` passes all 27 focused package-cache/deployment cases. Final types in `l6-t4-native-cache-types2.log` have zero errors/warnings and 192 hints across 458 files. `l6-t4-native-cache-vps2.log` proves actual pinned-pnpm pruning, installed-package usability, private-file preservation and over-limit command/descendant cancellation through real VPS cgroups. `l6-t4-native-cache-restart1.log` kills the service after pruning but before acknowledgement, then proves journal recovery and safe retry with the installed module still usable. All generated services/cgroups/files were removed and all command handles are closed. The bundle builds and passes Node syntax checks. See [the guide](../builder-quotas.md#native-package-cache-maintenance); these are fixture database responses, not live provider proof.

The pinned pnpm 10.32.1 uses `state/dependencies` and an explicitly configured `state/cache/pnpm`. A private record precedes the first command and binds both directory identities; populated unrecorded roots cannot be adopted automatically. The default 2 GiB cleanup trigger also runs under capacity pressure, then actual disk admission applies. Ordinary pruning never receives `--force`. The validator refuses unknown deletion targets, mounts and links; the specific hashed project-registration links are left untouched because the global `links` directory is refused. Other npm/XDG caches and retained package-integrity metadata remain charged; D3 must finish their policy. All maintenance is covered by the existing native operation and outer lock. No new inventory roots or provider/schema changes were introduced by D1; D4 still audits complete coverage.

**D2 capacity checkpoint:** new `scripts/release-storage.mjs` and nine cases in `client/visual-builder/release-storage.spec.ts`; `kaizen-releases.mjs` shares the existing activation lock with staging, measures retained bytes before materialization, uses bounded source/metadata copies and checks missing immutable additions before any serving switch. New defaults are 8 GiB store / 4 GiB immutable / 2 GiB free space. Current production/staging actual-size observations fit these defaults and the existing 200 MiB / 32 MiB / 10,000-file client envelope now applied to native staging. `l6-t4-release-storage-combined1.log` passes 149 cases across eight files with real Nginx enabled; `l6-t4-release-storage-types2.log` has zero errors/warnings and 192 hints across 459 files. The native bundle and syntax check pass. All command handles are closed. No production/provider/service deployment changed.

**D2 next actions:** connect the verified retirement coordinator to configured publication and recovery callers, update release-history availability, reclaim eligible immutable/abandoned state and complete D2f's combined proof. Preserve the documented retention/grace defaults, queued rollback targets, serving/recovery data and all retained immutable references. Do not mark D2 complete from its earlier capacity or eligibility tests.

**Current D2a checkpoint:** [migration 019](../../supabase/migrations/202609150019_builder_release_retention.sql) and the [18-case retirement database suite](../../client/visual-builder/release-retention-database.spec.ts) are verified. The final six-file regression run passes 252 cases, full types pass, and the actual PostgreSQL fixture passes all 24 contention groups plus stale-isolation refusal. The fixture now waits for an explicit statement-completion marker before starting its competitor. Retirement locks precede reference lookups; published artifact identities are immutable, and changed terminal recovery ownership is checked. SQL receipt retries survive project/destination deletion. D2b eligibility is now verified below. Continue [D2c–f](l6-t4-release-retirement-tasks.md); the deletion coordinator and removal/restart proof remain unfinished. Final logs are `l6-t4-release-retention-combined3.log`, `l6-t4-release-retention-types3.log` and `l6-t4-release-retention-pg6.log` under the private state directory. All handles are closed; no production mutation occurred.

**D2b checkpoint:** `scripts/release-retention.mjs` joins the existing store lock and inspects existing canonical stores without adopting them. Defaults keep five releases, require 30 days since creation/file changes/last journal use, and protect seven days of visitor grace. Local eligibility and the independent database grace must both hold. The inspector verifies complete manifests, owned ordinary paths, Linux mount boundaries, serving bytes and scoped receipts; incomplete recovery and tied rollback histories remain protected. Every retained manifest contributes immutable references, including candidates. Unknown root files and staging remain charged and untouched. It does not claim or remove files. Final evidence is `l6-t4-release-eligibility-combined2.log` (167 cases, real Nginx), `l6-t4-release-eligibility-vps1.log` (actual same-device bind mount and foreign owner) and `l6-t4-release-eligibility-types4.log` (461 files, zero errors/warnings, 192 hints). D2c and native caller integration are now verified; continue D2d.2 for client/domain/retry coverage. See [the detailed guide](../builder-quotas.md#filesystem-release-retention-eligibility).

**D2c records/session checkpoint:** [durable state](../../scripts/release-retirement-state.mjs) stores an immutable project/scope/worker/origin/store/release-directory binding, one bounded attempt with a stable token and file identities, and permanent retired-ID fences. Synced exclusive publication recovers only its exact temporary hard-link pair; damaged/foreign files stay untouched. Direct staging now refuses pending/retired IDs. The inspector offers `withReleaseRetentionStore` for capture and rechecks within one lock lifetime and refuses a mismatched persistent binding. Ten new [record tests](../../client/visual-builder/release-retirement-state.spec.ts) and two session cases contribute to the 179-case combined result. Actual metadata mount/owner refusal passes in the disposable VPS fixture. These are record/capture tests; no coordinator removal or killed-service retirement proof is claimed. See [the current boundary](../builder-quotas.md#durable-release-retirement-records).

**Latest D2c coordinator checkpoint:** [retireRelease](../../scripts/builder-release-retention.ts) uses the real native-operation protection object and systemd controller, [recorded-file removal](../../scripts/release-retirement-files.mjs), durable state and the existing store lock. SQL cancellation advances an attempt generation so an abandoned request can never claim later. Recovery records the current executor before further work, protects selected/rollback/recovery artifacts without requiring the partial target's manifest, syncs removals and acknowledges only exact physical absence. Existing `reconcileRelease` clears a stopped activation lock after the caller proves the previous native service stopped. Final evidence is `l6-t4-retirement-coordinator-combined3.log` (453 cases / seventeen files / real Nginx), `l6-t4-retirement-coordinator-pg1.log` (26 real contention groups) and `l6-t4-retirement-coordinator-vps1.log` (two actual service kills and successful completion). Types and bundles pass. See [the coordinator guide](../builder-quotas.md#release-removal-and-restart-coordination). D2d.1 below now composes these pieces into the native worker; client/domain callers remain D2d.2. No production service changed.

**D2d.1 worker checkpoint:** [native task orchestration](../../scripts/builder-native-release.ts) and [native maintenance](../../scripts/builder-native-release-maintenance.ts) use the configured worker identity and recover native/checkout state before retirement. New work refuses deferred native recovery; the raw release CLI refuses the maintenance action. The root launcher accepts maintenance for only main/stage, without source/release/request metadata. [The service](../../deploy/systemd/kaizen-native-maintenance@.service) and [timer](../../deploy/systemd/kaizen-native-maintenance@.timer) retain the existing deployment unit and lock through that launcher. They are not installed. See [native maintenance](../builder-quotas.md#native-release-maintenance-caller). Current proof is `l6-t4-retirement-worker-combined1.log` (257 cases/15 files), `l6-t4-retirement-worker-launcher1.log` (six), `l6-t4-retirement-worker-types2.log` and `l6-t4-retirement-worker-vps1.log` (actual caller, two kills/recovery). Bundles and syntax pass. No SQL or production configuration changed.

### E — Add publication abuse controls and data-preserving suspension

**State: plan publication allowance and general function limits exist; this task's abuse workflow is unfinished.**

Start with [publish endpoint](../../supabase/functions/builder-publish/index.ts), [billing handler](../../supabase/functions/_shared/builderBilling.ts), [projects endpoint](../../supabase/functions/builder-projects/index.ts), [release worker](../../scripts/builder-release-worker.mjs), [hosted publishing](../../scripts/builder-hosted-publishing.ts), [client publisher](../../scripts/builder-client-publisher.ts), [shared project types](../../shared/builderProjects.ts), existing billing/access/release tests and migrations. Add focused abuse SQL/handler/UI/operator modules only where necessary; their final filenames are not chosen yet.

- [ ] Add publication burst/rate limits at the authoritative claim/queue boundary, including repository and client publishing. Preserve idempotent retries and the monthly billing allowance.
- [ ] Implement a bounded report intake and private operator review/takedown path with authorization, audit records and plain-English outcomes. Do not send external notifications without Sean's explicit authorization.
- [ ] Implement suspension that preserves websites, drafts, assets, billing/recovery records and access needed for support, while preventing new publication.
- [ ] Make queued jobs and late callbacks respect suspension. For takedown, stop serving through the verified release/domain routing path while retaining recoverable data.
- [ ] Document operator restore/unsuspend behavior and recheck current release/domain ownership when restoring service.

**Done when:** database/function and appropriate route/worker tests prove burst limits, access isolation, duplicate/replayed reports, queued-job suspension, takedown and recovery without data deletion. Browser proof covers the messages and available actions.

### F — Assemble the installation and complete T4 integration proof

**State: upload/native bundles and examples partly exist; actual combined installation and browser/provider proof remain.**

Files: [upload entry](../../scripts/builder-upload-host.ts), [upload bundle](../../scripts/build-upload-worker.mjs), [upload unit](../../deploy/systemd/kaizen-upload-worker.service), [upload configuration example](../../deploy/systemd/upload-worker.env.example), the new native-cleanup service from B, [native release bundle](../../scripts/build-native-release-worker.mjs), [fixed deployment launcher](../../scripts/ops/run_public_deployment.py), [deployment configuration example](../../deploy/systemd/native-deploy.json.example), [deployment workflow](../../.github/workflows/deploy.yml), browser fixtures under `tests/builder/`, and [quota guide](../builder-quotas.md).

- [ ] Inventory every real native producer and retained server root: helper-managed projects, production/staging checkouts, other retained checkouts that can build/deploy, all-account drafts, build recovery/output, release stores and candidate collections. Include native client projects, not just Kaizen.
- [ ] Produce one matching configuration fingerprint for database registration, helper, deployment/recovery workers and root cleanup. Preserve retained coverage during reconfiguration.
- [ ] Prepare the upload account, private spool, immutable bundles, units and Apache/DirectAdmin proxy route to loopback 4336. The root cleanup worker gets its own identity/state. Do not activate the modified deployment workflow before its installed launcher exists.
- [ ] Verify the complete flow on disposable native/provider/browser fixtures: upload/resume/cancel, plan refusal, copy/resume/cancel, registration/adoption, references and cleanup, concurrency/restart, publication limits and suspension. Preserve unrelated buckets and Sean's websites.
- [ ] Capture and inspect desktop and phone screenshots of changed product flows; link them from the implementation log.
- [ ] Run `pnpm typecheck`, the complete `pnpm test`, relevant actual Edge/native/operations tests and affected browser journeys. Fix failures. A skipped PostgreSQL test is not a pass.

**Done when:** the full T4 result is reviewable, installation order and rollback are documented, actual provider evidence is distinguished from fixtures, and no agent-doable T4 implementation remains. Commit the entire T4 once with its guide/log evidence and `[skip ci]`; retain milestone CI for G.

### G — Finish L6 configuration, milestone CI and rollout

**State: signup, billing and domains are built on this branch; coordinated deployment is outstanding.**

Files: [signup guide](../builder-signup.md), [billing guide](../builder-billing.md), [domain guide](../builder-domains.md), [quota guide](../builder-quotas.md), L6 migrations under `supabase/migrations/`, matching `builder-*` functions, service/timer examples under `deploy/systemd/`, [deployment workflow](../../.github/workflows/deploy.yml), [builder CI](../../.github/workflows/builder-checks.yml), and existing deployment/release tooling.

- [ ] Reconcile prepared provider configuration and the current server state. Wire remaining agent-accessible signup/Stripe/domain settings; keep real customer charging and human-only dashboard steps explicit.
- [ ] Run full L6 application/operations/browser checks and milestone CI on the completed revision. Use the existing tests and deployment tooling; do not start per-subtask CI.
- [ ] Apply the documented compatible order for migrations, functions, private configuration, services and frontend. Establish the recovery point and verify each deployed revision.
- [ ] Activate matching schedulers and upload/native cleanup only after the complete producer/root/service inventory is installed and proven. Check logs, auth, staging, public pages, publication/recovery and usage accounting.
- [ ] Perform real fixture-domain acceptance where current access allows; collect any remaining Cloudflare/provider sign-in at the end.

**Done when:** all agent-doable L6 setup and deployment are complete, live evidence and rollback are recorded, and each unresolved item identifies the exact human action rather than a generic blocker.

### H — Final goal audit and Sean's handover

**State: pending G.**

Files: [launch brief](codex-launch-brief.md), [launch task map](../builder-launch-plan.md), [HANDOVER](../../HANDOVER.md), relevant guides and [Claude's UX brief](claude-ux-brief.md).

- [ ] Audit every L0–L6 acceptance requirement against implementation and evidence. Fix remaining agent-doable gaps; do not use task counts or fixture passes as manual acceptance.
- [ ] Run the requested final goal-wide CI verification and record the exact revision.
- [ ] Consolidate remaining human-only items: provider/account/dashboard access, approved legal wording, paid PITR/offsite decisions and provider recovery acceptance, notification/privacy-address delivery, and Sean's hands-on gate use. Recheck which actually remain before presenting them.
- [ ] Update the final handover with deployed versions, operator recovery steps and a short product walkthrough. Keep the previously accepted beta gate accepted and the earlier account/email-to-publish behavior as its recorded follow-up, not a new acceptance hold.

**Done when:** nothing remains that the agent can perform and the final human checklist is concrete. Do not mark the full goal complete merely because code is committed or credits are low.

## Host and evidence references

- SSH alias: **`kaizen-vps`**. Supabase project: `kbqraygsegcclzhsmpvz`. Use existing private configuration; do not ask Sean to reissue keys as a routine continuation step.
- Existing server private configuration locations include `/etc/kaizen/client-worker.env` and `/etc/kaizen-helper/helper.env`. The local Management API token file is `/home/sean/.config/kaizen/supabase-access-token.txt`. These are file references, not values; read through private tooling without printing contents.
- Apache/DirectAdmin currently owns ports 80/443 and TLS/proxies. Nginx uses 8091 main, 8092 demo and 8093 stage; helper is 4334. Upload 4336 is proposed and uninstalled. Domain-host preparation uses 8094 and an inactive timer. Verify before changing services.
- Immutable runtime: `/opt/kaizen-runtime/node-v22.23.2-immutable/bin/node`; package manager: `/opt/kaizen-runtime/pnpm-10.32.1/bin/pnpm.cjs`.
- Observe remote Git as the actual account or with `GIT_OPTIONAL_LOCKS=0`; mutate helper checkouts as `kaizen-helper` and deployment checkouts as `kaizen-deploy`. Root `git status` must not refresh a user-owned index.
- Private disposable VPS drivers: `/home/sean/.local/state/kaizen/run-l6-controlled-fixture.py`, `run-l6-native-restart-fixture.py`, and `run-l6-native-deploy-fixture.py`. The deployment driver's `--restart` path performs the actual interrupted-checkout service test. Read a driver before running it; it should remove only its generated fixture state.
- Real PostgreSQL contention uses `tests/ops/test_asset_reference_concurrency.py` with `KAIZEN_PG_BIN` pointing to an installed non-root fixture runtime. Prior VPS location: `/opt/kaizen-backup-check/usr/lib/postgresql/17/bin`. Run against its disposable Unix-socket cluster, never the production database.
- Latest earlier checkout evidence: `l6-t4-checkout-recovery-integration4.log`, `l6-t4-checkout-recovery-types4.log`, and `l6-t4-checkout-recovery-restart-vps3.log` under `/home/sean/.local/state/kaizen/`. Check exact filenames there before rerunning; older run failures were corrected.
- Historical feature-branch push helper; **do not use it for the consolidated main branch**: `python3 /home/sean/.local/state/kaizen/push-launch-task.py l6-quotas d7435e64926a09f72da7eca7503cf0ab7fbbe2e2`. Inspect the helper and current branch before use; the main handover supersedes its earlier commit restriction.

## First commands for the incoming agent

```bash
cd /home/sean/Documents/GitHub/kaizenNEW
git status --short
git branch --show-current
git log -4 --oneline
```

After implementation is resumed, read the files above and continue at **D2d.2 — client/domain/retry and direct-call coverage** in [the D2 tasks](l6-t4-release-retirement-tasks.md). D2b now exists in `scripts/release-retention.mjs` with `client/visual-builder/release-retention.spec.ts` and an actual VPS namespace fixture. No production/provider configuration, migration, service installation or frontend deployment changed. The private file inventory `/home/sean/.local/state/kaizen/l6-t4-handoff-files.txt` includes modified and untracked paths; it is an index, not a backup. Retain the actual launch files.

## Keep the takeover point usable

After each verified continuation, update the immediate next step and checklist state. Record exact proof logs, partially edited files and any running sessions in the private checkpoint, then refresh the modified/untracked inventory. D2a–c and D2d.1 are verified; D2d.2–4 client/domain/retry integration, history availability and combined caller proof remain. Preserve the committed checkpoint and record remaining T4 completion at F. All command-session status belongs in the latest private checkpoint.
