# Retained website releases

The public deployment workflow now stages a complete build outside the live web root, records checksums and switches one Nginx include. It does not copy a candidate over the live files. The original static HTML, CMS content, generated redirects and build assets are retained for rollback. There is no automatic pruning.

`scripts/kaizen-releases.mjs` provides `stage`, `init`, `activate`, `rollback`, `list` and `verify-live`. It uses Node 22 or later and ordinary filesystem operations. Activation runs `nginx -t` and `nginx -s reload`, using `sudo -n` when the deploy user is not root. [Nginx documents its configuration validation and graceful reload behaviour here](https://nginx.org/en/docs/control.html).

## Builder browser origins

Each service has one server-side allowlist. Supply the helper setting in the environment of the process running `pnpm dev`; set the two function settings through Supabase's function secrets. These are public origin names, not credentials, but they control server access and must not use a `VITE_` prefix.

| Service                  | Setting                     | Default when absent                                                                         |
| ------------------------ | --------------------------- | ------------------------------------------------------------------------------------------- |
| Local helper             | `BUILDER_COMPANION_ORIGINS` | `https://kaizenweb.co.uk`                                                                   |
| Editor/Studio functions  | `ALLOWED_STUDIO_ORIGINS`    | `https://kaizenweb.co.uk,http://localhost:3333` (the existing editor configuration example) |
| Builder contact receiver | `BUILDER_CONTACT_ORIGINS`   | `https://kaizenweb.co.uk,https://www.kaizenweb.co.uk`                                       |

A configured comma-separated list **replaces** the default; an explicitly empty list allows no browser origins. Use complete HTTPS origins, without a path, query, fragment, credentials or wildcard. Plain HTTP is accepted only for `localhost`, `127.0.0.1` or `[::1]`. Root trailing slashes, host casing and standard ports are normalised; duplicates are removed. Invalid entries reject configuration instead of falling back, and errors never echo the entered values.

For example, `BUILDER_COMPANION_ORIGINS=https://builder.example pnpm dev` accepts pairing from that hosted origin after the normal explicit folder approval. It does not grant account access, approve a folder automatically or change loopback/Host checks. `BUILDER_COMPANION_TEST_ORIGIN` remains a test-only supplement that accepts only an exact HTTP loopback origin; a remote origin cannot enter through it.

`PUBLIC_SITE_ORIGIN`, `VITE_PUBLIC_SITE_ORIGIN`, Studio URLs and redirect/cookie settings no longer add allowed origins implicitly. When deploying this change, explicitly include every authorised production, staging and Studio origin in `ALLOWED_STUDIO_ORIGINS`. Those URL settings still select their original redirect destinations; they simply do not grant browser access. Restart the helper or redeploy the affected functions after changing these values. Origin checks complement authentication and project membership; requests without an Origin retain the existing authenticated server-request behaviour.

## Required one-time VPS setup

**Complete this before deploying the updated workflow.** The workflow fails before changing the live files if its release store is absent. The current production host configuration is recorded below; new hosts still need the setup in this section.

1. Choose a dedicated directory outside all public web roots and outside the Git checkout. Use different directories for production and staging, for example `/var/lib/kaizen/production` and `/var/lib/kaizen/staging`. The deployment user must own the directory; Nginx workers need read/traverse access. Do not expose the store itself through a server root or alias. Only the selected `site/` and shared `_astro/` assets are public.
2. From a reviewed checkout containing these scripts, capture the **currently served** static directory as the baseline. It must contain `index.html` and `builder/index.html`. Ensure its `redirects.generated.conf` matches the current generated redirect include; if the live include is stored elsewhere, copy that file into a separate baseline copy first. Leave the existing live directory intact.

   ```bash
   node scripts/kaizen-releases.mjs stage --source /existing/public/web-root --store /var/lib/kaizen/production --id baseline
   node scripts/kaizen-releases.mjs init --store /var/lib/kaizen/production --id baseline
   ```

3. In the intended Nginx server block, replace its existing `root` directive and generated-redirect include with:

   ```nginx
   include /var/lib/kaizen/production/active.conf;
   ```

   Keep the site's TLS, security headers, location routing, error pages and `index index.html` configuration. The managed include supplies `root`, the release's redirects, `location ^~ /_astro/` and the exact release-marker location. Remove duplicate definitions of those locations. Do not leave an old `root` override in the location serving builder pages. Nginx validation and the public response checks must pass before proceeding. The first setup switches to a retained copy of the baseline; it does not delete the old live directory.

   ```bash
   sudo nginx -t
   sudo nginx -s reload
   node scripts/kaizen-releases.mjs verify-live --store /var/lib/kaizen/production --id baseline --origin https://kaizenweb.co.uk
   ```

4. Add `VPS_RELEASES_DIR_PROD` and, if used, `VPS_RELEASES_DIR_STAGE` to the existing GitHub Actions secrets. Keep the existing VPS SSH, application-directory and public-domain settings. The old `VPS_WEB_ROOT_*` and generated-redirect include secrets are no longer used by the public deployment step. Grant the deployment user permission to run the two specific Nginx commands without an interactive password. Do not grant a broad privileged Node command.

Repeat the baseline setup for staging before enabling staging deployment. Existing separate Studio hosting remains optional. It receives the exact Studio files from the retained public release; that separate copy still uses its existing rsync mechanism. The public release, including `/builder/` and its bundled `/studio/`, uses the new activation mechanism.

## Deployment and verification

### L6 output usage configuration (implementation pending rollout)

Both staging and production workers now require a fixed `BUILDER_RELEASE_PROJECT_ID`, the existing private `BUILDER_RELEASE_SERVICE_ROLE_KEY` and `VITE_SUPABASE_URL`. Set the project to `kaizen` for this repository's two destinations. An ordinary repository deployment needs these settings even when `VITE_BUILDER_CLOUD` is off. The workflow's `KAIZEN_DEPLOY_BRANCH` selects staging or production; other values fail before a release is queued. Use the [private release environment example](../deploy/systemd/public-release.env.example), preserving the destination's existing build settings and production coordinator switch. Apply billing migrations through `202609150008_builder_billing_retention.sql` before switching this worker. Coordinate the matching helper/function rollout so native Publish can require the exact measured staging artifact and commit.

The worker measures source before building, checks it again before activation, and reserves the verified retained manifest's bytes and HTML page count. Failed activation releases its reservation only after the old website is verified. An uncertain live acknowledgement keeps the serving website and reservation for explicit recovery. These logical plan counters do not replace physical upload and artifact-retention controls.

After establishing that the old worker and competing deployments are stopped, an operator can reconcile output usage and retained files with `node scripts/builder-release-worker.mjs --reconcile` (the earlier `--reconcile-usage` flag is retained). Load the destination's private `BUILDER_RELEASE_ENV_FILE` and its existing store/domain/branch settings; set `KAIZEN_RELEASE_ID` to the currently selected artifact. To restore a specific retained artifact, also set `KAIZEN_RESTORE_RELEASE_ID` to that inspected artifact's ID. Run from the actual website source directory. The command acquires the existing recovery lock, checks retained bytes and served responses, and records usage only after verification. A pre-accounting artifact is measured from its verified manifest and the current source directory; a mismatch with an existing ledger is refused. No build or Git push occurs.

With `VITE_BUILDER_CLOUD=1`, also set `KAIZEN_BUILDER_REQUEST_ID` to the inspected original-site publication UUID. Both recovery flags then use the combined coordinator path: it checks the exact current worker, baseline and intended artifact under the filesystem recovery lock, takes new database ownership, and fences callbacks from the old worker. With no previous database head, the retained activation journal must identify exactly one baseline artifact. An already committed publication can be reconciled in place; changing it requires a new reviewed rollback. After actual HTTP verification, publication promotion/refund and output settlement commit together. Uncertain responses retain the operation for an explicit retry of the same publication. Do not clear a queue entry or substitute an unrelated artifact. See [coordinated publication](#coordinated-builder-publication) and [billing](builder-billing.md).

### Current DirectAdmin production host

Production uses `144.91.72.17` with the dedicated `kaizen-deploy` SSH user. The application checkout is `/srv/kaizen/production` and the retained store is `/var/lib/kaizen/production`. GitHub Actions holds a separate deployment key, the verified SSH host fingerprint, and these paths. `VPS_NODE_BIN=/opt/kaizen-runtime/node/bin` selects the dedicated Node 22 runtime without changing the host's system Node installation. Runtime shims are installed by the operator; deployment runs without permission to modify that runtime.

Apache/DirectAdmin continues to own ports 80/443, TLS, the `/editor-api/` proxy and `/cms/` routing. The main Kaizen website is proxied to `kaizen-nginx` on `127.0.0.1:8091`, client-demo uses 8092, and staging uses 8093. Separate branches in the domain's custom HTTPD template retain these routes across DirectAdmin regeneration. The Nginx binary was extracted from Ubuntu's signed package repository into `/opt/kaizen-runtime/nginx`; the operator must update that isolated package when Ubuntu publishes security updates. The deployment user can validate and gracefully reload this Nginx service through two specific sudo commands.

The original Apache web root remains intact. Configuration backups and the original homepage comparison are in `/etc/kaizen-backups/20260911-deployment`. `legacy-baseline-20260911` retains the old static site, with an explicit temporary builder placeholder because the legacy site had no builder entry. It provides the initial verified rollback artifact.

Cloudflare can inject a changing browser-check script into HTML and prepend its [managed policy to robots.txt](https://developers.cloudflare.com/bots/additional-configurations/managed-robots-txt/). Consequently the VPS resolves `kaizenweb.co.uk` and `stage.kaizenweb.co.uk` to its own address in `/etc/hosts`: the release engine validates full response hashes over the HTTPS **origin**, with certificate verification. This does not verify CDN-transformed responses. Verify the public Cloudflare release marker and browser routes independently after deployment; do not disable Cloudflare protection or treat origin verification alone as CDN verification.

The workflow forwards public Sanity and Supabase build settings, including the Studio dataset. The release worker reads the private Sanity token from `/etc/kaizen/production.env` or the separate `/etc/kaizen/staging.env`; GitHub's old Sanity token is no longer forwarded. Server-only cloud publication credentials remain a separate operator setup. A normal code deployment does not enable the cloud builder coordinator automatically.

The current verified frontend revision is `16a1cc4`: production release `gh-34787575626-1` and staging release `gh-34787575689-1` each passed all 259 HTTPS origin response checks. Production's database head matches its artifact with no pending release, and the separate Studio copy succeeded. The permanent helper runs `/opt/kaizen-helper-releases/28b1e64`, verified to have the same helper source as the final revision. Its managed staging checkout is at `16a1cc4` with Sean's About-page edit, saved draft, source recovery and private build settings preserved. The old transient/bootstrap helpers are stopped. The production browser successfully built and displayed the real About page, and Sean provisionally accepted the Save attempt with its account-details notice; that does not assert a completed publication. The live Account page now loads the current email and missing-name notice, so Sean can enter his own name there.

Studio's browser environment must be restricted to `VITE_`, `PUBLIC_` and `SANITY_STUDIO_` settings. Never broaden that list to `SANITY_`: `SANITY_API_TOKEN` and `SANITY_AUTH_TOKEN` are server credentials. CI builds the actual Studio with synthetic server credentials and scans every emitted file using `pnpm test:studio:env`; the public project configuration must remain and every private canary must be absent. A credential found in a public artifact requires revocation at its provider as well as a corrected build. Replacing a release cannot invalidate downloaded or CDN-cached copies.

The 13 September Studio environment mitigation retains the existing production source revision and frozen publication snapshot. Production `kaizen-studio-safe-20260913` uses the deployed worker's compatible version-1 manifest with an additional full-file HTTPS verification; staging `kaizen-staging-studio-safe-20260913` uses version 3. Both passed 259 origin checks. The separate Studio host was inspected and did not contain the affected server settings; it was left unchanged. This mitigation does not deploy the launch frontend or constitute hosted-builder acceptance. The operator must also persist the origin host mappings through cloud-init regeneration when completing L1-T7.

Manual workflow runs support `preflight_only` to validate access, the release store, Node and Nginx without a build or activation. Staging uses repository variables `KAIZEN_STAGE_APP_DIR`, `KAIZEN_STAGE_RELEASE_STORE` and `KAIZEN_STAGE_DOMAIN`, defaulting to the verified `/srv/kaizen/staging` checkout, `/var/lib/kaizen/staging` store and `stage.kaizenweb.co.uk`. The legacy `VPS_*_STAGE` secrets are not used: their prior checkout path rejected deployment access. Its preflight requires the protected production checkout, store and hostname, and refuses equal, nested or symlink-overlapping paths and equivalent production hostnames. `tests/deploy/preflight.sh` covers those refusals alongside missing configuration, unsupported Node, rejected Nginx configuration and a separate valid destination; both deployment and builder CI run it. A separate staging Studio copy requires the explicit repository variable `KAIZEN_STAGE_STUDIO_ENABLED=true` and reviewed staging Studio settings. The release still includes `/studio/` without that optional copy.

The workflow resolves main/stage once, checks out that branch, records its exact commit and uses the same commit on the VPS. A repository dispatch targeting staging cannot also deploy production because the workflow happens to run on the default branch. Root and Studio dependencies are installed before the complete build. A process-held `flock` serialises use of the VPS checkout through staging and activation.

Each release lives under `releases/<id>/` with:

- `site/`: complete static output, including a public `.well-known/kaizen-release.json` marker containing only release ID, creation time and source commit.
- `redirects.conf`: the exact generated redirect rules, outside the served site.
- `release.json`: the file list, SHA-256 checksums, redirect checksum and expected public responses, outside the served site.

Staging rejects symlinks, file traversal, missing entry pages and local workspace/server files. Apache/PHP `.htaccess` and `.user.ini` files stay in the source output and are excluded from the Nginx public artifact. Activation rechecks every retained file, verifies the currently served baseline, installs immutable hashed build assets, writes `active.conf` atomically, validates Nginx and reloads it. New native manifests use version 3 and verify the exact marker and every public file, including existing native pages, styles, scripts, images and Studio assets. Client manifests retain version 2 with full-file checks; retained native version 1 releases keep their original page checks for rollback compatibility. Verification bypasses normal caches, closes connections and allows bounded observation of graceful reloads. A marker alone is insufficient to report success. New artifacts record `responseIdentity: release-id-v1` in the manifest and hashed marker. Their Nginx configuration emits `X-Kaizen-Release` on each response; every file and redirect check must have the expected ID as well as the expected bytes/status. This prevents responses from different graceful-reload workers from being combined into a successful verification. The complete baseline is observed with the same bounded retry before activation mutates anything. Older artifacts and the exact earlier configuration format remain compatible; retained files are not rewritten.

Shared `_astro/` files remain available to visitors with an earlier page open. Existing names must have identical bytes; a content collision fails activation. Assets are added through complete temporary copies before selection. Retained release files are not modified when switching or rolling back.

Transactions under `transactions/` record checking, activating, verifying and live states. A failed candidate restores the previous include, validates and reloads Nginx, and verifies the previous public responses. Only then does it record `rolled_back`. If the include was changed by an administrator, or recovery cannot be verified, it records `recovery_required` and reports the exact transaction; it does not claim the old site was restored. A candidate can be briefly served during its final HTTP verification; failure recovery returns serving to the prior release.

The generated redirect rules support exact internal paths with letters, numbers, slashes, dots, hyphens and underscores, with 301/302 responses. Invalid paths, configuration injection, duplicate sources and cycles fail the build. External redirects, query strings and regex patterns require deliberate additional support rather than raw Nginx interpolation.

## Hosted repository transport

The L1 client calls `POST /editor-api/builder-repository` on the builder's own origin. Each JSON request preserves its existing `repository-*` action fields and adds the selected `projectId`. The bearer token comes from the current Supabase session, is sent only to this fixed first-party endpoint, and is never forwarded across redirects. Neither the query string nor a local folder preference supplies an endpoint. Explicit developer tabs use `helper=local` and the existing approved companion window instead; transport selection is fixed for the document's lifetime.

The initial `repository-connect` action must verify the session, current project membership and configured working copy before returning `{ projectId, root, expiresAt }`. `root` identifies that project's folder and must stay the same within the connection; `expiresAt` is an absolute Unix timestamp in milliseconds. The client bounds the lease by the JWT expiry and rejects a mismatched project, expired lease or changed folder. Other actions return the existing helper result shape, or a non-2xx JSON `{ error }`. Every call still needs a server-side membership/path/plan check; browser checks are not an authorization boundary.

401/403, unavailable-service responses and connection loss end the client's connection while preserving recovery. Conflicts keep their original message. A timeout or account change can leave an already accepted write's outcome unknown; the client does not resend it automatically. Token refresh and return from the browser's page cache establish a fresh connection. Failed requests use the safe operator diagnostics path.

The endpoint is served by the loopback hosted-helper service. Add its exact proxy route ahead of the existing `/editor-api/` Supabase proxy when rolling out L1, together with the preview route below. Build jobs, protected HTTPS previews and explicit Save to website are implemented. Kaizen provisioning and full operations follow in L1-T6–T7; do not deploy the intermediate client default by itself.

Hosted preview URLs must be on the builder's origin under `/editor-preview/<project>/…`. The client refuses another project, outside origin, credentials or escaped path separators and uses an opaque `allow-scripts` sandbox, including previews opened in a window. The protected delivery below supports actual Astro/React snapshots in that sandbox. Earlier transport-only fixtures retain their HTTP adapter; the dedicated `hosted-preview.spec.ts` scenario exercises the actual hosted service through a real TLS proxy.

### Hosted working-copy service

`pnpm builder:hosted-helper` starts `scripts/builder-hosted-helper.ts` on Linux, listening only on `127.0.0.1` (default port 4334). Run it as a dedicated service account with a private home and no unrelated Git configuration or credentials. The service currently handles connection, repository/source inspection, account-specific source drafts, source/export review and apply, Git status, explicit fetch, reviewed build jobs with status/cancellation, protected general/selection/canvas previews, private folder-backup review/download and configured Save to website. The separate `repository-save` action commits and pushes after the editor clicks **Save to website**. Legacy `repository-commit` and opening/restoring builder archives remain unavailable through this service; they never fall through to the local helper. Kaizen provisioning and operations are still required for rollout.

Set these in the service environment, without `VITE_` prefixes:

| Setting                                | Purpose                                                                                                                                                                                                                    |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BUILDER_HOSTED_PROJECTS_FILE`         | Absolute path to operator-owned repository configuration; use [the example](hosted-repositories.example.json).                                                                                                             |
| `BUILDER_HOSTED_WORK_DIRECTORY`        | Dedicated private directory for working copies, drafts and locks, outside the web root and developer checkouts.                                                                                                            |
| `BUILDER_HOSTED_CREDENTIALS_DIRECTORY` | Separate private directory for per-project SSH files; must not overlap the work directory.                                                                                                                                 |
| `BUILDER_HOSTED_SUPABASE_URL`          | HTTPS Supabase project origin.                                                                                                                                                                                             |
| `BUILDER_HOSTED_SUPABASE_ANON_KEY`     | API key used with the caller's verified access token. A service-role key is not required.                                                                                                                                  |
| `BUILDER_HOSTED_BILLING_KEY` | Required private 32-byte random key encoded as 64 lowercase hex characters, shared only with the `builder-billing` function. Signs repository publication observations bound to the caller and durable attempt. Coordinate its configuration with the L6 helper/function rollout; hosted builds never receive it. |
| `BUILDER_HOSTED_PORT`                  | Optional loopback port.                                                                                                                                                                                                    |
| `BUILDER_HOSTED_EDITOR_ORIGIN`         | Required canonical HTTPS editor origin, with no trailing slash or path. It must also be in `ALLOWED_STUDIO_ORIGINS`.                                                                                                       |
| `BUILDER_HOSTED_GITHUB_READ_TOKEN`     | Optional server-only GitHub token with Actions read access to the configured private repository, used only to observe staging and production deployment runs. Never put it in client configuration or a build environment. |
| `ALLOWED_STUDIO_ORIGINS`               | Explicit allowed editor origins, with the parser/defaults described above.                                                                                                                                                 |
| `BUILDER_HOSTED_PROJECT_MAX_BYTES`     | Project storage bound; defaults to 8589934592 bytes (8 GiB), including the checkout, Git data, dependencies, drafts, retained setups and build data. |
| `BUILDER_HOSTED_MIN_FREE_BYTES`        | Free-space reserve on the work filesystem; defaults to 2147483648 bytes (2 GiB). |
| `BUILDER_HOSTED_DISK_CHECK_MS`         | Interval between serial growth checks; defaults to 2000 milliseconds. |

The repository file has `version: 1` and a `projects` array of `{ projectId, repositoryUrl, branch, saveToWebsite?, publishToWebsite? }`, or explicit empty setup admissions described below. It supports explicit `git@hostname:owner/repository.git` SSH addresses and ordinary branch names. Local/file/HTTP URLs, URL credentials, executable protocol extensions and Git-option inputs are refused. Owners can change the repository and branch through the dedicated settings actions; ordinary editing, save and publication requests cannot choose them. Credential paths and deployment approvals remain operator-controlled.

For each configured ID, provision `<credentials>/<projectId>/deploy-key` and `<credentials>/<projectId>/known_hosts` under the service account. Directories must be mode 0700 and these files mode 0600, owned by that account, without symlinks or hard-linked credential files. Put only the public half of that project's deploy key into the Git host. Pin the Git host's independently verified SSH host key in `known_hosts`; a scan alone does not verify its identity. Clone/fetch/push uses this one identity, strict host checking, no SSH agent, no interactive prompts, no global Git/SSH configuration and no inherited service secrets. Raw Git/SSH errors are not returned to the browser.

Each project receives `<work>/projects/<projectId>/checkout`. The first authorized action clones only the configured branch into a temporary folder and moves the completed clone into place. A private repository marker preserves its project ID, repository address and branch. Later calls verify the real folder, `.git` directory, origin and branch, and reject linked/shared Git stores, Git URL rewrites/includes or a changed identity. They do not reset, replace or silently reclone an existing working copy. Repository/branch changes use the explicit owner setup transition below, preserving the old folder and its drafts. Deployment display/tracking settings do not change that identity. `repository-fetch` updates remote-tracking refs only. It does not pull, merge, push or overwrite pending source edits.

Every request first verifies its bearer token with Supabase Auth. It calls the existing `builder_project_access` database function as that user with `capability: "edit"`, including a second check after waiting for the project lock. Archived projects and revoked/non-members are refused before repository work begins. The old editor cookie by itself grants no repository access. There is no membership cache. A request already authorized and executing can finish after membership is revoked; later calls must pass a fresh check.

One operation per project runs at a time, with a bounded in-process queue and an exclusive file lock shared by service processes. Different projects can proceed independently. A stopped process's lock is deliberately not stolen automatically: establish that its operation has stopped, preserve its working copy, and remove only that specific lock before retrying, following the operations procedure below. Review IDs are limited to their verified account/project, bounded and expiring, and consumed before an apply attempt. Account drafts live outside the checkout in that project's private data; another member cannot use their review IDs or backup downloads. Concurrent source changes retain the existing conflict/recovery protections.

The service bounds HTTP concurrency, project queues, upload size and upload/auth/Git waits. Repository POSTs reject unsupported content encodings, wrong origins, cookie-only calls, unknown actions and cross-folder/nested-root inputs. Preview reads and session revocation use the separate cookie rules below. Existing repository modules retain source path and symlink checks. Client-visible failures use the L0 safe reporting path; server startup errors contain a fixed configuration message. Build isolation for client code remains the L5-T4 decision. Only operator-approved Kaizen code belongs in this beta service.

### Hosted storage bounds

The T7 implementation uses GNU `du` on Linux to count apparent project bytes, without following symlinks or crossing a filesystem boundary. Sparse files count at their logical size. Node's filesystem statistics check the available-space reserve. Linked/replaced project roots, invalid limits and failed measurements stop new growth with a fixed operator message; no command output or private paths are returned to the editor.

Clones and Git fetches check before, during and after their work. An oversized initial clone is removed from its isolated temporary directory before it can become the working checkout. Apply reserves space for its verified recovery copies and replacement files before recording an intent or changing source. Save reserves space for Git objects before staging or recording commit intent, so a capacity refusal leaves the existing applied save retryable. Draft writes reserve their serialized size. Hosted Git disables automatic maintenance so it cannot launch an unobserved background cleanup.

A hosted build checks before moving existing `dist/`, monitors project growth while its subprocess runs, and checks again before accepting the output. Exceeding the project bound or free-space reserve stops the build process group; the normal runner restores previous output and retains the failed output for recovery. Samples are serial, and completion waits for an outstanding check. Growth between samples is possible: these are operational bounds for trusted beta code, not hard filesystem quotas or the later client-build isolation. Static snapshots keep the existing 200 MiB and 10,000-entry bound, with at most three live previews per runner.

Before a new build's capacity check, retention examines only completed outputs recorded by this helper. It keeps the two most recent completed recovery directories, allowing the new build to add a third. Cleanup is restricted to the recorded UUID under `checkout/.kaizen/build-recovery/`, containing only generated `previous-dist` and `failed-dist` directories. Unknown directories, source recovery, previous setups, links, unexpected files and other filesystem devices are preserved. A bounded scan refuses oversized trees; a maximum of 24 receipts requires operator reconciliation if preserved records accumulate. Retention records its intent before removal and can resume that cleanup after a restart without replaying a build.

These controls are installed in the permanent T7 helper. A 13 September check using that runtime measured 1,803,579,379 project bytes, within the default bound, and rejected a deliberately smaller read-only capacity threshold. This check wrote no test data to the website folder; growth cancellation and source/output recovery are covered by the temporary-repository tests.

### Save to website

Enable saving for a project with `saveToWebsite: { "environment": "staging", "url": "https://stage.example", "workflow": "deploy.yml" }` in the private repository configuration. The URL must be an exact HTTPS origin without a trailing slash, credentials or path. `workflow` is optional and must name a `.yml` or `.yaml` workflow file in the configured GitHub repository. Before enabling this capability, the operator must verify that pushing the configured branch deploys only staging, that the destination URL identifies that staging site, and that the named workflow actually performs its deployment. The service cannot infer those properties from a branch name. Give the project's deploy key write access to that repository and retain branch protections compatible with the intended staging push. Projects without this configuration do not advertise the Save button; the save API returns a setup error.

After **Review my changes → Apply changes to the folder**, the editor offers **Save to website**. Developer mode also shows a change summary and a separate **Save details** disclosure. Source and generated-export applies use the same existing commit module. Only the applied files enter the commit, with the name (`full_name`, then `name`) and email returned by verified Supabase Auth. Missing/invalid account details require correction before saving; request-supplied authorship is ignored. Unrelated staged files, unfinished merges/rebases, changed applied bytes or a moved local/remote branch refuse the save with an explanation. Unstaged unrelated work stays outside the commit. A preview build does not push. The page canvas disables Save during a build, and the server independently blocks saves from any screen while the build lock is held.

Saving holds the project lock, checks current remote HEAD against the base recorded at apply, creates the commit, and rechecks sign-in and membership before pushing. The push addresses the configured SSH repository and one exact branch ref. It uses an explicit `--force-with-lease=<ref>:<base>` as a compare-and-swap **only after independently proving that the actual commit object has exactly one parent equal to that base and is still local HEAD**. Replacement refs and ancestry grafts cannot disguise this proof. This combination permits one new commit without replacing remote history and rejects both remote advances and rewinds between checking and pushing. The lease never depends on a mutable tracking ref. Git's [explicit expected-value lease semantics](https://git-scm.com/docs/git-push) define the remote check. Hooks, signing, automatic tag pushes and submodule pushes are disabled for these service commands, which receive the existing restricted Git/SSH environment.

An interrupted or rejected push keeps the commit and files. **Check saved state** only reads the remote; it can resolve a lost acknowledgement when the exact commit is present and never pushes. **Retry saving to website** explicitly retries that same commit. The deployed helper restores this account's applied/saved receipt from private records after restart. An unsent commit or unresolved operation blocks another apply, preserving its recovery path; a completed later apply supersedes an earlier uncommitted apply as in the existing local workflow. Completed receipt history is bounded.

A confirmed push shows **Open staging** and observes the optional GitHub Actions workflow by the exact repository, workflow, commit, branch and `push` event. It reports waiting, deploying, success, failure or unavailable status, with a validated link to the run. Queries follow GitHub's [workflow-run API](https://docs.github.com/en/rest/actions/workflow-runs?apiVersion=2022-11-28#list-workflow-runs-for-a-workflow); responses are cached briefly and private repositories need the read token above. Missing tracking, API failures or a failed deployment do not turn a successful Git save into a failed push. Workflow success is evidence of that workflow's result, not proof that a browser is serving the intended revision. Open staging and check the page before **Publish** through Releases. Save never requests publication.

### Observing website status

The existing authenticated repository endpoint accepts the read-only `repository-website-status` action. It uses the current project lock, edit membership and configured folder; request data cannot choose a destination. It returns the checkout identity, Saved/On staging/Live state, observation time and only the requesting account's nonempty source-draft routes. No draft content or other account's draft metadata is returned.

The helper checks Git before and after observing the configured staging/production workflows and their `/.well-known/kaizen-release.json` markers. A changed checkout, unfinished Git operation, dirty folder, failed workflow, different marker or unavailable observation cannot produce a destination badge. Production takes precedence when both destinations report the same version. Marker reads send no GitHub authorization header, refuse redirects, bound the response size and reuse the publication marker validator. Workflow and marker observations have separate bounded caches with short expiry.

Save receipts add an observation-only `delivery` field (`waiting`, `reported`, `unavailable`) to their response after a successful workflow. It is not persisted into recovery receipts. Clients keep polling while a saved release is observable, including after success, so a later replacement can remove the destination badge. The Pages/editor whole-folder observation refreshes once per open view, discards results from an earlier connection or edit generation, and does not issue one Git/network request per page. These observations do not commit, push, publish or alter recovery records.

Deploy the matching helper and frontend together at the L3 milestone. Older helpers without the action leave Pages/editor conservatively Saved; receipt destination labels also require the new delivery field. Developer diagnostics remain available alongside the shared labels. See [saved and published states](visual-builder.md#saved-and-published-states) for the user-facing interpretation and limits.

**L4 runtime rollout:** starter creation requires the matching `builder-projects` function and shared starter module. Install the matching helper source and client publication worker alongside the frontend, so preview resolution and exported illustration licence records use the same implementation. The deployed L4 revision is `d2d9e99`, with projects function v10, `/opt/kaizen-helper-releases/d2d9e99` and `/opt/kaizen-builder-releases/d2d9e99`. Keep worker source and frozen dependencies root-owned; its dedicated `node_modules/.vite-client-worker` cache may be owned by `kaizen-builder`. Before switching the worker link, pause its timer and allow an active invocation to finish; retain the previous runtime, run the normal service once, check its result and restore the timer. Preserve configured destinations and release stores. This rollout requires no database migration.

### Publish the staged website

Native-source publication is a separate, explicit action in **Releases → Publish website changes**. Enable it only after verifying both destinations and their deployment workflows. Add `publishToWebsite: { "environment": "production", "branch": "main", "url": "https://www.example", "workflow": "deploy.yml" }` alongside the approved staging configuration. The production branch and exact HTTPS origin must differ from staging; the same SSH repository and private deploy key are used. The operator must verify that a push to that production branch deploys the intended live website, respects its branch protections, and verifies served output. Do not enable this approval on the live Kaizen helper until staging and production are wired and checked. The example is a configuration shape, not evidence that either destination is deployed.

1. Save changes to staging, open staging and check the page.
2. Choose **Review staged website** in Releases. This freezes the saved staging commit, the current production branch base, the changed-file list and staging release identity. Technical details remain in **Publication details**.
3. Choose **Publish reviewed changes** to advance the approved production branch to that exact staging commit. This creates no extra commit and leaves the website folder on its staging branch. The existing production workflow then builds and verifies that source revision.

The three `repository-publish-review`, `repository-publish` and `repository-publish-status` actions require current edit **and publish** permission before disk access and again after the project lock. Ownership alone does not grant publication. Immediately before starting the push, the helper re-verifies Auth and publish permission after resolving Git history and credentials. Reviews belong to one publisher and project, expire within the current session or 15 minutes, and cannot be selected through request-supplied commits, branches or destinations.

Review and publication refuse unsaved/untracked Git work, unfinished Git operations, active/queued builds, outstanding unsent saves, changed staging or production branches and unavailable staging deployment identity. The helper fetches production objects without changing the checkout and walks the actual commit parent headers to establish that production's reviewed base is an ancestor of staging. Replacement refs and ancestry grafts cannot disguise divergent history. The walk is bounded at 512 commits, with at most 500 changed paths in a review; larger histories need operator reconciliation. An explicit expected-value lease on the production ref rejects both advances and rewinds between checking and pushing. It only permits the already-proven fast-forward from the frozen production base.

Before review and again before publishing, the helper reads `/.well-known/kaizen-release.json` from the approved staging HTTPS origin. The second read must match both the commit and release ID reviewed. These reads carry no editor or GitHub credentials, reject redirects and bound the response size. **A public release marker identifies the reported revision; it does not attest every served byte.** The configured deploy workflow must retain its served-output verification, and Sean's real-page check remains part of acceptance. The Releases card separately reports the Git push, the exact production workflow's status and the live website's reported revision. It never labels a successful push or matching marker as complete release verification.

A rejected push keeps the reviewed staging revision. **Check publication state** observes the remote: it resolves an unknown acknowledgement when the exact commit reached production, allows an explicit retry when production still has the reviewed base, and refuses further publication when a different production revision makes the outcome ambiguous. It updates its private receipt but never pushes again automatically. An unresolved outcome blocks repository retargeting and new publication reviews. L1-T7 persists reviews and records uncertainty before starting a push, so a restart can observe the same operation. Builder-owned content continues through its existing release queue separately.

### Owner repository setup

In hosted projects, owners have a **Website repository** card in Settings. Editors do not see it. The four `repository-settings-read`, `repository-settings-save`, `repository-settings-key` and `repository-settings-connect` actions require both current edit access (including an unarchived project) and current ownership, before accessing the filesystem and again after acquiring the project lock. These explicit setup actions work before the website folder connects; other repository actions still require a connection. Account changes discard the previous account's setup screen and pending responses.

The operator first admits the project in `BUILDER_HOSTED_PROJECTS_FILE`. A complete repository entry supplies its initial SSH address, branch and optional staging approval. `{ "projectId": "<existing-project-id>", "setup": true }` admits an empty setup; it does not create a project, grant membership or enable staging. Before an owner connects, create the project's private credentials directory and a mode-0600 `known_hosts` file with independently verified Git-host keys. Directories are mode 0700; files and directories belong to the helper account. Only those pinned hosts are accepted. See [GitHub's published SSH fingerprints and host keys](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/githubs-ssh-key-fingerprints). Do not trust an unverified key scan.

1. Enter the repository's SSH address and intended website branch, then **Save repository settings**. Only these two values are accepted; private keys, local paths and deployment approval cannot be supplied by the browser.
2. Choose **Create deploy key**. The helper creates an Ed25519 key on the server. Copy the public half and add it to that repository's deploy keys. **Save to website** requires the Git host's write-access option; [GitHub documents deploy-key setup here](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/managing-deploy-keys).
3. The public key appears only in the creation response and remains on this screen until dismissed, refreshed or left. Later reads return its fingerprint, never either key half. **Copy public key** and **I've added this key** support that one-time handoff. An unconnected setup key can be explicitly replaced if its public half was lost; remove the obsolete key from the Git host after the replacement works. Connected-key rotation requires the operator.
4. Choose **Check website folder**. The helper uses the key to clone the configured branch and verifies remote access; checking an existing folder also rechecks the remote. A rejected key leaves the setup available to correct. Dependencies still require operator provisioning before preview builds.

Settings carry a version so an outdated tab cannot replace a newer setup. Changing repository or branch refuses pending Git work, a different remote HEAD, active/queued builds, unsaved apply/commit receipts and an unknown production push outcome. A successful change invalidates old plans, previews and completed save/publication receipts, retains the old checkout and its identity record under `<work>/projects/<project>/previous-setups/`, and requires a fresh connection. A branch change keeps the repository's key. A repository-address change requires a new key; the old private key is retained only under `<credentials>/<project>/previous-keys/`, outside the working-copy area. Retention limits and connected-key rotation procedures belong to the operations task.

Staging and production approval remain operator-controlled. An owner's change of address or branch removes **Save to website** and native-source publication unless the values exactly match the operator-approved entry. The owner cannot point at a production branch while retaining a staging label or production approval. Restoring that exact approved configuration restores its approval after reconnection. Settings are atomically replaced and survive a helper restart. A failed/interrupted key or folder transition leaves `repository-setup-transition.json`; subsequent requests fail closed. Preserve that marker and all current/retained files for operator inspection. Do not delete it to retry, replay a mutation, or restore a folder/key without reconciling its settings and Git state.

**Kaizen wiring checkpoint, 13 September:** Linux access uses `ssh kaizen-vps`. The dedicated `kaizen-helper` account has private work space at `/var/lib/kaizen-helper/work` and separate credentials at `/etc/kaizen-helper/credentials`. Sean registered its setup key; a fresh SSH `git ls-remote` with that identity and the independently pinned GitHub host key returns `main` at `aee20d6`. A write/push and creation of `stage` remain unverified. `/srv/kaizen/staging` is a clean deployment checkout at `aee20d6`, pending the stage branch. Staging is now connected through Apache TLS to Nginx port 8093 and `/var/lib/kaizen/staging/active.conf`. Release `kaizen-staging-verified2-9aca128` contains 259 files copied from production, excluding Apache/PHP control files; its source commit is explicitly `9aca128010aadd03aa9f45a4a88c56b74a87ea36`. Activation and a separate HTTPS origin verification pass all 259 responses. A workstation check through public Cloudflare matches 258 of 259 file hashes; only its managed `/robots.txt` differs. This is the existing application baseline, not the new hosted frontend. Production remains `gh-34712289074-1`, and client-demo remains unchanged. Configuration backups are under `/root/kaizen-launch-backups`.

**Native publication checkpoint:** the existing `builder-publish` function queues builder-owned content and dispatches a build of its configured branch; it did not promote native source changes from `stage` into `main`. The explicit reviewed publication above now fills that code gap and passes a browser journey using real temporary Git repositories, TLS and Astro builds. Auth, public release markers and GitHub workflow observations remain fixtures. Live stage-to-main deployment and Sean's Kaizen acceptance are still pending; neither a staging save nor the fixture result establishes them.

The VPS staging preflight passes as `kaizen-deploy` with `KAIZEN_NODE_BIN=/opt/kaizen-runtime/node/bin`, `KAIZEN_DEPLOY_BRANCH=stage`, `KAIZEN_APP_DIR=/srv/kaizen/staging`, `KAIZEN_RELEASE_STORE=/var/lib/kaizen/staging`, `KAIZEN_PUBLIC_DOMAIN=stage.kaizenweb.co.uk`, and the three production-protection settings matching the production checkout/store/domain above. It validates Node 22.23.2, path separation, the initialized store and current Nginx syntax. Public Cloudflare verification initially refused its managed robots.txt content and restored the baseline. The verified activation uses the same explicit origin mapping as production; CDN response checks remain distinct. Keep the production database release coordinator separate from an independent staging destination.

### Hosted build jobs

`repository-build-review` returns the existing command/scripts and source fingerprint. `repository-build-start` accepts that account's unexpired review once and returns a project/account-bound UUID with `status: "queued"` and `queuedAt`. Poll `repository-build-status` using that ID for queue position, `building` with `startedAt`, and terminal `succeeded`, `failed` or `cancelled` with `finishedAt`. The log contains at most the most recent 100,000 characters and is available during execution. The editor polls active jobs, retains their ID for reopening, and offers **Cancel build**. Reopening a known failed or cancelled build preserves its result; **Try building again** or **Build again** explicitly starts a fresh command review.

The queue follows the publication jobs' project scope, queued/building phases, exclusive claim, bounded log and explicit failure conventions. It reuses `RepositoryRunner` for command/source validation, process execution, recovery and snapshots; these preview-only jobs do not enter `builder_client_jobs`, activate a destination or publish. One build runs per project, with two projects building concurrently. Up to eight queued/running jobs per project and 64 across the service are accepted; older completed results are pruned as more jobs are added, bounding retained history per project. Source fingerprints and the 15-minute command review are checked when execution starts, as are the captured caller's sign-in and current membership. A token that expires while queued requires a fresh review. The captured token stays only in memory until that start check or cancellation; it is never written into a job or log response. A running authorized build can finish after access is revoked, but later status/cancel requests still require current access.

A separate `<work>/locks/<projectId>.build.lock` is claimed while holding the normal project lock, then retained through execution, process cancellation and output recovery. This blocks apply, fetch and native backup capture while keeping source inspection, draft saving, log/status polling and cancellation available. A cancelled queued job never runs. A running cancellation reports `cancelling: true` until the runner has finished; the terminal result follows recovery and build-lock release. The next queued job then starts. Changed source invalidates a queued review rather than building unreviewed code. Existing or replaced lock files fail closed and require operator inspection.

Build scripts receive a restricted environment with a private per-project `build-home` and `build-temp`, the service's executable search path, fixed locale and disabled global Git/npm configuration. They do not inherit the service's Supabase credentials or SSH agent. Dependencies must already be provisioned by the operator; accepting a build does not install packages. The existing runner enforces its five-minute build limit and preserves previous `dist/` output under `.kaizen/build-recovery/`, restoring it after failure or cancellation. This environment restriction does **not** isolate scripts from other files owned by the service account. It is suitable only for the trusted Kaizen beta; container/separate-host isolation for clients remains the L5-T4 decision.

Queue state and logs remain in memory. T7 records build intent durably before the runner moves output, and completion only after execution and output recovery. A graceful service close cancels queued/running jobs, waits for recovery and closes snapshots; jobs are never automatically replayed. An interrupted record blocks new builds, source/Git writes, publication and repository changes even if the old build lock is absent. Source inspection and saved drafts remain available when the project operation lock can be acquired. Successful status responses expose the protected HTTPS preview URL while the runner's snapshot remains available; closed or expired previews lose that URL. The runner's loopback URL is never returned to a hosted editor.

Local verification uses a real loopback service, temporary Git seed/bare/working repositories and a fixture SSH executable running real `git-upload-pack`. Auth and database HTTP responses are fixtures; the access function itself retains its existing database tests. Browser scenarios exercise real source inspection, saved drafts, review/apply, reopen and access revocation. The HTTPS scenario adds an actual Astro build, React hydration and original interaction, styles/fonts/images and canvas editing/rebuilding. Live Git-host authentication and the VPS proxy configuration still require deployment checks and the gate's actual Kaizen use.

### Hosted helper operations

#### Client build isolation (L5 implementation; rollout pending)

The hosted helper now defaults client repository builds to `scripts/builder-build-sandbox.ts` and its trusted Python entry point. It uses [Bubblewrap namespaces](https://github.com/containers/bubblewrap/blob/main/SECURITY.md) with an explicit mount/environment policy, plus [systemd cgroup delegation](https://github.com/systemd/systemd/blob/main/docs/CGROUP_DELEGATION.md) for per-build limits. This is a Linux container boundary sharing the host kernel; keep the kernel, Bubblewrap and Node runtime patched. It is not a separate VM or host.

Only filtered source, installed dependencies and trusted system/package-manager files are visible. Source `.env*`, key files, Git metadata, private configuration and helper recovery folders are omitted. Dependency links must be relative and remain inside their real `node_modules` directory. Builds receive fresh process/user/network/IPC/mount/cgroup namespaces, no capabilities, no new privileges, disabled further user namespaces, a read-only procfs and no network. Read-only source/dependency inputs feed disposable working copies so tools can create their normal build caches; tooling stays read-only. The working copies and temporary files are tmpfs: their writes count against the build's **2 GiB memory limit**, with swap disabled. Each build also has **128 processes** and **200% CPU**. The existing global build queue and execution deadline remain in force.

The host receives a bounded file stream, not an archive extraction or writable output mount: at most 10,000 regular files, 32 MiB per file and 200 MiB total, with relative paths, no links/duplicates and a required index.html. Only after the processes stop does the runner write those validated bytes to its own output folder. Its existing source fingerprint, previous-output recovery and served-preview checks remain authoritative. Resource failure or cancellation stops the cgroup; configuration failure never falls back to an unrestricted client process. The trusted client publication renderer separately renders validated page data and does not execute uploaded source packages.

Operator configuration at the L5 rollout:

- On Ubuntu hosts with `kernel.apparmor_restrict_unprivileged_userns=1`, Bubblewrap needs a loaded AppArmor profile that permits its namespace setup and constrains its child process. Follow [Canonical's scoped profile guidance](https://discourse.ubuntu.com/t/understanding-apparmor-user-namespace-restriction/58007); Ubuntu's `apparmor-profiles` package supplies `/usr/share/apparmor/extra-profiles/bwrap-userns-restrict`. Install that distribution-matched profile under `/etc/apparmor.d/` and load it with `apparmor_parser -r`; verify `bwrap (enforce)` and `unpriv_bwrap (enforce)` are loaded while the namespace restriction remains enabled. The existing VPS uses `/etc/apparmor.d/bwrap-directadmin`; preserve that working profile and include it in recovery inventory/backups. A replacement host must restore and load this prerequisite before running the isolation fixture. Do not disable the global namespace restriction to make a build pass.
- `BUILDER_HOSTED_SANDBOX_PACKAGE_MANAGER` points to an installed, immutable package-manager CLI (`bin/pnpm.cjs` or `bin/npm-cli.js`). Provision dependencies separately; a build cannot download packages. The CLI must belong to root or the supervisor's user and have no group/world write permission. Use a root-owned runtime in production.
- `BUILDER_HOSTED_TRUSTED_BUILD_PROJECTS=kaizen` retains the operator's existing Kaizen/CMS build path. Keep this exception in the operator environment file; it is never a project-owner setting. All other projects require isolation. An absent exception means all hosted projects require isolation.
- Install the updated systemd unit with systemd 254 or later, cgroup v2 and the cpu/memory/pids controllers. `DelegateSubgroup=supervisor` keeps the service outside child build cgroups. The host cgroup filesystem stays read-only except `/sys/fs/cgroup/system.slice/kaizen-hosted-helper.service`.
- `ProtectKernelTunables=false` is required because its overmounts prevent an unprivileged container from mounting fresh procfs. The build receives its own entirely read-only procfs; the supervisor keeps its unprivileged UID, no-new-privileges and empty capabilities. A transient VPS service reproduced the failure and verified this configuration change. Do not bind the host's procfs into a build.

**Verified in a separate VPS fixture; production rollout remains pending.** The npm run passes 14 isolation/runner/framework checks. The pnpm 10.32.1 run passes 17, adding rejected absolute/internal and escaping dependency links. Both include a real Astro 7.3.2/React island build with normal Vite caches, previous-output recovery after failure/cancellation and complete descendant/cgroup cleanup. The host dependency directory remains unchanged. CI reproduces the fixture in a temporary delegated service. Install an immutable package manager and roll out the unit/runtime together at the L5 milestone. Existing trusted-browser fixtures explicitly identify their operator-owned coordination scripts; they are not proof of client isolation. Tests must use their own temporary repositories and service, never a live client's work as a probe.

#### Builder request and database permissions (L5 implementation; rollout pending)

Apply migrations `202609140001`–`202609140003` before updating functions. Every builder endpoint uses an atomic fixed-minute counter through a service-only RPC, with global and verified-account buckets. Requests to projects allow 3,600 globally/360 per account per minute; account and content allow 300/30; invitations and publication allow 120/12. Contact uses a 120 global bucket plus its existing sender ledger. Missing or failed counter storage returns a safe 503; a full bucket returns 429 with Retry-After. No IP, email or token is stored in these counters. The hourly retention job removes rows idle for more than one day; verify the job and overdue row count at rollout. Platform Auth limits and upstream traffic protection remain separate.

All 23 builder tables are covered by a seeded access matrix in `access-matrix-database.spec.ts`. Browser table grants are explicit, including denial of operations such as TRUNCATE that [PostgreSQL RLS does not cover](https://www.postgresql.org/docs/current/ddl-rowsecurity.html). Legacy editor data stays scoped to configured legacy membership; project data stays scoped to membership; reviews stay scoped to their actor; expired project previews disappear. Operational tables stay behind validated RPCs. Public snapshots and the published-only redirect projection remain intentionally readable. Browser access to worker identity/claim columns stays denied. The migration also removes inherited browser RPC grants and sets safe definer search paths. No row data is rewritten by the permissions migration.

#### Auditing new changes and dependencies

`scripts/check-new-secrets.py <baseline-commit-sha>` requires an explicit ancestor commit. Its pinned, checksum-verified Gitleaks scanner reads only new commits after that baseline, added working-tree lines and regular untracked text files; it also rejects new private filenames. Output contains only finding rules and locations, with all matching values redacted. CI uses the pull request's base SHA, or the previous commit for a manual workflow run. New secrets introduced and then removed inside that range still fail. Synthetic fixture tests prove the scope and redaction. This check does not reopen historical incidents or scan old repository history.

CI audits the root and separate Studio lockfiles with pnpm, plus the actual six-function dependency graph with Deno. Registry failures fail the checks; high/critical advisories block CI. The 14 September 2026 audit reports no known Studio or function vulnerabilities and no high/critical root advisories. The root graph has one moderate [adm-zip destination-symlink advisory](https://github.com/advisories/GHSA-vwc7-r8mq-g2x9), with no patched version listed. The installed path is Sanity's CLI/runtime CLI; its inspected asset helper creates ZIPs rather than extracting them, and the builder's upload/import code does not use adm-zip. This scopes the finding; it does not claim the dependency is fixed. The advisory remains visible in every dependency audit, without an ignore rule.

The permanent `kaizen-hosted-helper.service` is installed and enabled on the VPS, serving `/opt/kaizen-helper-releases/594a523`. Its executable source matches deployed revision `8a9c94c`; the latter changes only the companion browser fixture and progress documents. It passed isolated startup and a real restart with a changed process ID while retaining the modified About page, Git state, saved draft, source recovery and private settings. Production and staging each pass all 260 HTTPS origin checks, and the managed checkout is `8a9c94c`. L3 requires this matching helper for its read-only website-status observation. Supabase remains at migrations `202609130001`/`202609130002`, `builder-projects` v9, `builder-invite` v1 and `builder-account` v1; L3 needs no backend migration or function deployment. See the launch task map for release IDs and evidence limits.

Save and publication records are `<work>/projects/<project>/save-receipts.json` and `publication-receipts.json`, outside the checkout and private preview output. They contain reviewed paths, hashes and operation state, with no source text or credentials. Files must be mode 0600, owned by the service account, and unlinked to other files; each is bounded at 4 MiB. Writes synchronize a private temporary file, atomically rename it, then synchronize its directory. A concurrent operator edit, damaged record or changed repository/destination fails closed. Do not erase a receipt to unlock a save.

Run Git observations as the checkout owner, or set `GIT_OPTIONAL_LOCKS=0` / use `git --no-optional-locks` when observing as root. `git status` can refresh its index even though the requested operation looks read-only. During the L3 rollout, root's restrictive umask left three refreshed indexes unreadable to their service accounts; restoring only their owners preserved the exact index bytes and resolved the failures. Keep the managed checkout owned by `kaizen-helper` and `/srv/kaizen/production` and `/srv/kaizen/staging` owned by `kaizen-deploy`. Check both ownership and source status after an upgrade; do not use a reset or a recursive permission change to repair an index-owner problem.

After a stopped operation, first establish that the old service and its child processes have exited. Preserve the project lock, operation records, source/recovery directories and staged files before resolving any stale lock. The permanent service lock prevents two managed service instances; a crash's project/build lock still requires operator inspection. Never infer that a missing process alone proves an operation failed before changing files or Git history.

Once access is restored, a complete applied change can be recognized by its recorded before/after hashes and unchanged local base. A wholly unstarted or rolled-back apply is removed only when every original hash and the base match. Unexpected/partial bytes remain untouched. If Git may have committed but the resulting revision was never recorded durably, the editor shows **An interrupted website save needs an operator check** and exposes only **Check saved state**. Inspect local/remote heads, the commit's parent and exact file diff, the Git index, recovery copies and drafts before an operator reconciles the record; never guess a commit or repeat the save. A known recorded commit can instead be checked on the remote, or retried explicitly without making another commit.

Build records are `<work>/projects/<project>/build-receipts.json`, with the same private, durable storage contract. Each records the runner UUID, repository/root binding hash, source fingerprint, start/finish timestamps, phase and terminal outcome. They contain no commands, logs, source bodies, Auth tokens or preview URLs. A `running` record from a previous helper process or a `recovery_required` record requires an operator check. Failure to persist completion closes the preview and reports a failed job while retaining the operation evidence. A `pruning` record authorizes only completion of the already-recorded generated-output cleanup.

To reconcile an interrupted build:

1. Stop the helper and confirm its complete service control group and build children have exited. Preserve the project/build locks, receipts, checkout, generated recovery and account drafts in a private operator backup before changing anything.
2. Match the receipt's UUID to `checkout/.kaizen/build-recovery/<UUID>`. Inspect `dist/`, `previous-dist/` and `failed-dist/`, along with tracked source, the Git index and local/remote heads. Build scripts may change source, so a stopped process or an existing `dist/` never establishes a safe result by itself.
3. Reconcile generated output only after establishing which bytes are the previous output and which belong to the interrupted build. Preserve uncertain output separately. Do not reset source or repeat a Git operation to make the folder appear clean.
4. Once the process and output are reconciled, retain the original receipt in the operator backup and record the established terminal outcome and completion timestamp in the private receipt. If the outcome remains uncertain, keep the record unresolved. Preserve ownership and mode 0600; never erase the entire receipt file to unlock the project.
5. Remove only the specifically reconciled stale project/build locks, start the helper, check health and reconnect. Inspect the source and saved draft before explicitly reviewing a fresh build. Automatic cleanup never resolves an interrupted build for the operator.

Preview grants and build reviews do not survive restart. An unavailable build returns 410 after authorization, keeping the editor connected to recover saves; a fresh review is allowed once interrupted work is reconciled. This does not replay a build or retain an old preview grant. A proxy outage while the service stops disconnects the transport; reconnect or reload after the helper is available, then check saved state before retrying. The VPS restart and storage checks are recorded in the launch task map.

`kaizen-hosted-helper.service` runs the helper as `kaizen-helper` from a root-owned runtime selected by `/opt/kaizen-hosted-helper`. It reads `/etc/kaizen-helper/helper.env`; persistent writes are limited to its work and project-credential directories, with a private temporary directory. The explicit `npm_execpath` lets the reviewed build runner find the VPS package manager. A process-held `flock` prevents two instances of this unit from owning the work directory. Its lock file can remain after exit; the existence of that file does not prove a process owns it. This differs from the per-project operation/build lock files: do not remove those or replay an interrupted operation based on age.

The unit limits restart attempts, terminates the complete process group on shutdown, and uses a dedicated journal namespace. Install its journal configuration as `/etc/systemd/journald@kaizen-helper.conf` when rolling out the unit. That namespace bounds retained log size and age without changing another service's logging. Inspect it with `journalctl --namespace=kaizen-helper -u kaizen-hosted-helper`; never log credentials or raw request bodies. Private preview routes remain excluded from Apache's access log as described below.

Install from a verified, committed runtime, separately from the website working copy:

1. Extract the exact reviewed revision under `/opt/kaizen-helper-releases/<revision>`, owned by root, with the matching frozen runtime dependencies. Keep the existing runtime and its full unit definition available; stopped transient units may be unloaded and need to be recreated. Do not deploy from an uncommitted checkout or make the runtime writable by the helper.
2. Retain the existing private `/etc/kaizen-helper/helper.env` and project configuration. Confirm its Node/package-manager paths, work directory, credentials directory and configured origin match the unit. The storage environment values above may override the documented defaults.
3. Validate `deploy/systemd/kaizen-hosted-helper.service` with `systemd-analyze verify`, then check startup through the actual runtime symlink on an isolated loopback port. The CLI resolves its entry path so that the symlink and direct launches both start the service. Before switching, stop the old helper gracefully, confirm its children have exited and inspect any remaining operation/build locks. Preserve a private copy of source, Git state, drafts, receipts and recovery output. Pending website edits must remain intact; switching the runtime must not reset or commit the website checkout.
4. Point `/opt/kaizen-hosted-helper` at the new root-owned runtime, install the unit as `/etc/systemd/system/kaizen-hosted-helper.service`, and install the namespace configuration as `/etc/systemd/journald@kaizen-helper.conf`. Reload systemd, then enable and start `kaizen-hosted-helper.service`. Keep the previous transient/bootstrap unit stopped.
5. Check health, unsigned/wrong-origin API denial, the effective unit restrictions and journal namespace. Restart the service once, verify a new process becomes healthy and compare the website edits and recovery data with the preserved state. Confirm actual project usage fits the configured bound, and that a second process cannot acquire the service lock.

A runtime startup failure can be rolled back to the previous unit before new work is accepted. Once the new runtime has recorded operations, preserve those records and reconcile their state before using older code that may not understand them. A failed restart must not be repaired by deleting receipts or resetting website source.

On this VPS, cloud-init manages `/etc/hosts`. The Kaizen production and staging mappings used for HTTPS origin verification are also retained in `/etc/cloud/templates/hosts.debian.tmpl`, preserving its existing localhost/IPv6 entries. A rendered-template check verifies both mappings without regenerating the live hosts file or rebooting the server. Cloud-init documents the [hosts-template behavior](https://docs.cloud-init.io/en/latest/reference/yaml_examples/update_etc_hosts.html); keep this template aligned when the approved origin address changes.

The operator can check process liveness directly on the VPS:

```bash
curl --fail --silent --show-error http://127.0.0.1:4334/health
```

The response is `{ "service": "kaizen-hosted-helper", "status": "running" }`. It contains no project, account, credential or filesystem details and does not contact Supabase. It is a liveness check, not proof of membership, Git access, project storage or a successful deployment. Shutdown reports `stopping` with HTTP 503 until the listener closes. The endpoint rejects browser origins, forwarded headers, foreign Host values and browser fetch-site headers. Do not add an Apache/public proxy for `/health`.

### Protected HTTPS previews

The service exposes `GET`/`HEAD /editor-preview/<project>/<build>/<view>/…` on loopback for TLS termination by Apache. Original pages and assets come from the runner's frozen build map; the service supplies the protected bootstrap, canvas and import loader. Request paths never become filesystem reads. Source editing first reuses the runner's source fingerprint and route checks. Rewriting is confined to served snapshot bytes; source and `dist/` stay authoritative. General website previews, the embedded canvas and the source-selection fallback share this delivery path.

Successful canonical-origin repository calls establish or renew `__Host-kaizen_builder_preview`: a random server-side session ID with `Path=/; Secure; HttpOnly; SameSite=Lax`, no Domain attribute and no token in the cookie. Its lease is bounded by 15 minutes and the verified JWT expiry. The unsigned legacy Studio cookie `kaizen_studio_auth=1` grants no access here. Sessions, public views and frame grants bind the verified account to its project and build. Each document, bootstrap and asset request checks Supabase Auth and current edit membership, fails closed during an Auth outage, and checks that the build is still available. There is no membership cache. Sign-out/account changes send an origin-checked DELETE to the existing repository endpoint to invalidate that session and its frame credentials. If the service is unreachable, the session expires without renewal. Already delivered bytes and an already authorized read cannot be recalled.

The iframe remains opaque (`sandbox="allow-scripts"` plus the same CSP sandbox), so page scripts cannot read the parent's document, editor storage or cookies. Authenticated HTML contains a private content namespace distinct from the shareable document URL and a one-use bootstrap grant. Its parser-blocking script installs a `Secure; HttpOnly; SameSite=None; Partitioned` frame cookie scoped to that namespace before the original assets are discovered. The bootstrap URL is a short-lived credential revealed only inside authenticated HTML; it expires after 30 seconds and cannot be reused. Frame credentials expire with the session or snapshot. The public URL alone cannot obtain bootstrap/asset access, including with `Origin: null` and an ambient editor cookie. No Supabase access token is placed in a URL, script or built file. Internal links in the general preview ask the trusted wrapper to load the next document: it checks the sending frame, opaque origin, view nonce and route within the same project/build/view. This keeps navigation initiated by the editor, where the document cookie is available. Direct script-driven navigation and client routers that require same-origin storage/history may be unavailable in this restricted preview.

Local scripts and module preloads opt into credentials, and authenticated frame responses allow credentialed CORS only for the opaque `null` origin. Dynamic imports, including computed Astro/React module addresses, run through a private `__kaizen-imports.js` module loaded with `crossorigin="use-credentials"`. This retains credentials in Firefox and WebKit without turning classic scripts into modules or granting same-origin access. Its loader has the same view-cookie, origin, current-membership and expiry checks as every original asset. Relative imports resolve against their original script/document base, and import options are retained. Each rebuilt canvas uses a fresh iframe so its browsing context belongs to that frozen build. The private namespace prevents a different opaque frame that knows only the shared URL from reading assets with an ambient cookie. This retains the [HTML script credentials rules](https://html.spec.whatwg.org/multipage/scripting.html#attr-script-crossorigin) without granting same-origin script access. Local CSS fonts are embedded from the same frozen map because [CSS font fetches use anonymous CORS](https://www.w3.org/TR/css-fonts-4/#font-fetching-requirements); limits are 5 MiB per font and 8 MiB of embedded bytes per stylesheet. Larger fonts retain their URL and may be unavailable in this restricted preview. Relative assets resolve against a trusted private base; root asset URLs and JavaScript URL literals are rebased only in the response.

CSP permits scripts only from the private snapshot plus existing inline scripts. It disables network calls (`connect-src 'none'`), forms, subframes and objects, limits framing to the canonical editor, and allows only the private base URL. HTTPS images, styles and fonts remain permitted for existing site presentation; remote scripts, application API calls and embedded third-party frames do not run. Preview responses use `private, no-store`, `nosniff`, `no-referrer` and restrictive permissions. The service bounds concurrent preview reads at 32, sessions at 512, views at 1024 and bootstrap/frame grants at 2048 each, with per-build/per-document eviction. Restart clears all grants; reconnect and build again. The core browser matrix now exercises this transport in Chromium, Firefox and WebKit; automated Linux WebKit coverage remains distinct from manual Safari acceptance. Arbitrary client build isolation remains L5-T4; the lifecycle and retention controls are documented above.

For Apache/DirectAdmin, place the following in the canonical editor's **HTTPS virtual-host custom configuration**, ahead of the generic `/editor-api/` and `/` proxy rules. Preserve it in the existing domain-specific DirectAdmin custom template; do not copy it into unrelated subdomain branches. Apache processes matching proxy rules in order, as documented in [ProxyPass](https://httpd.apache.org/docs/2.4/mod/mod_proxy.html#proxypass). The repository rule uses an explicit capture and backreference: without a backreference, [ProxyPassMatch](https://httpd.apache.org/docs/2.4/mod/mod_proxy.html#proxypassmatch) appends the original path to its target, duplicating a destination that already contains that path.

```apache
ProxyRequests Off
ProxyPassMatch "^(/editor-api/builder-repository)$" "http://127.0.0.1:4334$1" connectiontimeout=5 timeout=120 retry=0
ProxyPassReverse "/editor-api/builder-repository" "http://127.0.0.1:4334/editor-api/builder-repository"
ProxyPass "/editor-preview/" "http://127.0.0.1:4334/editor-preview/" connectiontimeout=5 timeout=120 retry=0
ProxyPassReverse "/editor-preview/" "http://127.0.0.1:4334/editor-preview/"
<IfModule mod_cache.c>
  CacheDisable /editor-preview/
  CacheDisable /editor-api/builder-repository
</IfModule>
```

Keep HTTP-to-HTTPS redirection and TLS certificates on Apache. Forward Authorization, Origin and Cookie and preserve Set-Cookie attributes, including Partitioned; do not widen cookie Domain/Path or rewrite the asset namespace. The preview service owns authentication and its per-response CSP. Exclude these routes from generic login redirects, static fallbacks, caching and any global CSP/header rule that would replace or append a conflicting policy. Do not authorize them by the Studio flag or add a blanket null-origin CORS grant. Keep snapshot URLs, cookie values, bearer tokens and bootstrap grants out of diagnostics; restrict proxy access logs and redact preview paths before sharing them.

Before rollout, validate the generated Apache configuration with its installed config-test command, reload only after it passes, and inspect the actual HTTPS response headers. In the signed-in editor, build and edit Kaizen, verify React and local fonts/images, follow a link in **Open website preview**, apply a reviewed change and inspect the rebuilt page at desktop/phone widths. Repeat with third-party cookies restricted, then verify an anonymous copied link fails and project revocation/sign-out deny subsequent reads. The automated Chromium fixture trusts its own temporary TLS certificate only; it does not disable web security. A passing fixture does not establish the live Apache configuration or Sean's gate acceptance.

## Builder error visibility

The default error sink is `public.builder_client_errors` in the existing Supabase project. This follows L0-T4 without adding another service. A hosted error service is an optional later alternative if Sean wants its search/alerting features; keep the same safe fields and access/retention boundary if changing the sink. No third-party error service is configured.

Apply `202609120002_builder_client_errors.sql` and `202609120003_builder_error_retention.sql` after the project-capabilities migration, then deploy the updated `builder-projects` function and frontend together. Migration 003 requires `pg_cron` and fails if the hourly cleanup cannot be scheduled. Supabase provides the scheduler; self-hosted Postgres must install it first. The syntax follows [Supabase's Cron installation](https://supabase.com/docs/guides/cron/install) and [job quickstart](https://supabase.com/docs/guides/cron/quickstart). Neither migration contains credentials.

The function accepts authenticated `record-error` actions, takes the account ID from the verified token and rechecks current membership and archived state in the recording transaction. A project lock serializes recording with membership changes. Each record contains project/user IDs, a server receipt time, fixed error category/source, known screen, optional page ID/route hash, browser family/major version/platform, and helper mode/state. No raw message, stack, URL, source path, build log, title or arbitrary JSON is stored. There is no diagnostic email or Slack integration.

Only database operators (SQL editor or a server-side service-role client) can read these records. Builder owners and editors cannot select, insert, update or delete them, impersonate an actor through the RPC, or run retention cleanup. Service-role recording must use the guarded RPC; it has no direct table write grant. The client uses explicit event-time authentication and drops pending sends on account changes. No extra secret belongs in the frontend.

Inspect the most recent records for one project using a bound project parameter in an operator SQL client:

```sql
select created_at, project_id, user_id, category, source, screen,
       page_id, route_hash, browser_family, browser_version, platform,
       helper_mode, helper_status
from public.builder_client_errors
where project_id = $1 and created_at >= now() - interval '14 days'
order by created_at desc, id desc
limit 100;
```

The route hash can be matched against a user's copied report. It does not recover the route or error text. Membership permits recording even without publish permission. Revoked members and archived projects are refused. Writes are capped at 20 per account/project per hour and 2,000 per project per day, checked against server receipt times under the project lock. Dropped duplicates/rate-limited events are not counted elsewhere. The browser also limits attempts, deduplicates a repeated category/source/page for one minute, bounds concurrent sends and abandons a slow request. This is troubleshooting evidence, not complete analytics or an availability monitor.

`builder-client-error-retention` runs hourly and physically removes records older than 14 days, including projects with no recent activity. With the job operating, deletion occurs at its next hourly run (up to 14 days plus one hour after receipt). Removing an account or project also deletes its records through foreign keys. This policy covers the live error table; database backups follow the separate backup-retention policy.

Before enabling beta recording, and after database maintenance, verify the job is active and inspect its latest execution in Supabase Cron. Check the actual deployment with:

```sql
select jobid, jobname, schedule, active, command
from cron.job where jobname = 'builder-client-error-retention';

select status, start_time, end_time, return_message
from cron.job_run_details
where jobid = (select jobid from cron.job where jobname = 'builder-client-error-retention')
order by start_time desc limit 5;

select count(*) as overdue
from public.builder_client_errors
where created_at < now() - interval '14 days 1 hour';
```

An inactive/failed job or overdue records require operator action. Correct the job or database issue, run `select public.builder_prune_client_errors();`, then verify the next scheduled run succeeds and `overdue` is zero. Do not claim retention is active based on applying the table migration alone. The database test executes the real schema, access rules, quotas and deletion function; PGlite substitutes only the unavailable `pg_cron` scheduler, so live scheduling still needs this deployment check.

## Rollback and operations

Inspect retained versions, then reactivate the chosen artifact without a build or CMS fetch:

```bash
node scripts/kaizen-releases.mjs list --store /var/lib/kaizen/production
node scripts/kaizen-releases.mjs rollback --store /var/lib/kaizen/production --id PREVIOUS_RELEASE_ID --origin https://kaizenweb.co.uk
```

The selected configuration is not itself proof that Nginx serves it. Use `verify-live` to check the server response. Keep release records and files together. Monitor storage usage: retained artifacts and immutable assets are not automatically removed.

An activation lock includes its owning process ID, host and creation time. Do not clear it because it is old. Inspect the process on that host and its transaction. If the process is confirmed stopped, preserve the orphaned lock and transaction for diagnosis, inspect the selected include and public marker, and recover under a maintenance window without competing deployments. There is no automatic stale-lock takeover. Interrupted or externally modified configurations require operator recovery.

For a coordinated cloud builder, use **Builder → Releases → Review rollback**. The raw filesystem rollback command above is an operator recovery tool: it does not reconcile Supabase by itself, and a later coordinated deployment will reject a mismatch between the serving artifact and database head. Rolling back files does not roll back database migrations, enquiries or external services.

## Coordinated builder publication

The hosted publish function now saves an immutable private request rather than promoting a draft immediately. The request freezes the selected resolved pages, image variants and shared design, with optimistic checks on page versions, site state and asset metadata. A queue entry contains the full candidate publication set, including unchanged live pages. Ordinary code/CMS-triggered deployments enter the same queue and use only the last verified publications. A pending editor release blocks a competing deployment instead of leaking through its build.

Roll out the following together, after the Nginx/store setup above:

1. Apply `202609100008_builder_releases.sql` after the earlier builder migrations, and deploy the updated `builder-publish` function. This adds the private release queue, one active release per workspace, editor-only status RPC, service-only worker operations and guards against legacy writes during deployment.
2. Set `BUILDER_RELEASE_SERVICE_ROLE_KEY` in the VPS checkout's private `.env`, alongside its existing `VITE_SUPABASE_URL` and `VITE_BUILDER_CLOUD=1`. This credential is only read by the Node worker. Never put it in browser configuration or give it a `VITE_` prefix. The queue and its lock owner are not readable by anonymous or authenticated clients; editors use the limited status RPC.
3. Deploy the updated workflow and `scripts/builder-release-worker.mjs`. Repository dispatch forwards a validated release request UUID. The worker claims that exact request, writes its private input under the release store's `requests/`, then runs the complete build with `BUILDER_RELEASE_SNAPSHOT_FILE`. The build refuses an invalid or missing configured snapshot rather than falling back to mutable publications. The redundant preliminary GitHub build is removed; the pinned VPS build is the deployed build.
4. Set `BUILDER_RELEASE_COORDINATOR=1` in the publishing function's secrets once this worker path is ready. Until this is enabled, hosted Publish returns a setup error and does not modify published data. Keep the existing GitHub deployment credentials there.
5. Use separate Supabase workspaces for production and staging. One database represents one independently deployed site; sharing a publication head between different origins is unsupported.

The worker progresses through queued, building, activating and verifying. Only after retained-file and complete public HTTP checks does it commit the candidate publication set and shared design in one database transaction. Current drafts, draft versions and newer autosaves remain intact. Published snapshots and bounded publication revisions are updated separately. Old data is not promoted simply because GitHub accepted a dispatch or the static build succeeded.

**Builder → Releases** polls status while visible, shows the currently verified release, exposes actionable recorded errors and lets an editor retry an unclaimed dispatch. Rollback and unpublish have explicit review steps. Rollback selects the original retained artifact without rebuilding or refetching CMS content and reconciles its publication snapshots into the database after verification. Unpublish removes the selected page from the candidate publication set while preserving its draft/history; it does not automatically add a redirect. The separate optional Studio hostname is not rolled back by a builder rollback; the public release's `/studio/` files are part of its retained artifact.

CMS bindings are frozen as page configuration for the worker, then resolved during that build. The retained artifact contains the exact resulting HTML. A subsequent new code/CMS deployment can legitimately resolve newer published CMS content; an explicit rollback uses the retained bytes. Builder redirect drafts and saved private previews are described in the [builder guide](visual-builder.md).

There is no atomic transaction spanning PostgreSQL and Nginx. If database promotion is definitively rejected, the activation code restores and checks the old artifact before recording recovery. If its acknowledgement is lost, the worker reads back the request: a confirmed committed release remains live. If the database cannot be reached to establish the outcome, the already verified candidate stays selected and the release remains pending with a local `recovery_required` transaction. It does not blindly restore files that may now disagree with a committed database. New deployments stay blocked until the owner/operator reconciles actual state. Never clear a queue entry or take over a worker based solely on elapsed time.

Setup/SSH failures before the worker can claim a request leave it queued. Inspect the GitHub workflow and VPS logs before using Retry dispatch; this UI does not yet fetch GitHub job diagnostics. A crash after claiming likewise requires inspection of the actual worker, serving marker and transaction. There is no automatic takeover. The L6 operator recovery command above requires the exact request and retained artifact, verifies that the original process stopped, and reconciles publication and usage together. Historical release artifacts and private request files remain subject to the separate physical retention policy.

### Coordinator verification

PostgreSQL tests apply the actual migration in PGlite, including permissions, immutable request/claim retries, single ownership, draft preservation, stale workspace rejection, failures staying out of later builds, rollback reconciliation, unpublish, route reservation and normal deployment queue isolation. Worker integration tests use real retained files and HTTP responses with a controlled Nginx adapter, covering accepted/rejected database promotion, lost acknowledgements and unavailable outcome checks. A Chromium test mounts the shipped Releases panel with a controlled service boundary and exercises observed progress, review/confirm actions, pending-state controls and service errors. These checks do not establish hosted authentication or a real VPS deployment as verified.

### Retained artifact checks

Local unit tests exercise real files and HTTP responses with an Nginx control adapter: activation, exact rollback, failed configuration/reload/health checks, immutable assets, changed archives, locks and external-edit recovery. `tests/builder/verify-releases.mjs` exercises the same activation code with a real Nginx process in an isolated directory on a temporary loopback port. It verifies a successful release, invalid-config recovery, wrong-route/content recovery, 301 redirects, old asset availability and rollback to the original CMS markup. The test stops only the Nginx instance it starts.

The real smoke test passed locally with the official Windows Nginx binary. Builder CI now runs it with Ubuntu's Nginx package. Linux CI, actual VPS configuration, hosted editor authentication/storage, production release activation and live rollback remain to be verified after deployment access and setup are available. No public release, remote configuration change or GitHub push was made for this increment.

## Builder redirects

Builder → Redirects provides a separate saved draft, a review of added/changed/removed rules and explicit publication. Both forms of a directory URL (with or without a trailing slash) receive the chosen 301/302 response, preserving query parameters. Temporary 302 is the default because browser caches can retain a permanent 301 after it is changed. History restoration changes the draft only. There is a 200-rule limit; paths are internal, at most 200 characters and use letters, digits, slashes, dots, underscores and hyphens. External URLs, query-specific rules and regular expressions are unsupported.

Install `202609100010_builder_routes.sql` after migrations 001–009, with no pending release, alongside the updated publishing function, generator and worker. Do not reapply an earlier backup migration over the wrapper functions added by 010. Anonymous access exposes only published rules; draft editing and backup access require editor membership. Queued releases freeze redirect rules, ordinary page/site/code releases retain the published set, and verified rollback restores the retained release's rules while preserving newer drafts. A missing redirect field in an older retained snapshot means no builder redirects.

A redirect cannot replace a current builder page or protected existing Astro/editor route. Publish a moved page's new URL first, then publish a redirect from the old URL. Published and pending sources are reserved against page edits. Build-time validation checks conflicts and cycles across builder, Sanity and fixed site redirects and verifies that every builder destination chain ends at an emitted public path. A missing destination fails the build and preserves the previous live site. Removing a page that is still a redirect destination therefore requires updating or removing the redirect first.

The generated `redirects.generated.json` records status, destination and query-preservation checks. Staging stores those checks in the private release manifest and excludes both generated redirect files from the served site. Verification requests both URL aliases with query parameters, checks the exact Location and status without following redirects, and includes these responses in publication evidence. Failure triggers the same retained-release recovery as a bad page response. Legacy artifacts without this metadata retain their previous page checks.

Local checks cover database permissions, draft and release isolation, backup restoration, redirects through a real Nginx process and recovery from a redirect that loses query parameters. The hosted migration, actual VPS activation and live cloud rollback still require deployment and verification.


## Builder invitations

Deploy `202609130001_builder_invitations.sql` before the matching `builder-invite` and `builder-projects` functions and frontend. The new function uses `verify_jwt=false` because its handler verifies the caller through Supabase Auth, then calls only service-owned owner-checking RPCs before using the Auth admin invitation API. The projects function reads the limited, owner-only member directory. Never expose the service-role client to the browser.

`BUILDER_INVITE_REDIRECT_ORIGIN` selects the canonical HTTPS builder origin; when absent it uses the existing configured public site origin. The handler constructs `/builder/?password=setup&project=<id>` itself. Ensure that destination is allowed by Supabase Auth's redirect configuration before the milestone rollout. A caller cannot supply an external redirect or account metadata. The existing `ALLOWED_STUDIO_ORIGINS` policy applies to the new function.

The implementation uses [Supabase's admin invitation API](https://supabase.com/docs/reference/javascript/auth-admin-inviteuserbyemail). Its [Auth implementation](https://github.com/supabase/auth/blob/master/internal/api/invite.go) can invite an unconfirmed account again and refuses an already confirmed account. Accordingly, confirmed existing accounts receive project membership without another creation email. The builder never changes an existing password, deletes an Auth account to retry an invitation, or returns an invitation link or token. Auth owns link expiry and provider delivery.

The database serializes preparation and completion with the project lock and rechecks current ownership and archive state. A membership-change trigger advances `access_version`; completion requires the observed version and matching Auth ID/email, preventing a delayed request from undoing a removal or permission change. Existing-member invitations do not change access. The existing membership RPC continues to protect the final owner. Auth and membership are separate transactions: an email may be accepted before completion fails, and loss of the completion response is not proof that membership was absent. The operator should refresh the member directory before an explicit retry. No background resend or compensating account deletion runs.

`builder_invitation_limits` holds one counter per project, with a maximum of 20 attempts in an hourly window. It stores no recipient address, message, token or request body and is deleted with the project. Browser roles cannot read or write it or call the preparation/completion RPCs. Account name/email visibility is limited to current owners of the selected active project. Database and handler tests include revoked/foreign/archived access, malformed and mismatched identities, delayed completion after removal, last-owner protection, rate limits, failed provider responses and lost acknowledgements.

At milestone rollout, check the migration and function versions, verify unsigned and foreign-origin denials, and exercise an explicitly requested invitation with its intended recipient. Development and CI use fake Auth administration and temporary database fixtures; they do not establish production email delivery.

## Builder accounts

Apply `202609130002_builder_accounts.sql` after the invitation migration, then deploy `builder-account` and the matching frontend. The function uses `verify_jwt=false` at the gateway and verifies every caller through Auth before invoking service-only RPCs. It uses the existing `ALLOWED_STUDIO_ORIGINS` policy and accepts bounded JSON account actions. Keep `/builder/?view=account` on each intended builder origin in Auth's redirect allowlist for email changes. Profile/password updates use the signed-in Supabase SDK directly; privileged account removal stays in the function. Provider email delivery and confirmation policy need their own production configuration.

Save remains unavailable while an existing page and its saved edits are loading, or while draft recovery remains unresolved. An early status response cannot make Save available before those checks finish.

The same release must update the hosted helper's `scripts/builder-hosted-auth.ts` **and** its new `shared/builderAccount.ts` import. This shares the valid name/email rules and points Save failures to Account. Package both with the helper runtime; preserve the managed checkout's pending source, drafts, recovery files and private settings using the existing [helper upgrade procedure](#hosted-helper-operations). A frontend/function-only rollout does not update the helper's old Save message.

`builder_account_deletions` retains one current request per Auth ID; `builder_account_deletion_projects` records its website approvals. Both use RLS with raw table grants revoked, including service-role table access. Only the service-role RPCs can operate the queue. Request/cancel actions use the caller's verified ID. A normal HTTP confirmation requires another current owner of its specific website; requester self-approval and foreign owners are refused. Each website requires its own approval, including archived websites. Approval is effective only while that reviewer remains an owner. Membership changes and final closure use account/project locks; changed website lists require a new request and a final owner cannot be removed. Owners see only their own website's review entries; the queue does not expose other memberships.

Final preparation first records `processing` and removes all memberships in one database transaction. New membership grants to a processing/completed account are refused. Only then does the server call Auth's admin `deleteUser(id, true)`. This irreversible soft deletion removes the Auth identity while retaining its ID for website/release/Storage foreign keys; it also ends Auth sessions. The completion RPC checks Auth's recorded deletion before marking the request completed. These semantics are documented in [Supabase's delete-user reference](https://supabase.com/docs/reference/javascript/auth-admin-deleteuser) and [Auth's admin implementation](https://github.com/supabase/auth/blob/master/internal/api/admin.go). Existing signed file URLs remain subject to their original expiry; already published website content and historical source authorship remain with the website.

Auth and the builder database cannot finish in one transaction. If the Auth call or completion acknowledgement fails, access remains closed and the request remains available for an explicit **Finish deletion** retry by the requester or a current approving website owner. Completed requests are idempotent. Do not hard-delete Auth rows or remove website files to recover a request. This is login/access removal, not an erasure of website content or historical records; broader retention/erasure policy belongs to L5 legal work.

### Accounts with no website owner

An account with no project memberships can submit a request, but has no website owner to approve it. An operator reviews these requests using `scripts/builder-account-delete.ts`. Run it only on an operator host with `SUPABASE_URL` and `BUILDER_ACCOUNT_SERVICE_ROLE_KEY` loaded from a private environment file; do not put credential values in command arguments or browser settings. The list contains request/account IDs, status and request date, with no copied email or token.

```bash
pnpm exec tsx scripts/builder-account-delete.ts --list
pnpm exec tsx scripts/builder-account-delete.ts --delete <request-id> --confirm <same-request-id>
```

Review the selected account's request before running the second command. It handles one explicitly named request; there is no bulk deletion. The database rechecks that both the saved website list and current memberships are empty. This operator route cannot bypass website-owner approvals. If removal is interrupted, list again and explicitly retry that same request. Cancellation, newly granted access or a stale request can prevent it from proceeding. The HTTP function never accepts this operator override.

All account tests use fake Auth services and temporary databases. Browser journeys use the real Supabase client and the actual deletion migration, but do not send real confirmation emails, sign out real sessions or delete real accounts. At L2 rollout, verify function/migration/helper versions and read-only account loading; any real profile change remains the account holder's explicit action.

### Sign-in callback handling

The shared browser client captures only callback intent and a bounded error code before SDK initialization consumes the URL. It does not copy tokens or raw provider descriptions into UI state. A listener attached when the client is created remembers the account from a real `PASSWORD_RECOVERY` event, including events emitted before React mounts. The authenticated event and current account must agree before a recovery form appears. A bare `password=setup` query is not recovery authorization; unfinished invitations use the existing account setup flag. After processing, callback material is removed from the current URL while retaining the website/account destination.

SDK initialization errors are checked before the workspace can mount. A failed callback preserves an existing session and presents an explicit account choice. Ordinary same-account `SIGNED_IN` notifications on focus do not dismiss that choice. Newer session events supersede pending initial reads; workspace loads are delivered outside Auth's callback lock, and sign-out/account changes invalidate pending loads and clear editor selections. Password setup reuses the isolated account mutation and cannot finish another account's form after a switch. A server-confirmed setup is remembered for the current account even if its shared cached profile has not refreshed yet.

Reset links are constructed from the current builder origin and validated project ID. Keep those builder origins allowed in Auth's redirect configuration. The deployed provider owns link validity, email delivery and rate limits. The [Auth error reference](https://supabase.com/docs/guides/auth/debugging/error-codes) describes structured error codes; expiry and prior use can have overlapping responses. Tests exercise both situations without inventing a provider-specific “used link” code. No additional endpoint or migration is needed for L2-T4.
