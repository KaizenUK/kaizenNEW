# Client builder goal — progress and evidence

Hosted/local connection update (deployed and verified): the hosted **Export & repositories** screen now connects through an explicitly approved localhost companion window. It scopes source operations to one folder and separates local drafts by hosted origin/account/project. The cross-origin browser scenario passed in 41.2 seconds with actual filesystem writes, saved-draft reopen/reconnect, reviewed build, rendered selection at desktop/mobile widths, backup download and restoration to a newly approved folder. Cloud identity/catalogue are fixtures in that test; local transport, consent, APIs and files are real. Three capability tests cover persistent isolation, root/nested-root and review-ID restrictions, revocation/expiry, linked folders and concurrent session limits. Full unit run: 202 passed, one optional skip; typecheck: 308 files, zero errors/warnings; full website/Studio build passed. Three repository/source/native-backup browser regressions passed in 1.1 minutes. An additional inventory test and final site build confirm the technical companion route is excluded from editable public pages. Production CI run 34631415856 succeeded and finalized live release gh-34631415856-1 from commit 0a855f6afd668b5c3bf6a3d607938bec18d1e3b2. The actual signed-in Sean account and deployed HTTPS editor then passed consent/pairing, isolated local inspection, draft save/reopen, reviewed source application with CSS/unrelated copy preserved, reviewed build verified over HTTP, rendered heading selection back into the hosted editor, mobile overflow checks, and explicit disconnection. Builder and companion console-error logs were empty. The temporary local preview/server were stopped; the hosted tab was reset ready for a fresh connection. Production cloud pages and personal repositories were not changed by this acceptance test. Overall goal still needs consolidated final acceptance and the remaining editor ResizeObserver warning investigation; form notifications and client CMS remain explicitly deferred. This update supersedes older statements below that the hosted connection has no implementation.

Dependency upgrade, deployed and verified: user authorised major upgrades. Astro 7.3.2, React integration 6.0.5, Vite 8.3.0 and Tailwind 4.3.3 now build the application; Studio and fresh client exports also use Vite 8. Root audit fell from 147 findings to one moderate Sanity CLI exception with no upstream patch; Studio and fresh independent Astro/React exports have zero findings. See [dependency maintenance](dependency-maintenance.md) for applicability and versions. Full unit suite: 199 passed plus one optional skip. Typecheck passed. Full site/Studio build passed. The complete Unity browser run passed 30 scenarios with two optional skips and a clean exit (4.6 minutes); independent installed fixtures and the main homepage/About page passed desktop/mobile checks. The nested rich-text form error is fixed; editor ResizeObserver warnings remain a follow-up. Sean selected his local GitHub Desktop checkout; its paired hosted connection is now verified above. No access decision is pending. Production CI run 34628265808 succeeded and finalized live release gh-34628265808-1. The VPS client compiler was independently verified before switching its service to /opt/kaizen-builder-releases/1179886; the subsequent scheduled run succeeded. Live browser checks confirmed the password sign-in gate and client demo load without JavaScript errors.

Requested 11 September 2026. The hosted builder is now live at https://kaizenweb.co.uk/builder/, deployed directly from main as authorised. Production release `gh-34631415856-1` serves code commit `0a855f6afd668b5c3bf6a3d607938bec18d1e3b2`; GitHub Actions run https://github.com/KaizenUK/kaizenNEW/actions/runs/34631415856 succeeded, including the full website/Studio build, served-file verification and database finalization. Staging was not deployed.

Supabase recovered after the authorised restart. Fifteen builder migrations and four builder functions are installed. Real unrelated-account API/database/private-storage isolation, editor/publish separation and fresh-access revocation checks pass. The production client worker service and timer are installed and verified, with the explicitly approved `client-demo.kaizenweb.co.uk` production destination now provisioned. See [client demo operations](client-demo-operations.md) for the isolated store, DirectAdmin subdomain, HTTPS renewal, real failed-release restoration and continuing acceptance. The client demo now passes real hosted publication, changed-revision publication, failed-release restoration, rollback, unpublication, private media download, draft retention and unrelated-account isolation. Desktop/mobile browser checks exposed and verified fixes for homepage order and renamed media extensions. Form receiver/recipient and client CMS remain explicitly deferred. Forced process-crash recovery has isolated real-Nginx evidence, not a deliberately induced crash on the public host. The original full goal therefore remains incomplete.

Password sign-in and invitation/recovery password setup are live. Open sign-up is disabled. Sean confirmed successful login; the account has owner/publish membership in the original Kaizen project. No password was chosen or recorded by the agent. A temporary real hosted invitation was also tested in the browser: desktop/mobile gate, first-password setup, project creation/reopen, sign-out and subsequent password sign-in all passed without page errors. Temporary test account and project were removed.

Current source-editing work (filesystem editing requires the local companion): original Astro and React source can now expose literal content, links and images and preserve its original components, CSS, scripts and hydration. Astro sections can be reordered without conversion to builder blocks. The local Existing site pages screen opens the current checkout directly; other repositories open through inspection. Proposals check all imported source versions, retain recovery copies, and leave Git untouched. Kaizen's actual homepage and About page were successfully inspected. The latest deployed code adds private per-project/repository/route autosaving of unapplied content and section-order drafts, reload/reopen restoration, two-window conflict detection, external-source-change recovery, JSON recovery download and retryable reads/writes. Applying clears only the reviewed draft version. Rendered-page selection now passes local acceptance: the actual built static Astro page opens in a private selection window, filters matching source fields, exposes repeated-content ambiguity and can restore original interactions. It requires a build matching current source. Six runner tests, TypeScript and the expanded desktop/mobile browser scenario (40.6 seconds) pass, including editing and saving a selected field. The selection change is deployed; filesystem editing and selection still require the local companion. Hosted source editing is now available through the paired connection described above. Native source and saved drafts now have their own reviewed backup/restore workflow, separate from builder page-data ZIPs; see the native-backup verification below.

Native backup and reopening (deployed code; local companion required): Unity can review/download a versioned native source ZIP and restore it into a new folder in another selected project. Source, public assets, package files and that project's saved source drafts are preserved; environment values, Git history, dependencies, build output and known private files are excluded with an explicit inventory and environment names. Restore rebinds draft roots, preserves file modes where supported, verifies bytes, refuses existing destinations and retains partial failures without replacing original repositories. Repository-folder preferences are now remembered per project in the current browser. Fourteen focused source/draft/runner/backup tests and TypeScript passed. Three browser scenarios passed in 59.9 seconds, including Unity download/restore into another project, recovered pending source edits, an independently installed/restored build, desktop/mobile image/layout checks, hydrated interaction, and preservation of original source. Desktop/mobile screenshots were inspected.

Latest native-content extension: imported TypeScript, JavaScript, MJS and JSON content files, barrel re-exports, and named literal content variables are now inspected and editable. Shared-file effects are explicit; computed code is preserved. JSON stays valid, and data files participate in proposal/apply version checks and recovery copies. Seven source/draft tests and TypeScript pass. Two browser scenarios pass in 41.6 seconds: Kaizen About-page read-only inspection and a separate installed native Astro/React site, including editing TypeScript/JSON content and seeing the result in the actual desktop/mobile build. An earlier run was invalidated by a development-server reload during an implementation edit; the final run used unchanged code. This extension is deployed after a successful full production build, served-output verification and database finalization; filesystem editing still requires the local companion.

Draft recovery verification: 11 focused source/draft/runner tests passed, followed by TypeScript with no errors. The expanded native browser scenario passed in 40.5 seconds: interrupted draft-read retry, close/reload restoration, section-order persistence, apply clearing, original styled/interactive build and preview, external source-change recovery, and two-window overwrite refusal. This extends the earlier source-editing evidence below; it does not establish hosted source editing.

Source-editing verification: 13 focused source/repository/runner tests pass; TypeScript passes; the site production build passes with 23 local routes. Two browser scenarios passed with a clean exit (46.4 seconds): the actual About page opens for read-only source inspection, and a separately installed native Astro/React fixture passes content edits, section order, source preservation, actual build/HTTP preview, desktop/mobile styling and images, navigation, reopen, and hydrated counter interaction. Earlier runs hung during Windows profile cleanup after browser exit. The two tests now reuse the isolated persistent-profile approach already used by the local-runner verifier; no personal browser profile is used. These results are not evidence of hosted source editing or completed client deployment.

Current remaining work: connect the hosted native-editing UI to Sean's local checkout; complete the consolidated acceptance audit of the current Unity interface and hosted/local controls; investigate remaining editor ResizeObserver warnings. Real client forms/notification delivery and a chosen CMS connection remain explicitly deferred. Dependency remediation and the remaining CLI exception are documented in dependency-maintenance.md. Production access and Sean's login are no longer blockers. The sections below retain the historical implementation record; earlier missing-access statements are superseded by this current status.

Verification: three auth tests; typecheck (294 files, zero errors/warnings); full suite 187 passed plus one optional Nginx skip, followed by the actual Nginx database suite (18 passed), and updated original-release database tests (9 passed). The local Studio build encountered workstation page-file/native memory failure; the complete server production build passed. Windows tests were bounded with NODE_OPTIONS="--max-old-space-size=4096 --v8-pool-size=1" and --maxWorkers=1.

The first two production activations exposed Supabase's pg_safeupdate requirement: DELETE requires an explicit WHERE clause. Both automatically restored and verified the previous live site. Migration 202609110005 retains the locked, transactional publication replacement and adds its explicit primary-key predicate. The fix passed local release tests and a rolled-back diagnostic through real PostgREST before successful production deployment. The temporary diagnostic function was removed.

## Inspected baseline

- Clean checkout at `a0438bd` (Unity editor UI), following `a01f8c0` (builder tool). Unity shell, Poppins/Inter styling and existing Puck editing preserved.
- Existing local workspace is one JSON store; hosted tables and editor membership are workspace-wide. Existing export is a standalone React/Vite static generator, not Astro. Local repository integration is absent.
- Existing backups, revisions, uploads, responsive fields, shared definitions and release machinery provide the foundation. Historical mocked/database tests do not establish hosted verification.

## Implemented in this work

- Acceptance review fixed a local-runner race: failed/cancelled build status is now published only after previous-output restoration finishes or its recovery error is recorded. It also strengthened the two-project browser scenario to use the same route and asset filename with different page content, media bytes and rendered accents, and to exercise dashboard duplicate/rename/archive/restore. The hosted verifier now requires explicit 403 permission responses rather than treating any API error as isolation evidence, and checks positive editor writes/media access plus a separate publish grant. That hosted verifier remains unexecuted against a service.
- Patched compatible tar 7 dependencies to 7.5.22 in the root and Studio lockfiles, resolving the remaining tar findings from Supabase/Sanity tooling without changing framework majors. Maintainer references: [archive recursion advisory](https://github.com/isaacs/node-tar/security/advisories/GHSA-r292-9mhp-454m) and [decompression advisory](https://github.com/isaacs/node-tar/security/advisories/GHSA-23hp-3jrh-7fpw). Other dependency findings remain; this is not a clean-audit claim.
- Updated the first-client walkthrough to describe the connected Unity Releases workflow and its draft/live behavior. Exported CONTACT-FORMS.md now directs receiver changes through Client settings for a visual round trip, explains durable receipt versus notification/provider/inbox delivery, documents CORS and idempotent retries, and gives explicit staging/production acceptance steps. The missing-receiver warning no longer directs users to modify generated source as the primary path. Standalone and Astro handoffs both retain this guide. Regression CI now also triggers for the publication-state helper and Vitest configuration.
- Recovery can now retry retained guards after the recovery process itself stops. Each stopped guard is protected by another exclusive guard and its ownership is rechecked before the original operation; no guard is reclaimed by age or renamed out from under another recoverer. Original job/worker ownership and downstream snapshot/configuration/served-output checks remain in force. Live/foreign/unidentifiable owners, changed guard ownership and chains exceeding eight interrupted guards stop for operator inspection. The shared helper applies to Unity/local recovery, server-only reconciliation and hosted worker recovery.
- Local publication history now validates every job's project/destination identity, phase/action, UUID/artifact references, timestamps and field types; duplicate job IDs, mismatched rollback references and invalid active pointers stop reads and writes. Index/snapshot files must be regular files. Retained snapshots additionally validate the workspace and action before recovery or baseline projection. A missing index with retained files stops for operator recovery; an empty directory initializes through an atomic, non-overwriting link, including concurrent companions. Errors retain existing metadata/locks and identify the recovery boundary.
- Updated the builder to Astro 6.4.8, Vitest 4.1.11 and jsPDF 4.2.1, constrained Sharp to 0.35.4, and removed the unused standalone React DevTools/Electron dependency. The independent Astro fixture uses the same Astro/Sharp versions and patched esbuild 0.28.1. Integration still preserves existing repositories' package/configuration files; these fixture updates do not silently upgrade client repositories.
- Added cursor-based client release history in pages of 50 for hosted and local projects, with Latest/Newer/Older controls. Current baselines and unfinished/recovery jobs stay visible on every page, and recovery-required destinations now disable new publish/rollback controls. Hosted history uses a read-only, RLS-scoped SQL function with explicit public fields, project-bound cursors, stable timestamp/ID ordering and a matching index. New releases do not shift an existing older-page boundary. Polling preserves the selected history page; read errors no longer masquerade as an empty history or unconfigured destination.
- Hosted media now has a rendering-only renewal context: one-hour signed URLs renew after 50 minutes, on tab return/network reconnection, or explicit retry. Old draft/undo references resolve to current display URLs without changing editor data or saving revisions. Mounted image/variant/background/font/video rendering and previews use this projection; private downloads/exports still request fresh Storage permission. Account changes clear display access and reject in-flight responses, including changes away and back. Preview polling reuses valid display URLs to avoid needless iframe reloads. Failed renewal retains still-valid media, suppresses expired URLs and exposes retry; no new server endpoint was added. Exports include an identity-only context and remain independent of hosted services.
- Added hosted operator recovery with the original database token and stopped-process evidence, companion/server recovery guards, service-only SQL authorization, verified candidate completion or explicit previous-artifact restoration, and idempotent reconciliation after lost acknowledgements. Recovery preserves the exact prior baseline identity and never writes drafts. Revoked/disabled uncommitted releases can only restore the previous artifact; already committed releases reconcile before ordinary reviewed rollback. Corrupt candidates do not prevent restoring intact previous artifacts. Private worker storage now rejects overlap with every configured client's release store and linked paths.
- Added Linux systemd service/timer and private environment templates, plus setup/update/recovery instructions. Queue runs continue after an individual item fails and return nonzero for monitoring. The supervisor templates have not been installed or executed on the destination host. Local WSL parsing reached the expected missing `/usr/bin/node` dependency; the selected host's explicit runtime and permissions still require verification.
- Added the hosted client destination/review/job migration, service-only provisioning and worker RPCs, RLS reads and private ownership columns. Reviews expire and recheck workspace/destination versions; claims cannot be transferred on a timer; permission/archive/configuration checks repeat before activation and finalization. Only the verified baseline pointer changes, preserving newer workspace payloads. The project access helper now supports PostgREST JSON JWT claims as well as the earlier per-claim setting.
- `builder-projects` now lists explicit configured destinations, creates publication reviews, starts jobs and projects the verified production snapshot on load. The trusted hosted worker compiles captured data, retrieves checksum-checked private media, stages/activates through the existing release engine and finalizes database evidence. Its CLI can provision a destination only after checking its served baseline and process an assigned queue. Hosted deployment and actual hosted verification remain incomplete; see [hosted publication setup](hosted-client-publication.md).

- Unity local client Releases now connects to the trusted static renderer and Nginx engine. The editor Publish action saves then opens the whole-project destination review. Snapshots and private backups are retained per project, jobs have persistent phases/logs, destination locks prevent concurrent work, and the verified production baseline is projected separately from newer drafts. Staging does not mark production pages published. Missing media stops publication; bounded remote images are limited to Unsplash and the project's explicitly configured public Sanity image path. The same media loader is shared by the local and hosted workers.
- Interrupted local publications now expose explicit reconciliation when their process is proven stopped on the same host. Companion/server recovery guards retain original locks until successful served-output verification and reconcile the corresponding retained snapshot without writing drafts. Only the interrupted job's artifact or recorded prior artifact is accepted. Running, foreign-host and unidentifiable owners remain protected. The client CLI also supports server-only `reconcile`. Corrupt metadata or unrelated configuration requires operator inspection; stopped recovery processes now have the guarded retry path described above. There is no age-based lock reclamation.
- Client release engine now has version-2 artifacts and one-time store bindings for project/destination/environment/origin. It checks all served client files and rejects legacy/cross-client store mixing. The server-only registry/CLI validates unique origins and non-overlapping stores and supports bind, stage, initialise, activate, verify, list, rollback and an unavailable-page unpublication artifact. See [server publication guide](client-publication.md).
- Exported media URLs now include content hashes and client stores retain `/assets/` as well as `/_astro/` files for older open pages. Exports/integrations include `hosting/redirects.json`; publication can generate and verify redirect rules from that reviewed file. A Windows catalogue rename failure found in the full suite led to bounded atomic-rename retries without truncating existing data.

- Local project catalogue with create, rename, duplicate, archive, restore and open actions in Unity navigation.
- Original Kaizen directory registered in place without rewriting its workspace or auxiliary files. New projects use separate workspace/asset/upload/preview directories; duplicated files and references belong to the destination project.
- Request-specific project identity, separate upload scopes, archived-write rejection and project metadata concurrency checks. No global server selection or cross-tab cookie.
- Active project and destination shown in shell/editor. New projects have no destination and cannot use Kaizen's publication or CMS path.
- Clipboard, private preview URLs and upload scopes carry project identity. Client URLs can use About/Contact while the original workspace retains existing-site protections.
- Website exports include `.kaizen/project.zip`, a separate original editable backup, plus versioned format metadata and editability documentation. Missing internal links are listed in the handoff.
- Local companion inspects supported repositories, classifies routes, proposes files, verifies versions/checksums, rejects external changes and competing routes, and applies reviewed changes. Recovery originals are retained before writing. These integration actions execute no Git commands or repository scripts.
- Separate build controls now review and run a local repository's build scripts, with source checks, cancellation/timeouts, previous-output recovery and bounded private static previews. Build success requires checking the served homepage against the frozen output. This is local build verification, not client publication. Inspection and file integration still do not execute scripts.
- Astro integration generates `src/kaizen` renderer modules and owned routes while preserving existing code/package/configuration files. Reopen creates a separate editable project; subsequent edits can be proposed back to the repository.
- Fixed a published mobile menu race discovered during external-browser verification: native link activation/focus now completes before the menu closes.
- Added hosted project catalogue, owner/editor membership and separate publish permission. The new migration isolates workspace, membership and private-preview reads with RLS, confines immutable private assets to project paths, and serializes validated writes with membership/version/archive rechecks. The legacy workspace stays in its original tables; its existing editors are migrated into explicit membership and its release insertion checks publish permission.
- Added JWT-verifying `builder-projects` Edge API and cloud editor/library/backup/private-preview adapters. Duplicates copy private assets into their own project; saved references contain no expiring access tokens. In-flight project responses are rejected if the account changes; asset resolution rechecks storage access. Signed URLs expire after one hour and cannot be immediately revoked once issued; reopening refreshes them. Long-lived editor media refresh still needs refinement.
- Added the compiled `content-panel-v1` registration with a heading and nested content slot. Nested blocks use the existing stable page structure, validation, backup and export paths. Arbitrary code import is not supported.
- Added client settings in each editable workspace with version checks: public site origin, favicon asset reference, public form receiver and explicit public Sanity project/dataset. New clients cannot use the legacy form receiver in editor previews. Duplication resets service connections. Settings backups/repository metadata use version 2, while readers retain version-1 support. Restore review shows the incoming services.
- Exports and Astro integration now generate escaped canonical/Open Graph metadata, favicon links and indexable-page sitemaps/robots. The standalone build regenerates sitemap output from current page data. Existing Astro sitemap/robots source routes create integration conflicts; code-managed sitemap coverage still needs developer integration. Hosting/service guides and redirect snippets are retained alongside integrated code.
- Updated Vite to 7.3.6 and constrained affected esbuild dependencies to 0.28.1 in the builder, Studio and exported package metadata. The export audit exposed Windows development-server file-access advisories; see [Vite advisory](https://github.com/vitejs/vite/security/advisories/GHSA-fx2h-pf6j-xcff) and [esbuild advisory](https://github.com/evanw/esbuild/security/advisories/GHSA-g7r4-m6w7-qqqr). Lockfiles retain the existing dependency versions apart from this targeted update.

## Verification

- Final local acceptance review: the full Unity browser run passed **29 scenarios**, with one optional licensed UI8-pack scenario skipped because its external pack directory was not configured. It covers editor interactions/responsive styles/revisions, shared pages, images/import/search/deduplication, backups, private previews, project settings/isolation, repository round-trip, local builds, redirects, forms and real Nginx publication/recovery. The final affected project/runner browser run passed two scenarios; desktop/mobile project screenshots were inspected. A concurrent unit run exhausted Node memory; the subsequent isolated run exposed the runner restoration race, which was fixed. The final isolated full unit run passed **185 tests across 35 files**. Its orphaned fixture process was identified by exact working directory and creation time and stopped; no unrelated process was targeted.
- Fresh external Astro fixture `kaizen-repository-test-tVS7x3` built four routes, and standalone fixture `kaizen-repository-test-N5Ni7o` installed, typechecked and built two pages. Both passed desktop/mobile navigation, keyboard menu, internal-link, console and private-source exclusion checks. The complete `pnpm build` built the public site and Studio and staged Studio into `dist/studio`. Final typecheck passed for 292 files with zero errors/warnings and 170 hints; both project/publication Edge functions passed Deno checks. Deployment shell syntax, both workflow YAML files and diff whitespace checks passed. Remote CI was not run. Logs: ignored `test-results/acceptance-*`.
- Current root audit: 8 low, 70 moderate, 68 high and 1 critical dependency entries; Studio: 9 low, 57 moderate, 46 high and no critical entries. Neither reports tar findings. Root's remaining critical version-range flag is Astro, with the patched Sharp mitigation and feature-applicability limits recorded above. Other transitive tooling/library findings remain maintenance work; no broad security certification is implied by the functional acceptance checks.
- Client-service handoff slice: **12 affected tests across four files passed** (export, contact, settings and repository). The generated contact guide was inspected in fresh standalone and integrated Astro fixtures outside the repository. Two Unity browser scenarios passed in 32.1 seconds: a form built visually validates, suppresses preview submission, persists one enquiry through a lost acknowledgement and unchanged retry, and retains mobile usability; client settings persist independently and duplication disconnects services. The form success copy confirms receipt; this local receiver test does not establish notification or inbox delivery. No receiving backend or live CMS service was provisioned by this slice. Logs: ignored `test-results/client-service-handoff-*`.
- Recovery-guard slice: **185 tests across 35 files passed**, including existing local/hosted database and real Nginx regressions. New tests terminate actual child processes during recovery three times, then verify successful retry, unchanged original ownership during the operation, concurrent-retry refusal, failed-check retention, wrong-job rejection and live/foreign/ownerless guard protection. The Unity real-Nginx scenario additionally terminates recovery after an actual Nginx reload, observes both companion and server guards, retries through the interface, verifies served content and newer drafts, and checks that the guards clear. That browser scenario passed in a clean 26.8-second run. Typecheck passed for 292 files with zero errors/warnings and 170 hints, final TypeScript checking passed, and the production site built 23 pages. Logs: ignored `test-results/recovery-guards-*`. This is local verification, not a hosted deployment claim.
- Publication-state slice: the full suite passed **181 tests across 34 files**, including isolated real Nginx recovery; the final focused publisher suite passed **14 tests** after adding the concurrent-initialization regression. Fault injection covers truncated JSON, malformed fields, foreign/path-like identities, duplicate jobs, invalid pointers, missing history and damaged/foreign/duplicate-page snapshots. It verifies that failed reads/recovery preserve index/snapshot bytes, selected configuration and original locks, then successfully recovers after restoring valid fixture data. Two Unity browser scenarios passed in 42.9 seconds (history paging/review and real Nginx publish/crash recovery/rollback/unpublish). Astro checking reported zero errors/warnings for 291 files; final TypeScript checking passed after removing an unsupported Error constructor option. The production site built 23 pages. Logs: ignored `test-results/publication-state-*`. Hosted services were not changed or verified by this slice.
- Dependency slice: **178 tests across 34 files passed**, including actual isolated Nginx recovery. Typecheck passed for 291 files (zero errors/warnings, 170 hints); the production site built 23 pages. Four Unity browser scenarios passed in 48.3 seconds: independent projects, repository round-trip, media renewal and real Nginx publication/recovery/rollback/unpublication. Fresh external Astro (`kaizen-repository-test-dIfEZH`, four routes) and standalone (`kaizen-repository-test-JHNjQe`, two pages) fixtures independently installed and built; standalone typecheck passed. Both passed desktop/mobile navigation, keyboard menu, internal links, console and private-source exclusion. The Astro fixture rebuilt and repeated browser verification after applying the esbuild override. Logs: ignored `test-results/dependency-*`.
- The installed Astro image service resolves Sharp 0.35.4/libheif 1.23.2 and successfully encoded, decoded and resized a representative AVIF image. This addresses the underlying dependency described by the [Astro AVIF advisory](https://github.com/withastro/astro/security/advisories/GHSA-26w7-cxv4-gfx2) and [Sharp advisory](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c), while the audit still flags Astro's version range. No advisories were suppressed. The final Astro fixture audit contains one affected package (Astro); standalone audit reports zero. The root audit remains nonzero (8 low, 76 moderate, 75 high, 3 critical dependency entries), including transitive tooling and node-tar, so this is targeted remediation rather than a complete security clearance.
- Remaining Astro advisories have fixes in major 7. Source inspection found no Astro View Transition/ClientRouter usage or custom-HTMLElement spread rendering; production is static with no non-root base or Astro authorization middleware. These observations limit applicability for this checkout, not arbitrary integrated repositories. The [custom-element advisory](https://github.com/withastro/astro/security/advisories/GHSA-f48w-9m4c-m7f5) requires untrusted prop keys and an HTMLElement-capable rendering runtime; the [base-path advisory](https://github.com/withastro/astro/security/advisories/GHSA-376h-93r7-7g6f) requires a non-root base and pathname-based middleware authorization. The requested Astro 6 architecture is retained; future use of those features needs renewed review or a supported patched framework version.
- Client history slice: **178 tests across 34 files passed**, including the real Nginx recovery regression. Actual PostgreSQL tests traverse 125 retained records with equal timestamps, add a newer release between pages, check no omissions/duplicates, keep old active/pending jobs visible, reject unrelated accounts and cross-project cursors, and verify that the public result excludes snapshots and worker ownership fields. The Edge history adapter is additionally tested against this database without a workspace payload. Local keyset paging has matching coverage. Both Edge functions passed Deno checks; typecheck passed for 291 files (zero errors/warnings, 166 hints), with a subsequent TypeScript pass for the final adapter assertions.
- Two affected browser scenarios passed in a clean 34.4-second run: Unity navigates three history pages, retains recovery visibility, blocks rollback until recovery clears, reviews an older retained artifact, and navigates history at mobile width; the existing real client publication/crash-recovery/rollback/unpublication scenario also passes. The history fixture's seeded records test browsing/review, not historical deployment verification. Production site build passed with 23 pages. Hosted polling now avoids reading the full workspace payload. Logs are under ignored `test-results/client-history-*`; remote migration and acceptance remain unverified.
- Hosted media slice: **176 tests across 33 files passed**, including real Nginx recovery regression. Lifecycle tests cover pre-expiry renewal, stable preview polling, fresh download permission checks, unchanged saved/undo references, temporary failure, expired suppression, retry, concurrent renewal and account changes away/back. Mounted React tests verify images, backgrounds, fonts and video update without remounting open controls. Typecheck passed for 290 files with zero errors/warnings and 162 hints. Production site build passed with 23 pages; `git diff --check` passed.
- Eight affected browser scenarios passed in a clean run: hosted media renewal/retry with an accelerated browser clock, Unity interactive blocks, responsive save/reopen, image crop/focal/hover preview, nested asset drag/copy/paste/undo, rich text, private preview access/revocation and real client publication/crash recovery. The hosted-media browser fixture uses the actual scheduler/provider/renderer with mocked Auth/Storage, so it is not hosted acceptance evidence. A focused rerun passed after adding readable mobile retry-notice styling; its 390px screenshot was inspected. Expected private-file denial logs came from the preview security checks.
- Fresh separate fixtures `kaizen-repository-test-BWhAVH` (Astro) and `kaizen-repository-test-16L5sV` (standalone) independently installed and built outside Kaizen with the new renderer context. Standalone typecheck and zero-vulnerability npm audit passed. Both passed desktop/mobile browser navigation, keyboard menu, internal-link, console and private-source exclusion checks; desktop/mobile screenshots were inspected. An initial install into a reused in-repository export fixture hit an npm internal `isDescendantOf` error; verification used the clean external fixtures instead. Logs are under ignored `test-results/hosted-media-*`.
- Hosted recovery slice: **170 tests across 32 files passed**, including 17 hosted publication/database cases. A separate trusted worker process was genuinely terminated after Nginx reload twice, leaving its original worker and activation locks. Recovery verified the candidate in one case and restored the previous site after destination disablement in the other; both checked actual Nginx responses, matching database baselines, lock cleanup and newer-draft preservation. This isolated fixture uses real PGlite SQL and loopback HTTP routing for a declared HTTPS identity; it does not establish hosted Auth/Storage, TLS or DNS verification. Further protocol tests cover running/foreign/mismatched ownership, revoked permissions, corrupt candidates, lost commit/recovery acknowledgements, failed checks and unrelated later publications. CI now runs this crash scenario after installing Nginx.
- Recovery typecheck passed (287 files, zero errors/warnings, 162 hints); production site build passed with 23 pages. The standalone worker renderer loaded under `NODE_ENV=production` and produced four static files plus an editable backup. Both affected Unity browser scenarios passed in a clean 29.9-second run: client publication/crash recovery/rollback/unpublication and two-project asset isolation, including their desktop/mobile checks. No isolated Nginx processes remained. `git diff --check` passed. Logs are under ignored `test-results/hosted-client-recovery-*`.
- Hosted queue/worker implementation: **162 tests across 32 files passed**. Nine new PostgreSQL tests exercise the actual migration with owner/editor/stranger identities and JSON JWT claims: RLS/column restrictions, service-only provisioning, stale review rejection, worker ownership, revocation, immutable snapshots, rollback/unpublication, worker media bundling, newer-draft preservation, verified-output failure and cancellation before claim. These worker cases use Nginx adapters, not a hosted Supabase or live destination. Deno checks passed for `builder-projects` and preserved `builder-publish`; typecheck passed (287 files, zero errors/warnings, 158 hints).
- The actual worker Vite SSR loader ran outside Vitest and outside Astro, producing four static files and a separate editable backup. Its first run exposed unwanted dependency scanning of generated fixture HTML; discovery is now disabled and the worker uses a separate cache. The clean rerun completed without that scan. The worker database test also caught build output placed inside the release store; it now requires a separate private work directory. The Storage download route was checked against the installed Supabase SDK source. Actual hosted Storage downloads remain unverified.
- After the shared media-loader refactor, Unity's real local Nginx publication/crash-recovery/unpublication scenario and project-isolation scenario passed with a clean exit (two tests, 38.1 seconds). This preserves local evidence; it is not evidence of hosted API, Storage or worker deployment.

- Interrupted-publication recovery: **153 tests across 31 files passed**, including six new recovery cases for before/after selection, failed HTTP checks, live/foreign owner protection, a live server activation owner and concurrent recovery attempts. The real Unity/Nginx scenario passed in 34.3 seconds with a clean exit: `interrupt-client-publication.ts` actually terminated its own publisher process after Nginx reloaded, leaving both real locks, then Unity reconciled the selected artifact and preserved newer drafts. The scenario also completed normal publication, failed media, rollback and unpublication. Updated mobile release-history screenshot inspected. The legacy real Nginx verifier passed unchanged activation/redirect/immutable-assets/failure-recovery/exact-rollback checks at `test-results/nginx-release-smoke/run-eHhD4I`. Typecheck passed (286 files, zero errors/warnings, 156 hints); the production site build passed with Astro reporting 23 pages.

- Unity publication slice: **147 tests across 31 files passed**. Typecheck passed (286 files, zero errors/warnings, 156 hints), and production site build passed (Astro reported 23 pages). Three Unity browser scenarios passed with a clean exit in 37.9 seconds: real Nginx publication/failure/rollback/unpublication, independent projects, and repository round-trip. The publication scenario imported an SVG and served two pages at 1440/390 pixels with keyboard mobile navigation and no website console errors, retained newer drafts through all operations and rejected another project's destination access. The first extended run exposed an immediate-operation timing issue and polling that cleared action errors; pending controls and persistent action errors were fixed. A subsequent run passed assertions but exceeded Nginx's graceful shutdown wait; the isolated fixture now bounds idle connections and worker shutdown. Final isolated Nginx exited cleanly. Release UI and site screenshots were visually inspected at both widths under `test-results/unity-{client-releases,published-client}-{1440,390}.png`.
- Seven new focused compiler/publisher tests cover frozen snapshots, failed media, trusted form endpoint rendering, stale review rejection, destination locks, persisted baseline after restart, staging separation and restoring the previous artifact after a served-output mismatch. The class tests use Nginx adapters; only the browser scenario proves actual Nginx output for this editor path. Automatic crash recovery and external-CLI reconciliation are not established by a successful clean restart.
- Fresh independent fixtures `kaizen-repository-test-7lTVD5` (Astro) and `kaizen-repository-test-9OVnRU` (standalone) installed and built outside Kaizen after adding the form endpoint context to exported renderer modules. Standalone typecheck passed. Both passed desktop/mobile navigation, keyboard menu, internal links, console and private-source exclusion with clean process exits. Standalone npm audit reported zero vulnerabilities; the Astro 6.0.4 fixture still reports three affected dependency entries (Astro critical, sharp high, esbuild low). Maintainer advisories confirm the AVIF issue is in Sharp/libheif when processing untrusted AVIF images; remediation and remaining Astro advisory applicability need a separate verified change while preserving the requested Astro 6 architecture. See [Astro advisory](https://github.com/withastro/astro/security/advisories/GHSA-26w7-cxv4-gfx2) and [Sharp advisory](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c).

- Client release slice: **140 tests across 29 files passed** after fixing the observed Windows catalogue rename failure. Typecheck passed (280 files, zero errors/warnings, 155 hints), `tsc --noEmit` passed again after the final changes, and the production site build passed with 23 routes. Unity's independent-project and repository round-trip scenarios passed with a clean exit (two tests, 27 seconds). The original Kaizen release verifier also passed against real isolated Nginx, including activation and failure recovery. `git diff --check` passed.
- Real Nginx client test passed against three loopback destinations: Alpha staging, Alpha production and Beta production. It verified the registry/CLI stage/list/live-check path (including explicit redirect JSON), target isolation, every-file response hashes, redirects/query preservation, recovery from a wrong CSS response, unpublication, exact rollback and desktop/mobile browser navigation with no console errors. Both client and environment identity are in the served marker. The final clean run is `test-results/client-nginx-releases/run-LDMnLo`; desktop/mobile screenshots were inspected and the isolated Nginx processes exited. This is local server evidence; it does not prove hosted membership or preservation of newer drafts through an editor release queue that is not yet connected.
- A fresh standalone media export outside Kaizen (`kaizen-client-media-6639e0ec-0ddd-446e-855d-69d193eca669` in OS temp) independently installed, typechecked and built four pages. Its npm audit reported zero vulnerabilities. Browser checks passed media, mobile layout, keyboard tabs/menu, accordion, video/captions, test receiver acknowledgement, shared content and snapshot CMS output. Nginx then verified all 13 retained files and 15 HTTP responses including its redirect aliases. The test receiver is isolated and does not establish production form notification delivery. Nginx artifacts are under ignored `test-results/client-nginx-releases/`; final run paths are recorded by the verifier.

- Local runner slice: **135 tests across 28 files passed**, including real command execution, project-scoped job access, concurrent build rejection, source-change refusal, failed/empty output recovery, cancellation, timeout and linked-output protection. Typecheck passed for 279 files (zero errors/warnings, 153 hints); production site build passed with 23 routes. Unity review/build/reload/preview/stop passed at desktop/mobile sizes and from a localhost editor to the 127.0.0.1 preview. The test initially found mobile path overflow, now fixed. The repository integration browser scenario also passed. `git diff --check` passed.
- Both independently installed client fixtures (`kaizen-repository-test-msJSuk` and `kaizen-repository-test-ExvkmQ`) were built again through the actual pnpm runner and passed private-preview desktop/mobile navigation, keyboard menu, canonical, overflow and console checks. The final verification process exited cleanly. An earlier Edge run finished its assertions but hung removing its temporary browser profile; the verifier now retains a uniquely named OS-temp test profile and closes the browser cleanly. Screenshots: `test-results/repository-build-{1440,390}.png` and `test-results/runner-client-{0,1}-{1440,390}.png`; desktop/mobile layouts inspected.
- Client settings slice: **130 tests across 27 files passed**; typecheck passed for 277 files (zero errors/warnings, 153 hints); `pnpm build:site` passed with 23 routes. Both Edge functions passed Deno checking. Unity settings/repository checks passed, followed by a clean settings/legacy-backup browser run. Desktop/mobile settings screenshots were inspected. Fresh independent Astro and standalone fixtures (`kaizen-repository-test-msJSuk` and `kaizen-repository-test-ExvkmQ`) installed, built and passed desktop/mobile navigation, console, canonical and sitemap checks. The standalone npm audit was clean; the Astro 6.0.4 fixture reported three affected dependency entries, which still require applicability/version triage.

- `pnpm test -- --maxWorkers=2`: 113 tests passed across 23 files. An earlier unrestricted run exhausted Windows resources; Vitest now defaults to two workers. Focused project/repository/export suites passed again after the runtime fix.
- Subsequent full suite: 120 tests across 25 files passed. Additional focused tests then passed for legacy publish permission, private previews, direct-table mutation denial, malformed workspace rejection, account changes and private-media access rechecks. These are local PostgreSQL/client tests, not hosted acceptance.
- Latest full suite after the dependency changes: **127 tests across 26 files passed**. `pnpm typecheck` passed (273 files, zero errors/warnings, 153 hints), both project/publish functions passed Deno checking, and full `pnpm build` passed (site, Studio and staging Studio files). Existing chunk-size and Studio `process` externalization notices remain.
- Full Unity browser attempt: 20 passed, three failed and one optional real-pack test skipped. Clean focused reruns passed responsive/undo/redo/revision restoration and rich text. The lost-upload and delayed-conversion test matchers were corrected to include scoped API queries. Registered slots exposed and fixed a structural Puck field-map crash and compatibility with empty children on existing registrations. Both registered-card publication and nested-panel edit/save/reopen/preview then passed with clean process exits. These results are recorded separately rather than claiming a single uninterrupted full-suite pass.
- A fresh export outside the repository (path recorded in ignored `test-results/slot-export-fixture-path.txt`) independently installed, typechecked, built four static pages and passed browser checks for media, mobile layout, keyboard tabs/menu, accordion/video/captions, form acknowledgement, shared content, nested registered content and snapshot CMS output. Its npm dependency audit reported **zero vulnerabilities** after the targeted update. This does not imply the entire parent repository has a clean dependency audit.
- After the tooling update, four targeted Unity browser checks passed with a clean exit: private previews and raw/Windows-alternate-path file denial, independent client projects, nested registered content, and repository review/apply/reopen/edit. Private-path denial messages in that test's server log are expected. CI now also builds and browses the independent Astro/export repository fixtures; that workflow has not been run remotely.
- `pnpm typecheck`: 267 files, zero errors/warnings, 153 hints. Full `pnpm build` (site, Studio and staging Studio files) passed. After the mobile-runtime/favicon refinement, `tsc --noEmit` and `pnpm build:site` passed again (23 routes).
- Unity browser scenarios passed for two independent projects/tabs and for inspect → proposal → apply → reopen → visual edit → save/reopen. Screenshots inspected at desktop/mobile sizes. Windows browser-server teardown needed interruption; do not present those runs as clean process exits. Later dependency reoptimization also reported Windows resource exhaustion while other builds ran.
- Both fixtures were generated, separately installed and built outside the Kaizen repository under the OS temporary directory. Standalone export typecheck passed; the Astro fixture built About/Contact plus the preserved existing page and generated root. Fixture paths are in ignored `test-results/*-fixture-path.txt` files.
- External desktop/mobile browser verification found and drove the mobile menu fix. Fresh separately installed/built fixtures (`kaizen-repository-test-A6ugbp` and `kaizen-repository-test-enCzF1` in the OS temp directory) then passed desktop/mobile navigation, keyboard menu activation, internal-link checks, console checks and private-source exclusion. Screenshots are `test-results/client-repository-{0,1}-{1440,390}.png`. No hosted isolation or client deployment success has been claimed.

## Historical acceptance before production access was supplied

1. **Separate projects — locally verified; hosted acceptance pending.** Dashboard actions and two populated projects pass browser checks. Project-scoped workspace/asset paths, selected-project exports/backups and in-place migration are covered by project/storage tests. Hosted JWT, membership, publish permission and private Storage rules are implemented and tested in the local database, not a live Supabase deployment.
2. **Unity editing — locally verified.** The complete browser suite exercises drag/drop, nested and registered content, inline editing, layers/reordering, duplication/copy/paste, undo/redo, autosave/reopen, revision restoration, templates, shared headers/footers, responsive overrides and private previews. Inherited values/reset controls are implemented in ResponsiveField; the site's rendered theme remains scoped separately from Unity.
3. **Export and handoff — locally verified.** Fresh separately installed/built websites pass browser checks outside Kaizen. Exports contain routes, assets, renderer/runtime, service/hosting/link warnings and a separate private editable backup. Generated versus editable files and environment/configuration boundaries are documented in the included guides.
4. **Local repository integration — locally verified.** Inspection, framework/route ownership boundaries, reviewed proposals, conflict checks, preserved unrelated code and real build/preview execution are covered by repository/runner tests and Unity scenarios. The registered content-panel contract demonstrates editable properties and nested content. The companion performs no Git writes.
5. **Editability after handoff — locally verified within the documented format.** Versioned .kaizen source/backup/ownership data survives reopening and another visual edit; external edits and unsupported versions stop integration. Arbitrary source round-trip is explicitly unsupported.
6. **Publication and deployment — local/staging behavior verified; remote setup pending.** Real isolated Nginx verifies destination isolation, draft/live separation, activation, failure/recovery (including a recovery crash), rollback and unpublishing. CI/workers/supervisor/configuration templates are implemented, but remote CI, DNS/TLS, host permissions and actual production/staging deployment require the selected targets and credentials.
7. **Client essentials — locally verified; chosen services pending.** Browser/export checks cover navigation, shared layouts, SEO/sitemap, redirects, responsive media and keyboard mobile navigation. Forms distinguish receipt from notification delivery; CMS exports are explicit public-Sanity snapshots. The actual client receiver, recipient delivery and client CMS dataset have not been verified.
8. **Completion gate — not achieved.** Isolated unit/database/browser/type/build evidence is recorded above. The first-client delivery workflow is documented in builder-client-projects.md and the publication guides. Required live hosted isolation and destination/service acceptance remain unproven, so the goal is not complete.

## Historical missing prerequisites (superseded by current status above)

- Select and authenticate the Supabase deployment and client staging destination, then apply the migrations/functions and run `tests/builder/verify-hosted-projects.mjs` with real temporary unrelated users. Missing prerequisites were rechecked; the verifier exits before network activity without its three credentials.
- Provision the selected destination and install/configure the hosted worker and supervisor. Verify queueing, permissions, private media, served output, failure/recovery and rollback end to end on that actual host. Preserve newer drafts and verify DNS/TLS and staging/production separation there.
- Configure the chosen client receiver and public CMS dataset, verify durable receipt and notification delivery separately, and record the real service results in the handoff. Private CMS datasets require a separately scoped server integration if selected.
- Keep the nonzero dependency audit visible during deployment review. Functional checks do not establish that every transitive dependency advisory is inapplicable.

## Historical external-dependency checks

The same missing live configuration persisted through three consecutive goal turns. The final recheck found no required Supabase credentials, access token, VPS host or destination registry in process, user or machine environment settings, and none of the checked local environment files exists. No target details or credential-setup response has arrived. The goal is marked blocked rather than complete; resume when the selected targets and private access are available.

No local `.env`, `.env.local`, `.env.production` or `.env.stage` exists. The acceptance review rechecked the Supabase credentials, VPS target and client worker/destination-registry environment names without printing values; none were configured. The checked example files contain placeholders. The earlier `pnpm dlx supabase projects list --output json` call failed with “Access token not provided”; the current hosted verifier fails its prerequisite check before network activity. Hosted migration/function deployment requires authenticated Supabase CLI access (login or `SUPABASE_ACCESS_TOKEN`) and an explicitly selected target project. Live acceptance additionally requires `SUPABASE_URL`, `SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` supplied privately. The user has been asked for the project reference and staging destination/host and to configure credentials privately. No remote deployment or hosted verification has been claimed; the goal remains active pending that configuration.
