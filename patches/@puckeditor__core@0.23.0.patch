diff --git a/dist/chunk-55V3NZVF.mjs b/dist/chunk-55V3NZVF.mjs
index 01fc32e03adc11d014291a99cc8dbee1806880f8..5acb9e71e4b11da0acbb73b46225ef7bdd48bb80 100644
--- a/dist/chunk-55V3NZVF.mjs
+++ b/dist/chunk-55V3NZVF.mjs
@@ -411,7 +411,7 @@ import { PointerActivationConstraints as PointerActivationConstraints2 } from "@
 // lib/dnd/use-sensors.ts
 init_react_import();
 import { useState as useState3 } from "react";
-import { PointerSensor } from "@dnd-kit/react";
+import { PointerSensor, KeyboardSensor } from "@dnd-kit/react";
 import { PointerActivationConstraints } from "@dnd-kit/dom";
 import { isElement } from "@dnd-kit/dom/utilities";
 var { Delay, Distance } = PointerActivationConstraints;
@@ -431,6 +431,7 @@ var useSensors = ({
   other: otherDefault
 }) => {
   const [sensors] = useState3(() => [
+    KeyboardSensor,
     PointerSensor.configure({
       activationConstraints(event, source) {
         var _a;
@@ -1517,7 +1518,7 @@ init_react_import();
 var styles_module_default5 = { "Modal": "_Modal_g5xob_1", "Modal--isOpen": "_Modal--isOpen_g5xob_15", "Modal-inner": "_Modal-inner_g5xob_19" };
 
 // components/Modal/index.tsx
-import { createPortal } from "react-dom";
+import { createPortal, flushSync as flushNestedTarget } from "react-dom";
 import { jsx as jsx10 } from "react/jsx-runtime";
 var getClassName6 = get_class_name_factory_default("Modal", styles_module_default5);
 var Modal = ({
@@ -2936,6 +2937,25 @@ var DraggableComponent = ({
   const ref = useRef2(null);
   const refSetter = useCallback8(
     (el) => {
+      // Canvas containers can contain links, inputs and nested blocks.
+      // A labelled group preserves those descendants in the accessibility tree.
+      if (el && el.tagName === "DIV" && !el.hasAttribute("role")) {
+        el.setAttribute("role", "group");
+        el.setAttribute("aria-label", `${label || componentType} block`);
+        el.setAttribute("aria-roledescription", "movable block");
+        el.setAttribute("aria-keyshortcuts", "Enter Space");
+        // aria-describedby must resolve in the canvas document, not its parent.
+        const descriptionId = "puck-canvas-keyboard-instructions";
+        const doc = el.ownerDocument;
+        if (!doc.getElementById(descriptionId)) {
+          const instructions = doc.createElement("p");
+          instructions.id = descriptionId;
+          instructions.hidden = true;
+          instructions.textContent = "Press Enter to select this block. Press Space to pick it up, arrow keys to move it, Space to drop it or Escape to cancel.";
+          doc.body.appendChild(instructions);
+        }
+        el.setAttribute("aria-describedby", descriptionId);
+      }
       sortableRef(el);
       if (ref.current !== el) {
         ref.current = el;
@@ -2945,7 +2965,7 @@ var DraggableComponent = ({
         }
       }
     },
-    [itemRef, sortableRef]
+    [itemRef, sortableRef, label, componentType]
   );
   const [portalEl, setPortalEl] = useState9();
   useEffect8(() => {
@@ -3146,12 +3166,20 @@ var DraggableComponent = ({
     el.setAttribute("data-puck-component", id);
     el.setAttribute("data-puck-dnd", id);
     el.style.position = "relative";
+    const onKeyboardSelect = (event) => {
+      if (event.target === el && event.key === "Enter") {
+        event.preventDefault();
+        onClick(event);
+      }
+    };
+    el.addEventListener("keydown", onKeyboardSelect, true);
     el.addEventListener("click", onClick);
     el.addEventListener("mouseover", _onMouseOver);
     el.addEventListener("mouseout", _onMouseOut);
     return () => {
       el.removeAttribute("data-puck-component");
       el.removeAttribute("data-puck-dnd");
+      el.removeEventListener("keydown", onKeyboardSelect, true);
       el.removeEventListener("click", onClick);
       el.removeEventListener("mouseover", _onMouseOver);
       el.removeEventListener("mouseout", _onMouseOut);
@@ -3700,10 +3728,9 @@ var createNestedDroppablePlugin = ({ onChange }, id) => class NestedDroppablePlu
           onChange(findDeepestCandidate(position, manager), manager);
         }
       };
-      const handleMoveThrottled = throttle(handleMove, 50);
-      const handlePointerMove = (event) => {
-        handleMoveThrottled(event);
-      };
+      // Resolve the current nested target before pointerup can commit a drop.
+      // A delayed callback can otherwise commit the preceding ancestor zone.
+      const handlePointerMove = handleMove;
       document.body.addEventListener("pointermove", handlePointerMove, {
         capture: true
         // dndkit's PointerSensor prevents propagation during drag
@@ -4281,14 +4308,13 @@ var DragDropContextClient = ({
     (params, manager) => {
       const { zoneChanged, areaChanged } = getChanged(params);
       if (!zoneChanged && !areaChanged) return;
-      zoneStore.setState({
+      // Enable the destination's droppables before resolving this pointer event.
+      flushNestedTarget(() => zoneStore.setState({
         zoneDepthIndex: params.zone ? { [params.zone]: true } : {},
         areaDepthIndex: params.area ? { [params.area]: true } : {}
-      });
+      }));
       tempDisableFallback(manager);
-      setTimeout(() => {
-        manager.collisionObserver.forceUpdate(true);
-      }, 50);
+      manager.collisionObserver.forceUpdate(true);
       debouncedParamsRef.current = null;
     },
     [zoneStore]
@@ -4339,18 +4365,8 @@ var DragDropContextClient = ({
             return;
           }
           if (areaChanged) {
-            if (isDragging) {
-              const debouncedParams = debouncedParamsRef.current;
-              const isSameParams = debouncedParams && debouncedParams.area === params.area && debouncedParams.zone === params.zone;
-              if (!isSameParams) {
-                cancelDb();
-                setDeepestDb(params, manager);
-                debouncedParamsRef.current = params;
-              }
-            } else {
-              cancelDb();
-              setDeepestAndCollide(params, manager);
-            }
+            cancelDb();
+            setDeepestAndCollide(params, manager);
             return;
           }
           if (zoneChanged) {
@@ -4398,7 +4414,8 @@ var DragDropContextClient = ({
             stopLinePlaceholderScrollTracking();
             const entryEl = (_a = getFrame()) == null ? void 0 : _a.querySelector("[data-puck-entry]");
             entryEl == null ? void 0 : entryEl.removeAttribute("data-puck-dragging");
-            const { source, target } = event.operation;
+            const { source } = event.operation;
+            let { target } = event.operation;
             if (!source) {
               setLinePlaceholderActive(false);
               zoneStore.setState({ draggedItem: null });
@@ -4406,9 +4423,32 @@ var DragDropContextClient = ({
             }
             const { zone, index } = source.data;
             const { previewIndex = {} } = zoneStore.getState() || {};
-            const thisPreview = (_b = Object.values(previewIndex).find(
+            let thisPreview = (_b = Object.values(previewIndex).find(
               (preview) => (preview == null ? void 0 : preview.props.id) === source.id && !preview.ghost
             )) != null ? _b : null;
+            // A release can precede the asynchronous drag-over preview update.
+            // Commit using the actual final pointer, with the same nesting/allow checks.
+            const finalFrame = document.querySelector("iframe#preview-frame");
+            const finalPointer = manager.dragOperation.position.current;
+            if (finalFrame && finalPointer) {
+              const bounds = finalFrame.getBoundingClientRect();
+              const insideCanvas = finalPointer.x >= bounds.left && finalPointer.x <= bounds.right && finalPointer.y >= bounds.top && finalPointer.y <= bounds.bottom;
+              if (!insideCanvas) {
+                // Custom editor sidebars are outside the canvas, never a root drop.
+                target = { type: "void" };
+              } else if (thisPreview && !event.canceled) {
+                const finalLocation = findDeepestCandidate(new GlobalPosition(document.body, finalPointer), manager);
+                const finalZone = finalLocation.zone;
+                const zoneElement = finalZone && finalZone !== "void" ? getFrame()?.querySelector(getZoneSelector(finalZone)) : null;
+                if (zoneElement) {
+                  const finalIndex = getNearestGapIndex(zoneElement, getFramePointer(zoneElement, finalPointer), getZoneContentIds(appStore.getState().state, finalZone));
+                  if (finalIndex !== null) {
+                    thisPreview = { ...thisPreview, zone: finalZone, index: finalIndex, linePlaceholder: true };
+                    target = manager.registry.droppables.get(finalZone) || target;
+                  }
+                }
+              }
+            }
             const playCommitFlip = !event.canceled && (target == null ? void 0 : target.type) !== "void" && (thisPreview == null ? void 0 : thisPreview.linePlaceholder) ? prepareCommitFlip({
               zones: initialSelector.current ? [initialSelector.current.zone, thisPreview.zone] : [thisPreview.zone],
               itemId: thisPreview.type === "move" ? thisPreview.props.id : void 0,
@@ -4479,6 +4519,13 @@ var DragDropContextClient = ({
           },
           onDragMove: (event, manager) => {
             var _a;
+            // Keyboard movement has no pointermove event to enable nested zones.
+            // Resolve the same destination before dnd-kit applies this movement.
+            if (event.nativeEvent?.type === "keydown") {
+              const current = manager.dragOperation.position.current;
+              const position = event.to || { x: current.x + (event.by?.x || 0), y: current.y + (event.by?.y || 0) };
+              setDeepestAndCollide(findDeepestCandidate(new GlobalPosition(document.body, position), manager), manager);
+            }
             updateLinePlaceholder(manager);
             (_a = dragListeners.dragmove) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -7237,6 +7284,8 @@ var Layer = forwardRef2(function Layer2({
           {
             className: getClassName24("inner"),
             ref: rowRef,
+            role: "group",
+            "aria-label": `${node.label} layer`,
             onMouseEnter: (e) => {
               e.stopPropagation();
               if (outlineStore.getState().status !== "idle") {
@@ -11878,6 +11927,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         AutoFrame_default,
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName35("frame"),
           "data-rfd-iframe": true,
           syncHostStyles: iframe.syncHostStyles,
@@ -11899,6 +11949,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         "div",
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName35("frame"),
           ref,
           "data-puck-entry": true,
diff --git a/dist/chunk-K2LNXU54.mjs b/dist/chunk-K2LNXU54.mjs
index 81075c39650a79a9eccf7cefffdc7a65cbd00c97..c87639f345eba8ca5b33c580daffa3e9c5f5ef7f 100644
--- a/dist/chunk-K2LNXU54.mjs
+++ b/dist/chunk-K2LNXU54.mjs
@@ -800,13 +800,22 @@ var useHotkey = (combo, cb) => {
 // store/slices/history.ts
 var EMPTY_HISTORY_INDEX = 0;
 function debounce(func, timeout = 300) {
-  let timer;
-  return (...args) => {
+  let timer, pending;
+  const flush = () => {
     clearTimeout(timer);
-    timer = setTimeout(() => {
-      func(...args);
-    }, timeout);
+    if (!pending) return;
+    const args = pending;
+    pending = undefined;
+    func(...args);
   };
+  const deferred = (...args) => {
+    clearTimeout(timer);
+    pending = args;
+    timer = setTimeout(flush, timeout);
+  };
+  deferred.flush = flush;
+  deferred.cancel = () => { clearTimeout(timer); pending = undefined; };
+  return deferred;
 }
 var tidyState = (state) => {
   return __spreadProps(__spreadValues({}, state), {
@@ -818,7 +827,7 @@ var tidyState = (state) => {
   });
 };
 var createHistorySlice = (set, get) => {
-  const record = debounce((state) => {
+  const commit = (state) => {
     const { histories, index } = get().history;
     const history = {
       state,
@@ -831,7 +840,13 @@ var createHistorySlice = (set, get) => {
         index: newHistories.length - 1
       })
     });
-  }, 250);
+  };
+  const deferredRecord = debounce(commit, 250);
+  const record = (state) => {
+    if (state.ui.field.focus?.startsWith("builder-style:")) {
+      queueMicrotask(() => { deferredRecord.flush(); commit(state); });
+    } else deferredRecord(state);
+  };
   return {
     initialAppState: {},
     index: EMPTY_HISTORY_INDEX,
@@ -848,6 +863,7 @@ var createHistorySlice = (set, get) => {
     },
     currentHistory: () => get().history.histories[get().history.index],
     back: () => {
+      deferredRecord.flush();
       var _a;
       const { history, dispatch } = get();
       if (history.hasPast()) {
@@ -862,6 +878,7 @@ var createHistorySlice = (set, get) => {
       }
     },
     forward: () => {
+      deferredRecord.flush();
       var _a;
       const { history, dispatch } = get();
       if (history.hasFuture()) {
@@ -871,6 +888,7 @@ var createHistorySlice = (set, get) => {
       }
     },
     setHistories: (histories) => {
+      deferredRecord.cancel();
       var _a;
       const { dispatch, history } = get();
       dispatch({
@@ -880,6 +898,7 @@ var createHistorySlice = (set, get) => {
       set({ history: __spreadProps(__spreadValues({}, history), { histories, index: histories.length - 1 }) });
     },
     setHistoryIndex: (index) => {
+      deferredRecord.cancel();
       var _a;
       const { dispatch, history } = get();
       dispatch({
diff --git a/dist/index.js b/dist/index.js
index aebc9c0e4d4eed1a407ea72d0d1469d16544d834..12a4bfa8efd20a29abd1ec39c4e1f006d31b2241 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -2785,14 +2785,23 @@ var init_use_hotkey = __esm({
 });
 
 // store/slices/history.ts
-function debounce(func, timeout3 = 300) {
-  let timer;
-  return (...args) => {
+function debounce(func, timeout = 300) {
+  let timer, pending;
+  const flush = () => {
     clearTimeout(timer);
-    timer = setTimeout(() => {
-      func(...args);
-    }, timeout3);
+    if (!pending) return;
+    const args = pending;
+    pending = undefined;
+    func(...args);
+  };
+  const deferred = (...args) => {
+    clearTimeout(timer);
+    pending = args;
+    timer = setTimeout(flush, timeout);
   };
+  deferred.flush = flush;
+  deferred.cancel = () => { clearTimeout(timer); pending = undefined; };
+  return deferred;
 }
 function useRegisterHistorySlice(appStore, {
   histories,
@@ -2841,7 +2850,7 @@ var init_history = __esm({
       });
     };
     createHistorySlice = (set, get) => {
-      const record = debounce((state) => {
+      const commit = (state) => {
         const { histories, index } = get().history;
         const history = {
           state,
@@ -2854,7 +2863,13 @@ var init_history = __esm({
             index: newHistories.length - 1
           })
         });
-      }, 250);
+      };
+      const deferredRecord = debounce(commit, 250);
+      const record = (state) => {
+        if (state.ui.field.focus?.startsWith("builder-style:")) {
+          queueMicrotask(() => { deferredRecord.flush(); commit(state); });
+        } else deferredRecord(state);
+      };
       return {
         initialAppState: {},
         index: EMPTY_HISTORY_INDEX,
@@ -2871,6 +2886,7 @@ var init_history = __esm({
         },
         currentHistory: () => get().history.histories[get().history.index],
         back: () => {
+          deferredRecord.flush();
           var _a;
           const { history, dispatch } = get();
           if (history.hasPast()) {
@@ -2885,6 +2901,7 @@ var init_history = __esm({
           }
         },
         forward: () => {
+          deferredRecord.flush();
           var _a;
           const { history, dispatch } = get();
           if (history.hasFuture()) {
@@ -2894,6 +2911,7 @@ var init_history = __esm({
           }
         },
         setHistories: (histories) => {
+          deferredRecord.cancel();
           var _a;
           const { dispatch, history } = get();
           dispatch({
@@ -2903,6 +2921,7 @@ var init_history = __esm({
           set({ history: __spreadProps(__spreadValues({}, history), { histories, index: histories.length - 1 }) });
         },
         setHistoryIndex: (index) => {
+          deferredRecord.cancel();
           var _a;
           const { dispatch, history } = get();
           dispatch({
@@ -6133,6 +6152,7 @@ var useSensors = ({
   other: otherDefault
 }) => {
   const [sensors] = (0, import_react13.useState)(() => [
+    import_react14.KeyboardSensor,
     import_react14.PointerSensor.configure({
       activationConstraints(event, source) {
         var _a;
@@ -8864,6 +8884,25 @@ var DraggableComponent = ({
   const ref = (0, import_react53.useRef)(null);
   const refSetter = (0, import_react53.useCallback)(
     (el) => {
+      // Canvas containers can contain links, inputs and nested blocks.
+      // A labelled group preserves those descendants in the accessibility tree.
+      if (el && el.tagName === "DIV" && !el.hasAttribute("role")) {
+        el.setAttribute("role", "group");
+        el.setAttribute("aria-label", `${label || componentType} block`);
+        el.setAttribute("aria-roledescription", "movable block");
+        el.setAttribute("aria-keyshortcuts", "Enter Space");
+        // aria-describedby must resolve in the canvas document, not its parent.
+        const descriptionId = "puck-canvas-keyboard-instructions";
+        const doc = el.ownerDocument;
+        if (!doc.getElementById(descriptionId)) {
+          const instructions = doc.createElement("p");
+          instructions.id = descriptionId;
+          instructions.hidden = true;
+          instructions.textContent = "Press Enter to select this block. Press Space to pick it up, arrow keys to move it, Space to drop it or Escape to cancel.";
+          doc.body.appendChild(instructions);
+        }
+        el.setAttribute("aria-describedby", descriptionId);
+      }
       sortableRef(el);
       if (ref.current !== el) {
         ref.current = el;
@@ -8873,7 +8912,7 @@ var DraggableComponent = ({
         }
       }
     },
-    [itemRef, sortableRef]
+    [itemRef, sortableRef, label, componentType]
   );
   const [portalEl, setPortalEl] = (0, import_react53.useState)();
   (0, import_react53.useEffect)(() => {
@@ -9074,12 +9113,20 @@ var DraggableComponent = ({
     el.setAttribute("data-puck-component", id);
     el.setAttribute("data-puck-dnd", id);
     el.style.position = "relative";
+    const onKeyboardSelect = (event) => {
+      if (event.target === el && event.key === "Enter") {
+        event.preventDefault();
+        onClick(event);
+      }
+    };
+    el.addEventListener("keydown", onKeyboardSelect, true);
     el.addEventListener("click", onClick);
     el.addEventListener("mouseover", _onMouseOver);
     el.addEventListener("mouseout", _onMouseOut);
     return () => {
       el.removeAttribute("data-puck-component");
       el.removeAttribute("data-puck-dnd");
+      el.removeEventListener("keydown", onKeyboardSelect, true);
       el.removeEventListener("click", onClick);
       el.removeEventListener("mouseover", _onMouseOver);
       el.removeEventListener("mouseout", _onMouseOut);
@@ -11383,10 +11430,9 @@ var createNestedDroppablePlugin = ({ onChange }, id) => class NestedDroppablePlu
           onChange(findDeepestCandidate(position, manager), manager);
         }
       };
-      const handleMoveThrottled = throttle(handleMove, 50);
-      const handlePointerMove = (event) => {
-        handleMoveThrottled(event);
-      };
+      // Resolve the current nested target before pointerup can commit a drop.
+      // A delayed callback can otherwise commit the preceding ancestor zone.
+      const handlePointerMove = handleMove;
       document.body.addEventListener("pointermove", handlePointerMove, {
         capture: true
         // dndkit's PointerSensor prevents propagation during drag
@@ -11851,14 +11897,13 @@ var DragDropContextClient = ({
     (params, manager) => {
       const { zoneChanged, areaChanged } = getChanged2(params);
       if (!zoneChanged && !areaChanged) return;
-      zoneStore.setState({
+      // Enable the destination's droppables before resolving this pointer event.
+      import_react_dom.flushSync(() => zoneStore.setState({
         zoneDepthIndex: params.zone ? { [params.zone]: true } : {},
         areaDepthIndex: params.area ? { [params.area]: true } : {}
-      });
+      }));
       tempDisableFallback(manager);
-      setTimeout(() => {
-        manager.collisionObserver.forceUpdate(true);
-      }, 50);
+      manager.collisionObserver.forceUpdate(true);
       debouncedParamsRef.current = null;
     },
     [zoneStore]
@@ -11909,18 +11954,8 @@ var DragDropContextClient = ({
             return;
           }
           if (areaChanged) {
-            if (isDragging) {
-              const debouncedParams = debouncedParamsRef.current;
-              const isSameParams = debouncedParams && debouncedParams.area === params.area && debouncedParams.zone === params.zone;
-              if (!isSameParams) {
-                cancelDb();
-                setDeepestDb(params, manager);
-                debouncedParamsRef.current = params;
-              }
-            } else {
-              cancelDb();
-              setDeepestAndCollide(params, manager);
-            }
+            cancelDb();
+            setDeepestAndCollide(params, manager);
             return;
           }
           if (zoneChanged) {
@@ -11968,7 +12003,8 @@ var DragDropContextClient = ({
             stopLinePlaceholderScrollTracking();
             const entryEl = (_a = getFrame()) == null ? void 0 : _a.querySelector("[data-puck-entry]");
             entryEl == null ? void 0 : entryEl.removeAttribute("data-puck-dragging");
-            const { source, target } = event.operation;
+            const { source } = event.operation;
+            let { target } = event.operation;
             if (!source) {
               setLinePlaceholderActive(false);
               zoneStore.setState({ draggedItem: null });
@@ -11976,9 +12012,32 @@ var DragDropContextClient = ({
             }
             const { zone, index } = source.data;
             const { previewIndex = {} } = zoneStore.getState() || {};
-            const thisPreview = (_b = Object.values(previewIndex).find(
+            let thisPreview = (_b = Object.values(previewIndex).find(
               (preview) => (preview == null ? void 0 : preview.props.id) === source.id && !preview.ghost
             )) != null ? _b : null;
+            // A release can precede the asynchronous drag-over preview update.
+            // Commit using the actual final pointer, with the same nesting/allow checks.
+            const finalFrame = document.querySelector("iframe#preview-frame");
+            const finalPointer = manager.dragOperation.position.current;
+            if (finalFrame && finalPointer) {
+              const bounds = finalFrame.getBoundingClientRect();
+              const insideCanvas = finalPointer.x >= bounds.left && finalPointer.x <= bounds.right && finalPointer.y >= bounds.top && finalPointer.y <= bounds.bottom;
+              if (!insideCanvas) {
+                // Custom editor sidebars are outside the canvas, never a root drop.
+                target = { type: "void" };
+              } else if (thisPreview && !event.canceled) {
+                const finalLocation = findDeepestCandidate(new GlobalPosition(document.body, finalPointer), manager);
+                const finalZone = finalLocation.zone;
+                const zoneElement = finalZone && finalZone !== "void" ? getFrame()?.querySelector(getZoneSelector(finalZone)) : null;
+                if (zoneElement) {
+                  const finalIndex = getNearestGapIndex(zoneElement, getFramePointer(zoneElement, finalPointer), getZoneContentIds(appStore.getState().state, finalZone));
+                  if (finalIndex !== null) {
+                    thisPreview = { ...thisPreview, zone: finalZone, index: finalIndex, linePlaceholder: true };
+                    target = manager.registry.droppables.get(finalZone) || target;
+                  }
+                }
+              }
+            }
             const playCommitFlip = !event.canceled && (target == null ? void 0 : target.type) !== "void" && (thisPreview == null ? void 0 : thisPreview.linePlaceholder) ? prepareCommitFlip({
               zones: initialSelector.current ? [initialSelector.current.zone, thisPreview.zone] : [thisPreview.zone],
               itemId: thisPreview.type === "move" ? thisPreview.props.id : void 0,
@@ -12049,6 +12108,13 @@ var DragDropContextClient = ({
           },
           onDragMove: (event, manager) => {
             var _a;
+            // Keyboard movement has no pointermove event to enable nested zones.
+            // Resolve the same destination before dnd-kit applies this movement.
+            if (event.nativeEvent?.type === "keydown") {
+              const current = manager.dragOperation.position.current;
+              const position = event.to || { x: current.x + (event.by?.x || 0), y: current.y + (event.by?.y || 0) };
+              setDeepestAndCollide(findDeepestCandidate(new GlobalPosition(document.body, position), manager), manager);
+            }
             updateLinePlaceholder(manager);
             (_a = dragListeners.dragmove) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -16361,6 +16427,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         AutoFrame_default,
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName30("frame"),
           "data-rfd-iframe": true,
           syncHostStyles: iframe.syncHostStyles,
@@ -16382,6 +16449,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         "div",
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName30("frame"),
           ref,
           "data-puck-entry": true,
@@ -17039,6 +17107,8 @@ var Layer = (0, import_react90.forwardRef)(function Layer2({
           {
             className: getClassName34("inner"),
             ref: rowRef,
+            role: "group",
+            "aria-label": `${node.label} layer`,
             onMouseEnter: (e) => {
               e.stopPropagation();
               if (outlineStore.getState().status !== "idle") {
diff --git a/dist/no-external.js b/dist/no-external.js
index af3fa6755f42679605e6805aa46fdeb90df48281..d106005d43fcb049aad4fb44c728ef618242608e 100644
--- a/dist/no-external.js
+++ b/dist/no-external.js
@@ -2785,14 +2785,23 @@ var init_use_hotkey = __esm({
 });
 
 // store/slices/history.ts
-function debounce(func, timeout3 = 300) {
-  let timer;
-  return (...args) => {
+function debounce(func, timeout = 300) {
+  let timer, pending;
+  const flush = () => {
     clearTimeout(timer);
-    timer = setTimeout(() => {
-      func(...args);
-    }, timeout3);
+    if (!pending) return;
+    const args = pending;
+    pending = undefined;
+    func(...args);
+  };
+  const deferred = (...args) => {
+    clearTimeout(timer);
+    pending = args;
+    timer = setTimeout(flush, timeout);
   };
+  deferred.flush = flush;
+  deferred.cancel = () => { clearTimeout(timer); pending = undefined; };
+  return deferred;
 }
 function useRegisterHistorySlice(appStore, {
   histories,
@@ -2841,7 +2850,7 @@ var init_history = __esm({
       });
     };
     createHistorySlice = (set, get) => {
-      const record = debounce((state) => {
+      const commit = (state) => {
         const { histories, index } = get().history;
         const history = {
           state,
@@ -2854,7 +2863,13 @@ var init_history = __esm({
             index: newHistories.length - 1
           })
         });
-      }, 250);
+      };
+      const deferredRecord = debounce(commit, 250);
+      const record = (state) => {
+        if (state.ui.field.focus?.startsWith("builder-style:")) {
+          queueMicrotask(() => { deferredRecord.flush(); commit(state); });
+        } else deferredRecord(state);
+      };
       return {
         initialAppState: {},
         index: EMPTY_HISTORY_INDEX,
@@ -2871,6 +2886,7 @@ var init_history = __esm({
         },
         currentHistory: () => get().history.histories[get().history.index],
         back: () => {
+          deferredRecord.flush();
           var _a;
           const { history, dispatch } = get();
           if (history.hasPast()) {
@@ -2885,6 +2901,7 @@ var init_history = __esm({
           }
         },
         forward: () => {
+          deferredRecord.flush();
           var _a;
           const { history, dispatch } = get();
           if (history.hasFuture()) {
@@ -2894,6 +2911,7 @@ var init_history = __esm({
           }
         },
         setHistories: (histories) => {
+          deferredRecord.cancel();
           var _a;
           const { dispatch, history } = get();
           dispatch({
@@ -2903,6 +2921,7 @@ var init_history = __esm({
           set({ history: __spreadProps(__spreadValues({}, history), { histories, index: histories.length - 1 }) });
         },
         setHistoryIndex: (index) => {
+          deferredRecord.cancel();
           var _a;
           const { dispatch, history } = get();
           dispatch({
@@ -6133,6 +6152,7 @@ var useSensors = ({
   other: otherDefault
 }) => {
   const [sensors] = (0, import_react13.useState)(() => [
+    import_react14.KeyboardSensor,
     import_react14.PointerSensor.configure({
       activationConstraints(event, source) {
         var _a;
@@ -8864,6 +8884,25 @@ var DraggableComponent = ({
   const ref = (0, import_react53.useRef)(null);
   const refSetter = (0, import_react53.useCallback)(
     (el) => {
+      // Canvas containers can contain links, inputs and nested blocks.
+      // A labelled group preserves those descendants in the accessibility tree.
+      if (el && el.tagName === "DIV" && !el.hasAttribute("role")) {
+        el.setAttribute("role", "group");
+        el.setAttribute("aria-label", `${label || componentType} block`);
+        el.setAttribute("aria-roledescription", "movable block");
+        el.setAttribute("aria-keyshortcuts", "Enter Space");
+        // aria-describedby must resolve in the canvas document, not its parent.
+        const descriptionId = "puck-canvas-keyboard-instructions";
+        const doc = el.ownerDocument;
+        if (!doc.getElementById(descriptionId)) {
+          const instructions = doc.createElement("p");
+          instructions.id = descriptionId;
+          instructions.hidden = true;
+          instructions.textContent = "Press Enter to select this block. Press Space to pick it up, arrow keys to move it, Space to drop it or Escape to cancel.";
+          doc.body.appendChild(instructions);
+        }
+        el.setAttribute("aria-describedby", descriptionId);
+      }
       sortableRef(el);
       if (ref.current !== el) {
         ref.current = el;
@@ -8873,7 +8912,7 @@ var DraggableComponent = ({
         }
       }
     },
-    [itemRef, sortableRef]
+    [itemRef, sortableRef, label, componentType]
   );
   const [portalEl, setPortalEl] = (0, import_react53.useState)();
   (0, import_react53.useEffect)(() => {
@@ -9074,12 +9113,20 @@ var DraggableComponent = ({
     el.setAttribute("data-puck-component", id);
     el.setAttribute("data-puck-dnd", id);
     el.style.position = "relative";
+    const onKeyboardSelect = (event) => {
+      if (event.target === el && event.key === "Enter") {
+        event.preventDefault();
+        onClick(event);
+      }
+    };
+    el.addEventListener("keydown", onKeyboardSelect, true);
     el.addEventListener("click", onClick);
     el.addEventListener("mouseover", _onMouseOver);
     el.addEventListener("mouseout", _onMouseOut);
     return () => {
       el.removeAttribute("data-puck-component");
       el.removeAttribute("data-puck-dnd");
+      el.removeEventListener("keydown", onKeyboardSelect, true);
       el.removeEventListener("click", onClick);
       el.removeEventListener("mouseover", _onMouseOver);
       el.removeEventListener("mouseout", _onMouseOut);
@@ -11383,10 +11430,9 @@ var createNestedDroppablePlugin = ({ onChange }, id) => class NestedDroppablePlu
           onChange(findDeepestCandidate(position, manager), manager);
         }
       };
-      const handleMoveThrottled = throttle(handleMove, 50);
-      const handlePointerMove = (event) => {
-        handleMoveThrottled(event);
-      };
+      // Resolve the current nested target before pointerup can commit a drop.
+      // A delayed callback can otherwise commit the preceding ancestor zone.
+      const handlePointerMove = handleMove;
       document.body.addEventListener("pointermove", handlePointerMove, {
         capture: true
         // dndkit's PointerSensor prevents propagation during drag
@@ -11851,14 +11897,13 @@ var DragDropContextClient = ({
     (params, manager) => {
       const { zoneChanged, areaChanged } = getChanged2(params);
       if (!zoneChanged && !areaChanged) return;
-      zoneStore.setState({
+      // Enable the destination's droppables before resolving this pointer event.
+      import_react_dom.flushSync(() => zoneStore.setState({
         zoneDepthIndex: params.zone ? { [params.zone]: true } : {},
         areaDepthIndex: params.area ? { [params.area]: true } : {}
-      });
+      }));
       tempDisableFallback(manager);
-      setTimeout(() => {
-        manager.collisionObserver.forceUpdate(true);
-      }, 50);
+      manager.collisionObserver.forceUpdate(true);
       debouncedParamsRef.current = null;
     },
     [zoneStore]
@@ -11909,18 +11954,8 @@ var DragDropContextClient = ({
             return;
           }
           if (areaChanged) {
-            if (isDragging) {
-              const debouncedParams = debouncedParamsRef.current;
-              const isSameParams = debouncedParams && debouncedParams.area === params.area && debouncedParams.zone === params.zone;
-              if (!isSameParams) {
-                cancelDb();
-                setDeepestDb(params, manager);
-                debouncedParamsRef.current = params;
-              }
-            } else {
-              cancelDb();
-              setDeepestAndCollide(params, manager);
-            }
+            cancelDb();
+            setDeepestAndCollide(params, manager);
             return;
           }
           if (zoneChanged) {
@@ -11968,7 +12003,8 @@ var DragDropContextClient = ({
             stopLinePlaceholderScrollTracking();
             const entryEl = (_a = getFrame()) == null ? void 0 : _a.querySelector("[data-puck-entry]");
             entryEl == null ? void 0 : entryEl.removeAttribute("data-puck-dragging");
-            const { source, target } = event.operation;
+            const { source } = event.operation;
+            let { target } = event.operation;
             if (!source) {
               setLinePlaceholderActive(false);
               zoneStore.setState({ draggedItem: null });
@@ -11976,9 +12012,32 @@ var DragDropContextClient = ({
             }
             const { zone, index } = source.data;
             const { previewIndex = {} } = zoneStore.getState() || {};
-            const thisPreview = (_b = Object.values(previewIndex).find(
+            let thisPreview = (_b = Object.values(previewIndex).find(
               (preview) => (preview == null ? void 0 : preview.props.id) === source.id && !preview.ghost
             )) != null ? _b : null;
+            // A release can precede the asynchronous drag-over preview update.
+            // Commit using the actual final pointer, with the same nesting/allow checks.
+            const finalFrame = document.querySelector("iframe#preview-frame");
+            const finalPointer = manager.dragOperation.position.current;
+            if (finalFrame && finalPointer) {
+              const bounds = finalFrame.getBoundingClientRect();
+              const insideCanvas = finalPointer.x >= bounds.left && finalPointer.x <= bounds.right && finalPointer.y >= bounds.top && finalPointer.y <= bounds.bottom;
+              if (!insideCanvas) {
+                // Custom editor sidebars are outside the canvas, never a root drop.
+                target = { type: "void" };
+              } else if (thisPreview && !event.canceled) {
+                const finalLocation = findDeepestCandidate(new GlobalPosition(document.body, finalPointer), manager);
+                const finalZone = finalLocation.zone;
+                const zoneElement = finalZone && finalZone !== "void" ? getFrame()?.querySelector(getZoneSelector(finalZone)) : null;
+                if (zoneElement) {
+                  const finalIndex = getNearestGapIndex(zoneElement, getFramePointer(zoneElement, finalPointer), getZoneContentIds(appStore.getState().state, finalZone));
+                  if (finalIndex !== null) {
+                    thisPreview = { ...thisPreview, zone: finalZone, index: finalIndex, linePlaceholder: true };
+                    target = manager.registry.droppables.get(finalZone) || target;
+                  }
+                }
+              }
+            }
             const playCommitFlip = !event.canceled && (target == null ? void 0 : target.type) !== "void" && (thisPreview == null ? void 0 : thisPreview.linePlaceholder) ? prepareCommitFlip({
               zones: initialSelector.current ? [initialSelector.current.zone, thisPreview.zone] : [thisPreview.zone],
               itemId: thisPreview.type === "move" ? thisPreview.props.id : void 0,
@@ -12049,6 +12108,13 @@ var DragDropContextClient = ({
           },
           onDragMove: (event, manager) => {
             var _a;
+            // Keyboard movement has no pointermove event to enable nested zones.
+            // Resolve the same destination before dnd-kit applies this movement.
+            if (event.nativeEvent?.type === "keydown") {
+              const current = manager.dragOperation.position.current;
+              const position = event.to || { x: current.x + (event.by?.x || 0), y: current.y + (event.by?.y || 0) };
+              setDeepestAndCollide(findDeepestCandidate(new GlobalPosition(document.body, position), manager), manager);
+            }
             updateLinePlaceholder(manager);
             (_a = dragListeners.dragmove) == null ? void 0 : _a.forEach((fn) => {
               fn(event, manager);
@@ -16361,6 +16427,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         AutoFrame_default,
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName30("frame"),
           "data-rfd-iframe": true,
           syncHostStyles: iframe.syncHostStyles,
@@ -16382,6 +16449,7 @@ var Preview2 = ({ id = "puck-preview" }) => {
         "div",
         {
           id: "preview-frame",
+          title: "Page canvas",
           className: getClassName30("frame"),
           ref,
           "data-puck-entry": true,
@@ -17039,6 +17107,8 @@ var Layer = (0, import_react90.forwardRef)(function Layer2({
           {
             className: getClassName34("inner"),
             ref: rowRef,
+            role: "group",
+            "aria-label": `${node.label} layer`,
             onMouseEnter: (e) => {
               e.stopPropagation();
               if (outlineStore.getState().status !== "idle") {
